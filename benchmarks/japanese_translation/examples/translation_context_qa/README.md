# v3.288 translation context / QA contract fixture

This is a synthetic, contract-only fixture. It exercises confirmed terminology,
person-name/addressing policy, a revoked term, read-only previous-batch context,
strict numbered-tag rejection, numeric consistency, source leakage, target
language density/length gates, block-only retry, cancellation, and partial
persistence.

It is not a translation quality claim and must not select a product model.
Real GGUF artifacts, authorized clean-text data, human review, and target-device
measurements remain required before promotion.
