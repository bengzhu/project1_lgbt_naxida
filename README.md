# AITRANS iOS Prototype

v3.359（2026-08-30，已完成）继续收紧普通图片日语→简体中文的共享汉字 QA 边界：v3.358 已让模型清洗器只放行规范化后完全相同的纯汉字共享词，但产品 `TranslationBatchQualityEvaluator` 仍会把 `日本→日本人` 这类“原文子串+额外内容”误当作共享汉字例外。现在 source-leakage gate 复用同一窄 policy，只有日语→简体中文、源/输出规范化后完全相同、源文本至少两个字符且纯汉字时才免于泄漏失败；单字、假名/拉丁/数字、额外原文和其它语言对继续拒绝。失败仍只触发当前 block 的既有补译/QA，不重跑 OCR、检测、布局或整页，不改变请求预算、标签、取消、持久化、UI/renderer 和非日语路径。新增 `scripts/test-v3359-japanese-shared-han-qa-exactness-contract.py`，工程版本推进至 `3.359`，CI 已接入；本地安全回归为 358 个安全合同通过、27 个进程/编译入口合同按约束跳过（385 总计），Python AST `405/405`、JSON `144/144`、YAML `3/3`、shell `32/32`、plist/project `4/4` 与 `git diff --check` 通过；实现 SHA `2dc4b0fd6162a01052a08200e53e4f7005c3c01d` 的 exact-SHA full [33303966969](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33303966969)、PR [#423](https://github.com/bengzhu/project1_lgbt_naxida/pull/423) checks [33304583260](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33304583260)、merge SHA `e3be7f7a76db0abb732d064a4954470d6bc6bc1a` 与合入后 push CI [33304633237](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33304633237) 均成功；exact full 的 Xcode build、JUnit `10/10` 与 `AITRANS CI/full-validation=success` receipt 通过。`test/3.png` 仍未提供，不用合成样图代替普通 OCR/翻译证据；Koharu/GGUF、授权语料和目标设备证据继续只作可选研究/质量证明。

v3.358（2026-08-30，已完成）继续提升普通图片日语→简体中文翻译的召回：已有产品 QA 对纯汉字日语词的共享汉字例外已放行，但 `GemmaLocalService` 的标准与 `[N]` 漫画 batch 清洗仍把规范化后同形的合法译文判为原文重复。现在由 `TranslationOutputPolicy` 统一提供窄条件放行：仅限日语→简体中文、源/输出规范化后相同且源文本为至少两个字符的纯汉字；假名、拉丁字母、数字、额外原文、其它语言对仍拒绝。OCR、候选/geometry/layout、模型、请求预算、标签/QA、取消、持久化、UI/renderer 和非日语路径不变。新增 `scripts/test-v3358-japanese-shared-han-model-cleanup-contract.py`，工程版本推进至 `3.358`，CI 已接入。本地安全回归为 357 个安全合同通过、27 个进程/编译入口合同按约束跳过（384 总计），Python AST `403/403`、JSON `144/144`、YAML `3/3`、shell `32/32`、plist/project `4/4` 与 `git diff --check` 通过；实现 SHA `a0b79e70aed0ef5ed10db8e55e3aca3192913a31` 的 exact-SHA full [33302341558](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33302341558)、PR [#422](https://github.com/bengzhu/project1_lgbt_naxida/pull/422) checks [33302812433](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33302812433)、merge SHA `020f4eb30cf69c5864024eae1178660f410a83b0` 与合入后 push CI [33302865592](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33302865592) 均成功；exact full 的 Xcode build 成功、JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`；本地未运行 Xcode、Swift、Core ML、App runtime、Rust/Cargo 或 GGUF。`test/3.png` 仍未提供，收到真实文件后再纳入普通 OCR/翻译回归；不以本轮合同、静态结果或共享汉字例外宣称通用 OCR/CER 或翻译质量提升，Koharu/GGUF、授权语料和目标设备证据继续是可选研究/质量证明。

v3.357（2026-08-30，已完成）继续提升普通图片日语 OCR→翻译的漫画声效召回：`TranslationTextKindClassifier` 在保留既有片假名门控的前提下，增加完整假名重复（如 `ふらふら`）、词尾促音（如 `ぐっ`）和极短长音/省略号（如 `ふー`）的高信号 `.sfx` 提示，并要求拟声词块使用简短中文拟声或动作表达，不补写主语、不解释动作或扩写成完整句子。较长的平假名长音对白不因单个标记自动升级；OCR、候选选择、geometry/layout、请求预算、标签/QA、取消、持久化和非日语路径不变。新增 `scripts/test-v3357-japanese-sfx-translation-recall-contract.py`，工程版本推进至 `3.357`，CI 已接入。本地安全回归为 356 个安全合同通过、27 个进程/编译入口合同按约束跳过（383 总计），Python AST `402/402`、JSON `144/144`、YAML `3/3`、shell `32/32`、plist/project `4/4` 与 `git diff --check` 通过；exact-SHA full [33300948175](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33300948175)、PR [#421](https://github.com/bengzhu/project1_lgbt_naxida/pull/421) checks [33301514792](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33301514792)、merge SHA `84b9a681fa95b9978db465888013784d8a005454` 与合入后 push CI [33301557301](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33301557301) 均成功；未运行本地 Xcode、Swift、Core ML、App runtime、Rust/Cargo 或 GGUF。`test/3.png` 目前仍未在工作树、分支或 `origin` 历史中出现，收到真实文件后再纳入普通 OCR/翻译回归，不用合成样图代替证据；本轮不声称通用 OCR/CER 或翻译质量提升，Koharu/GGUF、授权语料和目标设备证据仍为可选研究/质量证明。

v3.356（2026-08-30，已完成）继续改善普通图片日语 compact line 的空间召回：pixel-first 路径虽为 compact 候选预留最多 4 个名额，但当前只按 `y/x` 取前四个，多个 band 的短 SFX/短列可能被同一 band 占满。现在仅在 compact pool 超过 4 且存在多个 band 时采用稳定 band round-robin，保留既有 compact 优先级、under-budget/single-band 行为、12 个 pixel crop 总预算、4 次方向 fallback、quality/geometry、OCR/layout、翻译 QA/取消/持久化边界不变。新增 v3.356 contract，工程版本 `3.356`；本地安全回归为 355 个安全合同通过、27 个进程/编译入口合同按约束跳过（382 总计），Python AST `382/382`、JSON `237/237`、YAML `3/3`、shell `32/32`、plist `4/4` 与 `git diff --check` 通过。实现 SHA `a3b53349bc28013ba674d0d4a026e7db6e55d62c` 的 exact-SHA full [33299312826](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33299312826)、PR [#420](https://github.com/bengzhu/project1_lgbt_naxida/pull/420) checks [33299888372](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33299888372)、merge SHA `aa8fc40b9054fd943f8e1ece4db0d31d6dda4ae2` 与合入后 push CI [33299935514](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33299935514) 均成功；不声称通用 OCR/CER 或翻译质量提升。Koharu/GGUF、授权语料和目标设备证据仅作可选研究/质量证明，不阻塞普通 OCR/翻译修复。

v3.355（2026-08-30，已完成）继续改善普通图片日语 line geometry 的 owner 覆盖：现有 2 个 geometry-only line 保留名额在同一 known TextRegion 有多个高几何候选时可能被单一 owner 占满，其他 owner 的 line polygon 代理因此没有进入 Manga OCR。现在在既有 text-backed coverage filter、严格单 owner geometry gate 与按高度/位置排序之后，复用 owner-only bounded selector，只有 geometry pool 超过保留名额且存在多个 known owner 时平衡候选；under-budget、single-owner、ownerless 兼容、8 个 line OCR 总预算、2 个 geometry 名额、Vision/Manga OCR、布局/翻译 QA/取消/持久化边界不变。新增 v3.355 contract，工程版本 `3.355`；实现 SHA `0568d4d10b2132ce55b2a64b2ce3317606291f3f` 的 exact-SHA full [33298161646](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33298161646)、PR [#419](https://github.com/bengzhu/project1_lgbt_naxida/pull/419) checks [33298597548](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33298597548)、merge SHA `be23a758952d7b8713267acb8d0667ab85ffefc5` 与合入后 push CI [33298644345](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33298644345) 均成功；文档收口 push CI [33298747648](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33298747648) 亦成功；不声称通用 OCR/CER 或翻译质量提升。

v3.354（2026-08-30，已完成）继续收紧普通图片日语竖排 owner 边界：v3.352 已让风险排序拒绝非有限/越界方向置信度，但中等高度竖排候选筛选、line owner 匹配和 geometry-only line owner 匹配仍直接比较 `directionConfidence >= .25`；现在三处统一经过有限闭区间 `[0,1]` helper，非法方向分数不再伪装成跨块 owner，合法 `.25`/`.45` 门、ownerless 兼容、16/8/4/24/12/48 既有预算与 OCR/layout/翻译 QA/取消/持久化边界不变。新增 v3.354 contract，工程版本 `3.354`；实现 SHA `86500c81563042abee526b08d43ce8dd9928c4fd` 的 exact-SHA full [33297024649](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33297024649)、PR [#418](https://github.com/bengzhu/project1_lgbt_naxida/pull/418) checks [33297508110](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33297508110)、merge SHA `885dc9ea2a9eadd5d6d18a9598e75a505fe44d76` 与合入后 push CI [33297547961](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33297547961) 均成功；文档收口 push CI [33297705934](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33297705934) 亦成功；不声称通用 OCR/CER 或翻译质量提升。

v3.353（2026-08-30，已完成）继续修复普通图片日语 vertical block crop 的有限预算调度：v3.345 的多 band 轮询保证了空间覆盖，但在一个 band 聚集大量弱 block、其它 band 聚集强 block 时，强 block 仍可能挤占 16 个既有限额；现在先在风险 block 类内按 band 轮询，再用剩余名额对非风险类按同一空间策略补齐，未超预算、单 band 与无风险队列保持历史选择。总计最多 16 个 vertical block crop、8 次方向 fallback，line/pixel/tile/frontier、owner、OCR/layout、翻译 QA、取消、持久化、Koharu/GGUF、授权语料和目标设备证据边界不变。新增 v3.353 contract，工程版本 `3.353`；实现 SHA `9e60fcbca3ca4c36a7ad67ed99ae45d42ce81ed2` 的 exact-SHA full [33295448242](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33295448242)、PR [#417](https://github.com/bengzhu/project1_lgbt_naxida/pull/417) checks [33295982257](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33295982257)、merge SHA `16db200bfc881702363eca2f67dd7bd513d72ae9` 与合入后 push CI [33296036459](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33296036459) 均成功；文档收口 push CI [33296186523](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33296186523) 亦成功；不声称通用 OCR/CER 或翻译质量提升。

v3.352（2026-08-30，已完成）修复普通图片日语 vertical recovery 的方向置信度合法域漂移：pixel/tile 覆盖门已要求有限 `[0,1]`，但 weak-block recovery 与 vertical crop 风险排序此前只拒绝非有限或低于 `.45` 的值；现在共享 `validJapaneseDirectionConfidence`，让 `NaN/±∞` 与有限越界值在风险判定和 tie-break 中统一 fail closed，合法 `[0,1]` 与 `>=.45` 语义不变。最多 16 个 vertical block crop、8 次方向 fallback、4 个 weak-block scoped reread、pixel/line/tile/frontier、owner、OCR/layout、翻译 QA、取消和持久化边界不变。新增 v3.352 contract，工程版本 `3.352`；实现 SHA `321e52831b4ca9d202ae897276316c980f541fe7` 的 exact-SHA full [33294222837](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33294222837)、PR [#416](https://github.com/bengzhu/project1_lgbt_naxida/pull/416) checks [33294632461](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33294632461)、merge SHA `180f47f50cfa483a5796801d6247ccebef4ff493` 与合入后 push CI [33294679936](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33294679936) 均成功。文档收口 push CI [33294843498](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33294843498) 亦成功；不声称通用 OCR/CER 或翻译质量提升，Koharu/GGUF、授权语料和目标设备证据仍独立于普通 OCR 主路径。

v3.351（2026-08-30，已完成）修复普通图片日语 pixel-first geometry fallback 仍被弱 layout block 几何拦截的边界：只有 vertical、有限 `[0,1]` 且 `directionConfidence >=.45`，并通过既有非空、有限 OCR confidence `>=.48`、真实日语文字及日语/脚本文本密度 `>=.5` 的 block 才能声明 pixel candidate 已覆盖；弱/空/非日语/低密度/方向不确定 block 继续保留既有最多 12 个 pixel-first crop、4 次方向 fallback 与后续 tile 机会。新增 v3.351 contract，工程版本 `3.351`；实现 SHA `09cd27d2931ed5aa5f79a0325beac06d9e106975` 的 exact-SHA full [33293068254](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33293068254)、PR [#415](https://github.com/bengzhu/project1_lgbt_naxida/pull/415) checks [33293551364](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33293551364)、merge SHA `db302ec1bf281533d01e31e3bc1b919cc95b6a57` 与合入后 push CI [33293585668](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33293585668) 均成功。不声称通用 OCR/CER 或翻译质量提升；Koharu/GGUF、授权语料和目标设备证据仍独立于普通 OCR 主路径。

v3.351 文档收口 push CI [33293685336](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33293685336) 已成功；该 CI 只验证文档收口后的当前 `smalldata_test` 状态，不新增编译或模型证据。

v3.350（2026-08-30，已完成）继续收紧普通图片日语 tile fallback 的 existing block coverage：只有 vertical、有限 `[0,1]` 且 `directionConfidence >=.45` 的方向判定，加上既有非空、有限 OCR confidence `>=.48`、真实日语文字和日语/脚本文本密度 `>=.5`，才可抑制重复 tile；方向不确定的 vertical block 继续保留既有 tile 复读机会，最多 6 个 tile、18 个窗口、4 次方向 fallback 与 pixel-first/line/block/OCR/layout/翻译 QA/取消/持久化边界不变。新增 v3.350 contract，工程版本 `3.350`；实现 SHA `46c105605c215ccef61e4bd013b71d7e1c8e1799` 的 exact-SHA full [33291869290](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33291869290)、PR [#414](https://github.com/bengzhu/project1_lgbt_naxida/pull/414) checks [33292290297](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33292290297)、merge SHA `0d53fa31e12e3dcd653aa25a9c230c44ce8c1c46` 与合入后 push CI [33292322548](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33292322548) 均成功。不声称通用 OCR/CER 或翻译质量提升。

v3.350 文档收口 push CI [33292525345](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33292525345) 已成功；该 CI 只验证文档收口后的当前 `smalldata_test` 状态，不新增编译或模型证据。

v3.349（2026-08-30，已完成）收紧普通图片日语 tile fallback 的 existing block coverage：`verticalBlocks` 不再仅凭几何阻止宽窗口，只有 vertical、非空、有限 confidence `>=.48`、真实日语文字和日语/脚本文本密度 `>=.5` 的 block 才抑制重复 tile；弱/空/非日语/低密度 block 继续保留既有最多 6 个 tile、18 个窗口、4 次方向 fallback 与下游 pixel-first/line/block/OCR/layout/翻译 QA/取消/持久化边界。新增 v3.349 contract，工程版本 `3.349`；实现 SHA `f70cdf40a5474b82d410c9c9371d4be17ebe6a87` 的 exact-SHA full [33290699231](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33290699231)、PR [#413](https://github.com/bengzhu/project1_lgbt_naxida/pull/413) checks [33291114888](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33291114888)、merge SHA `475aab7517d09cce374cc978a4ac12a7b27b980f` 与合入后 push CI [33291151655](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33291151655) 均成功；文档收口 push CI [33291243730](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33291243730) 已成功。不声称通用 OCR/CER 或翻译质量提升。

v3.348（2026-08-30，已完成）收紧普通图片日语 pixel-first recovery 的 existing vertical coverage：`.vertical` provenance 或 `.verticalLine` role 不再单独声明几何已覆盖，只有非空、有限 confidence `>=.48`、真实日语文字和日语/脚本文本密度 `>=.5` 的读数才抑制重复 crop；弱页面读数继续保留既有最多 12 个 pixel-first crop、4 次方向 fallback 与下游 line/tile/OCR/layout/翻译 QA/取消/持久化边界。新增 v3.348 contract，工程版本 `3.348`；实现 SHA `02e4a51e47f4efc6e68b0a4fc2ea56d398bfe3ba` 的 exact-SHA full [33289570887](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33289570887)、PR [#412](https://github.com/bengzhu/project1_lgbt_naxida/pull/412) checks [33290091078](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33290091078)、merge SHA `9bd5f4238e1abc690e7371a6aa5c534268746b07` 与合入后 push CI [33290131510](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33290131510) 均成功；文档收口 push CI [33290225605](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33290225605) 已成功。不声称通用 OCR/CER 或翻译质量提升。

v3.347 文档收口 push CI：[33288959454](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33288959454) 已成功；该 CI 只验证文档收口后的当前 `smalldata_test` 状态，不新增编译或模型证据。

v3.347（2026-08-30）修复普通图片日语 OCR recovery frontier：line-first 与 pixel-first 复读产生的可靠 `.verticalLine` observation 共同进入既有 tile fallback 的只读 coverage frontier，避免可靠 pixel-first 区域被宽 tile 重复处理并挤出后续未覆盖窗口；弱／空／非竖排结果仍由既有 `japaneseLinePathRegion` gate 保留 fallback。最多 18 个 tile window、4 次反向方向复读、OCR/layout、翻译 QA、取消、持久化和非日语路径不变。新增 v3.347 contract，工程版本 `3.347`；本地 346 个安全合同通过，27 个进程／编译／runtime 合同跳过，未运行本地 Xcode、Swift、Core ML、Rust/Cargo、GGUF 或 App runtime。实现 SHA `a3fd100d908da4ade62ad8b4311afad95cb7a690` 的 exact-SHA full [33288126373](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33288126373)、PR [#411](https://github.com/bengzhu/project1_lgbt_naxida/pull/411)、merge SHA `112c5dfcf4e4c3eb1ce51d8501911eb9112982bf` 与合入后 push CI [33288808855](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/33288808855) 均成功。Koharu/GGUF、授权语料和目标设备证据仍独立于普通 OCR 主路径，不据此外推通用 OCR／CER 或翻译质量提升。

v3.272（2026-08-12）继续收紧 Koharu MIT48 reference 的右上竖排 line：`right-column-a` 从 `(864,136)-(908,342)` 收紧到四个主字符范围 `(864,136)-(908,298)`，去掉省略号和下方背景；云端仍准确读出 `やがって`，confidence 从 `0.23084585` 提升到 `0.98682830`。compact `ニコッ` 为 `0.59093815`，accepted `6/7`，既有质量 gate 保持不变。这是固定 `test/jap.jpg` 的云端 crop 诊断改进，不是通用日语 OCR／翻译质量声明；不打包 GPL MIT48 artifact，不替换 bundled Apache Manga OCR。工程版本 `3.272`，新增 text-only quality contract；本地只做安全静态检查，未本地编译。exact-SHA full [31582991645](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31582991645) 对实现 SHA `6c28599fbc205939fc1e50c0dc25d34cda28b184` 成功；PR #353 CI [31584467786](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31584467786) 通过，merge `4c9e50d7e41dc99a1289fa82e0601f5e7f924fb2`，push CI [31584537250](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31584537250) 通过。远端仅保留 `main` 与 `smalldata_test`。

v3.271（2026-08-12）继续收紧 Koharu MIT48 reference 的右上竖排 line 云端诊断：`right-column-a` crop 从 `(846,116)-(906,432)` 收紧到实测 `(864,136)-(908,342)`，云端仍读出 `やがって`，confidence 从 `0.14473826` 提升到 `0.23084585`，但仍低于可靠 `>=.55` 门槛，不伪造为可靠 owner。既有 7 crop 至少 6 个日语预测与 compact `ニコッ` `>=.55` gate 保持通过，compact 为 `ニコッ`／`0.59093773`，accepted `6/7`。这是固定 `test/jap.jpg` 的云端 crop 诊断改进，不是通用日语 OCR／翻译质量声明；不打包 GPL MIT48 artifact，不替换 bundled Apache Manga OCR。工程版本 `3.271`，新增 right-column quality contract；本地只做安全静态检查，未本地编译。exact-SHA full [31580831851](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31580831851) 对实现 SHA `deed8f9c9b67c535167daa3989ce7e4654aed1a9` 成功；PR #352 CI [31582462714](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31582462714) 通过，merge `e3ab42b4d9a6ad368a4dd4c27e5e2a35ddbe315f`，push CI [31582532563](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31582532563) 通过。远端仅保留 `main` 与 `smalldata_test`。

v3.270（2026-08-12）继续把 Koharu MIT48 parity 从“能编译”推进到可验证质量边界：云端使用与 Koharu image-rs `rotate270` 等价的 Pillow `rotate(90)`，并按 `test/jap.jpg` 实际黑字组件收紧 compact `ニコッ` crop。质量 gate 要求 7 个 crop 至少 6 个通过日语脚本密度 `>=.5`，compact 必须输出 `ニコッ` 且 confidence `>=.55`；最终 cloud evidence 为 `ニコッ` confidence `0.590938`、6/7 accepted，主列包含 `持ち帰る！`、`この爆乳を`、`前は生意気に`、`俺の誘い断り`。仍不把 GPL MIT48 权重/runtime 打进 iOS、不替换 bundled Apache Manga OCR，也不把固定样图外推为通用日语 OCR／翻译质量提升。新增 v3.270 orientation/quality 合同，工程版本为 `3.270`；本地只做静态 Python、YAML／shell 语法与 diff 检查，未本地编译。exact-SHA full [31578984648](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31578984648) 对 SHA `56473a2803dcfc94d9b198409e2dca4df4bcb3e4` 的云端 parity/Xcode/JUnit/UI/Home/Paste/Speech 全部成功；PR #351 merge `7651dacb2a8cc4e768f57fefdc0e2c38c0376a56`，合入后 push CI [31580218296](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31580218296) 通过。远端仅保留 `main` 与 `smalldata_test`。

v3.269（2026-08-12）新增云端 Koharu MIT48 reference parity：固定 `mayocream/mit48px-ocr` revision `205395b155a041b068fd754a6e417cd71b4cb1de`，固定 Koharu source revision `35f3e6d1a418d9617fd922e2bc865fe5b8fff818`，使用其 `LLAMA_CPP_TAG=b8935`，仅在 Ubuntu 24.04 云端构建 `koharu-ml/bin/mit48px-ocr`，对 `test/jap.jpg` 固定竖排 crop／rotate270 做 CPU smoke，并校验非空日语输出密度 `>=.5`。MIT48 权重和 GPL Koharu runtime 不进入 AITRANS，也不替换现有 bundled Apache Manga OCR；固定样图不代表通用日语 OCR／翻译质量提升。图片单块 scoped 取消现在同步 transfer message、failure generation 并持久化，保留其它译文与复查进度。工程版本为 `3.269`，新增 artifact validator、cloud smoke、parity workflow 与 v3.269 合同；本地遵守“不要本地编译”，只做 Python、YAML／shell 语法和 diff 检查。exact-SHA full [31573726047](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31573726047) 对实现 SHA `031e1c98b198a0aadf3e78c5448701ba964d4a01` 的 parity 与 full validation 成功；PR #350 merge SHA `e827e53252bad8b8e9a9e2190d619abdc52ea9c0`，合入后 push CI [31575090711](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31575090711) 通过。远端仅保留 `main` 与 `smalldata_test`。

v3.268（2026-08-12）继续向 Koharu `TextRegion -> line crop -> OCR` 迁移：在已经建立的日语竖排 layout block 内，使用 Vision `characterBoxes` 形成严格的 recognition-only line geometry 候选；候选必须有至少 2 个字符、有效 quad、单一竖排 block 归属，并通过覆盖、面积、尺寸和重复 geometry 门控。最多保留既有 8 个 line OCR 请求中的 2 个 geometry 位，沿用 bundled Manga OCR 的 bounded line crop、rotate270、confidence `>=.55` 与日语脚本密度 `>=.5` 门控，不接管 detector bbox owner、不改变取消／布局／翻译／渲染／非图片路径。新增 v3.268 合同，工程版本为 `3.268`。本地仅执行相关 Python 合同、YAML 解析与 `git diff --check`，未运行本地 `xcodebuild`、`swiftc` 或 runtime 编译；云端 exact-SHA full [31570211484](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31570211484) 对实现 SHA `e64d5b19f31b999d9c3232c4ccb731b24e988a32` 的 Xcode/JUnit 全部成功，PR #349 合入 `smalldata_test` 的 merge `6f02c217119afaed13d7bcb93df44bbd7052a50e`，合入后 push CI [31571032333](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31571032333) 通过。该版本仍不声称真实 Koharu MIT48px/PaddleOCR-VL artifact 已迁移或固定样图证明通用日语 OCR 质量提升；artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.267（2026-08-12）继续向 Koharu `TextRegion -> line crop -> OCR` 迁移：对 Vision 已有且严格判定为竖排 line 的 geometry 候选，使用现有 bundled Manga OCR 的 line-specific bounded crop 与 rotate270 做受控 supplement；最多 8 个请求，输出经过清洗、confidence `>=.55` 与日语脚本密度 `>=.5` 门控，作为 recognition-only 结果，不改变可靠 detector bbox owner、既有 fallback、预算、布局、翻译、渲染或非图片路径。图片单块翻译重试支持命令栏、结果行、局部预览和 VoiceOver 的 scoped cancel，取消后恢复当前 block 终态并保留其它译文与复查进度。新增 v3.267 两个合同，工程版本为 `3.267`；本地 `288` 个合同与 `test/jap.jpg` 单页 5 blocks／长页 20 blocks／compact `ニコッ` runtime 通过，exact-SHA full [31567998753](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31567998753) 对实现 SHA `6d582e931241440f3d19b5ddadb0be468816de0c` 的 Xcode/JUnit/UI/Home/Paste/Speech 全部成功，PR #348 合入 `smalldata_test` 的 merge `891452ef662f1725d64394f02ec26979dc1cbf78`，合入后 push CI [31568653502](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31568653502) 通过。该版本不声称真实 Koharu MIT48px/PaddleOCR-VL artifact 已迁移，也不把固定样图外推为通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.266（2026-08-12）继续向 Koharu vertical `warp_line_region` 迁移：bundled Manga OCR 严格 vertical quad 采用 direct-first bounded target canvas、canonical DeviceRGB projective bilinear sampler 与 rotate270，失败时回退 natural projection；Vision 旋转侦察只为满足日语脚本密度与列状 geometry 的结果保留竖排 provenance。结果行和局部预览在单块重识别期间可直接取消当前文字块，沿用 scoped task cancel，不影响其它 OCR、译文与复查进度。新增 v3.266 合同，工程版本为 `3.266`；本地 `286` 个合同与云端 exact-SHA full [31564844704](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31564844704) 通过，PR #347 已合入 `smalldata_test`，merge `b53fe37390a56bf76b62aeeb0927745178cd79ad`，合入后 push CI [31565649871](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31565649871) 重跑成功。该版本只改善 Koharu crop／方向 provenance／操作边界，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.259 对齐 Koharu Manga OCR 的最近邻预处理边界：bundled Manga OCR 的 224×224 灰度输入改用 `UpsampleNearest2D` 等价的 floor/nearest 空间采样（保留 iOS `DeviceGray` 渲染，未宣称 RGB/luma 位级相等），新增独立 checksum/runtime harness；同时修正图片 OCR 方向选择器提交待定 override 后仍显示“方向待定”的 review warning，并为历史 runtime 路由补齐 checkout scope guards。OCR 模型、detector ownership、bbox／quad、batch／单 crop fallback、取消传播、12／48 预算、翻译、布局、渲染和非图片路径不变。新增 v3.259 nearest-preprocess 合同与 runtime harness，工程版本为 `3.259`。本地 `279` 个合同通过；3×2 灰度 fixture checksum 为 `82320641c2833265`，单页仍为 5 个 vertical blocks，长页为 20 个 vertical blocks、4 个 compact `ニコッ`、底部 `y=0.940558`，方向 crop 目标文字保持 `今度こそこの爆乳を持ち帰る！`。exact-SHA full [31498693384](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31498693384) 对 SHA `4ac896f1826002a42cef181b44f264ff20f4b249` 使用 Xcode `26.6` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；PR #333 已合入 `smalldata_test`，merge `4365fa490270bd098164c867574c23252cd633f1`，合入后 CI [31499851239](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31499851239) 通过。该版本只改善 Koharu 预处理对齐与方向复查的操作可发现性，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.258 收紧图片日语 OCR 的 detector-owner 可靠性：`.comicTextBubble` 传递 detector confidence provenance，弱 owner 只有在 detector 与 Manga OCR 证据都可靠时才保留；Vision supplement 不伪造 detector score。重新识别的 VoiceOver focus intent 在用户切换 block／筛选／预览／修正方向或 review 队列后失效，过期完成或失败不会把焦点拉回旧 block；OCR／翻译任务本身不被取消。补齐 v3.236 CI 历史合同路由，新增 detector-confidence 合同，工程版本为 `3.258`。本地 `278` 个合同、YAML、project plist、Python 语法与 `git diff --check` 通过；固定 `test/jap.jpg` 单页仍为 5 个 vertical blocks，长页为 20 个 vertical blocks、4 个 compact `ニコッ`、底部 `y=0.940558`。exact-SHA full [31494116765](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31494116765) 对 SHA `3c4d3c172c59c3b40d202a476f67899ca03598f6` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #331 已合入 `smalldata_test`，merge `9c38c9941a1ef1d1da3399ada4d81a47d82a2693`，合入后 CI [31495336273](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31495336273) 通过。该版本只改善弱 owner 防护与焦点安全，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.257 修复图片 OCR 修正页改文字方向后的 VoiceOver 焦点来源：结果行入口保留 `.row`，局部预览入口保留 `.preview`，方向 Picker 延迟焦点消费原来源；当前 block 仍可见时取消／关闭回到原 row 或 preview，隐藏 block 继续走可见结果行、完成态、筛选控件或空态 fallback。revision、保存、忽略和 sheet dismiss 清理 view-only origin；方向覆盖仍只更新显示、筛选和导出，不重跑 OCR、翻译或整图请求。新增 v3.257 focus-origin contract，工程版本为 `3.257`。本地 `277` 个合同、YAML、project plist、Python 语法与 `git diff --check` 通过；现有 `test/jap.jpg` fixture 未证明额外 OCR 质量提升，因此不改预处理或模型 artifact。exact-SHA full [31491372691](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31491372691) 对 SHA `f4e1a5d31090ed72b6d3e2fc29ac89468d3a40b9` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #329 已合入 `smalldata_test`，merge `26c0de278c3ca298afae209025a073717f906ec8`，合入后 CI [31492342250](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31492342250) 通过。该版本只改善焦点连续性与操作可发现性，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.256 修复图片 OCR 复查方向筛选变化后的 VoiceOver 焦点边界：方向覆盖继续只更新当前 block 的显示、筛选和导出方向；若 block 离开当前筛选，清理隐藏 selection 并聚焦可见结果行、完成态、筛选控件或空态，校正 sheet 延迟焦点也拒绝过期隐藏目标。OCR、翻译、布局、渲染、取消传播和非图片路径不变；CI 路由同时覆盖 MangaOCR service 与资源。新增 v3.256 focus contract，工程版本为 `3.256`。本地 `276` 个合同通过；固定 `test/jap.jpg` 既有真实 Core ML runtime 保持单页 5 个 vertical blocks、长页 20 个 vertical blocks、4 个 compact `ニコッ`、底部 `y=0.940558`。exact-SHA full [31488626549](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31488626549) 对 SHA `97db12152b6d8b4c5fd55305439f01558b7044c9` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #327 已合入 `smalldata_test`，merge `5c6651b45f72eddd81507e819d889bc5b21365ed`，合入后 CI [31489553054](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31489553054) 通过。该版本只改善筛选后的操作可发现性与焦点安全，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.255 修复 bundled Manga OCR 可变长度 batch decoder 的位置对齐：已结束的行继续追加 EOS 占位，避免活跃行因右侧 `[SEP]` padding 取错最终位置 logits；保留 Koharu `0..<300` token／EOS、最多 4 crop batch、单 crop fallback、取消传播、请求预算、detector bbox／quad、布局、翻译与非日语路径不变。新增 v3.255 batch EOS alignment 合同，工程版本为 `3.255`。本地 `275` 个合同通过；`test/jap.jpg` 单页真实 Core ML batch runtime 为 5 个 vertical blocks，长页为 20 个 vertical blocks、4 个 compact `ニコッ`、底部 `y=0.940558`。exact-SHA full [31484005038](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31484005038) 对 SHA `276527069366433ed4adccf12cc92f29d6e09b92` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #325 已合入 `smalldata_test`，merge `1091a98ca2ec970497b0304fd2b41b777f62f3fc`，合入后 CI [31484915985](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31484915985) 通过。该版本只修复 batch 解码执行边界，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.254 继续向 Koharu 的 `TextRegion -> crop -> Manga OCR` 迁移：短而紧的日语候选只有在 detector／Vision 重叠、置信度、日语脚本密度、字符数和邻域几何都通过严格门控时，才允许恢复 compact owner；可靠 detector bbox、quad fallback、取消、12／48 预算和非日语路径不变。新增 long-page diagnostic harness 与 contract，工程版本为 `3.254`。本地 274 个合同通过；`test/jap.jpg` 单页仍为 5 个 vertical blocks，长页为 20 个 vertical blocks、4 个 compact vertical blocks，底部 `y=0.940558`，本地 compact read 为 `ニコッ`；云端高图宽比渲染允许 host variant，只证明 ownership／geometry／方向回归，不声称通用日语 OCR、翻译或识别质量。exact-SHA full [31478786162](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31478786162) 对 SHA `622c6112f9833bd657bdcedfc855f5bfe99fd946` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #323 已合入 `smalldata_test`，merge `046c6339a126fd800105e02e939b3a192fa83a48`，合入后 CI [31479548671](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31479548671) 通过。Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.253 改善图片 OCR 单块重新识别的 VoiceOver 可发现性：结果行只在 action 可执行且不忙时提示“重新识别此文字块”，局部预览所有 geometry 分支统一追加可执行／进行中提示；锁定状态不误报“恢复 Vision OCR”。不改变 Store、OCR、翻译、取消、generation、焦点回跳或图片数据。新增 accessibility-hint 合同，工程版本为 `3.253`。本地 `273` 个合同通过；exact-SHA full [31471779981](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31471779981) 对 SHA `b091f248f03ca22049acd78bd358fe58b01e4bb2` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #321 [31471794330](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31471794330) 已合入 `smalldata_test`，merge `bcfe1af6835ea313d79550e2f01019074e5d85b7`，合入后 CI [31472763286](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31472763286) 通过。该版本只改善操作可发现性，不声称日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.252 收紧固定样图中短而弱的日语 detector owner：保留 Koharu bbox-primary 默认，只有小而弱的 detector owner 被严格重叠、更多日语字母的 Vision 竖排 crop 复读确认时才移除，让紧凑块恢复 `ニコッ`；可靠 owner、quad fallback、预算、取消、布局、翻译、渲染和非日语路径不变。新增 compact-crop recovery 合同，工程版本为 `3.252`。本地 `272` 个合同通过；固定单页为 `5` 个 vertical blocks，紧凑块由 `こっ、` 恢复为 `ニコッ`，长页仍为 `17` blocks／`16` vertical、底部 `y=0.940558`。exact-SHA full [31469872746](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31469872746) 对 SHA `7aad047d7338fde2190a6ebde3cfb625acf75254` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #319 [31469897145](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31469897145) 已合入 `smalldata_test`，merge `f81969291f9c05c582bba578f0f78bd005db0956`，合入后 CI [31470555207](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31470555207) 通过。固定样图只作 compact crop／ownership／runtime 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.251 修复图片 OCR 单块重新识别的焦点上下文：预览入口成功后回到预览文字块，结果行入口成功后回到结果行；失败/取消仍聚焦状态行，revision/generation/stale/终态边界不变。新增 focus-origin 合同，工程版本为 `3.251`。本地 `271` 个合同通过；exact-SHA full [31454922438](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31454922438) 对 SHA `7ff333b2702cd416e3e2179c67f45030fa9188a1` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #317 [31467723651](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31467723651) 已合入 `smalldata_test`，merge `1148ffa0dff8cc5403c8d70105c6cc008ac5a00c`，合入后 CI [31467791183](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31467791183) 通过。固定样图只作 OCR/runtime/UI 回归，Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.250 修正 Koharu detector crop role：只有 `ctd`／非空 `line_polygons` 才扩展，RT-DETR `.comicTextBubble` TextRegion 现在直接使用原始 bbox；Vision `.vision` supplement 保留 `cropRectHint`、竖排 `.18/.12` padding 与相邻列 bisector，ownership、quad fallback、预算、取消、既有竖排 provenance 与非日语路径不变。新增 v3.250 role-boundary 合同，更新历史等价合同，工程版本为 `3.250`。本地 `270` 个合同、单页 `5/5` vertical、长页 `17/16` vertical（底部 `y=0.940558`）、bbox-primary/方向 crop runtime 通过；exact-SHA full [31453792838](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31453792838) 对 SHA `59c9b145da31ee4240895321aa9f18c665065147` 的 Xcode/JUnit `10/10` 成功，`AITRANS CI/full-validation=success`，probe `skip`；PR #315 [31454386498](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31454386498) 已合入 `smalldata_test`，merge `30163b918698cafc8d48ae6f1298cd5ade190afe`，合入后 CI [31454423404](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31454423404) 通过。固定样图只作 crop/ownership/runtime/UI 回归，Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；远端仅保留 `main` 与 `smalldata_test`。

v3.249 对齐 Koharu detector crop padding：bundled RT-DETR detector TextRegion 使用横排默认 `.12/.18`，Vision supplement 使用竖排 `.18/.12`；detector bbox ownership、Vision `cropRectHint` supplement、相邻列 bisector 与整页竖排默认保持不变。新增 v3.249 role-aware padding 合同并更新 v3.215/v3.240 等价 helper 合同，工程版本为 `3.249`。exact-SHA full [31451224061](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31451224061) 对 SHA `ddea0a13e236e73b61e85e5b63ddae4cdc9ee6be` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；PR #313 [31451898240](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31451898240) 已合入 `smalldata_test`，merge SHA `ff6ed75d0a9f0b1370b8aaf7439e90e3efa8bb49`。固定 `test/jap.jpg` 只作 detector role／crop／runtime/UI 回归，不外推通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。合入后远端仅保留 `main` 与 `smalldata_test`。

v3.248 修复图片 OCR 单块重新识别失败／取消后的 VoiceOver 状态焦点：当前 request/content guard 通过并写入 scoped 失败／取消消息后才递增 failure generation，复查页校验 revision、generation、无 active rerecognition 与终态，再聚焦图片翻译状态行；过期请求、成功 replacement、OCR、翻译、布局、渲染和非图片路径不变。新增 v3.248 合同并接入 CI，工程版本为 `3.248`。本地全量合同、YAML、project plist 与 `git diff --check` 通过；exact-SHA full [31445579893](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31445579893) 对 SHA `7df0b57233a1ce2bdd5d0b573384ba8a7d3d3da2` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；PR #311 [31446307417](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31446307417) 通过并以 merge `a7e5a6f4147b8316d21d8544e8e2b6720759b45c` 合入 `smalldata_test`。固定 `test/jap.jpg` 只作 OCR／UI 回归，不外推通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。receipt 文档合入后远端仅保留 `main` 与 `smalldata_test`。

v3.247 继续收紧 Koharu Manga OCR 后处理与图片 OCR 单块复查焦点：先去空白、归一化省略号、合并 `.`／`・`，再统一 ASCII 标点为全角；单块重新识别成功提交并进入终态后才更新 completion generation，复查页按 revision／generation／block／state 聚焦更新行或新的完成／空态。新增 v3.247 合同并接入 CI，工程版本为 `3.247`。本地全量 `269` 个合同文件通过；单页真实 Core ML runtime 为 `5` 个 vertical blocks，长页为 `17` blocks／`16` vertical、底部 `y=0.940558`，省略号为全角；exact-SHA full [31442768415](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31442768415) 对 SHA `001eb996ea76cf17c9bd715ce20c66a9ec1f8a48` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`。固定 `test/jap.jpg` 只作后处理/runtime/UI 回归，不外推通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。待 PR 合入后远端仅保留 `main` 与 `smalldata_test`。

v3.246 继续对齐 Koharu 的方向感知 crop padding：日语 scoped block retry 横排使用 `.12/.18`、竖排与 unknown 使用 `.18/.12`，恢复 Vision OCR 保留用户方向覆盖；新增 v3.246 合同、横排 Core ML runtime gate，并更新历史 padding/restore 合同。工程版本为 `3.246`，本地全量 `267` 个合同文件通过；exact-SHA full [31440504163](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31440504163) 对 SHA `b05b0ca03c5f2efa3c3cee1575a7c5e1eda4c456` 完成 Xcode、static/UI/Speech/home/paste 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；PR #307 已合入 `smalldata_test`，merge SHA `18194a396893b0a837ed8e30e87979220c140a87`，PR fast [31441269747](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31441269747) 通过。固定 `test/jap.jpg` 只作方向／crop／runtime 回归，不外推通用日语 OCR、翻译或识别质量；当前远端仅保留 `main` 与 `smalldata_test`。

v3.245 让图片日语单块重新识别把当前 block 的 `effectiveSourceDirection` 传到 Manga OCR crop：竖排覆盖请求 `rotate270`，但宽 RT-DETR detector bbox 只有在高宽比超过 `1.75`、明显像 line crop 时才旋转，宽 ownership crop 保持 natural 输入；日语继续复用 font-relative padding，模型／Vision fallback、取消、ownership、整页 OCR、翻译、布局、渲染和非日语边界不变。新增 v3.245 合同与真实 Core ML runtime harness，工程版本为 `3.245`。exact-SHA full [31437080333](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31437080333) 对 SHA `ec80a3efaa5087608ab064acfed022af7609c5ff` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；PR #305 已合入 `smalldata_test`，merge SHA `9f3d6784645902afe8c3ee90998a23c798af96b9`，PR fast [31437769765](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31437769765) 通过。固定 `test/jap.jpg` 只作方向／crop／runtime 回归，不外推通用日语 OCR、翻译或识别质量；当前远端仅保留 `main` 与 `smalldata_test`。

v3.244 让图片日语单块重新识别消费当前 block 的 `effectiveSourceDirection`：竖排覆盖优先 `[270, 90]`，横排覆盖只用 `[0]`，自动方向仍为 `[270, 90, 0]`；整页、非日语、翻译、布局和渲染边界不变。四个 standalone Core ML harness 同步该方向模型契约，新增 v3.244 合同并接入 CI，工程版本为 `3.244`。exact-SHA full [31429739511](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31429739511) 对 SHA `1859d8a3615b502e67fd3a8abc93590f96ae250f` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；PR #303 已合入 `smalldata_test`，merge SHA `d38ac11aff9afae1aba6a67cef856cb547bd1e26`，PR fast [31430655623](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31430655623) 通过。固定 `test/jap.jpg` 只作方向／布局／runtime 回归，不外推通用日语 OCR、翻译或识别质量；当前远端仅保留 `main` 与 `smalldata_test`。

v3.243 为图片 OCR 结果增加来源文字方向覆盖：原始 `sourceDirection` 与用户设置的 `sourceDirectionOverride` 分离，结果行、复查页、局部预览、摘要筛选、竖排渲染、PNG 导出和 VoiceOver 统一使用 `effectiveSourceDirection`。用户可选择自动／横排／竖排；覆盖只更新当前文字块的方向显示与导出，不重跑 OCR、翻译或整页布局。新增 v3.243 静态合同和 executable evaluator，工程版本为 `3.243`。本地合同 `7/7` 通过；exact-SHA full [31426517160](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31426517160) 对 SHA `ef37c1a2c36cbb0792a933868ae4f70bf3af6149` 完成 Xcode、static/UI/Speech/home/paste 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；PR #301 已合入 `smalldata_test`，merge SHA `8675a7dca6fbff27ed5bc1a8725cff967d5637e1`，PR fast [31427377302](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31427377302) 通过。固定 `test/jap.jpg` 只作方向／布局／渲染回归，不外推通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.242 同时对齐 Koharu VerticalRl 与 text-node crop 消费边界：竖排预览与 PNG 导出共享强调符号归并（`!!`/`??`/`!?`/`?!`，含全角输入）、全角标点分类与 Unicode vertical presentation glyph，并在 bounded cell 中居中全角标点；`.replace` 且来源为可信竖排并含 CJK 时才启用，Latin-only、`.adjacent`、横排来源与 OCR/layout/翻译边界不变。图片结果行和局部预览新增单块“重新识别此文字块”：沿用当前 block bbox，日语先走 bundled Manga OCR、失败回退 Vision crop，其它语言走 Vision crop；成功只替换原文／置信度并刷新该块翻译，保留 UUID、geometry、方向与顺序，失败／取消／过期结果保留原 block，不重跑整页 detector、聚类或布局。新增静态合同及可执行 evaluator 并接入 CI，工程版本为 `3.242`。本地 v3.242 定向合同、竖排标点 evaluator 与真实 Manga OCR harness 通过；exact-SHA full [31422608636](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31422608636) 对 SHA `bf9cb74163c4fbccf5c92cfa5f47829ec8bdffa4` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；随后通过 PR #299 合入 `smalldata_test`，merge SHA `5702eaa8349a6d6f9d0e0dd7d10b1fa0aaf36e50`，PR fast [31423431910](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31423431910) 通过。固定 `test/jap.jpg` 只证明既有 runtime／渲染回归，不外推通用日语 OCR、翻译或识别质量；Koharu artifact readiness 为 `manifestMissing / stopUntilArtifactsProvided`。

v3.240 修复 v3.239 后真实产品请求路径的 crop 几何边界：detector Manga OCR 请求现在始终以 RT-DETR `region.rect` 为主 crop，只有 Vision supplement 使用严格门控的 `cropRectHint`；`cropQuadHint` 仍保留为弱 bbox 的内容回退。detector ownership、bbox-first／quad-fallback、batch／单 crop fallback、取消、12／48 预算、Vision fallback、布局、翻译、渲染和非日语路径不变。新增静态合同并接入 CI，工程版本为 `3.240`。本地 `240` 份合同、历史 bbox／quad fallback 合同、Python／shell 语法和 `git diff --check` 通过；exact-SHA full [31410644573](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31410644573) 对 SHA `22ff6b1491aba22b8233bd65811333591cbf74a1` 使用 Xcode `26.6 (17F113)` 完成，JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；随后通过 PR #295 合入 `smalldata_test`，merge SHA `62a6c8ca3ca33c6d5a328677dea1ad76fbb8540c`。固定 `test/jap.jpg` 只证明 crop／ownership 回归，不外推通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.239 对齐 Koharu 当前 Manga OCR 的 bbox-first 边界：扩展 detector TextRegion bbox 是主 crop，严格 line quad 仅在 bbox 缺失、非日文、脚本密度不足、置信度非有限或低于 `0.55` 时补跑，再沿用可靠性、置信度与日文字符数择优。高置信但错投相邻列的 quad 不再偷换 detector owner，弱 bbox 仍可由 quad 恢复；batch／单 crop fallback、取消、12／48 预算、Vision fallback、布局、翻译、渲染和非日语路径不变。新增静态合同与真实 Core ML 相邻列错配 runtime，工程版本为 `3.239`；exact-SHA full [31407753378](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31407753378) 对 SHA `6dc7388a4c1eece3cad4f6e7bc864e0edb245b64` 使用 Xcode `26.6 (17F113)` 完成真实 bbox-primary／bbox→quad fallback、单页／长页 runtime、static/UI/Speech/home/paste 与 JUnit `10/10`，`AITRANS CI/full-validation=success`，probe `skip`；云端 target `爆乳...` 为 `0.99948496`、distractor `生意気...` 为 `0.94102454`，错配后仍返回 target，单页 5 个 vertical blocks，长页 16 blocks／16 vertical、底部 y=`0.940558`。本地长页额外保留横排 `ニコッ`，为 17 blocks／16 vertical。Koharu 当前 `mayocream/manga-ocr` 与随包 kha-white 权重的 `264/264` 个共有 tensor 完全相等，因此不做无收益模型资源替换。固定 `test/jap.jpg` 仅验证 ownership/crop 回归，Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.238 为严格 line-quad Manga OCR 增加内容质量回退：quad 为空、失败、非日文、日文脚本密度 `<0.5`、非有限或低置信时，用 Koharu 的扩展 detector bbox 只重试该项并按可靠性、置信度与日文字符数择优；可靠 quad、bbox-only 请求、取消、ownership、12／48 预算、Vision fallback、布局、翻译、渲染和非日语路径不变。新增静态合同与真实 Core ML blank-quad→bbox runtime，工程版本为 `3.238`。exact-SHA full [31403964016](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31403964016) 对 SHA `12b4f17c864019d88b3128373b4d393dd61ae628` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode build 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 为 `skip`；blank quad 由 detector bbox 以 `0.99948084` 置信度恢复 `今度こそこの爆乳を持ち帰る！`，单页保持 5 个 vertical blocks，四页长图保持 16 个 vertical blocks、底部 y=`0.940558`。固定 `test/jap.jpg` 仅验证该回退和既有单页／长页回归，Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.237 修复日语图片 OCR detector 取消传播边界：`recognizeJapaneseMangaOCR` 不再吞掉 RT-DETR detector 的 `CancellationError`；取消会终止当前有界 OCR 批次，普通 detector 错误仍回退 Vision，且错误分支会再次检查任务取消状态。detector ownership、Manga OCR crop／batch fallback、12／48 请求预算、Vision fallback、布局、翻译、渲染和非日语路径不变。新增 `scripts/test-v3237-image-japanese-detector-cancellation-contract.py` 并接入 CI，工程版本为 `3.237`；v3.216 历史 detector 合同同步接受新的取消安全 fallback。候选 exact-SHA full [31398074816](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31398074816) 对 SHA `c7c5fa7383b15c2c1167feccc63a56c33641e2a9` 使用 Xcode `26.6 (17F113)` 完成 Xcode build 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。本地单页保持 5 个 vertical blocks，四页长图保持 17 blocks／16 vertical／底部 y=`0.940558`。本版本只收紧取消与回退控制流，不声称固定 `test/jap.jpg` 的通用日语 OCR、翻译或识别质量提升。

v3.231 继续沿 Koharu `TextRegion + line_polygon -> crop -> OCR` 收紧 detector-owned 日语竖排 crop：RT-DETR 的 `rect` 仍是布局与 ownership 几何，只有 Vision 至少两个字符框形成的竖排 envelope 同时满足 overlap `>=0.80`、detector coverage `>=0.55`、candidate coverage `>=0.80`、面积比 `0.35...1.05`、横向 coverage `>=0.45`、纵向 coverage `>=0.85` 且宽度至少收窄 `10%` 时，才作为 Manga OCR 的 crop-only hint；失败、缺失或取消安全回退 detector bbox，不改变 detector ownership、预算、Vision fallback、布局、翻译、渲染和非日语路径。新增 `scripts/test-v3231-image-japanese-detector-tight-crop-hint-contract.py` 并接入 CI，版本为 `3.231`。本地 `249` 份合同和单页／长页真实 Core ML runtime 通过；exact-SHA full [31372501552](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31372501552) 对 SHA `bb579e97f9ded42d7b0352dcccc5592194610e20` 完成 Xcode `26.6 (17F113)`、UI/Speech/home/paste、JUnit `10/10` 与 `AITRANS CI/full-validation=success`，probe `skip`。固定 fixture 只证明 crop 几何与既有 runtime 边界，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。v3.157/v3.158 merge 已在祖先链，本地／origin 活动分支和 stale ref 已清理。

v3.230 修复 batch Manga OCR runtime harness 的 provenance 解析：`batchInference=true`、`blocks=5` 等元数据不会再被误计为 block，只验证方向记录；旧 run [31368735253](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31368735253) 已显示模型实际加载且五个单页结果均为 vertical，旧 parser 的误报由新增 v3.230 合同锁定。exact-SHA full [31370139122](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31370139122) 对 SHA `064810987125742b31ff0e5cc422d35b576a37f3` 完成单页／长页真实 batch runtime、Xcode 与 JUnit `10/10`（0 failures），四页长图 16 个 blocks 全为 vertical，最底 y=`0.940558`，probe `skip`。该版本不声称日语 OCR 准确率提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.229 修复 v3.228 云端真实 runtime 暴露的 flexible-batch encoder 图错误：ViT CLS token 不再以动态 `tile(reps)` 扩展，改为动态 shape `fill` 广播；encoder 的 1…4 batch 使用 `EnumeratedShapes` 专化，decoder 保留动态 sequence 与 legacy 单 crop 回退。v3.228 云端 Xcode build 已通过，但 batch encoder runtime 因 `tile` shape inference 回退 Vision，run [31366707811](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31366707811) 的 UI contract 正确失败；新增 `scripts/test-v3229-image-japanese-batch-model-shape-contract.py`，新的 Core ML runtime 尚待验证。该版本只修复执行边界，不声称日语 OCR 准确率提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。v3.157/v3.158 merge 已在祖先链，旧分支已清理。

v3.228 将 v3.227 的 flexible-batch Manga OCR 从可选接口推进为实际随包资源：encoder／decoder 均为 1…4 flexible batch，runtime 仍保留单 crop 模型作为兼容回退；单页和长页 harness 会明确校验 batch 模型已加载，坏 batch 仍逐 crop 隔离回退。新增 `scripts/test-v3228-image-japanese-bundled-batch-runtime-contract.py`，云端 Core ML 编译与真实 runtime 尚待当前提交验证。该版本只证明执行路径和性能资源可用，不声称日语 OCR 准确率提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.227 继续向 Koharu 的批量 Manga OCR 迁移：转换脚本现在可生成上限为 4 的 flexible-batch encoder／decoder，Swift runtime 仅在成对的可选 `MangaOCREncoderINT8Batch`／`MangaOCRDecoderINT8Batch` 资源存在时启用，batch 失败会按 crop 隔离回退，当前已入库的 batch=1 模型行为不变。竖排覆盖预览按真实行列容量截断并追加省略号，与 PNG 导出保持一致；补齐已被 Xcode 引用但缺失的 `AITRANS/Resources/MangaOCR/conversion.json`。新增 `scripts/test-v3227-image-japanese-batch-preview-contract.py`；本地合同通过后再等待云端 Xcode／Core ML full validation。该版本只证明批量执行边界、预览可见性与转换 provenance，不声称日语 OCR 准确率提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.226 收紧 Koharu detector TextRegion ownership 的质量边界：bundled Manga OCR 结果只有在 confidence 有限且 `>=0.55`、日文脚本密度 `>=0.5` 时才继续作为受保护 detector owner；弱结果仍保留为普通候选并允许 page-level Vision fallback 参与，避免低质量文本压制更可靠的页面识别。模型加载、单 crop 故障隔离、取消传播、长页预算、布局、翻译、渲染与非日语路径不变。新增 `scripts/test-v3226-image-japanese-manga-ocr-quality-gate-contract.py`，更新 v3.219/v3.221/v3.223 历史合同接受等价质量门；本地完整 `226` 份合同全部通过，真实单页／四页长图 Core ML runtime、`plutil`、workflow YAML 与 `git diff --check` 通过。候选 exact-SHA full [31361469086](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31361469086) 对 SHA `a2258f8de4205e7cbb99ca059bd30307ffba96b6` 使用 Xcode `26.6 (17F113)` 完成，JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`；probe 按配置为 `skip`。Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，固定 `test/jap.jpg` 只证明当前 ownership 回归，不外推通用日语 OCR、翻译或识别质量。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 已在祖先链，本地／远端候选分支均已清理。

v3.223 分离 Koharu detector TextRegion 的 ownership provenance 与日语竖排方向提示：bundled Manga OCR 成功结果现在使用独立 `detectorTextRegion` role，同时保留 detector 没有提供 `source_direction` 时所需的 `.vertical` hint；detector-owned supplement 过滤不再误把所有结果限定为 `.verticalLine`。横排与竖排 layout 都保护两个 detector TextRegion 不被通用 clustering 合并，模型失败、取消、Vision fallback、翻译、渲染、非日语路径与既有 detector ownership 边界不变。新增 `scripts/test-v3223-image-japanese-detector-direction-provenance-contract.py`，并更新 v3.214/v3.221 历史合同。单页真实 Core ML runtime 继续精确返回 5 个竖排块；四页 `1136x6400` 长页继续返回 17 blocks、16 vertical，底部覆盖保留，未重叠的 `ニコッ` 横排噪声保留。当前本地定向合同、真实 Core ML runtime、`plutil`、Python/YAML 语法与 `git diff --check` 通过；本机只有 CommandLineTools，云端 full [31356368877](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31356368877) 对 SHA `13864d12154b77f86eb177127d77fc1a25eed321` 使用 Xcode 26.6 完成，static/UI/Speech/home/paste 与 JUnit `10/10` 均通过并发布 `AITRANS CI/full-validation=success`，probe `skip`。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 仍在当前祖先链，本地／远端均无活动候选分支或待 cherry-pick；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称通用日语 OCR、翻译或识别质量提升。

v3.222 修复图片翻译最后一个 scoped block retry 完成后的 VoiceOver 焦点丢失：单块重试不再依赖不会变化的 `imageTranslationRevision`，Store 只在所有空译文块都完成时递增独立 completion generation，View 在当前内容 revision、generation 和 `.translated` 状态仍有效时复用既有终态结果焦点；部分失败、取消、过期结果、OCR、日语批翻译协议、导出重绘和非图片路径不变。新增 `scripts/test-v3222-image-translation-block-retry-focus-contract.py`，并让旧 v3.221 合同接受后续版本。实现提交 `5fbccb16fd4db4489ac4bb054f0422ea0c591af9` 已推送到 `smalldata_test`；exact-SHA full [31354629914](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31354629914) 完成 Xcode、静态／UI／speech 合同与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`。本地定向 v3.3112、v3.3115、v3.3153、v3.3210、v3.3220、v3.3221、v3.3222 合同及 `plutil`／YAML／`git diff --check` 通过；本轮只改善重试完成后的辅助功能焦点，不声称日语 OCR、翻译或识别质量提升，Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.221 对齐 Koharu 的 detector-owned OCR 消费边界：成功 bundled Manga OCR 的 vertical TextRegion 以最小面积覆盖 `>= 0.60` 作为 owner，日语最终 layout 不再保留与其重叠的 page／旋转 Vision duplicate；未重叠的真实横排音效继续保留，模型失败时完整回退历史 Vision 路径。`test/jap.jpg` 单页仍精确返回 5 个竖排块；四页 `1136x6400` 长图本地为 17 blocks／16 vertical，云端 Core ML runner 为 16 blocks／16 vertical，长页 gate 接受 `>=16` 且每季度至少 4 个 vertical，`やがってま` 重叠噪声被拒绝，本地 `ニコッ` 保留，四份 `爆乳` 与 `挨拶` 均通过。新增 v3.221 detector-owned Vision noise contract 与几何 runtime gate；固定样图只证明该 ownership 路径改善，不外推通用日语 OCR、翻译或识别质量，Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。已合入 `smalldata_test`（merge `159ef9f1d30f24842056d6d6a66c24098e03afcc`）；full [31353229779](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31353229779) 对 SHA `6ba4797feac679d5dbce19602e4b966bfec03e2e` 的 Xcode/JUnit `10/10` 成功，probe `skip`。

v3.220 对齐 Koharu `ImageSlicer -> TextRegion -> crop/OCR` 的长页分辨率边界：普通图片继续使用 1800px 有界 thumbnail，显示高宽比超过 `3.5` 的长页改为按显示宽度（最多 `1800px`）和总像素（最多 `16,000,000`）计算 thumbnail 上限，`test/jap.jpg` 四次拼接的 `1136x6400` 页面不再先缩成约 `320px` 宽后才切片；bundled detector TextRegion 的扩展 bbox 也不再被 Vision supplement 的 ownership bisector 截断。长页真实产品 runtime 由 v3.219 的 `撮乳`／`城乳`／`授乳` 与 `技拶` 改为四份均为 `爆乳`、`挨拶`，本地输出 `18` blocks、云端输出 `19` blocks，两者均为 `16` vertical、`0` unknown；弱横排 Vision 噪声随运行环境变化，未把它们误报为目标文字。单页继续精确返回 5 个竖排块，detector-only 长页继续返回 17 regions。本地 v3.157-v3.220 共 `70` 份合同脚本／`373` tests、三条真实 Core ML runtime、generic Simulator build 和语法检查通过；exact-SHA full [31350115177](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31350115177) 对 SHA `51e779ac81ec1706c4a846dc44ab55ba0d14f161` 完成三条真实 runtime、Xcode 与 JUnit `10/10`（0 failures），full-validation status `success`，probe `skip`。固定重复样图只证明长页分辨率路径的实测改善，不外推为通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

候选 metadata [31350695078](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31350695078) 对 docs SHA `f8cb059827fc639e0fe8cf340dc14f93ab15027a` 复用实现 full；PR #284 fast [31350751298](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31350751298) 与 merge fast [31350811748](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31350811748) 均复用成功 receipt，merge SHA `e878ada9ab11e747a361d482cefe61c9dfdebcdb`；后三者 JUnit `10/10`、Xcode skipped，不是新的编译证据，v3.220 功能分支已清理。

v3.219 对齐 Koharu 的 detector TextRegion OCR 后边界：bundled Manga OCR 结果携带独立 TextRegion provenance，日语去重在选择 Manga OCR 或 Vision 文本时都保留该标记，竖排 layout 不再把两个受保护 detector nodes 串成一个块；普通 Vision 行碎片仍按历史规则聚合。4 份 `jap.jpg` 的真实完整产品长页从 v3.218 本地的 15 blocks／13 vertical 改为本地 18 blocks、云端 17 blocks，两者均为 16 vertical／0 unknown，四个季度各保留 4 个竖排块，4 份页头与页尾均独立存在，`お願いします前は...` 跨页串联由 3 次归零；单页仍精确为 5 块，detector-only 长页仍为 17 regions。本地 v3.157-v3.219 共 `63` 份合同／`328` tests、三条真实 Core ML runtime、generic Simulator build 和语法检查通过；exact-SHA full [31348116248](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31348116248) 对 SHA `f90f48a6a98c8d4d3ca722f9d1abf993bae633a1` 完成三条 runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`。固定重复样图在本地仍有 2 个、云端 1 个弱横排 Vision 噪声和 Manga OCR 错字，不据此声称通用质量；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

候选 metadata [31348550765](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31348550765) 对 docs SHA `d7ccbe2f07f9878bafaa59905348d83cc4ced057` 复用实现 full；PR #283 fast [31348617791](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31348617791) 与 merge fast [31348684301](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31348684301) 均复用成功 receipt，merge SHA `6c6bb7ae2b300d0934fd30d7a170979ba66d390f`；后三者 JUnit `10/10`、Xcode skipped，不是新的编译证据。

v3.218 让长图 Manga OCR 预算跟随 v3.217 detector slice：普通图仍为 12 次，长图按每片 12 次扩展且 CPU-only 总上限 48；专用 detector regions 先按纵向 band 公平取样，Vision supplement 后补，避免页面上部高 confidence 候选独占预算。4 份 `jap.jpg` 的完整产品链路本地返回 15 块、云端返回 14 块，两者都从 v3.217 的 11 个 vertical／2 个 unknown 改善为 13 个 vertical／0 个 unknown，四个纵向季度各至少 3 个 vertical，最底结果 y 约 `0.943`；普通单页继续精确返回 5 块。本地 v3.157-v3.218 共 `62` 份合同／`321` tests、三条真实 runtime、generic Simulator build 和语法检查通过；exact-SHA full [31346445800](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31346445800) 对 SHA `0a275445fcbe3ce3c86e14be0567ab4204251f06` 完成三条 runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。本地／云端的总块数差异来自弱横排 Vision 噪声；48 次上限仍是 iOS 对 Koharu 全量 TextRegion OCR 的有界差异，不据重复样图声称通用质量。

候选 metadata [31346910257](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31346910257) 对 docs SHA `80ad7e136f9ab16392259b604a8bf12a1fbb5924` 复用实现 full；PR #282 fast [31346989809](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31346989809) 与 merge fast [31347051715](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31347051715) 均复用成功 receipt，merge SHA `e19c1187c8b9f79b2d97ce2f4418b2f37539cb37`；后三者 JUnit `10/10`、Xcode skipped，不是新的编译证据。

v3.217 补齐 Koharu comic detector 的长图 `ImageSlicer`：页面高宽比超过 `3.5` 时按目标比 `3.0`、20% 重叠切片，短尾并入末片；每片运行真实 Core ML detector，映射回整页后按 Koharu containment／IoU／相邻片规则去重。4 份 `jap.jpg` 拼成的 `1136x6400` 长页稳定得到 17 个区域，四个纵向季度均有覆盖；普通单页仍保持 5 个完整竖排块。本地 v3.157-v3.217 共 `61` 份合同／`314` tests、两条真实 runtime、generic Simulator build 和语法检查通过；exact-SHA full [31323013519](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31323013519) 对 SHA `8c037482e175818bb5c3f3714c5a3992fbf70644` 完成两条 runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；不据重复固定样图声称通用质量。

候选 metadata [31323359308](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31323359308) 对 docs SHA `30990b6c78671c27448aafed23e59652688740d3` 复用实现 full；PR #281 fast [31323414600](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31323414600) 与 merge fast [31323579377](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31323579377) 均复用成功 receipt，merge SHA `32e7b30c1b3f069a405c2d1b1aaaceb8b2660442`；后三者 Xcode skipped，不是新的编译证据。

v3.216 内置 Koharu 默认的真实 comic text detector：固定 `ogkalu/comic-text-and-bubble-detector` revision，将 RT-DETR-v2 R50 转为约 `43 MB` 的 iOS 17 Core ML INT8 权重，由 CPU-only actor runtime 缓存并复刻 top-300 sigmoid、`0.30` threshold、text label 过滤及 TextRegion 合并。专用 detector geometry 优先驱动既有 Manga OCR，Vision detector 只补充未覆盖区域，合计仍最多 12 个请求；模型失败完整回退旧路径。`test/jap.jpg` 从 v3.215 的 12 个碎片收敛为 5 个完整竖排块，并补回 `やがって...`；本地 v3.157-v3.216 共 `60` 份合同／`307` tests、真实 Core ML runtime、generic Simulator Xcode build 和语法检查通过；exact-SHA full [31321620928](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31321620928) 对 SHA `e25bf798323dbbabe154c1849436711d90ab8888` 完成真实 runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；只声明固定样图改善，不外推通用质量。

候选 metadata [31322010365](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31322010365) 对 docs SHA `fb9385645ed8524bf0a6dc58df22d7064153d1be` 复用实现 full；PR #280 fast [31322080575](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31322080575) 与 merge fast [31322150176](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31322150176) 复用成功 receipt，merge SHA `f0a85c2b3f60d5d37652dc76c83aa8703692a245`；后三者 Xcode skipped，不是新的编译证据。

v3.215 收紧真实 Manga OCR 的相邻竖列 ownership：glyph-sized Vision 字符框以中位中心／宽度替代跨列 outlier envelope；2–4 字偏下列只有在相邻列几何与字符支持同时成立时才向上补齐，水平 padding 则在相邻列 bisector 截止且不裁 detector core。`test/jap.jpg` 保持 12 个块，但把 v3.214 的跨列／截断结果恢复为 `今度こそ`、`この爆乳を`、`そのせいで`、`つまんねー女に`、`お願いします`；仍有漏列与错字，不外推通用质量。本地 v3.157–v3.215 共 `59` 份合同／`299` tests、真实 Core ML runtime 和 generic Simulator Xcode build 通过；exact-SHA full [31317279248](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31317279248) 对 SHA `a9433f2649c22388aa54a72530c2c8db1404fcdd` 完成真实 runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`。

候选 metadata [31317671420](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31317671420) 对 docs SHA `2e047f835f1aaedb91dc4c648be3579b67c8d776` 复用实现 full；PR #279 fast [31317731083](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31317731083) 与 merge fast [31317789538](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31317789538) 均复用成功 receipt，merge SHA `83111349937a5163ea1aad5402df55b194972dc2`；后三者 Xcode skipped，不是新的编译证据。

v3.214 内置 Koharu 同源的专用 Manga OCR Core ML 路径：固定 `kha-white/manga-ocr-base` revision 转为 iOS 17 ViT encoder／BERT decoder，decoder 保持 FP32、常量权重 INT8 后约 `106 MB`，由 actor 缓存的 CPU-only runtime 直接复用 Vision 已完成 EXIF 校正的输入图，对 pixel-first TextRegion 执行 224×224 灰度输入和最多 300 token greedy decode；模型结果先进入日语 `verticalLine` 去重／布局，原 Vision line/block/tile 全部保留为失败回退。真实 `test/jap.jpg` 产品链路从 v3.213 的 3 个弱块提升到 12 个竖排块，并读出 `前は生意気に`、`持ち帰る`、`監督より挨拶を`；固定样图仍存在错字与跨列误读，不据此声称通用 corpus 质量。本地 v3.157–v3.214 共 `58` 份合同／`293` tests、真实 Core ML runtime 和 Simulator Xcode build 通过；exact-SHA full [31315520990](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31315520990) 对 SHA `0a92317fae8a9e6dae89fe5592dec27b94995000` 完成真实 runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`。

候选 metadata [31316016491](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31316016491) 对 docs SHA `02c3ea0632298878baffdfbec4af1fe8d9660f69` 复用实现 full；PR #278 fast [31316067683](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31316067683) 与 merge fast [31316119282](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31316119282) 均复用成功候选 receipt，merge SHA `ad7a265abfa620e93ea345429d14558ed61536be`；后三者 Xcode skipped，不是新的编译证据。

v3.213 对齐 Koharu `TextRegion.line_polygons` 的紧行几何边界：普通图片日语 pixel-first detector 对 90°／270° 旋转侦察开启 Vision character boxes，映射并合成至少两个有效字符框的紧 envelope；只有 envelope 通过竖排形状、宽框 overlap、面积与横／纵覆盖门控时才用于 crop，否则安全回退宽 request box。新增 `scripts/test-v3213-image-japanese-koharu-character-envelope-contract.py`，并兼容 v3.208 历史合同；本地 v3.157–v3.213 共 `57` 份合同／`286` tests 和 Simulator Xcode build 通过，v3.157/v3.158 已合入且无活动分支；exact-SHA full [31310411961](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31310411961) 对 SHA `2ba635fd6e39f24954d8e731cbdfb59dc83b9900` 完成 Xcode/JUnit `10/10`，probe `skip`；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

候选 metadata [31310728862](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31310728862) 对 docs SHA `bf0ed503ad534870f33ef17f30d6eeca06c71c97` 复用实现 full；PR #277 fast [31310775114](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31310775114) 与 merge fast [31310809139](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31310809139) 均复用成功候选收据，merge SHA `6ef85fce62d24a1f668a60484369dd24774caf59`；后三者 Xcode skipped，不是新的编译证据。

v3.212 对齐 Koharu `MangaOcr::inference` 的直接解码边界：普通图片日语 block／line／perspective line crop reread 关闭 Vision `usesLanguageCorrection`，避免局部日语文字在进入既有 Koharu 风格后处理前被语言改写；页级 reconnaissance 与非日语路径继续使用默认 correction。请求 helper 显式保留 `true` 默认，仅由受限 crop caller opt out，并兼容 v3.156 历史合同。新增 `scripts/test-v3212-image-japanese-koharu-raw-crop-recognition-contract.py`；本地 v3.157-v3.212 共 `56/56` 个合同通过，v3.157/v3.158 已合入当前基线、无活动分支；exact-SHA full [31302657064](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31302657064) 对 SHA `bd7c510b99ac78c22ca330ae2e125a5193610fe4` 完成 Xcode/JUnit `10/10`，probe `skip`；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

候选 metadata [31309651292](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31309651292) 对 docs SHA `174a7180d229381b41c5184e1525a2ad7651baa3` 复用 `bd7c510b99ac78c22ca330ae2e125a5193610fe4 / success`；PR #276 fast [31309712340](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31309712340) 与 merge fast [31309783552](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31309783552) 均复用候选成功 receipt，merge SHA `ccab9e318b0c71447b59cb2b370d5778a9c68904`；后三者 Xcode skipped，不是新的编译证据。

v3.211 将普通图片日语竖排 line/block/tile reread 的 Vision 语言集合收敛为 `ja-JP`/`ja`，保留页级 90°／270° reconnaissance 的 `ja-JP`/`ja`/`en-US`/`en` 混合集合，避免窄竖排被邻近拉丁候选抢占；不支持日语语言 profile 时 language-specific reread 安全返回空结果，页级路径继续可用。新增 `scripts/test-v3211-image-japanese-vertical-language-gate-contract.py`；exact-SHA full [31300669764](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31300669764) 对 SHA `c0c26aaddb3db641c59cb878d1434207b9879f54` 完成 Xcode/JUnit `10/10`，probe `skip`；readiness 保持 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.210 改善图片翻译失败后的恢复体验：保留已识别文字块、几何和已完成译文，在 `.failed`／可恢复的 `.translated` 状态下只允许空译文块单独重试；日语块继续复用 `[N]` 漫画批翻译协议，非日语复用单块翻译，绝不重新调用 Vision OCR。retry/content ID 防止过期结果写回，部分失败继续保留其它块，全部译文完成后才恢复 `.translated` 并重绘导出；结果行、局部预览和 VoiceOver 均提供受门控的重试 action。新增 `scripts/test-v3210-image-translation-block-retry-contract.py`，本地 v3.157–v3.210 共 54 个合同通过；exact-SHA full [31299660925](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31299660925) 对 SHA `b9ec296d0cdafbe0bfbbe0aebc90e1255a44d6d2` 完成 Xcode/JUnit `10/10`（0 failures）；候选 metadata [31300023503](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31300023503) 对 docs SHA `0dc5b019e231aa35137de705a6a2b8e0a377d029` 复用成功 full，PR #274 fast [31300071078](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31300071078) 与 merge fast [31300107560](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31300107560) 均复用候选 full，merge SHA `ee7e41f0679fd999b8f9337a3bf0622742e095c3`，后续 fast Xcode skipped，不是新的编译证据；v3.157/v3.158 已合入当前基线，无活动分支；probe `skip`，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.209 将普通图片日语竖排 OCR 调度收敛为 line-first：先消费 `verticalLine` line reread，pixel detector 与 tile fallback 只处理可靠 line 结果未覆盖的区域，block crop 最后兜底；可靠 coverage 需要非空文本、`confidence >= 0.48`、日语脚本密度 `>= 0.5` 和有效紧 geometry，缺失 geometry 安全回退宽 `rect`，pixel detector 保留 `verticalLine` provenance，tile fallback 保持历史 `.crop` provenance。新增 `scripts/test-v3209-image-japanese-koharu-line-first-dispatch-contract.py`；本地 v3.157–v3.209 共 53 个日语合同通过；exact-SHA full [31297254547](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31297254547) 对 SHA `17f19bb2505d504e1255ab925d2aa7572020435a` 完成 Xcode/JUnit `10/10`；PR #273 fast [31297894114](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31297894114) 与 merge fast [31297941634](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31297941634) 均复用候选 full，merge SHA `4e2b8fdceec51359bb923bd583687fa2e3ed9e24`；后三者 Xcode skipped，不是新的编译证据；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针 skip，不声称 OCR、翻译或识别质量提升。

v3.208 在普通图片日语竖排 OCR 中增加受限 pixel-first detector 代理：系统 `VNDetectTextRectanglesRequest` 对 90°／270° 旋转图发现未被现有 vertical block 覆盖的文字矩形，最多 12 个候选进入既有 grayscale／有界放大／crop／Vision OCR，弱结果最多 4 次反方向复读；不加载真实 Koharu 模型，不读取探针或 ground truth，本地 v3.157–v3.208 共 52 个日语合同通过；exact-SHA full [31295791350](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31295791350) 对 SHA `41eff6cb86073900332d9785eea32606a5688dce` 完成 Xcode/JUnit `10/10`；PR #272 fast [31296131671](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31296131671) 与 merge fast [31296221789](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31296221789) 均复用候选 full，merge SHA `7c8642af855fcbf79cfad7a3a9052a5465d83632`；后三者 Xcode skipped，不是新的编译证据。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.207 收紧 Koharu line-first OCR 的覆盖判定：普通图片日语竖排 block 只有每个 source line 都由独立 `verticalLine` reread 通过 `confidence >= 0.48`、日语脚本密度 `>= 0.5` 且多 glyph line 不被单 glyph 结果冒充时才跳过 block crop；弱结果继续走 block fallback，避免竖排漏字。本地 v3.157–v3.207 共 51 个合同通过；候选 exact-SHA full [31294146132](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31294146132)（`f7a18e4c008903fd50f15183fa3b9a8629216bef`）Xcode/JUnit `10/10`、0 failures；候选 metadata [31294486910](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31294486910)、PR #271 fast [31294542723](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31294542723) 与 merge fast [31294614418](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31294614418) 复用候选 full，merge SHA `15577b7a184f9b6a8d281f1551367b1c72231a65`；fast 不是新的 Xcode 编译证据。探针 skip，真实 Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译或识别质量提升。

v3.206 对齐 Koharu `sort_manga_reading_order` 的行级无切分回退：普通图片日语竖排 observation 在 Recursive XY-cut 无有效切分时按 `4 × min_gap_y` 行桶从上到下、同一行从右到左稳定排序，避免重叠列按全局 x 优先造成跨行交错。新增 `scripts/test-v3206-image-japanese-koharu-observation-row-fallback-contract.py`；v3.205 及 v3.157/v3.158 合同继续回归。实现 full [31293120347](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31293120347)（`5aefe027e97f37aa209c09d2a9d33ecf0a1d848c`）Xcode/JUnit `10/10`；PR #270 fast [31293135057](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31293135057) 与 merge fast [31293388944](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31293388944) 复用成功 full，merge SHA `9513cd7c9d33610f0b93a4e435f9e3f1867328bb`，后两者不是新的编译证据。v3.157/v3.158 已在当前基线，不存在未合入活动分支；探针 skip，真实 Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.205 修复 Koharu line-first OCR 的完整覆盖边界：普通图片日语竖排 block 只有每个 source line 都被独立有效的 `verticalLine` reread 覆盖时才跳过 block crop；部分成功、合成结果、噪声或缺少紧 geometry 时继续回退 block crop，避免多行文字丢失。新增 `scripts/test-v3205-image-japanese-koharu-line-coverage-fallback-contract.py`。实现 exact-SHA full [31292332659](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31292332659)（`7891cfeaf3486eb6a507d1b2045a9b662b8c66ca`）Xcode/JUnit `10/10`；探针 skip，真实 Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.204 对齐 Koharu `extract_text_block_regions` 的 line-first OCR：普通图片日语竖排 block 有有效 line polygon proxy 时先跑 line perspective／轴对齐 reread，只有 line OCR 无可用结果才回退 block crop；v3.203 tight ownership、方向 provenance、fallback 与预算边界保持不变。新增 `scripts/test-v3204-image-japanese-koharu-line-first-ocr-contract.py`，并继续回归 v3.157/v3.158 合同。实现 exact-SHA full [31290525270](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31290525270)（`ae922bda0bf566cc14d422a6d9c9a4c042b34218`）Xcode/JUnit `10/10`；候选 metadata [31290904290](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31290904290)（`5e288e4fb477ecbc6489e71b19415ebade6b8137`）复用实现 full，PR #268 fast [31290942391](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31290942391) 与 merge fast [31290981606](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31290981606) 均复用成功 receipt，merge SHA `b65653bc577ba65c51ed6cc1c2bd373b00e76aec`；后三者 Xcode skipped，不是新的编译证据。探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.203 对齐 Koharu `TextRegion` 的 line ownership：日语竖排 line candidate、碎片合成与 block line envelope 在宽 request-level overlap 之外，若存在有效 `lineRegionRect` 必须与当前 block 相交；缺失／非法 geometry 回退宽 `rect`。新增 `scripts/test-v3203-image-japanese-koharu-line-block-ownership-contract.py`。实现 exact-SHA full [31287319601](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31287319601)（`01fdaf16dde9029079231eb7c5406042fcab8cfc`）Xcode/JUnit `10/10`；候选 metadata [31287648270](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31287648270)（`cc3d24f68dfb4b391ba019d2549a1084381d1305`）、PR #267 fast [31287677451](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31287677451) 与 merge fast [31287707581](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31287707581) 均复用成功 receipt，merge SHA `295c59a72a886cbc19cd9c3126d9e162ce525afd`；后三者 Xcode skipped，不是新的编译证据。探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.202 对齐 Koharu `extract_text_block_regions` 的紧 line-region gate：普通图片日语竖排 line candidate 的几何门控优先使用 `lineRegionRect`，缺失时回退 `rect`；与 block 的 overlap 仍用宽 request-level box，布局／去重边界不变。新增 `scripts/test-v3202-image-japanese-koharu-tight-line-candidate-gate-contract.py`，并兼容 v3.160 历史合同。实现 exact-SHA full [31286506178](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31286506178)（`2f198f9f12e62c5e10fe7a73b76cdc0af9d69107`）Xcode/JUnit `10/10`；候选 metadata [31286843872](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31286843872)（`a2ddc0819ecc9d92c6cbcb1d21d9473d8d2b1a94`）复用实现 full，PR #266 fast [31286875844](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31286875844) 与 merge fast [31286908329](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31286908329) 复用成功 receipt，merge SHA `839c5e9d9f5705f50c85d65e7085157c5b876b07`；后三者不是新的编译证据。探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.201 对齐 Koharu `warp_line_region` 的竖排 line orientation provenance：普通图片日语 line reread 继续携带 `verticalLine` 角色，使原图映射、合成与最终去重保留 `rotate270` 主方向；page、block、tile 保持历史方向与 fallback 边界。新增 `scripts/test-v3201-image-japanese-koharu-line-orientation-provenance-contract.py`。实现 full [31262554391](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31262554391)（`97120cf38d85729f605e3d8bdbc836a0271e1c99`）Xcode/JUnit `10/10`；候选 metadata [31262979444](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31262979444)、PR #265 fast [31263017146](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31263017146)、merge fast [31263072991](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31263072991) 均通过并复用成功 receipt，merge SHA `3aac10326a987a47ea1786cd76c37b96b3ca9b36`，后两者不是新的编译证据。探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.200 对齐 Koharu `expanded_text_block_crop_bounds` 的字体锚点：日语竖排 block 的 line-region union envelope 仍按原始 `block.rect` 最小边计算字体 padding，再扩展 envelope，避免多行合并后过度扩边；缺失 geometry 回退旧 crop，其他语言、line/tile、翻译与探针路径不变。新增 `scripts/test-v3200-image-japanese-koharu-font-anchor-contract.py`。实现 exact-SHA full [31260969111](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31260969111)（`c91858868cace78b846bd742f69a95f765b5737a`）Xcode/JUnit `10/10`；候选 metadata [31261967648](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31261967648)、PR #264 fast [31261881073](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31261881073)、merge fast [31261995539](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31261995539) 均通过并复用成功 receipt，后两者不是新的编译证据。探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.199 对齐 Koharu `crop_text_block_bbox` 的 block line envelope：普通图片日语竖排 block crop 先把相关有效 `lineRegionRect` 并入原 block envelope，再执行既有方向感知 padding；不缩小原 block，缺失或非法 geometry 安全回退旧 crop。新增 `scripts/test-v3199-image-japanese-koharu-block-line-envelope-contract.py`，并兼容 v3.175/v3.178 历史合同。候选 exact-SHA full [31260137161](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31260137161)、PR #263 fast [31260466644](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31260466644)、merge fast [31260501796](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31260501796) 均通过；候选 SHA `b85f12389eddf1cd82d8aa2892fe5f730d4b8a56` Xcode/JUnit `10/10`，merge SHA `20d5987c245bf6768257e61d6ee030cf2324aef0` 复用候选 full，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.198 对齐 Koharu `warp_line_region` 的竖排 line 方向：普通图片日语 perspective 与轴对齐 line reread 统一以 `rotate270`（270°）作为主读取方向，弱／空结果仍在既有 12 次页级预算内回退 90°；block、tile、非日语、翻译、探针、metrics 与 output 路径不变。新增 `scripts/test-v3198-image-japanese-koharu-vertical-line-orientation-contract.py`。候选 exact-SHA full [31259014271](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31259014271)、PR #262 fast [31259242739](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31259242739)、merge fast [31259272510](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31259272510) 均通过；候选 SHA `24c8f9c23f3bec3e6536ad4752e99a4703ff07f4` Xcode/JUnit `10/10`，merge SHA `0b7352be9393e17fc55224269604dfc6859d2351` 复用候选 full，PR/merge fast 不是新的编译证据，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.197 对齐 Koharu `pp_doclayout_v3` 的竖排检测门控：普通图片日语 block crop 放宽到 `height / width >= 1.15`、方向置信度 `>= 0.25`、归一化高度 `>= 0.035`，保留原有标准／紧凑门控、最多 16 个 block、方向 fallback 与安全回退；非日语和其他 OCR／翻译路径不变。新增 `scripts/test-v3197-image-japanese-koharu-vertical-threshold-contract.py`。候选 exact-SHA full [31258318641](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31258318641)、PR #261 fast [31258329662](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31258329662)、merge fast [31258606990](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31258606990) 均通过；候选 SHA `e099acb375cf799d9f3d26b203eead6505b9b65c` Xcode/JUnit `10/10`，merge SHA `bd91910ccfe38b78d2827095b7e2970c1347d881` 复用候选 full，PR fast 重跑后记录 `reusedFullValidationState=success`，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.196 将 Koharu `translate_texts` 的编号文本块协议迁入普通图片日语翻译：OCR blocks 按全局 `[N]` 标签分成最多 8 块／约 1800 字符的有界批次，保留漫画角色语气、情绪、拟声词与块顺序；批输出缺 tag、乱序或空块时回退逐块翻译，非日语图片与修正 sheet 不变。新增 `scripts/test-v3196-image-japanese-koharu-batch-translation-contract.py`。候选 exact-SHA full [31257482066](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31257482066)、PR #260 fast [31257709942](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31257709942)、merge fast [31257752177](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31257752177) 均通过；候选 SHA `4282395bd982c2c6fcb92058b3cdfb8409defd01` Xcode/JUnit `10/10`，merge SHA `dd007a40705903bcc92800f30a905ead44810503` 复用候选 full，PR/merge fast 不是新的编译证据，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.195 对齐 Koharu `sort_manga_reading_order`：日语漫画阅读模式把横排与竖排 block 合并后统一递归 XY-cut，横向切分右侧优先、纵向切分顶部优先；无切分时按 `4 × min_gap_y` 行桶右到左回退，非日语保留原交错路径。新增 `scripts/test-v3195-image-japanese-mixed-layout-reading-order-contract.py`。候选 exact-SHA full [31233606259](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31233606259)、PR #259 fast [31233872614](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31233872614)、merge fast [31234023270](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31234023270) 均通过；候选 SHA `52963a4c1cbe16f8662a99ae443aea36f1dbb486` Xcode/JUnit `10/10`，merge SHA `12b0217c7c56a2993986f4a42ba6e6f98c7df6a2` 复用候选 full，PR/merge fast 不是新的编译证据，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.194 继续对齐 Koharu `merge_slice_regions`：日语 OCR 的双方紧 `lineRegionRect` 在 containment-like overlap `>= 0.85` 或 IoU `>= 0.50` 时按几何重复合并；宽 request-level box、缺少任一紧区域与非日语路径仍走文本相似度门控。新增 `scripts/test-v3194-image-japanese-tight-region-iou-dedupe-contract.py`。候选 exact-SHA full [31232966715](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31232966715)、PR #258 fast [31233259741](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31233259741)、merge fast [31233297104](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31233297104) 均通过；候选 SHA `e4388409785bc60278b819e36e8b4afdc7ec7307` Xcode/JUnit `10/10`，merge SHA `5ce315332aa6ab073e3e9733f174cbf2ccac0121` 复用候选 full，PR/merge fast 不是新的编译证据，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.193 对齐 Koharu `merge_slice_regions` 的 containment-first 去重：日语 OCR 候选只有在双方都带 `lineRegionRect` 且紧几何 minimum-area overlap `>= 0.85` 时才可绕过文字相似度直接合并；宽 request-level box、缺少任一紧区域与非日语路径继续走原文本门控，避免相邻竖排误合并。新增 `scripts/test-v3193-image-japanese-tight-region-dedupe-contract.py`。候选 exact-SHA full [31232333781](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31232333781)、PR #257 fast [31232570686](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31232570686)、merge fast [31232612519](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31232612519) 均通过；候选 SHA `a645943b85345790309adde433344a53209bbccc` Xcode/JUnit `10/10`，merge SHA `c00ffc306a193351179e86b7fd7ad0783bd1ad4b` 复用候选 full，PR/merge fast 不是新的编译证据，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.192 在 Koharu `ImageSlicer` 局部窗口内按漫画阅读方向调度日语竖排 fallback：窗口先按 x 右到左，再在每列内按 y 上到下；最多 18 个有效窗口、覆盖抑制、方向 fallback、过滤、映射与去重边界不变。新增 `scripts/test-v3192-image-japanese-manga-window-order-contract.py`。候选 exact-SHA full [31231339154](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31231339154)、PR #256 fast [31231689829](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31231689829)、merge fast [31231720315](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31231720315) 均通过；候选 SHA `d4a7811b634df7f300fb60a99e331a27f69287bd` Xcode/JUnit `10/10`，merge SHA `2aae0d7ff0e5394bed6a0c13edbb3a2624f67c1a` 复用候选 full，PR/merge fast 不是新的编译证据，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.191 将 Koharu `ImageSlicer` 的目标比例 `3.0`、纵向 overlap `0.20` 与尾片阈值 `0.70` 迁入日语竖排 reconnaissance；每个横向窄条的局部窗口保证覆盖底边，总有效窗口最多 18 个，既有 OCR 方向 fallback、过滤、映射与去重不变。新增 `scripts/test-v3191-image-japanese-koharu-slicer-contract.py`。最终 exact-SHA full [31230729061](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31230729061)、PR #255 fast [31231091935](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31231091935)、merge fast [31231128313](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31231128313) 均通过；最终 SHA `32c350c6ce4c40d90dd1d9505a830c083a62ce49` Xcode/JUnit `10/10`，merge SHA `16cce70a48afda648f045edf9d97f9a1191890cd` 复用候选 full，PR/merge fast 不是新的编译证据，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.190 让日语竖排漏列侦察更接近 Koharu 的局部 TextBox→crop/OCR：原全高窄条拆成约 58% 页面高度、18% 纵向重叠并强制覆盖底边的局部窗口，最多执行 12 个有效窗口，既有 4 次方向 fallback、覆盖抑制、日语过滤、原图映射与去重不变。新增 `scripts/test-v3190-image-japanese-localized-tile-window-contract.py`。候选 exact-SHA full [31229567448](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31229567448)、PR #254 fast [31229946643](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31229946643)、merge fast [31229977158](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31229977158) 均通过；候选 SHA `44e0030aa1914468fceedfc5404eaf7510e9673b` Xcode/JUnit `10/10`，merge SHA `ec8ad6333005527042fd7f13f8f203bfa36fafe5` 复用候选 full，PR/merge fast 不是新的编译证据，探针 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.189 对齐 Koharu 的 `TextBox.source_direction` 单字边界：日语竖排 block／line／tile crop reread 映射回原图后，只要仍有 CJK 文本，即使 Vision 只返回一个 glyph 也保留 `sourceDirectionHint=.vertical`，避免被 `wideBox` 横排回退误判；日语 manga-order、页级 observation、普通语言与横排路径保持原门控。新增 `scripts/test-v3189-image-japanese-single-glyph-direction-hint-contract.py`。候选 exact-SHA full [31228731388](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31228731388)、PR #253 fast [31229051329](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31229051329)、merge fast [31229096792](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31229096792) 均通过；候选 SHA `8c89b0c0d1c0dff91efd3c115583ab5f04d14017` Xcode/JUnit `10/10`，merge SHA `6a510efece0b7ef6deeb9f1c3dd19c11f625ba1a` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.188 对齐 Koharu 的“同一右到左文字列内自上而下”读取顺序：普通图片日语竖排 observation 合并成 `Cluster` 后显式按 `rect.y` 上到下、同 y 时右侧优先，并以几何／文字／confidence 做确定性 tie-breaker 后拼接，避免 crop/tile 到达顺序造成乱序；横排换行与同列合并门控不变。新增 `scripts/test-v3188-image-japanese-vertical-cluster-reading-order-contract.py`。候选 exact-SHA full [31227754288](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31227754288)、PR #252 fast [31228156354](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31228156354)、merge fast [31228206021](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31228206021) 均通过；候选 SHA `1580a5c145efede4fefa23fd743c8170e077b1f9` Xcode/JUnit `10/10`，merge SHA `bbba2eee0a31a3bfd8b82508c77caac3677e1810` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.187 对齐 Koharu 的 `TextBox.source_direction` provenance：普通图片日语 block／line／tile crop reread 映射回原图后继续携带受限 `sourceDirectionHint=.vertical`，日语 manga-order 布局先尊重该 hint，再决定是否落入 `wideBox` 横排回退；页级 observation、普通语言与非日语路径保持几何边界。新增 `scripts/test-v3187-image-japanese-crop-direction-hint-contract.py`。候选 exact-SHA full [31226671116](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31226671116)、PR #251 fast [31226855629](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31226855629)（重跑后复用 full）、merge fast [31227082382](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31227082382) 均通过；候选 SHA `881d2b4fe49d8adde181d8e33d210a38515fcdd8` Xcode/JUnit `10/10`，merge SHA `c1bb36ba956e5a48b583ff1e2a2214e3dcee916c` 复用候选 full，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.186 收敛 v3.185 全高 tile fallback 的候选边界：日语漏列 reconnaissance 结果先经过日语脚本密度与竖排几何过滤，高竖框保留，只有一／二字紧凑 CJK 片段在受限尺寸下保留；过滤后的结果才决定是否消耗 270° fallback 预算，避免 tile 内横排日语噪声进入最终布局。新增 `scripts/test-v3186-image-japanese-vertical-tile-filter-contract.py`。候选 exact-SHA full [31225584307](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31225584307)、PR #250 fast [31225981653](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31225981653)、merge fast [31226027759](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31226027759) 均通过；候选 SHA `86cf3fd289b4d42c18658d48d5c93308e4f5f91c` Xcode/JUnit `10/10`，merge SHA `5bb34b44d0c93ab93d816a848266f65c95ad9d6c` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.185 对齐 Koharu 在 detector 未产出整列 TextBox 时的恢复边界：普通图片日语 OCR 在既有竖排 block crop 前增加最多 6 个带 18% 重叠的全高 tile reconnaissance，跳过已被竖排 block 覆盖的 tile；未覆盖 tile 复用日语灰度／放大、90° Vision crop pass，最多 4 次弱结果 270° fallback，再交回原图映射、日语去重与布局。新增 `scripts/test-v3185-image-japanese-vertical-tile-fallback-contract.py`。候选 exact-SHA full [31224644168](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31224644168)、PR #249 fast [31225019712](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31225019712)、merge fast [31225064534](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31225064534) 均通过；候选 SHA `0c0585a850f4a0a7a4fc4a5735c791439713c4c2` Xcode/JUnit `10/10`，merge SHA `ccd46c169ae7447d4f0485e4d982277f8ba33e46` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.184 对齐 Koharu 的 line-region 去重边界：普通图片日语 OCR 融合与竖排 line candidate 去重在双方都有 Vision 字符范围 `lineRegionRect` 时使用紧几何，缺少任一 hint 时回退 request-level `rect`，普通语言保持原 request-box 路径；新增 `scripts/test-v3184-image-japanese-line-geometry-dedupe-contract.py`。候选 exact-SHA full [31223348790](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31223348790)、PR #248 fast [31223808151](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31223808151)、merge fast [31223883384](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31223883384) 均通过；候选 SHA `1bc212b0cf5c8190a9aa9746fb44c0f03ae638dd` Xcode/JUnit `10/10`，merge SHA `211dab273bf9b0830b411f0586938ba77c93b46d` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.183 对齐 Koharu `warp_line_region` 的目标画布几何：日语竖排 perspective line 的四点 quad 先计算长短轴、`textHeight` 与 `ratio`，再把 Core Image 自然 warp 有界重采样到 `(textHeight, textHeight × ratio)`，边长最多 4096、面积最多 4M；几何不可用或重采样失败回退自然 warp。保留 24 line／16M warp 像素预算、灰度／放大、方向 fallback、映射与去重，普通语言及翻译渲染路径不变。新增 `scripts/test-v3183-image-japanese-line-warp-target-contract.py`。候选 exact-SHA full [31221970026](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31221970026)、PR #247 fast [31222386728](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31222386728)、merge fast [31222451794](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31222451794) 均通过；候选 SHA `6c6c040b095aadff18eea5f9f518ce50551fa8f7` Xcode/JUnit `10/10`，merge SHA `b6410f032d36e5f9205e15327107ca8069589c20` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.182 修正 v3.172 合成日语竖排 line 的 axis 路径：满足短日语片段、同列与 bounded gap 的 `synthesizeJapaneseVerticalLineCandidates` 现在只替代被其 `lineRegionRect` 重叠比 `>= 0.72` 覆盖的原始 axis 候选；原始候选仍保留在最多 24 条 perspective quad 路径，弱／失败 perspective 的方向 fallback、16M warp 像素预算、灰度／放大、映射与去重边界不变。仅作用于普通图片日语 line reread，其他语言、block crop、布局、翻译、渲染、Store、探针和 Koharu gate 保持边界。新增 `scripts/test-v3182-image-japanese-synthetic-line-replacement-contract.py`，并让 v3.172/v3.173 历史合同接受等价 `axisSeeds` helper。候选 exact-SHA full [31220601488](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31220601488)、PR #246 fast [31221025113](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31221025113)、merge fast [31221074919](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31221074919) 均通过；候选 SHA `42ad3abf8deba67d11b4fd3a93b16a1f09756657` Xcode/JUnit `10/10`，merge SHA `fb97ce105e5ae2d8fdda3d2acae631c86513be02` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.181 对齐 Koharu `warp_line_region` 的“成功的 line region 不再重复轴对齐读取”边界：普通图片日语竖排 perspective line reread 只有在 perspective 结果成功且不需要方向 fallback 时，才记录已覆盖的 line 候选；与其重叠比达到 `0.72` 的轴对齐候选跳过重复 Vision OCR，弱／失败 perspective 仍保留轴对齐与方向 fallback。最多 24 个 line 候选、16M warp 像素预算、灰度／放大、映射与去重保持不变。仅作用于普通图片日语 line reread，其他语言、block crop、布局、翻译、渲染、Store、探针和失败回退保持边界。新增 `scripts/test-v3181-image-japanese-line-path-dedupe-contract.py`。候选 exact-SHA full [31218314967](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31218314967)、PR #245 fast [31218876431](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31218876431)、merge fast [31218932836](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31218932836) 均通过；候选 SHA `e24ce08b798b1f205a4d812e626d27ba801db1de` Xcode/JUnit `10/10`，merge SHA `1107998858e3879750cba8dc8a27248ad1497589` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.180 对齐 Koharu `warp_line_region` 的局部几何边界：日语竖排 perspective line crop 先按四点 line polygon 的 bbox 裁剪源图，再把四点平移到局部坐标执行 `CIPerspectiveCorrection`，避免整页无关像素进入小文字行的透视复读；保留最多 24 个 line 候选、16M 像素预算、灰度／放大、方向 fallback 与原图映射。仅作用于普通图片日语 perspective line reread，其他语言、轴对齐 crop、布局、翻译、渲染和失败回退不变。新增 `scripts/test-v3180-image-japanese-line-warp-bbox-contract.py`。候选 exact-SHA full [31217320435](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31217320435)、PR #244 fast [31217749775](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31217749775)、merge fast [31217813652](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31217813652) 均通过；候选 SHA `eb522b28c1e9649278342f227aaef03995d67a41` Xcode/JUnit `10/10`，merge SHA `54b4cf750615efe54962f4247c72003d6d04f761` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.179 对齐 `reference/koharu-main/koharu-ml/src/manga_ocr/mod.rs` 的日语 `post_process` 顺序：先移除空白、统一省略号并压缩连续 `.`／`・`，再对压缩结果执行 ASCII 到全角映射，避免压缩点号留在半角形态。仅作用于普通图片日语 OCR 候选与既有 crop／line reread，普通语言、布局、翻译、渲染和失败回退不变。新增 `scripts/test-v3179-image-japanese-koharu-postprocess-order-contract.py`。候选 exact-SHA full [31216151856](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31216151856)、PR #243 fast [31216723888](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31216723888)、merge fast [31216783591](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31216783591) 均通过；候选 SHA `c16e5593ef63113e2d3ba5ef1b72d7a09ee2396a` Xcode/JUnit `10/10`，merge SHA `5f3c0aa1f45d9cee9774db4d0020370666b69273` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.178 让 v3.177 标记的紧凑日语竖排文字块进入既有 Koharu 风格 block crop／OCR 复读：标准高竖框保留 `aspectRatio >= 1.45`、`height >= 0.04`，仅 `cjkCompactColumnTextRun` 原因的紧凑日语块受限放宽到 `aspectRatio >= 1.20`、`height >= 0.022`，总预算仍为最多 16 个块并继续沿用 `crop_text_block_bbox` 对齐的裁剪、预处理、方向 fallback、映射与去重。新增 `scripts/test-v3178-image-japanese-compact-block-crop-contract.py`。候选 exact-SHA full [31214729647](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31214729647)、PR #242 fast [31215410769](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31215410769)、merge fast [31215485897](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31215485897) 均通过；候选 SHA `ec80c63d1b0d25903f0d462a020dec6bca768f94` Xcode/JUnit `10/10`，merge SHA `4f6aeca133c9684c6800ec795a9f0fac4f24fdca` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.177 补齐紧凑日语竖排文字列的方向判定：日语 manga-order 将偏好传入布局方向解析，只有 CJK 多字、框体较窄高、存在列邻居且不存在横排行邻居时才进入 `cjkCompactColumnTextRun`，从而可继续走 Koharu 风格 crop／line reread；其他语言和既有方向门控不变。新增 `scripts/test-v3177-image-japanese-compact-vertical-direction-contract.py`。候选 exact-SHA full [31213076831](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31213076831)、PR #241 fast [31213569259](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31213569259)、merge fast [31213642909](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31213642909) 均通过；候选 SHA `9777d167cca71deb753f5d0f721f6c2f9f2af48f` Xcode/JUnit `10/10`，merge SHA `f2c8a33ba66666a69a941d24fb5d8d78284b1695` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.176 修正日语竖排 perspective line crop 的拆分文字顺序：90° pass 按旋转图 x 轴正序、270° pass 按 x 轴逆序，同一位置以 y 和日语评分作稳定 tie-breaker，贴近 Koharu 的 `rotate270` line crop 语义。新增 `scripts/test-v3176-image-japanese-line-reading-order-contract.py`。候选 exact-SHA full [31211585649](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31211585649)、PR #240 fast [31212154910](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31212154910)、merge fast [31212217877](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31212217877) 均通过；候选 SHA `6a61068f292e4e842b570a455eb357bd5b9a7c40` Xcode/JUnit `10/10`，merge SHA `eaa523f4d29f8be9e7e2f16131bbc21a9363706f` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.175 按 Koharu `detected_font_size_px` 推导日语竖排 crop padding：从源图片像素的 block/line 尺寸计算字体大小，应用 8% base、18% 水平、12% 垂直 padding（最小 2px），映射回归一化坐标并保留安全 fallback；仅作用于日语 block/line reread。新增 `scripts/test-v3175-image-japanese-font-size-padding-contract.py`。候选 exact-SHA full [31210073265](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31210073265)、PR #239 fast [31210705708](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31210705708)、merge fast [31210782269](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31210782269) 均通过；候选 SHA `e47014bb6cc68ec70029b3000d0b84c0156fe21e` Xcode/JUnit `10/10`，merge SHA `7b7a57b4d091fc3bd10305a6997e9dd24fba42ba` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.174 延续 Koharu 的“先形成文字块再交给 crop/OCR”边界：日语竖排高而窄的 Vision line box 在同列／重叠门控下加入有界平均高度 gap 信号，避免 line-region reread 前过早拆列；非日语、横排和 OCR 内容路径不变。新增 `scripts/test-v3174-image-japanese-vertical-cluster-gap-contract.py`。候选 exact-SHA full [31208462786](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31208462786)、PR #238 fast [31209161098](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31209161098)、merge fast [31209248983](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31209248983) 均通过；候选 SHA `49b987b3765e0df0c0511e30f955aa6aa7f487bf` Xcode/JUnit `10/10`，merge SHA `5efc690d0f8c3b41282518a8bc76d12559efa114` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.173 继续按 Koharu 的语言专用识别边界改进普通图片日语 OCR：日语最终去重、竖排 block/line 候选与弱方向 fallback 使用有界脚本／标点证据，普通语言保留原评分；新增 `scripts/test-v3173-image-japanese-observation-fusion-contract.py`。候选 full [31206796785](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31206796785)、PR #237 fast [31207387731](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31207387731)、merge fast [31207465845](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31207465845) 均通过；候选 SHA `d86f875d1040d69259b62b52754c73be3ccb59dd` Xcode/JUnit `10/10`，merge SHA `3fe6e719e064fe261f97530a7f16ff3b39ea4903` 复用候选 full，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.172 将 Koharu 的“文本块先拆成 line-region 再 OCR”边界迁入普通图片日语竖排：对既有竖排 block 内近方形短日语片段按列中心和垂直连续性合成 line crop，最多 24 条，并以合成候选替换被覆盖的轴对齐碎片复读；原始 quad 仍走 perspective path。新增 `scripts/test-v3172-image-japanese-vertical-fragment-line-contract.py`。候选 full [31204989011](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31204989011)、PR #236 fast [31205608084](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31205608084)、merge fast [31205688629](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31205688629) 均通过；候选 SHA `c2e7edd13818c9c46b65d1aa318e4c91c3479c09` Xcode/JUnit `10/10`，merge SHA `fab5ddb6d9ebdaa3d4a9dd34bfcdf6c0f676c84c` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.171 将 Koharu 风格 crop 预处理统一到普通图片日语竖排 line reread：轴对齐与透视 line crop 都先灰度化，再在最多 4M 像素内优先 2× 放大；轴对齐路径保留实际 `cropScale` 回映射，透视路径把放大后的像素计入每页 16M 上限。新增 `scripts/test-v3171-image-japanese-line-crop-preprocess-contract.py`，并同步 v3.160/v3.162/v3.169 历史合同。候选 full [31203452238](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31203452238)、PR #235 fast [31204110506](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31204110506)、merge fast [31204194868](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31204194868) 均通过；候选 SHA `9968f3083f9b19e9401dd9b48d9e35a480c99e9b` Xcode/JUnit `10/10`，merge SHA `df0145f9a2cb5dc5ea4ffdd515042f84cb8de108` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.170 将 Koharu Manga OCR crop 预处理的模型无关部分迁入日语竖排文字块 reread：局部 crop 先灰度化，再在最多 4M 像素内优先 2× 放大；`cropScale` 继续用于旋转框回映射，Core Image 或放大失败安全回退。只影响日语 block crop，普通语言、整页 OCR、line／perspective 路径不变。新增 `scripts/test-v3170-image-japanese-crop-preprocess-contract.py`。候选 full [31201978062](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31201978062)、PR #234 fast [31202618966](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31202618966)、merge fast [31202690968](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31202690968) 均通过；候选 SHA `0b2f011398457e410b366d1c10d80a902eecd173` Xcode/JUnit `10/10`，merge SHA `536b21f83670220ea5364b70badfe375a0df355c` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.169 为日语竖排文字块与 line crop 增加受预算保护的反方向复读：先走当前 90°／270°，仅在结果为空、脚本密度低或置信度弱时改走 opposite orientation；文字块／line 页级预算分别为 8／12，统一 helper 保持后处理、坐标映射、去重与原有翻译渲染边界。新增 `scripts/test-v3169-image-japanese-crop-orientation-fallback-contract.py`。候选 full [31200276655](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31200276655)、PR #233 fast [31200973375](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31200973375)、merge fast [31201060977](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31201060977) 均通过；候选 SHA `bbe47bd89e4413580482b07e52799867c844ec64` Xcode/JUnit `10/10`，merge SHA `a2ad829bf519a2f5cec02d82cc6d7b40168c2d62` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.168 将 Koharu `post_process` 的日语识别后处理边界迁入普通图片 OCR：去空白、统一省略号与点号／中点串、ASCII 全角化；在置信度窗口内融合最多 5 个 Vision 候选，非日语保持 top-1，所有日语旋转／crop／line／perspective 复读共用 helper。新增 `scripts/test-v3168-image-japanese-ocr-postprocess-contract.py`。候选 full [31197172635](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31197172635)、PR #232 fast [31197811891](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31197811891)、merge fast [31197884476](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31197884476) 均通过；候选 SHA `9438e3d40ffb133073921fc4f4a0e1de36cc042d` Xcode/JUnit `10/10`，merge SHA `033d66b5c62434e5685b1e8d7d1feebdfa90c15e` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.167 让日语／横排 OCR 的行分组容差按当前 observation 的中位文字框高度动态计算（`median(height) * 0.55`，限制 `0.012...0.04`），兼顾不同缩放／字体的同一行聚合与相邻面板分离；RTL/LTR 排序及 OCR、翻译、导出路径不变。新增 `scripts/test-v3167-image-horizontal-band-dynamic-tolerance-contract.py`。候选 full [31195627325](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31195627325)、PR #231 fast [31196179149](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31196179149)、merge fast [31196269343](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31196269343) 均通过；候选 SHA `6c63dd0a5170a0fb230046d7d2129b26fd8dbb4d` Xcode/JUnit `10/10`，merge SHA `a0f1e72ea36be0932dae75fe774af1186ed29b1c` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.166 让日语竖排常见 CJK 标点与半角片假名保留为列方向证据：`cjkCharacterCount` 新增 `U+3000–U+303F`、`U+FF61–U+FF9F`，短 observation 仍须通过列邻居、无横排行邻居和尺寸门控；非竖排路径不变。新增 `scripts/test-v3166-image-japanese-punctuation-column-contract.py`。候选 full [31193812409](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31193812409)、PR #230 fast [31194473761](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31194473761)、merge fast [31194535297](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31194535297) 均通过；候选 SHA `8c6dfe278a9644dd0dc37ffa5381a968dc7748c7` Xcode/JUnit `10/10`，merge SHA `0acfd7f62c9ecbe048c7630d0358c85dce325edb` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.165 让允许竖排的 CJK OCR 在 Vision 将日语单字拆成近方形 observation 时保留列证据：短 CJK observation 只有在列邻居存在、横排行邻居不存在且尺寸达到下限时才进入竖排方向与既有局部 crop reread；宽框横排、孤立高框、横排碎片和非竖排语言不变。新增 `scripts/test-v3165-image-japanese-glyph-column-contract.py`。候选 full [31192480905](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31192480905)、PR #229 fast [31193220150](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31193220150)、merge fast [31193292477](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31193292477) 均通过；候选 SHA `5f24c4b7d2de47a095ee15b19994087ebde4dff7` Xcode/JUnit `10/10`，merge SHA `631c4d25acacb6b0497e8c95dab41f9a22e6c266` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.164 让日语源图片的混合版面横排 OCR 沿用 Koharu 漫画方向：`ImageOCRLayoutEngine.layout` 增加默认关闭的 `prefersMangaReadingOrder`，日语 Vision 主路径与 crop 路径开启后保持横排行内上到下、同一行右到左；其他语言继续走原有左到右默认。新增 `scripts/test-v3164-image-japanese-horizontal-reading-order-contract.py`。候选 full [31190984866](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31190984866)、PR #228 fast [31191645282](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31191645282)、merge fast [31191716497](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31191716497) 均通过；候选 SHA `7e584045f12fefa995866b7479db4cd440d52a03` Xcode/JUnit `10/10`，merge SHA `3943843d61f331630f7c6764f5639273aea4bd90` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.163 继续按 Koharu `sort_manga_reading_order` 的 Recursive XY-Cut 改进普通图片日语竖排布局：以文字块中位宽／高中位数计算动态空白阈值，递归按最大横／纵间隙切分，右侧面板优先、竖排列自上而下，无法切分时使用稳定回退；范围只在 `ImageOCRLayoutEngine` reading order，不改变 OCR、翻译或探针。新增 `scripts/test-v3163-image-japanese-reading-order-contract.py`。候选 full [31189049773](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31189049773)、PR #227 fast [31189799793](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31189799793)、merge fast [31189875449](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31189875449) 均通过；候选 SHA `c37808634df8d87cfb9f24c22acadc472f71d3c0` Xcode/JUnit `10/10`，merge SHA `e93f3844c359214c0cdfe09cd609fb11c51b924d` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.162 继续按 Koharu `extract_text_block_regions` 迁移普通图片日语竖排 OCR：Vision 字符范围的四角 geometry 与 request-level box 分离，最多 24 条 line crop 使用 Core Image `CIPerspectiveCorrection` 后 2× 复读，单条 4M／总计 16M 像素受限，失败回退既有轴对齐 crop，整页／局部旋转映射保持布局 box 稳定。新增 `scripts/test-v3162-image-japanese-line-perspective-ocr-contract.py`。候选 full [31186264941](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31186264941)、PR #226 fast [31186901253](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31186901253)、merge fast [31186979637](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31186979637) 均通过；候选 SHA `8a8e653f953c233f5b0d28249bb9b324ef0baab3` Xcode/JUnit `10/10`，merge SHA `b1c272b9fea90e07967e21db082538be50c8b516` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.161 继续按 Koharu 的 line-region 几何边界改进普通图片日语竖排 OCR：读取 Vision `VNRecognizedText.boundingBox(for:)` 的整段字符范围作为更紧的 crop hint，保留 request-level box 作为布局／去重几何，并让该 hint 穿过 90°／270° 与 2× crop 坐标回映射；Vision 不提供范围几何时回退原框。新增 `scripts/test-v3161-image-japanese-line-geometry-contract.py`。候选 exact-SHA full [31184241208](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31184241208)、PR #225 fast [31184939184](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31184939184)、merge fast [31185021159](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31185021159) 均通过；候选 SHA `9164066706faed78494384d79ec1544d46084c20` Xcode/JUnit `10/10`，merge SHA `1eb026c2ce3df861b6efcc7ae9d61a5d08fd86ea` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.160 继续按 Koharu 的 `extract_text_block_regions` 分层改进普通图片日语竖排 OCR：从既有竖排 block 中按重叠与纵向候选筛选最多 24 个 line-region proxy，方向感知扩边后用 2× crop 复读（`minimumTextHeight=0.002`、关闭自动语言），再按缩放比例映射回原图并去重。由于当前没有 line polygons，这仍是保守的 Vision line-region 过渡层，不是 Manga OCR/PaddleOCR 模型替换；新增 `scripts/test-v3160-image-japanese-line-region-ocr-contract.py`。候选 exact-SHA full [31182335743](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31182335743)、PR #224 fast [31183007517](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31183007517)、merge fast [31183084173](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31183084173) 均通过；候选 SHA `68c1c1b8eb1390b808204c3c16b7b1dbc72a28b9` Xcode/JUnit `10/10`，merge SHA `19b018101a4937474e2f3b030a1e24dc58807704` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.159 让普通图片日语源语言的 OCR 识别状态按语言分流：Vision OCR 进行中明确说明正在识别日语文字并复查竖排方向与文字块位置，其他语言保留通用文案；状态继续复用既有 `imageTranslationMessage`，图片状态行与 VoiceOver 空态上下文同步。新增 `scripts/test-v3159-image-japanese-ocr-status-context-contract.py`。候选 exact-SHA full [31180141884](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31180141884)、PR #223 fast [31180615748](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31180615748)、merge fast [31180708039](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31180708039) 均通过；候选 SHA `f30fbab503ff9c694af0d4f2c123113b1802648d` Xcode/JUnit `10/10`，merge SHA `9c68b5c9f7e5e5d341a3cfaec1f764964b71b9f0` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.158 继续按 Koharu 的 `TextBoxes → crop_text_block_bbox → OCR` 分层，在普通图片日语竖排候选上增加最多 16 个文字块的 Vision 裁剪复读：按 90°／270° 方向读取、映射回原图、再去重并交给既有布局；新增 `scripts/test-v3158-image-japanese-crop-ocr-contract.py`。候选 exact-SHA full [31178774530](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31178774530)、PR #222 fast [31179342519](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31179342519)、merge fast [31179390133](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31179390133) 均通过；候选 SHA `ee21c07d5175b38b41161822043b7ce1bbeea3ff` Xcode/JUnit `10/10`，merge SHA `c940815a43e300685667d8b01888e53af910ec9c` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.157 在 v3.156 的日语方向复查上补齐受限 90°／270° 双向比较：两次 Vision 结果都映射回原图、统一去重，再交给既有日语竖排／右到左布局；新增 `scripts/test-v3157-image-japanese-bidirectional-orientation-ocr-contract.py`。候选 exact-SHA full [31177442783](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31177442783)、PR #221 fast [31177914749](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31177914749)、merge fast [31177971252](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31177971252) 均通过；候选 SHA `894c7063e18a6dc40ea047dca015e7cf73af8e65` Xcode/JUnit `10/10`，merge SHA `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.156 参考 `reference/koharu-main` 的检测／布局与识别分层，在普通图片日语 OCR 中加入受限的 90° 方向复查：使用 `ja-JP/ja/en-US/en` profile、较低文字高度门槛，映射旋转框回原图后去重并交给既有日语竖排／右到左布局；新增 `scripts/test-v3156-image-japanese-orientation-ocr-contract.py` 与 `test/jap.jpg` fixture。候选 exact-SHA full [31176163879](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31176163879)、PR #220 fast [31176662793](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31176662793)、merge fast [31176739499](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31176739499) 均通过；候选 SHA `99a333a8297faf193c8058d7f919626bb17daf80` Xcode/JUnit `10/10`，merge SHA `7750c7e62b82cb952ab302b9afd206ecf15068dd` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.155 让普通图片在没有可显示 OCR 文字块、但当前图片仍可重试且没有待重试语言变更时，在结果空态提供就地“重试当前图片”按钮与同名 VoiceOver action；两者直接复用既有 `store.retryImageTranslation`，当前重试语言已更新时保留上方状态入口，避免重复 action。hint 明确使用当前图片语言重新识别并翻译，源图片不可用或处于其他阶段时不暴露入口。新增 `scripts/test-v3155-image-empty-result-retry-action-contract.py` 并接入 UI/full fail-fast；候选 exact-SHA full [31173412868](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31173412868)、PR #219 fast [31173840102](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31173840102)、merge fast [31173897707](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31173897707) 均通过；候选 SHA `a6283b1be84ec4e6b227b6d5fbf74961a4fd108f` Xcode/JUnit `10/10` 成功，merge SHA `2c26886ee6676c549b88ad48b0447e595c636a40` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。

v3.154 让普通图片“没有可显示识别结果”空态的可见标题与说明随图片处理状态变化：idle、读取／OCR／翻译进行中、translated 与 failed 分别说明当前阶段和恢复边界；保留既有 VoiceOver label/value/hint、受 `store.canRerunImageRecognition` 门控的“重新识别” action 与可见按钮。新增 `scripts/test-v3154-image-empty-result-state-contract.py`，并同步 v3.133/v3.152/v3.153 历史合同接受 View-only 动态标题／说明；候选 exact-SHA full [31171837188](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31171837188)、PR #218 fast [31172320096](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31172320096)、merge fast [31172393014](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31172393014) 均通过；候选 SHA `11028f3de4886aad18e911dd8dc3f60e6593ba9f` Xcode/JUnit `10/10` 成功，merge SHA `b51ab8a880f3a1998a5a4e249e6c7113e0a3c451` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。

v3.153 让普通图片翻译已完成且没有可显示 OCR 文字块时的结果空态获得稳定 VoiceOver 焦点 identity：保留源图片且处于 `.translated` 时聚焦可操作的空态，优先级仍是全部忽略空态，其次是翻译完成空态，否则回到图片状态行；焦点请求继续受 `imageTranslationRevision` 与 View 私有 generation guard 约束。新增 `scripts/test-v3153-image-empty-result-focus-contract.py`；候选 exact-SHA full [31170387940](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31170387940)、PR #217 fast [31170963538](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31170963538)、merge fast [31171022668](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31171022668) 均通过；候选 SHA `6c838ef220470753cb6abf4867babc48a6ea795c` Xcode/JUnit `10/10` 成功，merge SHA `a938b8b73803e0570e0ec9bb8e6ec354e3cf85b0` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。

v3.152 让普通图片在翻译已完成但没有可显示 OCR 文字块时，在结果空态提供可见“重新识别”按钮；仅在既有 `store.canRerunImageRecognition` 为真时显示，直接复用 `store.rerunImageRecognition`，VoiceOver 继续沿用 v3.144 同名 action 与“只重跑当前图片 Vision OCR 与翻译”的范围提示。新增 `scripts/test-v3152-image-empty-result-rerun-button-contract.py` 并接入 UI/full fail-fast；候选 exact-SHA full [31167004721](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31167004721)、PR #216 fast [31170006883](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31170006883)、merge fast [31170055419](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31170055419) 均通过；候选 SHA `0c08bfda4548b996a2e3bad86d2adde950276378` Xcode/JUnit `10/10` 成功，merge SHA `29b42e3d839b5d9f225658fb24e4f88e4ec4d69d` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。

v3.151 让 Developer Console 的漫画探针在报告没有逐块文字块时提供就地“重新运行漫画覆盖翻译探针”按钮，并在探针未运行时向 VoiceOver 暴露同名 action；运行中按钮保持 disabled 且不暴露只会被 guard 拒绝的 action。入口复用既有 `store.runMangaOverlayProbe`，hint 明确会重新读取 `test/1.png`、清理 Output 并只更新漫画探针诊断，不改变普通图片 OCR、翻译或覆盖图。新增 `scripts/test-v3151-manga-probe-empty-retry-action-contract.py`，并让 v3.56 历史状态合同接受受门控的第二入口；候选 exact-SHA full [31165387991](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31165387991)、PR #215 fast [31165964091](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31165964091)、merge fast [31166051842](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31166051842) 均通过；候选 SHA `8579bdabc8f0dbbeafe19b4804cfedcea9dfbe04` Xcode/JUnit `10/10` 成功，merge SHA `79f92aad2a12e682eba609a1f49814025e9920a7` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。

v3.150 让普通图片人工修正后的文字块在局部放大预览中提供可见“恢复 Vision OCR”按钮与同名 VoiceOver action；仅在 `isManuallyCorrected && canEdit` 时暴露，直接复用面板现有确认入口，锁定时保留具体禁用 hint，不新增 OCR／Store 管线。新增 `scripts/test-v3150-image-focus-restore-action-contract.py`；候选 exact-SHA full [31163470178](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31163470178)、PR #214 fast [31164127307](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31164127307)、merge fast [31164207376](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31164207376) 均通过；候选 SHA `04cef3c01b802627366587dc1a3c76eddc534e3f` Xcode/JUnit `10/10` 成功，merge SHA `b52dabc245f0d140115074b37c34975ce0b743c9` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.149 让普通图片“本次复查完成”空态的 VoiceOver“重新复查” action 与 `canReviewImageTranslation` 同步：可复查时直接复用既有 `restartReviewQueue()`，翻译未完成或导出重绘锁定时保留稳定完成上下文、具体禁用 hint 和可见按钮边界，但不向 VoiceOver 暴露只会被 guard 拒绝的 action。新增 `scripts/test-v3149-image-review-completion-action-gate-contract.py`，并让 v3.128/v3.129/v3.148 历史合同接受同一 View-only helper 形式；候选 exact-SHA full [31161816278](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31161816278)、PR #213 fast [31162344568](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31162344568)、merge fast [31162426726](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31162426726) 均通过；候选 SHA `b0fc332c565fe501c8e2e939a086b79c142c9853` Xcode/JUnit `10/10` 成功，merge SHA `137d8d61a4c6b10d4126b3ae07e9163edbf07878` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.148 修正普通图片全部 OCR 文字块被忽略时的 VoiceOver 空态边界：只有 `canModifyImageTranslation` 为真才暴露同名“恢复全部” action；翻译未完成或导出重绘期间保留可读空态、具体锁定 hint 和禁用的可见按钮，不再把无效恢复入口交给 VoiceOver。新增 `scripts/test-v3148-image-ignored-empty-state-action-gate-contract.py`；候选 exact-SHA full [31160052402](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31160052402)、PR #212 fast [31160532637](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31160532637)、merge fast [31160619661](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31160619661) 均通过；候选 SHA `01231917a86696cfc3a864d9a6382119b8c13455` Xcode/JUnit `10/10` 成功，merge SHA `dc55005a501c4a2f59036de27312cc1163c84b3e` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.147 让普通图片 OCR 修正 sheet 的“修正后的文字”输入框提供明确的 VoiceOver label/value/hint：按空文本、实际修改、确认无误和保存中的状态说明可执行边界；保存／重译进行中明确输入框与“忽略此文字块”不可用，忽略提示继续说明只影响当前图片会话并可在图片检查区恢复。新增 `scripts/test-v3147-image-ocr-correction-input-accessibility-contract.py`；候选 exact-SHA full [31158590713](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31158590713)、PR #211 fast [31159215608](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31159215608)、merge fast [31159309690](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31159309690) 均通过；候选 SHA `3a60ad6b431acfc11f2296ec59ec86609d107546` Xcode/JUnit `10/10` 成功，merge SHA `7e0bb80e4c3d077d56143d7375b34b6e6ef5ee61` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.146 让普通图片“已忽略 OCR 文字块”行的父级 VoiceOver 容器在可修改状态下提供同名“恢复” action，直接复用既有恢复回调；父级 hint 说明恢复会回到图片预览、导出和当前转录，需要复查时重新进入待复查队列，锁定时不暴露父级 action。现有 44pt 子按钮、禁用原因与 `image-ignored-row-<UUID>` 焦点保持不变。新增 `scripts/test-v3146-image-ignored-row-restore-action-contract.py`；候选 exact-SHA full [31157259172](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31157259172)、PR #210 fast [31157792746](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31157792746)、merge fast [31157872257](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31157872257) 均通过；候选 SHA `21768ac2c9a9cd5efdb87aebae62def5f1e20071` Xcode/JUnit `10/10` 成功，merge SHA `f850351e67734fb28d1f5672cd55bfd4de28e87f` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.145 让普通图片文件导入的 VoiceOver hint 与照片入口一致：无图片时说明从文件首次选择，已有图片时明确说明会更换当前图片并开始新的本机 OCR 与翻译；读取、OCR 或翻译进行中仍说明新选择会取消当前任务并开始新任务，文件入口保持可用。新增 `scripts/test-v3145-image-file-selection-replacement-hint-contract.py`；候选 exact-SHA full [31155971109](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31155971109)、PR #209 fast [31156530851](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31156530851)、merge fast [31156622662](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31156622662) 均通过；候选 SHA `92f495212a40910be1540c06f28cf4402c0b956f` Xcode/JUnit `10/10` 成功，merge SHA `f66f6e2fa7877a212e057648ec18663ae8ba9c83` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.144 让普通图片 OCR 的“没有可显示文字块”空态在图片翻译已完成且源图片仍可重跑时提供同名“重新识别” VoiceOver action，直接复用 Store `rerunImageRecognition()`；提示明确只重跑当前图片的 Vision OCR 与翻译，源文件不可重跑时不暴露 action。新增 `scripts/test-v3144-image-empty-result-rerun-action-contract.py`；候选 exact-SHA full [31154791726](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31154791726)、PR #208 fast [31155305272](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31155305272)、merge fast [31155356211](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31155356211) 均通过；候选 SHA `ff31f66f71698c6f34a7e1b7e52a940485984eea` Xcode/JUnit `10/10` 成功，merge SHA `914bf3d5c35354ce4e23e5f6bc470d6e2ba886f0` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.143 让普通图片 OCR 结果行的 VoiceOver hint 动态列出当前真正暴露的“修正识别文字／恢复 Vision OCR／完成并继续复查或撤销本次复查”操作；锁定、非人工修正或非风险状态不会虚构 action，同时保留定位与几何不可用上下文。新增 `scripts/test-v3143-image-review-row-action-hint-contract.py`；候选 exact-SHA full [31153705887](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31153705887)、PR #207 fast [31154097383](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31154097383)、merge fast [31154147898](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31154147898) 均通过；候选 SHA `3a6da4be92c3275c2efb31dab550b5a227af5da8` Xcode/JUnit `10/10` 成功，merge SHA `6bd71ae8ed1e34f2ad4ce0675c6b05e2c9e8b936` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.142 让普通图片风险 OCR 结果行在 `isReviewRequired && canReview` 时提供同名“完成并继续复查／撤销本次复查” VoiceOver action，直接复用既有 `toggleReviewCompletion()`；非风险或锁定时不暴露该 action，保留可见复查按钮、禁用原因、定位／焦点语义。新增 `scripts/test-v3142-image-review-row-review-action-contract.py`；候选 exact-SHA full [31152734900](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31152734900)、PR #206 fast [31153171846](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31153171846)、merge fast [31153229469](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31153229469) 均通过；候选 SHA `e94fb6a1fdfbeaddd64a0995dda5d1e872e91d84` Xcode/JUnit `10/10` 成功，merge SHA `28101601c1ed455950736bcc7ae010ff580f5636` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.141 让已人工修正的普通图片 OCR 结果行在 `isManuallyCorrected && canEdit` 时提供同名“恢复 Vision OCR” VoiceOver action，直接复用既有 `restoreVisionOCR()`；未修正或锁定时不暴露该 action，保留可见恢复按钮、现有禁用原因、定位／焦点语义。新增 `scripts/test-v3141-image-review-row-restore-action-contract.py`；候选 exact-SHA full [31151758844](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31151758844)、PR #205 fast [31152271664](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31152271664)、merge fast [31152319773](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31152319773) 均通过；候选 SHA `3a98ecb36bcfef8dfc77823b0eae26b06f0980bd` Xcode/JUnit `10/10` 成功，merge SHA `7b0684765acdf7f79ae01b462b8a0eb1ddaee674` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.140 让普通图片 OCR 结果行在 `canEdit` 时提供同名“修正识别文字” VoiceOver action，直接复用既有 `edit()`；锁定时不暴露该 action，保留可见修正按钮、定位／焦点语义和 `modificationUnavailableHint`。新增 `scripts/test-v3140-image-review-row-edit-action-contract.py`；候选 exact-SHA full [31150859808](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31150859808)、PR #204 fast [31151298078](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31151298078)、merge fast [31151339355](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31151339355) 均通过；候选 SHA `9571f142b35193cc86151b4e2971ccd6becfafbc` Xcode/JUnit `10/10` 成功，merge SHA `44394ba2068f7e173a7ba72223fc4210eda89239` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。

v3.139 让图片局部放大容器在对应邻居存在时提供同名“上一个文字块／下一个文字块” VoiceOver action，直接复用既有 `selectPrevious()`／`selectNext()`；首尾或单项筛选时分别隐藏不可用 action，保留可见按钮的 disabled hint 和当前位置 value。新增 `scripts/test-v3139-image-focus-navigation-action-contract.py`；候选 exact-SHA full [31149836170](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31149836170)、PR #203 fast [31150269494](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31150269494)、merge fast [31150318388](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31150318388) 均通过；候选 SHA `044e137c1fae3ca08bccbfd2ab37422bdce95c40` Xcode/JUnit `10/10` 成功，merge SHA `8a337da354936c3e7b0327cd154f7519b64fbed3` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。
v3.138 让图片局部放大容器在需要复查且 `canReview` 时提供同名“完成并继续复查／重新加入待复查” VoiceOver action，直接复用既有 `toggleReviewCompletion()`；不需要复查或复查被锁定时不暴露 action，父级 hint 说明现有 `reviewUnavailableHint`。新增 `scripts/test-v3138-image-focus-review-action-contract.py`；候选 exact-SHA full [31148861374](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31148861374)、PR #202 fast [31149234166](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31149234166)、merge fast [31149285259](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31149285259) 均通过；候选 SHA `52e78935232cff22ef4bb45285a45218e6bd1b85` Xcode/JUnit `10/10` 成功，merge SHA `490c98062bd989f09334e202b73942f684c7c5f2` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。
v3.137 让图片局部放大容器在 `canEdit` 允许时提供同名“修正识别文字” VoiceOver action，直接复用既有 OCR 修正入口；锁定时不暴露该 action，并由父级 hint 说明修改不可用原因。新增 `scripts/test-v3137-image-focus-edit-action-contract.py`；候选 exact-SHA full [31147358078](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31147358078)、PR #201 fast [31147793085](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31147793085)、merge fast [31147924273](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31147924273) 均通过；候选 SHA `46f617f8ce9a78628c4bdef54a800e4a4dc4e5a3` Xcode/JUnit `10/10` 成功，merge SHA `9f8a05bd0b6001e8cfc0a94213cfee8a5f2e3583` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。
v3.136 让图片局部放大预览容器提供同名“关闭局部放大” VoiceOver action；可见关闭按钮的 hint 明确返回当前文字块结果行，action 复用既有 View 私有焦点交接。新增 `scripts/test-v3136-image-focus-close-action-contract.py`；候选 exact-SHA full [31144595687](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31144595687)、PR #200 fast [31144958126](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31144958126)、merge fast [31144998556](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31144998556) 均通过；候选 SHA `0c4bddf96354989d9d2efc445987de3e8a3eafb4` Xcode/JUnit `10/10` 成功，merge SHA `321f0fbcdcea704455174772da486a0a00f04754` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。该 View-only 改动不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。
v3.135 让图片预览加载／失败状态成为完整 VoiceOver 上下文：状态容器新增动态 hint；失败时明确可执行“重试预览”且只重建屏幕预览，加载时说明完成后可定位文字块与失败恢复边界。该 View-only 语义不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。新增 `scripts/test-v3135-image-preview-status-hint-contract.py`；候选 exact-SHA full [31143646549](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31143646549)、PR #199 fast [31144019839](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31144019839)、merge fast [31144057333](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31144057333) 均通过；候选 SHA `728b96a06491f41f5a8809fc2649486bdae81444` Xcode/JUnit `10/10` 成功，merge SHA `b45bd83b24a7d021e3f414990ad85be60b494312` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.134 让图片预览失败状态成为可执行的 VoiceOver 上下文：仅当当前 `imageTranslationRevision` 的屏幕预览生成失败时，状态容器提供同名“重试预览” action；可见按钮、状态 label/value/hint 和 action 共用既有 `retryPreview()`，仅重建屏幕预览，不重新 OCR 或翻译。新增 `scripts/test-v3134-image-preview-status-retry-action-contract.py`；候选 exact-SHA full [31142629553](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31142629553)、PR #198 fast [31142975439](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31142975439)、merge fast [31143030561](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31143030561) 均通过；候选 SHA `94e26b435226a966a3c866fa222da92e7eff69c3` Xcode/JUnit `10/10` 成功，merge SHA `730312af21e2bc081d8d414f3d6b28acd0e3277b` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.133 让普通图片的空预览与识别结果空态成为稳定 VoiceOver 上下文：预览读出“图片翻译预览／当前没有图片”及从照片或文件开始本机 OCR、翻译和屏幕预览；结果空态按 idle、载入／识别／翻译、完成和失败动态说明当前阶段、恢复边界与 `canRetryImageTranslation`，不新增 Store／持久化，不改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 `output`。新增 `scripts/test-v3133-image-empty-result-accessibility-context-contract.py`；候选 exact-SHA full [31140850232](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31140850232)、PR #197 fast [31141276534](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31141276534)、merge fast [31141320676](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31141320676) 均通过；候选 SHA `a1bc1ba4a73d4337e83c2a99f911ce3f709dc207` Xcode/JUnit `10/10` 成功，merge SHA `30678f089d5764b06bdf2d459c55ba2677d872cb` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.132 让普通图片 OCR 在所有文字块都被忽略时提供可操作的空态：空态成为稳定 VoiceOver 上下文，读出忽略数量与恢复边界，并提供同名“恢复全部” action；同一状态只显示一个受 `canModifyImageTranslation`／导出重绘门控的可见恢复按钮，部分忽略状态仍保留下方批量入口。最后一个 block 被忽略后，View 私有焦点在 sheet 收起后交给该空态，终态也保留空态上下文；新增 `scripts/test-v3132-image-ignored-empty-state-action-contract.py`，历史 v3.330/v3.333/v3.335 合同允许这条额外合法焦点目的地。候选 exact-SHA full [31139110055](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31139110055)、PR #196 fast [31139576598](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31139576598)、merge fast [31139633331](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31139633331) 均通过；候选 SHA `012f25ffd7edc4009b33c600bb57d0d6d65005c2` Xcode/JUnit `10/10` 成功，merge SHA `df3f7ecbf59319a0c59d13164bb6b0f9cd8ab553` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.131 让普通图片 OCR 的“已忽略文字块”区域提供带确认的可见“恢复全部”操作：Store 按原始顺序一次性恢复忽略快照，保留人工修正与 Vision OCR 元数据，清除恢复块的复查完成状态，并只触发一次转录／导出重建；View 受 `.translated` 与导出重绘门控，恢复后将 VoiceOver 焦点交给首个恢复结果行。新增 `scripts/test-v3131-image-ignored-blocks-bulk-restore-contract.py` 并接入 UI/full fail-fast；候选 exact-SHA full [31090186819](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31090186819)、PR #195 fast [31137606603](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31137606603)、merge fast [31137651196](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31137651196) 均通过；候选 SHA `26fc6bba61c277747673f9fb29e4a6e1eb849aaf` Xcode/JUnit `10/10` 成功，merge SHA `8514924340486fd86344298f3b9493540fe9ab4c` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.130 让 Developer Console 漫画探针在已有逐块报告但当前诊断筛选为空时提供可见“显示全部诊断”按钮与同名 VoiceOver action；恢复只改 View 私有 `diagnosticFilter`，复用既有筛选变化、展开重置和焦点路径，并保留“本次探针未生成文字块”的独立空态。新增 `scripts/test-v3130-manga-diagnostic-filter-empty-action-contract.py` 并接入 UI/full fail-fast；候选 exact-SHA full [31088767018](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31088767018)、PR #194 fast [31089351045](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31089351045)、merge fast [31089424245](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31089424245) 均通过；候选 SHA `f3965b43c38b7682decec9aaf46d046cafc3f16f` Xcode/JUnit `10/10` 成功，merge SHA `ed719558db8edca6aff02124517fb0035bc3c458` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.129 让普通图片 OCR 的低置信／方向待定／待复查筛选在没有结果时提供可见“显示全部结果”按钮，并让同一空态成为可执行的 VoiceOver 上下文；action 只复用既有 `prepareReviewFilterChange(to: .all, focusID: nil)`，不新增 Store／持久化、不改变 OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。新增 `scripts/test-v3129-image-review-filter-empty-action-contract.py` 并接入 UI/full fail-fast；候选 exact-SHA full [31087461275](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31087461275)、PR #193 fast [31088057693](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31088057693)、merge fast [31088114103](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31088114103) 均通过；候选 SHA `b4617eacf4ff4ef0c7ccce847fe3742ff7002861` Xcode/JUnit `10/10` 成功，merge SHA `5b2792d065fcb83ec10ccb4c2b55cb5ddab84bc8` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.128 让普通图片 OCR 的“本次复查完成”空态成为单一、稳定的 VoiceOver 上下文：读出已完成风险块／总风险块数量与当前筛选，并提供直接“重新复查”无障碍 action，复用既有 `restartReviewQueue()`，仍受 `canReviewImageTranslation` 保护。新增 `scripts/test-v3128-image-review-completion-action-contract.py` 并接入 UI/full fail-fast；该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 exact-SHA full [31085406753](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31085406753)、PR #192 fast [31085987796](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31085987796)、merge fast [31086053876](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31086053876) 均通过；候选 SHA `d31745f8461ec6eeee0e8ce75e5965874a10b7b7` Xcode/JUnit `10/10` 成功，merge SHA `14a11feff4413058f5c02348ce5d16485044702f` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.127 让图片翻译状态行按失败优先级提供直接 VoiceOver 操作：分享准备失败时可直接“重试分享”，导出重绘失败时可直接“重试导出”，只有没有这两类失败时才保留“重试当前图片”；状态 hint 与状态显示优先级一致。新增 `scripts/test-v3127-image-failure-status-actions-contract.py` 并接入 UI/full fail-fast；该 View-only 改动复用既有 Store retry 方法，不新增 Store／持久化、不改变 OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31084281958](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31084281958)、PR #191 fast [31084713250](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31084713250)、merge fast [31084803922](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31084803922) 均通过；候选 SHA `a7ef8ce984234bf4631f206186f70bbec3ff2b64` Xcode/JUnit `10/10` 成功，merge SHA `03d5b82b97c102043a0c262775061401cc38ac8a` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.126 补齐普通图片 OCR 导出重绘／分享失败后的 VoiceOver 终态焦点：当既有 `imageTranslationShareState` 或 `imageTranslationExportRenderState` 从运行中转为 `.failed` 时，`ImageTranslationPanel` 将焦点交给现有“图片翻译状态”行；`.preparing`／`.rendering` 等非终态变化不抢焦点。新增 `scripts/test-v3126-image-export-share-failure-focus-contract.py` 并接入 UI/full fail-fast；该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31082994159](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31082994159)、PR #190 fast [31083400009](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31083400009)、merge fast [31083557316](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31083557316) 均通过；候选 SHA `244f97435d340207c7684c3a2ab553b552b3b780` Xcode/JUnit `10/10` 成功，merge SHA `e023e9c016c64a65596fe70151ad6f6a2d6615d9` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.125 修复普通图片导入／Pro 门控等未创建新 revision 的直接失败没有 VoiceOver 终态焦点的问题：当图片状态进入 `.failed` 且没有当前 revision-scoped 终态请求时，`ImageTranslationPanel` 将焦点交给既有“图片翻译状态”行，用户会立即听到失败详情与重试／重新选择边界；正常 OCR／翻译 revision 失败继续沿用原有终态焦点。新增 `scripts/test-v3125-image-direct-failure-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31081494834](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31081494834)、PR #189 fast [31081976028](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31081976028)、merge fast [31082019649](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31082019649) 均通过；候选 SHA `1c068d538728a1195fdd08197f16f7e82d06dd4b` Xcode/JUnit `10/10` 成功，merge SHA `9bd54490d09573d351c1e09da148393c3036a20` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.124 修复普通图片清空后的 VoiceOver 焦点断点：当图片 revision 变化后确认为 `imageTranslationData == nil` 且状态为 `.idle`，`ImageTranslationPanel` 将焦点交给稳定的“等待图片”空态；新图片仍处于 loading 时不会抢走状态焦点。空态提供“当前没有图片”value 与从照片／文件开始本机 OCR 与翻译的下一步 hint。新增 `scripts/test-v3124-image-clear-empty-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31080208334](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31080208334)、PR #188 fast [31080768687](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31080768687)、merge fast [31080830286](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31080830286) 均通过；候选 SHA `e02b7a6d2c0f41098eb2cf82e6aa97f9b40c1ff9` Xcode/JUnit `10/10` 成功，merge SHA `738f82384e027058ae0e53cad5f7842ce92a010f` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.123 修复普通图片屏幕预览失败／重试后的 VoiceOver 焦点断点：`ImageTranslationPanel` 为预览状态提供稳定的 `imagePreviewStatusAccessibilityFocusID`，`ImageTranslationPreview` 的状态容器使用 `.accessibilityFocused`；预览生成失败和“重试预览”开始时都通过既有 View 私有焦点 handoff 将焦点交回状态行，让用户立即听到失败／加载详情与重试边界。新增 `scripts/test-v3123-image-preview-status-focus-contract.py` 并接入 UI/full fail-fast；CI 路由将 v3.122/v3.123 合并进既有正则并保留顶层兼容标记，避免 GitHub Actions 表达式长度超限。该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31079060685](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31079060685)、PR #187 fast [31079520917](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31079520917)、merge fast [31079590205](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31079590205) 均通过；候选 SHA `92e68b60e74dd61fb471584bba9cf00bf1696868` Xcode/JUnit `10/10` 成功，merge SHA `6309370bc47974964a2aa181075469fb29e928e7` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.122 修复普通图片读取／OCR／翻译被取消后的 VoiceOver 焦点断点：仅当图片状态从 loading／recognizing／translating 转为 idle 时，将焦点交给现有“图片翻译状态”行，让用户立即听到取消结果和可重试边界；初始 idle、成功、失败和清空路径不抢焦点。新增 `scripts/test-v3122-image-cancel-status-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31077891466](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31077891466)、PR #186 fast [31078311141](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31078311141)、merge fast [31078359581](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31078359581) 均通过；候选 SHA `9c0ed87838d7eb621fb762c67230df74321f5ab6` Xcode/JUnit `10/10` 成功，merge SHA `61401075429df063b7726037b81b91aade9178d3` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.121 让普通图片 OCR 的“图片翻译状态”在失败／取消且没有待重试语言变更、源图片仍可用时提供受 `store.canRetryImageTranslation` 保护的“重试当前图片”VoiceOver action；状态 hint 同步说明可直接重试或需要重新选择图片，已有“重试语言已更新”状态继续保留唯一的待语言变更 action。新增 `scripts/test-v3121-image-status-retry-accessibility-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31076710802](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31076710802)、PR #185 fast [31077094866](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31077094866)、merge fast [31077152440](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31077152440) 均通过；候选 SHA `9551c0c53bae0b8816d490a3da03c9472995e859` Xcode/JUnit `10/10` 成功，merge SHA `16caff957c0252c2014750cc6c3d56dfa8463c29` 复用候选 full receipt，PR/merge fast 不是新的编译证据，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.120 让普通图片 OCR 的“重试语言已更新”VoiceOver 状态成为可操作上下文：在 v3.119 已聚焦该状态的基础上，新增受 `store.canRetryImageTranslation` 保护的“重试当前图片”无障碍动作，直接复用 `store.retryImageTranslation()`；hint/value 继续说明当前待重试语言与重新识别／翻译边界，状态不可重试时动作安全无效。新增 `scripts/test-v3120-image-retry-language-accessibility-action-contract.py` 并接入 UI/full fail-fast。该 View-only 操作改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31075361390](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31075361390)、PR #184 fast [31075697828](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31075697828)、merge fast [31075745893](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31075745893) 均通过；候选 SHA `25855f7e6b757c2ae901c794c56990215399cb68` Xcode/JUnit `10/10` 成功，merge SHA `205e78e0f7dd39c51a9c8dbf93748c1a5ef0a090` 复用候选 full receipt，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.119 修复普通图片 OCR 失败／取消后的待重试语言提示 VoiceOver 焦点断点：当输入语言或目标语言确实改变并生成既有 `imageTranslationRetryLanguageSummary` 时，`ImageTranslationPanel` 通过 revision-scoped、generation 仲裁的 View 私有 handoff 将焦点交给“重试语言已更新”状态行；label/value/hint 说明下一次重试会使用的语言与重新识别／翻译范围，未发生真实变化时不抢焦点，也不触发重试。新增 `scripts/test-v3119-image-retry-language-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、probe_report、Koharu active gate、metrics 或 `output`。候选 full [31074379707](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31074379707)、PR #183 fast [31074819588](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31074819588)、merge fast [31074863470](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31074863470) 均通过；候选 SHA `5248bd705fc6c0d963146060917e2a6a94a6c421` Xcode/JUnit `10/10` 成功，merge SHA `de5ccebdeda7294a491183c53676e65caaaefb6a` 复用候选 full receipt，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.118 修复 Developer Console 漫画探针报告到达后的 VoiceOver 焦点优先级：当既有 `externalArtifactReadinessReport` 的下一步明确阻断真实 Koharu 四件套、契约修正或真实 detector 来源声明时，先把共享 generation focus 交给“Koharu 工件就绪状态”，再由用户筛选回到逐块结果；非阻断 readiness、筛选变化、空筛选和逐块展开仍沿用原有焦点路径。新增 `scripts/test-v3118-manga-koharu-readiness-focus-contract.py`，并让 v3.336 历史合同接受带共享焦点 binding 的等价初始化。该 View/report-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、probe_report、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31073337578`、PR #182 fast `31073688262`、merge fast `31073828173` 均通过；候选 SHA `067077bcb146e2e0c8bb2e066350cac2466b7460` Xcode/JUnit `10/10` 成功，merge SHA `036bada7aebd70da50d6d31cc4e4da2cce4b8dda` 复用候选 full receipt，探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.117 修复 Developer Console 漫画探针诊断筛选切换后的 stale 展开状态：筛选变化先递增既有 View 私有 expansion reset token，逐块行收起旧详情并抑制 reset 触发的焦点回抢，再由 v3.116 generation requester 聚焦新筛选首项。新增 `scripts/test-v3117-manga-diagnostic-filter-expansion-reset-contract.py` 并接入 UI/full fail-fast。该 View/report-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、probe_report、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31072107788`、PR #181 fast `31072405558`、merge fast `31072447592` 均通过；候选 SHA `4bda6f5a0ea20e7d8223546d0b380758d839c253` Xcode/JUnit `10/10` 成功，merge SHA `b209880723b3d82c7df3d78240ad8d05a16d48e3` 复用候选 full receipt，探针默认 skip，真实 Koharu 四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.116 修复 Developer Console 漫画探针诊断焦点的多入口竞态：`MangaProbeSection` 统一用 View 私有 request generation 仲裁筛选、重跑终态和逐块展开／收起的 VoiceOver handoff，最新请求才会生效；新一轮探针 loading 会使旧请求失效。新增 `scripts/test-v3116-manga-diagnostic-focus-generation-contract.py` 并接入 UI/full fail-fast，同时让 v3.113 历史合同接受等价的共享 requester。该 View-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、probe_report、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31071423891`、PR #180 fast `31071714254`、merge fast `31071752236` 均通过；候选 SHA `4788bb213eeff010775a35798dc6fb28aabb7c0c` Xcode/JUnit `10/10` 成功，merge SHA `b9607e246b070222e4ef4cc4dbd78b8ee925b8ea` 复用候选 full receipt，探针默认 skip，真实 Koharu 四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.115 修复普通图片 OCR 多入口 VoiceOver 焦点请求的同 revision 竞态：每次 View focus handoff 都递增 request generation，异步让渡只在 generation 与 imageTranslationRevision 同时仍匹配时生效；新图片 revision 会先使旧请求失效，避免快速筛选、定位、复查或关闭预览后旧任务抢回焦点。新增 `scripts/test-v3115-image-focus-request-generation-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31070650744` 与手动 full `31070655111`、PR #179 fast `31070940503`、merge fast `31070976672` 均通过；候选 SHA `50029c449f86fbf4f0d43e7e35967a0c1e0ec8ce` 的 Xcode/JUnit `10/10` 成功，merge SHA `b4d3acd1c6bdaf51429ae5acdfed05013a2b37c6` 复用候选 full receipt，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.114 继续隔离 Developer Console 漫画探针逐块诊断的展开状态：新一轮探针进入 loading 时递增 View 私有 reset token，已展开旧 block 会收起；reset 引发的状态变化抑制一次旧焦点 handoff，避免焦点抢回上一轮结果。结果行 VoiceOver value/hint 明确“详细诊断已展开／已收起”和对应收起／展开动作。新增 `scripts/test-v3114-manga-diagnostic-expansion-state-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31069913494` 与手动 full `31069918901`、PR #178 fast `31070264175`、merge fast `31070323190` 均通过；候选 SHA `e62a168e57e2f2cbc6cb47b5a8c8dde571d73f42` 的 Xcode/JUnit `10/10` 成功，merge SHA `118afee6e096f73e6ab97d71c23af189d997f6ca` 复用候选 full receipt，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.113 修复 Developer Console 漫画探针逐块诊断展开后的 VoiceOver 焦点断点：DisclosureGroup 展开后将焦点交给稳定的“详细诊断”容器，收起后回到原文字块结果行；详情继续保留 OCR、译文、失败原因、报告风险、下一步和 report-only 边界。新增 `scripts/test-v3113-manga-diagnostic-expansion-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不重新运行探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31068769954`（push）与手动 full `31068778764`、PR #177 fast `31069311041`、merge fast `31069349841` 均通过；候选 SHA `fd2cf8d32b9576dc2620ce3c281403421aa1ca02` 的 Xcode/JUnit `10/10` 成功，merge SHA `1f3612f54410aef39ea8c5a34195f9d9f9296573` 复用候选 full receipt，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.112 修复普通图片 OCR 新图／重试完成后的 VoiceOver 焦点断点：当新的 `imageTranslationRevision` 进入 `.translated` 或 `.failed`，有文字块时聚焦当前筛选的首个 OCR 结果行，没有文字块时聚焦动态图片翻译状态行；revision guard 防止旧任务在后续图片开始后抢回焦点。保留既有筛选、选中项、修正 sheet 和复查焦点边界。新增 `scripts/test-v3112-image-translation-terminal-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31067968394`、PR #176 fast `31068324104`、merge fast `31068365757` 均通过；候选 SHA `a150982ab83dac47000bb6bce34caa9aa74ecf26` 的 Xcode/JUnit `10/10` 成功，merge SHA `f467a72a3de9d5ab6e876a51e228e19e037f4174` 复用候选 full receipt，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.111 修复 Developer Console 漫画探针重跑后的 VoiceOver 焦点断点：每次新报告写入后，有逐块结果时聚焦当前筛选的第一个诊断 block；报告没有 blocks 时聚焦“未生成逐块诊断”状态，保留 `test/1.png`、Output 清理与重试范围；探针 loading 仍先清除旧筛选焦点。新增 `scripts/test-v3111-manga-probe-terminal-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不重新运行探针、不改变 OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31067283530`、PR #175 fast `31067583454`、merge fast `31067629934` 均通过；候选 SHA `70222e95035c7cd5a71799735e11539f755b5d08` 的 Xcode/JUnit `10/10` 成功，merge SHA `9a28692cebf3360ca00b5e0b7f77463f103bdaa2` 复用候选 full receipt，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.110 修正普通图片 OCR 筛选焦点的意图竞争：用户切换筛选时仍将 VoiceOver 焦点交给第一个可见结果行，但开始／重启复查、恢复忽略 block、预览直接选中和完成／撤销复查等程序化筛选变化会保留明确的结果行、局部预览或完成态焦点；图片 revision 重置继续清除旧意图并保持空态回退。新增 `scripts/test-v3110-image-filter-focus-intent-contract.py`，同步让 v3.15/v3.16/v3.17/v3.29/v3.81/v3.93 历史合同接受等价 helper。该 View-only 改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31066203170`、PR #174 fast `31066589776`、merge fast `31066628727` 均通过；候选 SHA `43e75f22be1f1acc55045942f9c617bb0e4675e9` 的 Xcode/JUnit `10/10` 成功，merge SHA `08577f31c3dcd3da09ef64c6d9aa050e8d639794` 复用候选 full receipt，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.109 让普通图片 OCR 的识别结果筛选在切换类别后将 VoiceOver 焦点交给第一个可见 OCR 结果行；继续保留隐藏选中项清理、revision-scoped 焦点和无结果空态回退，筛选与焦点仍为 View-only。新增 `scripts/test-v3109-image-filter-result-focus-contract.py`，候选 full `31064198524`、PR #173 fast `31064487760`、merge fast `31064532453` 均通过，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.108 让 Developer Console 漫画诊断筛选在切换到有结果的类别后，将 VoiceOver 焦点交给第一个可见诊断结果行；筛选仍只读既有报告，空筛选继续交给可操作空态，焦点 identity 保持 View 私有。新增 `scripts/test-v3108-manga-filter-result-focus-contract.py`，并让历史 v3.399 报告交接合同接受额外的 View-only 行上下文。该改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31063355633`、PR #172 fast `31063761078`、merge fast `31063805810` 均通过；候选 SHA `c6bee294a7a5e59b51e39aa3b27cc09c5a913616` 的 Xcode/JUnit `10/10` 成功，merge SHA `859bac7b6e47f31ab3b14220737fe3d9c4048a07` 复用候选 full receipt，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.107 让普通图片 OCR 与 Developer Console 漫画探针筛选器在结果为空时把 VoiceOver 焦点交给可操作的空态：明确当前筛选、显示 `0 / 总数` 和恢复路径，用户可直接切换回“全部”或其他筛选继续查看；图片会在筛选切换后优先交给空筛选态（或复查完成态），漫画诊断会在只读报告有 blocks 但筛选无结果时交给诊断空态。该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31020576411`、PR #171 fast `31062338507`、merge fast `31062372361` 均通过；候选 full `f513bcd71f48ec34c2820941b86da7093bc86761` 的 Xcode/JUnit `10/10` 成功，merge SHA `0e7e692543aa34f8fb368cea766fb82391295929` 复用候选 full receipt，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.106 让普通图片 OCR 与 Developer Console 漫画探针筛选器的 VoiceOver value 统一读出当前筛选类别、显示数量和总数量；图片筛选在有风险块时额外读出已复查／剩余数量，避免用户只听到分类而不知道当前结果范围。该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、探针报告或 Koharu active gate。候选 full `31017118790`、PR #170 fast `31017809552`、merge fast `31017909329` 均通过；候选 full Xcode build 成功且 JUnit `10/10`，PR/merge fast 复用 full receipt，探针默认 skip，真实四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.105 让 Developer Console 漫画探针诊断总览只读消费既有收敛报告的闭环快照：汇总开放／已闭环／要求停止工单、block path 与 work-item ledger 规模、状态 breakdown 及真实外部工件边界，并把同一快照用于状态 tone、可复制 summary 和 VoiceOver。存在开放或阻断收敛项时保持 warning/仅报告，不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31015086472`、PR #169 fast `31015765732`、merge fast `31015838087` 均通过；候选 full Xcode build 成功且 JUnit `10/10`，PR/merge fast 复用 full receipt，探针默认 skip，真实四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.104 让 Developer Console 漫画探针逐块结果只读消费既有 `MangaKoharuArtifactConvergenceReport` 的 block path、开放工单与执行边界：结果行、可复制 action summary 和 VoiceOver 共用首阻断、结构瓶颈、真实工件等待、工单状态及 CI-fast/full/外部工件边界；存在未闭环工单或 report-only 边界时统一显示 warning/仅报告，不再把收敛未完成误读为成功。该 View/report-only 改动不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31013385953`、PR #168 fast `31014071238`、merge fast `31014141913` 均通过；候选 full Xcode build 成功且 JUnit `10/10`，PR/merge fast 复用 full receipt，探针默认 skip，真实四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.103 在 Developer Console 漫画探针诊断总览显示既有 native promotion、artifact contract dry-run、artifact identity reconciliation 与 convergence 的“晋级边界”：晋级 verdict、候选预览是否仅 dry-run、active export 禁止、真实工件/CI manifest 身份对账和未闭环工单会汇总到状态行、可复制摘要与 VoiceOver。只读 `MangaOverlayProbeReport`，不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31011231211`、PR fast `31011777761`、merge fast `31011846424` 均通过；候选 full Xcode build 成功且 JUnit `10/10`，PR/merge fast 复用 full receipt，探针默认 skip，真实四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.102 在 Developer Console 漫画探针逐块行显示既有 resolver、work-order、外部工件请求包和 native replay 的“执行边界”：目标执行项、首阻断、CI-fast/full/外部工件门、禁止本地调参与 shadow-only 状态，并与建议、依据、Koharu 工件门共用视觉和 VoiceOver summary。只读 `MangaOverlayProbeReport`，不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31009117560`、PR fast `31009686004`、merge fast `31009749466` 均通过；探针默认 skip，真实四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.101 在 Developer Console 漫画探针逐块行继续显示既有 report-only 诊断依据：OCR 字符损伤、气泡拆分/归属、模型底线、字号预算和 Koharu DAG 首阻断阶段；该依据与推荐下一步、工件门控共用视觉与 VoiceOver 上下文。只读 `MangaOverlayProbeReport`，不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR 候选、翻译 prompt/model、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `30993659770`、PR fast `30994207268`、merge fast `30994272480` 均通过；探针默认 skip，真实四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.100 让 Developer Console 的漫画探针逐块结果消费既有 report-only ledger 的推荐下一步：优先补充文本框/segment 证据、复核气泡归属、比较提示词/模型、确认模型底线或保留布局报告；若报告仍受 Koharu 工件 DAG 阻断，同一行同时显示“提供真实 Koharu 工件”门控。视觉建议与 VoiceOver value/hint 共用该上下文，只读 `MangaOverlayProbeReport`，不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR 候选、翻译 prompt/model、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `30992318412`、PR fast `30992932438`、merge fast `30993004271` 均通过；探针默认 skip，真实四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.99 让 Developer Console 的漫画探针逐块结果消费当前 `probe_report` 的既有 OCR、翻译和布局 risk set，在结果行显示“风险：OCR／翻译／布局”标签，并把相同标签加入 VoiceOver value/hint，解释为什么 block 出现在某个只读筛选中。该改动不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR 候选、翻译 prompt/model、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `30991030339`、PR fast `30991418709`、merge fast `30991478674` 均通过；探针默认 skip，真实四件套仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

这是一个基于 SwiftUI 的 iOS 本地 AI 翻译原型。默认使用 `MockGemmaService` 做界面和数据流冒烟；切换到 `Local` 并导入 GGUF 后，App 会通过 `llama.cpp` 加载本地模型生成翻译或总结。

当前正式版本：`3.208`（普通图片 OCR 的文字块在逐块翻译期间仍可查看和定位，但会改变当前图片结果或复查进度的动作只在最终 `.translated` 状态开放：OCR 修正、恢复 Vision OCR、恢复已忽略 block、旁贴／覆盖切换还会在导出重绘期间保持禁用；开始／继续／重启复查及完成／撤销复查同样等待完整翻译。v3.154 让没有可显示识别结果的可见空态标题与说明按 idle、读取／OCR／翻译进行中、translated、failed 动态分流，同时保留既有 VoiceOver 上下文与重新识别门控，不新增 Store／持久化或改变 OCR、翻译、renderer/export、探针、Koharu active gate、metrics 或 output。v3.42 会在仍有实际文字块但操作被锁住时显示状态行，并让覆盖方式、复查、局部预览、结果行和忽略恢复的 VoiceOver 提示复用 loading／translating／failed／export-rendering 的具体原因；v3.43 继续让图片局部预览的前后导航在筛选首尾给出“第一个／最后一个文字块”的边界提示，并让结果行定位提示随当前选中状态切换为“取消定位”或“在图片预览中定位”；v3.44 还让前后导航按钮读出当前筛选位置（如 `当前位置 1 / 3`），避免只知道方向而不知道序列上下文；v3.45 让完整图片预览中的 OCR 覆盖块也复用结果行的定位提示，已定位时读出“取消此文字块在图片中的定位”，未定位时读出“在图片预览中定位此文字块”，避免从图片入口和列表入口听到不一致的操作语义；v3.46 让图片预览加载与失败状态提供稳定的 VoiceOver label/value，失败时的“重试预览”明确只重建屏幕预览，不会重新 OCR 或翻译；v3.47 让图片命令栏的照片／文件、取消、重试、重新识别、导出、清空等高频操作读出影响范围，且照片选择器会区分首次选择与替换当前图片；v3.48 让完整图片预览把识别块数量、待复查数量和当前定位位置作为稳定的 VoiceOver 上下文，并隐藏重复朗读的原始背景图，点按文字块仍可定位并打开局部放大。逐块翻译会明确仍可查看和定位，失败会引导重试。View 的状态门和 Store 的 mark／reopen／reset 二次校验共同防止逐块翻译中提前写入复查进度；成功 OCR 修正先恢复 translated 再沿用既有自动复查。v3.37–v3.40 的文本规范化、弃改保护、键盘收起、滚动收起、保存期输入锁定和 revision-scoped 焦点交接继续保留）。v3.49 让图片输入语言与目标语言菜单的 VoiceOver hint 按运行中、Pro 门槛、无图片、已完成和失败／取消重试状态分流：运行中明确需完成或取消，Pro 锁定说明不会污染文本页语言，已完成输入语言说明会重新识别和翻译、目标语言说明会重新翻译当前图片，失败／取消说明下一次重试使用新语言；选回当前内容语言会撤销待重试差异。v3.50 进一步让照片与文件导入按钮在读取、OCR 或翻译进行中明确说明“选择新图片会取消当前任务并开始新的本机 OCR 与翻译”，同时保留有意设计的替换入口与 Store run-id 隔离。v3.51 让图片状态行成为单一的 VoiceOver 状态元素，动态读出当前阶段、逐块进度和下一步操作：载入／Vision OCR／翻译可取消或换图，导出重绘／分享准备／失败各自说明边界，完成后说明修正、复查、覆盖和导出入口。该改动只改善 View 语义，不新增 Store／持久化状态，也不改变 OCR、翻译、renderer/export、探针或质量基线。v3.52 让图片结果行的 VoiceOver value 在定位状态之外读出 OCR 置信度、低置信／方向待定、人工修正、复查进度和等待翻译，并继续沿用定位 hint；只改善 View 语义，不新增 Store／持久化或改变 OCR、翻译、renderer/export、探针和质量基线。v3.53 让已忽略 OCR 文字块恢复行成为稳定的 VoiceOver 上下文，读出不在图片预览／导出／转录、是否保留现有译文和恢复是否可用，同时保留恢复按钮的禁用原因与焦点交接；只改善 View 语义，不新增 Store／持久化或改变 OCR、翻译、renderer/export、探针和质量基线。v3.54 修复图片状态行 VoiceOver value 的实现回归：从字面量 `(statusTitle)：(statusDetail)` 改为实时插值，确保载入、逐块进度、失败、导出重绘和完成详情会随状态实际朗读；同步强化 v3.51 回归合同，仍不新增 Store／持久化或改变 OCR、翻译、renderer/export、探针和质量基线。v3.55 让开发控制台的 Koharu readiness 状态成为稳定的 VoiceOver 上下文，读出就绪 verdict、缺失四件套、下一步和 shadow-only 边界；缺少工件时明确给出 `test/koharu_artifacts/` 与四个文件名。该改动只改善开发者操作与诊断可理解性，不新增 Store／持久化，不调用第二次探针，不改变 active artifact gate、普通图片 OCR、翻译、renderer/export、Koharu 主路径或质量基线。v3.56 让漫画覆盖翻译探针状态行成为单一、稳定的 VoiceOver 上下文，按载入、Vision OCR、翻译、绘制、完成和失败读出状态详情；运行按钮明确 bundle 内 `test/1.png`、Output 诊断文件和只影响漫画探针的边界。该改动只改善开发者操作语义，不新增 Store／持久化，不改变普通图片 OCR、翻译、renderer/export、Koharu 主路径或质量基线。v3.57 让开发控制台的漫画探针逐块结果成为稳定的 VoiceOver 诊断上下文，按 block index 读出 PASS/FAIL、OCR 原文、旋转角度、置信度、质量标签、译文与失败详情；展开提示明确只属于漫画探针，不改变普通图片 OCR、翻译或覆盖图。该改动只改善开发者操作语义，不新增 Store／持久化状态、不读取 ground truth 作为候选、不改变漫画探针诊断、renderer/export、Koharu 主路径或质量基线。v3.58 让图片复查结果行提供稳定的 VoiceOver label/value：label 明确“图片文字块 + OCR 原文”，value 在等待翻译时读出等待状态、完成后读出译文，并为空 OCR 提供稳定回退；只改善 View 语义，不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。v3.59 让完整图片预览的覆盖文字块与图片复查结果行共用稳定的 VoiceOver label“图片文字块 + OCR 原文”，空 OCR 回退为“空”，并保留既有等待翻译／译文 value、定位 hint 和选中状态；该改动只改善 View 语义，不新增 Store／持久化状态，不改变选择、Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。 v3.60 让完整图片预览的覆盖文字块与图片复查结果行对齐 VoiceOver value：读出 OCR 置信度（clamp 到 0–100%）、人工修正、低置信／方向待定、待复查／本次已复查及等待翻译／译文；相邻与替换模式共用这套上下文，只改善 View 语义，不新增 Store／持久化状态，不改变选择、Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。 仓库仍没有真实 Koharu 四件套、Speech corpus 或真实竖排图片 corpus，因此不声称 OCR、翻译或识别质量提升。云端 receipt 传播规则（v3.26）仍适用；日常开发合入 `smalldata_test`，不合并到 `main`。

v3.81 让结果行或完整图片覆盖块选中 OCR 文字块后，VoiceOver 焦点立即进入对应局部预览；取消定位时焦点回到对应结果行，保持定位上下文与关闭路径连续。该 View-only handoff 不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu 主路径或质量基线；同步压缩 v3.47–v3.81 历史合同路由正则以避开 GitHub Actions 表达式长度上限，不代表 OCR、翻译、识别或 Koharu 质量提升。

v3.82 修正漫画覆盖探针失败 fallback 的显式换行布局测量：`wrappedLines` 逐段保留显式换行和空行，让 fit plan 与 CoreText 绘制共享同一垂直预算；新增 `scripts/test-v382-manga-render-newline-contract.py` 并接入 CI。该修复只改善探针失败覆盖的布局可观测性，不改变 OCR 候选、翻译模型／prompt、ground truth 决策、Koharu active artifact gate、普通图片主路径、`metrics` 或 `output` 质量基线。候选 full `30868588679`、ci-fast 探针 `30868948454`、PR #146、merge `8ff612269bb9c57691284179ad09ce281dd20a7c` 均已通过；ci-fast 报告仍诚实标记 block 5 的空间截断，不能据此声称 OCR、翻译或 Koharu 质量提升。

v3.83 让 Koharu fit planner 与实际渲染预算一致：`makeKoharuRenderSpriteFitPlannerReport` 现在复用 `wrappedLines` 的显式换行与空行预算，并把 block／render lock 的 `renderTextTruncated` 传播到失败覆盖风险判断；新增 `scripts/test-v383-koharu-fit-budget-contract.py`，接入 Koharu changed-file 路由和 full 静态检查。该 report-only 修复不改变 OCR 候选、翻译模型／prompt、ground truth 决策、生产 renderer/export、Koharu active artifact gate、普通图片 OCR 主路径、`metrics` 或仓库 `output`。候选 full `30870546266`、同 SHA 的 ci-fast 探针 `30870974176`、PR #147 fast 和 merge `02c36903824cef14c9ec7935954c3813e4dda2e4` 的 merge fast `30871766042` 均通过；探针报告仍为 13 blocks、`overallPassed=false`，block 5 从旧的 `2 lines / fontBudgetComfortable / currentSpriteFits` 如实变为 `12 lines / fontBudgetOverflowRisk / failureFallbackLongTextRisk`，不能据此声称 OCR、翻译或 Koharu 质量提升。
v3.84 保留既有 Koharu 渲染锁的 `renderMinFontSizeReached` 证据：`makeKoharuRenderSpriteFitPlannerReport` 现在把该信号传播到逐 block ledger、汇总与 report-only gate，并明确报告 block 5 的最小字号压力；新增 `scripts/test-v384-koharu-render-min-font-contract.py`，同步收紧 CI 路由兼容。该诊断改动不改变 OCR 候选、翻译模型／prompt、ground truth 决策、生产 renderer/export、Koharu active artifact gate、普通图片 OCR、`metrics` 或仓库 `output`。候选 full `30873895093`、同 SHA ci-fast 探针 `30874183417`、PR #148 fast `30874885165` 和 merge fast `30874929145` 均通过；探针报告记录 `renderMinFontSizeReachedBlocks=[5]`、block 5 为 `renderTextTruncated=true`，这只是诊断证据，不能据此声称 OCR、翻译或 Koharu 质量提升。
v3.85 让 `MangaKoharuRenderRegressionLockReport` 汇总既有 block/render lock 的 `renderMinFontSizeReached`，并在每个 render-lock decision trace、顶层 `renderMinFontSizeReachedBlocks`、`G-render-min-font-evidence` gate 与 Developer Console 诊断摘要中保持一致；新增 `scripts/test-v385-koharu-render-lock-min-font-contract.py`，同步 CI 路由兼容。该 report-only 修复不改变 OCR 候选、翻译模型／prompt、ground truth 决策、生产 renderer/export、Koharu active artifact gate、普通图片 OCR、`metrics` 或仓库 `output`。候选 full `30875436621`、同 SHA ci-fast 探针 `30875716372`、PR #149 fast `30876264990` 和 merge fast `30876301079` 均通过；探针报告记录 `renderMinFontSizeReachedBlocks=[5]`，block 5 同时保留实际截断，不能据此声称 OCR、翻译或 Koharu 质量提升。

v3.61 让图片复查结果行和完整图片预览继续消费既有 Vision OCR 方向证据：已判定的横排／竖排与有限方向置信度会进入复查上下文，结果行同时显示已知方向；OCR 置信度显示会安全夹到 0–100%。该改动只改善复查与 VoiceOver 语义，不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。仓库仍没有真实 Koharu 四件套、Speech corpus 或真实竖排图片 corpus，因此不声称 OCR、翻译或识别质量提升。

v3.62 让图片识别结果摘要在已有 `ImageOCRResultSummary` 基础上补充横排与竖排 block 计数，用户无需逐行浏览即可理解当前图片的方向分布；该 View/Store 摘要只消费既有字段，不重新运行 OCR、翻译或写入持久化，也不改变 renderer/export、漫画探针、Koharu 主路径或质量基线。真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，因此本版不声称 OCR、翻译或识别质量提升。

v3.63 将“识别结果”摘要合并成单一 VoiceOver header，稳定读出已有 OCR block 摘要，并按无图片、翻译未完成、没有待复查块或可以复查时说明下一步；它只改善图片复查的可操作语义，不新增 Store／持久化状态、不重跑 OCR／翻译，也不改变 renderer/export、漫画探针、Koharu 主路径或质量基线。真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，本版不声称 OCR、翻译或识别质量提升。

v3.64 统一处理图片 OCR 置信度边界：NaN/∞ 安全回退为 0，越界值夹到 0–1；布局、摘要、复查筛选、覆盖层和结果行共同消费安全值，避免摘要污染、错误复查状态或无障碍百分比转换崩溃。该修复不改变 OCR 候选、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线；真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，本版不声称 OCR、翻译或识别质量提升。

v3.65 让图片 OCR 修正 sheet 的低置信度提示也复用 ImageOCRResultSummary.normalizedConfidence，NaN/∞ 和越界置信度不会直接进入百分比格式化；只改善异常识别数据下的修正体验，不新增 Store／持久化状态，不改变 OCR 候选、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。历史 v3.47–v3.64 图片合同同步允许后续正式版本，避免版本推进造成 CI 路由误报；真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，本版不声称 OCR、翻译或识别质量提升。

v3.66 对 Vision OCR bounding box 做有限、正面积、整矩形单位坐标归一化，布局引擎丢弃 NaN/∞、零面积或完全越界 observation，避免图片覆盖、局部定位和阅读排序泄漏异常几何；新增可执行 geometry evaluator，历史 v3.47–v3.65 图片合同同步允许后续正式版本。该安全边界不改变 OCR 候选、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线；真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，本版不声称 OCR、翻译或识别质量提升。

v3.67 将同一 finite／正面积／单位空间整矩形边界下沉到 Codable 的 NormalizedImageRect，旧会话或外部解码得到的异常框不会进入完整图片覆盖、局部定位或导出 renderer；无效框会跳过显示/绘制，不新增 Store 持久化状态，不改变 OCR 候选、翻译、漫画探针、Koharu 主路径或质量基线。历史 v3.47–v3.66 图片合同同步允许后续正式版本；真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，本版不声称 OCR、翻译或识别质量提升。

v3.68 将无效或过期 OCR 框的局部放大从整图回退改为明确的“局部预览不可用”状态，并在 VoiceOver hint 中说明仍可关闭、编辑 OCR 原文或切换文字块；图片 OCR 修正对照继续允许编辑，不新增 Store／持久化状态，不改变 Vision OCR、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，因此本版不声称 OCR、翻译或识别质量提升。

v3.69 在图片结果行和完整图片预览的 VoiceOver 摘要中提前标明无效或过期 OCR 框的“定位不可用”数量；结果行保留 OCR 修正和切换文字块入口，视觉提示使用位置不可用图标。该改动只消费 `NormalizedImageRect.normalizedToUnit()` 的既有安全边界，不新增 Store／持久化状态，不改变 Vision OCR、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，因此本版不声称 OCR、翻译或识别质量提升。

v3.70 让完整图片预览的 VoiceOver hint 根据无效或过期 OCR 框数量动态分流：有效文字块可打开局部放大，异常文字块会明确局部预览不可用；既有 OCR 修正、切换文字块和状态门边界保持不变。该改动只消费 `NormalizedImageRect.normalizedToUnit()` 的既有安全边界，不新增 Store／持久化状态，不改变 Vision OCR、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，因此本版不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.71 让开发控制台的 Koharu readiness 摘要继续只读既有报告，同时显示坐标、mask payload、mask 拓扑和工件身份门控：可复制摘要记录 payload/topology verdict、阻塞项、源图哈希匹配和必需文件哈希状态，VoiceOver 在 shadow-only 边界内说明下一层阻塞与 CI 对账要求。该改动不创建或修改 active `test/koharu_artifacts`，不放宽 readiness gate，不改变普通图片 OCR、翻译、renderer/export、漫画探针或 Koharu 主路径；真实四件套、Speech corpus 与竖排图片 corpus 仍缺失，因此不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.72 让开发控制台正确区分 Koharu v1 `summary-only` 工件与尚未通过的 v2 mask payload／topology：摘要显示“未要求（v1 summary-only）”和“未要求（v2 拓扑）”，VoiceOver 说明 v2 门控尚未要求，可复制报告也保留解释后的门控状态；真实 v2 工件仍按实际失败与阻塞显示。该改动只读既有 readiness report，保持 report-only／shadow-only 边界，不创建或修改 active `test/koharu_artifacts`，不改变 readiness gate、普通图片 OCR、翻译、renderer/export、漫画探针或 Koharu 主路径。真实四件套、Speech corpus 与竖排图片 corpus 仍缺失，本版不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.73 让已忽略 OCR 文字块在原文为空时仍显示明确的“空 OCR 原文”，VoiceOver label 使用“空”作为稳定回退；非空原文、恢复按钮、焦点交接和恢复范围保持不变。该改动只改善图片复查与无障碍语义，不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，本版不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.74 统一普通图片 OCR 结果行与完整图片预览的旁贴／覆盖文字块对空 OCR 的可见回退：当 `block.original` 为空时显示“空 OCR 原文”；旁贴／覆盖仅在译文为空时使用该回退，非空译文继续优先显示。该改动只改善 View 语义，不新增 Store／持久化状态，不改变选择、Vision OCR、模型翻译、renderer/export、图片复查、漫画探针、Koharu 主路径或质量基线。
v3.75 让图片 OCR 修正对照预览与完整图片局部聚焦预览在 `block.original` 为空时继续提供稳定的 VoiceOver 上下文：无障碍 value 使用“空”回退，非空 OCR 原文仍原样读出。该 View-only 修复不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu 主路径或质量基线。
v3.76 隐藏完整图片局部聚焦预览中仅用于视觉识别的“局部放大”角标，避免 VoiceOver 与容器的“已定位文字块局部放大”上下文重复朗读；关闭、修正、复查、前后定位及 OCR value 保持不变。该 View-only 修复不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu 主路径或质量基线。
v3.77 让完整图片局部聚焦预览在无效或过期 OCR 框时隐藏重复的不可用状态子视图，由父容器统一读出“局部预览不可用；仍可关闭、编辑 OCR 原文或切换文字块”；关闭、修正、复查、前后定位及 OCR value 保持不变。该 View-only 修复不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu 主路径或质量基线。
v3.78 让关闭完整图片局部聚焦预览后的 VoiceOver 焦点回到对应的 OCR 结果行，避免预览容器消失后焦点丢失；该 View-only 焦点交接不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu 主路径或质量基线。

v3.79 让完整图片局部聚焦预览的上一个／下一个文字块导航在更新筛选后的目标 block 后，立即把 VoiceOver 焦点交给新的预览容器；仍沿用既有筛选顺序、边界 disabled、位置 value 与 View 私有 focus identity，不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu 主路径或质量基线。同步让 v3.14 历史导航合同接受等价的局部 target ID 写法。该 View-only 改动不代表 OCR、翻译、识别或 Koharu 质量提升。

v3.80 让筛选器隐藏当前选中的 OCR 文字块时，先清除失效的局部预览选择，再把 VoiceOver 焦点交给首个仍可见的结果行；若待复查列表已空则交给“本次复查完成”状态，没有完成态时交回筛选器本身，避免切换筛选后焦点落在已消失的预览容器。该 View-only 焦点 handoff 不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu 主路径或质量基线；同步让历史图片合同接受后续正式版本，不代表 OCR、翻译、识别或 Koharu 质量提升。

v3.86 修正漫画探针 render-lock 输出工件 ledger 的时序误报：`probe_report.json` 在最终重写 `1_ocr_probe_text.txt` 前组装，现将两者标记为 planned final write，避免实际非空 OCR 文本被误记为空，使 `coreOutputFilesNonEmpty` 和 `G-render-core-png-retained` 与云端探针真实输出一致；失败写入仍不会生成成功报告。该 report-only 修复不改变 OCR、翻译、renderer/export、Koharu active artifact gate、metrics 或仓库 output。full `30876931497`、同 SHA ci-fast `30877262645`、PR #150 fast `30877905238`、merge fast `30877976728` 均通过；block 5 的实际文字截断仍只是诊断证据，真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失。

v3.87 修正上述 planned final write 的推荐动作：`probe_report.json` 的 `plannedFinalReportWrite` 与 `1_ocr_probe_text.txt` 的 `plannedFinalOCRTextRewrite` 在 `nonEmpty=true` 时统一报告 `recommendedAction=keepReportOnly`，只有缺失或未检查输出才建议 `inspectRenderOutputExport`；新增 `scripts/test-v387-koharu-render-output-action-contract.py` 并接入 Koharu changed-file/full 静态路由。该 report-only 修复不改变 OCR、翻译、renderer/export、Koharu active artifact gate、metrics 或仓库 output。候选 full `30878519259`、同 SHA ci-fast 探针 `30878916261`、PR #151 fast `30879584339`、merge fast `30879645680` 均通过；探针仍记录 13 blocks、block 5 的真实截断和 `openRenderIssueDetected`，不能据此声称 OCR、翻译、识别或 Koharu 质量提升。真实 `test/koharu_artifacts/` 四件套、Speech corpus 与竖排图片 corpus 仍缺失。

v3.88 对齐 Koharu render-lock 的核心输出 gate：当 required ci-fast 输出均已留存且非空时，`G-render-core-png-retained` 与 `outputFileChecks` 统一推荐 `keepReportOnly`；只有缺失或空输出才引导 `inspectRenderOutputExport`。新增 `scripts/test-v388-koharu-render-core-output-gate-action-contract.py`，并同步历史 v3.82–v3.87 合同与 CI 路由。该 report-only 修复不改变 OCR、翻译、renderer/export、Koharu active artifact gate、metrics 或仓库 output。full `30880132762`、同 SHA ci-fast 探针 `30880751340`、PR #152 fast `30881554748`、merge fast `30881613654` 均通过；探针仍诚实记录 13 blocks、block 5 截断与 `openRenderIssueDetected`，真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，不能据此声称质量提升。

v3.89 让开发控制台的 `outputFiles` 摘要直接显示 required 输出的 `recommendedAction` breakdown（例如 `actionBreakdown=keepReportOnly=5`），与 `G-render-core-png-retained`／`outputFileChecks` 共用既有报告状态；新增 `scripts/test-v389-koharu-render-output-summary-action-contract.py` 并接入 Koharu changed-file/full 路由。该 report-only UX 不新增 Store／持久化，不改变 OCR、翻译、renderer/export、Koharu active artifact gate、metrics 或仓库 output。候选 full [30882033347](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30882033347)、同 SHA ci-fast 探针 [30882428016](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30882428016)、PR #153 fast [30883254522](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30883254522) 与 merge fast [30883306577](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30883306577) 均通过；探针仍记录 13 blocks、block 5 截断和 `openRenderIssueDetected`，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不能据此声称 OCR、翻译、识别或 Koharu 质量提升。

v3.90 修正漫画探针失败覆盖的显示排版：保留完整 OCR fallback 与首行 `翻译失败` 标记，仅把 OCR continuation 的显式换行压缩为空格，并让安全布局诊断、fit planner 与实际覆盖绘制共用同一 probe-render display transform。该 probe-render 修复不改变 OCR 候选、翻译输入、Store、普通图片 renderer/export、Koharu active artifact gate、ground truth、metrics 或仓库 `output/`。候选分支 `codeb/v3.90-koharu-failure-overlay-compaction` 的 commit `3344a8cd192684bf66e6cdff3397314c3fd8da05` 已通过 PR [#154](https://github.com/bengzhu/project1_lgbt_naxida/pull/154) 合入，merge SHA 为 `ee6046d491f327d6c71744876e0a4b4b6aab3947`，远端候选分支已删除，`main` 未触碰。full [30884104150](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30884104150)、同 SHA ci-fast 探针 [30884547044](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30884547044)、PR fast [30885582974](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30885582974) 与 merge fast [30885667505](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30885667505) 均通过；ci-fast 报告的 `renderLockVerdict=renderStableWithProxyBoundaries`、`renderTextTruncatedBlocks=[]`，block 5 为 `renderTextTruncated=false`、`renderMinFontSizeReached=false`、`failureFallbackFitVerdict=failureFallbackAccounted`，覆盖 PNG 非空。Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，Speech corpus 与真实竖排图片 corpus 仍缺失，不能据此声称 OCR、翻译、识别或 Koharu 质量提升。
v3.91 让开发控制台的漫画探针结果先按既有报告信号给出“诊断分流”摘要：汇总 OCR 疑似 block、翻译模型／语言质量 block、覆盖布局异常与 Koharu 工件下一步，并显示 clean-text baseline／variant pass rate、floor verdict 和 report-only／main-flow 边界；失败 block 行同步显示现有 failureCategory 的“模型输出失败／译文质量失败／OCR 疑似损坏”标签，VoiceOver 读出相同分流上下文。该 View-only 改动只消费既有 `MangaOverlayProbeReport`，不新增 Store／持久化、不运行第二次探针、不修改 OCR 候选、翻译 prompt／model、renderer/export、metrics 或 output。候选分支 `codeb/v3.91-koharu-diagnostic-triage` 的最终 SHA `bbb73b14a90a10438d4cacf46344881d21d6206e` 已通过 PR [#155](https://github.com/bengzhu/project1_lgbt_naxida/pull/155) 合入 `smalldata_test`，merge SHA `1ab18c3e9f2d04fbd51680b5b1b606113d86d032`，远端候选分支已删除，`main` 未触碰；full [30886955217](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30886955217)、同 SHA ci-fast 探针 [30887582600](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30887582600)、PR fast [30888608909](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30888608909) 与 merge fast [30888676363](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30888676363) 均通过。ci-fast 报告保持 13 blocks／12 failures、`renderLockVerdict=renderStableWithProxyBoundaries`、render issue/truncation/min-font 列表为空，model floor 仍为 `promptVariantRegresses`（baseline `0.4545`、variant `0`），Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；PNG 仅作探针输出检查，真实四件套、Speech corpus 与竖排图片 corpus 仍缺失，不能据此声称 OCR、翻译、识别或 Koharu 质量提升。
v3.92 让普通图片 OCR 复查筛选从“全部／待复查”扩展为“全部／待复查／低置信／方向待定”：低置信与方向待定直接复用既有 `ImageOCRResultSummary` 判定，待复查仍是两类风险的未完成并集；筛选属于 View 本地状态，不改变 `imageTranslationBlocks`、图片预览、导出、Store 或 OCR／翻译链路。切换筛选提供具体 VoiceOver hint 与无结果空态，修正或忽略文字块后在当前风险筛选中保持下一步定位。Developer Console 同时增加只读漫画诊断筛选（全部／失败／OCR／翻译／布局），只消费既有 `MangaOverlayProbeReport` 的 diagnostics、failureCategory 与 render flags，探针重新运行时重置筛选；不修改 `probe_report`、OCR 候选、翻译 prompt／model、renderer/export、metrics 或 output。候选 SHA `83a3e30d25483cb67d788babd4998f05afb42c08` 已通过 PR [#156](https://github.com/bengzhu/project1_lgbt_naxida/pull/156) 合入 `smalldata_test`，merge SHA `118c8c039d752a24a6c992e8f942ec34cb43e009`，远端候选分支已删除，`main` 未触碰；full [30889811326](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30889811326)（Xcode/UI 合同/JUnit 10/10）、PR fast [30890241624](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30890241624) 与 merge fast [30890322575](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30890322575) 均通过。push 与 fast 均按默认跳过漫画探针；真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.93 修复普通图片 OCR 复查筛选的跨图片残留：当 Store 的 `imageTranslationRevision` 变化（新图、重试、重新识别或清空）时，View 私有筛选恢复为“全部”，并同步清除旧选中 block 与焦点，避免新图被旧的低置信／方向待定筛选误显示为空。该改动不把筛选写入 Store／持久化，不改变 `imageTranslationBlocks`、Vision OCR、翻译、预览、导出或漫画探针；新增 `scripts/test-v393-image-review-filter-reset-contract.py`。候选 SHA `a3cea5a202f1de8a9b13aa4809db583b55480dcd` 已通过 PR [#157](https://github.com/bengzhu/project1_lgbt_naxida/pull/157) 合入，merge SHA `3f6565f65cc8ff965ba909f5d6e27ad0a508436c`；full [30890823578](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30890823578)（Xcode/UI 合同/JUnit 10/10）、PR fast [30891431628](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30891431628) 与 merge fast [30891485989](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30891485989) 均通过。探针默认 skip，真实 Koharu 四件套仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.94 修复漫画探针失败入口的状态与输出隔离：每次运行在查找 bundle 内 `test/1.png` 前清空上一轮 `mangaOverlayProbeReport` 与 blocks；缺失图片时重建 App 沙盒 `Output`，失败报告传播清理条目数和是否成功，清理失败时明确旧输出可能残留。新增 `scripts/test-v394-manga-probe-failure-cleanup-contract.py` 并接入 Koharu changed-file/full 路由。该 report-only/状态一致性修复不改变 Vision OCR 候选、翻译 prompt/model、生产 renderer/export、普通图片 OCR、Koharu active artifact gate、metrics 或仓库 `output`。候选 commit `31c61bf0a682d8a0e28376a123285f037272a60e` 已通过 PR [#158](https://github.com/bengzhu/project1_lgbt_naxida/pull/158) 合入，merge SHA `ea6be1ddacaa34953b0c7f3389c342dc6e9ff4e3`；full [30893309273](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30893309273)、PR fast [30893920011](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30893920011)、merge fast [30893993759](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30893993759) 均通过，full Xcode/JUnit `10/10`，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.95 让 Developer Console 在漫画探针报告没有任何 OCR blocks 时显示明确的“本次探针未生成文字块”状态，隐藏无意义的诊断筛选器，并通过 VoiceOver 说明当前错误/重试范围；只有存在 blocks 时才继续显示筛选与逐块诊断。新增 `scripts/test-v395-manga-probe-empty-state-contract.py` 并接入 UI/full 路由。该 View-only/诊断 UX 修复不新增 Store／持久化，不重新运行探针，不改变 OCR 候选、翻译 prompt/model、renderer/export、普通图片 OCR、Koharu active gate、metrics 或仓库 `output`。候选 SHA `802103e413261cc1632d1129362695728af45215` 已通过 PR [#159](https://github.com/bengzhu/project1_lgbt_naxida/pull/159) 合入，merge SHA `a57e65b2c8220de39b59177ec873a394a3398781`；full [30984932342](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30984932342)、PR fast [30985360673](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30985360673)、merge fast [30985413482](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30985413482) 均通过，full Xcode/JUnit `10/10`，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.96 修正 Developer Console 漫画探针诊断分流的状态色：当真实 Koharu active artifact gate 仍为阻断时，即使当前探针 blocks 全部通过，分流摘要仍显示 warning，不再用成功色暗示已可进入 shadow OCR；非阻断且报告通过时才显示 success。新增 `scripts/test-v396-koharu-triage-tone-contract.py` 并接入 UI/full 路由。该 View-only/report-only 修复不新增 Store／持久化，不调用探针，不改变 OCR、翻译 prompt/model、renderer/export、普通图片 OCR、Koharu active gate、metrics 或仓库 `output`。候选 SHA `ab4d0ae59fbf2e0d6c6747fce331060ecfcc57ee` 已通过 PR [#160](https://github.com/bengzhu/project1_lgbt_naxida/pull/160) 合入，merge SHA `12140ca11d3e888e74a974dffcfda41a0ca8357d`；full [30985776084](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30985776084)、PR fast [30986258687](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30986258687)、merge fast [30986307343](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30986307343) 均通过，full Xcode/JUnit `10/10`，探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.97 让漫画探针的“布局”诊断筛选与 triage 摘要共享既有 report-only render risk 集合：纳入 fit planner 的字号预算紧张、sprite containment、sibling overlap、failure overlay，以及 render-lock 的已有 issue/min-font/truncation 信号；不再因顶层 `diagnostics` 暂无 unresolved/truncated 而把实际布局压力显示为 0。新增 `scripts/test-v397-koharu-layout-triage-contract.py` 并接入 UI/full 路由。该 View-only/report-only 修复不新增 Store／持久化、不调用探针、不改变 OCR、翻译 prompt/model、renderer/export、普通图片 OCR、Koharu active gate、metrics 或仓库 `output`。新 ci-fast 报告 [30986469563](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30986469563) 观察到 10 个 font-budget tight、7 个 sprite-containment、6 个 sibling-overlap block；候选 SHA `c552e6170cfd7b6daab4cbebd885bdb44314b007` 已通过 PR [#161](https://github.com/bengzhu/project1_lgbt_naxida/pull/161) 合入，merge SHA `87b102cdd0d8f08bcea876cfcd08645ddc10cc58`；full [30987210261](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30987210261)、PR fast [30987676638](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30987676638)、merge fast [30987725142](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30987725142) 均通过，full Xcode/JUnit `10/10`，候选探针默认 skip，真实 Koharu readiness 仍受四件套门控，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.98 让 Developer Console 的漫画探针 OCR 与翻译筛选、triage 摘要共享只读 `mangaProbeOCRRiskBlockSet` 与 `mangaProbeTranslationRiskBlockSet`：合并既有 diagnostics、model-floor、translation failureCategory 与 OCR 疑似证据，避免 floor 报告存在时丢掉 diagnostics。新增 `scripts/test-v398-koharu-diagnostic-risk-union-contract.py` 并接入 UI/full 路由；不新增 Store／持久化、不运行探针、不改变 OCR、翻译、renderer/export、Koharu gate、metrics 或 `output`。候选 SHA `7a352a5050c8846765ad4b139e7a6a125dfb4712` 已通过 PR [#162](https://github.com/bengzhu/project1_lgbt_naxida/pull/162) 合入，merge SHA `3b953b83d12a092df1995c974cb33522dc02331a`；full [30988262491](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30988262491)、PR fast [30988802078](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30988802078)、merge fast [30988876405](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30988876405) 均通过，候选 full Xcode/JUnit `10/10`，PR/merge fast 明确复用 full receipt；本轮探针默认 skip，真实 Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

## 运行

1. 用 Xcode 打开 `AITRANS.xcodeproj`。
2. 选择 iPhone 模拟器或已连接的 iPhone。
3. 运行 `AITRANS` target。

如果命令行构建提示当前开发目录不是完整 Xcode，可以临时这样构建：

```sh
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild \
  -project AITRANS.xcodeproj \
  -scheme AITRANS \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath .derivedData \
  CODE_SIGNING_ALLOWED=NO build
```

`Assets.xcassets` 已进入 target 的 Resources build phase，包含 AppIcon、AccentColor 和日间/夜间语义色。设置页可选择跟随系统、日间或夜间；默认跟随系统。

项目根目录的 `test/` 已作为 folder resource 打进 App bundle。往 `test/` 放入音频或 OCR 图片后，需要重新构建安装 App，开发控制台的测试按钮才会扫描到新文件。

## 协作与云端验证

- 日常工作主分支是 `smalldata_test`；`main` 只作外观展示，不合并日常开发成果。
- Agent B 候选实现分支使用 `codeb/vX.Y-短标题`。核心代码 push 执行一次 task-scoped full；成功 SHA 获得 `AITRANS CI/full-validation` status，随后 PR 和已验证 merge 只做 fast follow-up。
- 本机默认只跑轻量检查；除非人工明确要求，不默认跑本机 Xcode build 或漫画探针。
- Agent C 验收使用未加密的 `AITRANS CI Results` artifact；`xcodeBuildRequired=true` 时核对 `.xcresult`，build-skip 快路径则核对 skip reason，同时核对 `junit.xml`、`xcodebuild.log`、`ci-artifact-manifest.json` 和失败摘要。
- 云端验证必须先用 `gh auth login` 拿到 GitHub 权限，Agent C 才能下载 Actions 结果包。
- Agent C 下载的云端测试缓存默认放在 `/private/tmp/aitrans-c-review-<run_id>/`，由人工确认后删除。
- Agent C 通过 PR 合并后必须删除远端 `codeb/...` 候选分支，避免分支无限堆积；无权限删除时要明确说明。
- 现有加密软件包 artifact 只用于软件包交付，workflow 仅手动触发，不再随 merge 自动 archive，也不作为 Agent C 验收依据。
- `AITRANS CI Results` 对 `codeb/**` 核心 push 默认 `validationProfile=full`、`probe_mode=skip`：按变更领域运行契约，App 相关任务跑一次 Xcode build，不下载 GGUF、不启动漫画探针。
- PR 只在 opened / reopened / ready-for-review 时执行 fast，不监听 synchronize；merge 只有核验第二父 SHA 的 full-validation status 成功后才执行 fast，否则自动回退 full。
- fast follow-up 跳过 Xcode、Speech/UI/Koharu 领域大契约和 UI evidence，manifest 必须写明复用 SHA、status、profile、reason 和 skip reason；Agent C 仍需验收候选 full 结果，不能只看 fast 包。
- 版本整合分支 `1.*` 也会触发同一快验；云端探针从构建出的 App `Info.plist` 动态读取 bundle ID，当前工程值为 `com.local.aitransform114`。
- full CI 会按变更范围选择 Speech、UI、文本首页或 Koharu 契约；非 App 构建相关 full 仍可跳过 Xcode。父提交 full 成功后的纯文档/metrics follow-up 可传播收据；父收据缺失或失败时自动扩展到整条候选 diff。
- UI evidence 默认不运行。重大 UI 任务在候选 commit 标记 `[ui evidence]` 或手动选择 `ui_evidence_mode=full`；Speech 和普通 PR/merge 不截图。漫画/翻译结果图由手动 `ci-fast/full` 的 `output/` 产物提供。
- 手动 `workflow_dispatch` 可选填 `koharu_artifact_release_tag`、`koharu_artifact_asset`、`koharu_artifact_sha256`，从 Release 下载真实 Koharu 四件套 archive 并在 Xcode build 前注入 `test/koharu_artifacts/`；archive 必须只有一个目录同时包含四件套，CI 结果包会记录 source image 和四件套文件的 SHA256 / size identity；`koharu_artifact_required=true` 时下载、SHA、解压、唯一目录检查或 validator 失败会直接失败。
- 注入 Koharu artifact 且运行 `ci-fast` / `full` 时，云端探针 smoke 必须证明 App receipt、source image / 四件套 size + SHA256、CI manifest identity、dry-run 和 reconciliation 全部匹配。external shadow OCR 除 executed / candidate / OCR count sanity 外，还必须证明 TextBox ID 非空唯一、一对一 assignment、matched / succeeded / failed / skipped 分区一致、`duplicateAssignedTextBoxIDs = []`、`coverageVerdict = complete`、`successfulCoverageRatio = 1`；不能只看下载、validator、`readyForShadowOCR` 或局部 OCR 成功。
- 手动 `ci-fast` / `full` 探针会从 Release `model-gemma-3-270m-it-qat-q4_0-v1` 下载 `gemma-3-270m-it-qat-Q4_0.gguf`，校验 SHA256 `3626e245220ca4a1c5911eb4010b3ecb7bdbf5bc53c79403c21355354d1e2dc6`，用 Actions cache 复用 `.ci-models/`，并把模型导入模拟器 App 沙盒。
- 需要探针验收时，手动 `workflow_dispatch` 选择 `ci-fast` 或 `full`。探针复用同一次 Debug simulator build 产物安装 App，把缓存模型复制到 App sandbox 的 `Application Support/Models/Gemma-1.5B/model.gguf`，用 `AITRANS_RUN_MANGA_PROBE=1` 和 `AITRANS_MANGA_PROBE_MODE` 启动 App，导出本轮 `output/` 到未加密结果包。`Gemma-1.5B` 是历史目录名；实际文件必须用 Release asset 名、字节数和 SHA256 校验确认。
- `ci-fast` 仍是真模拟器、Local GGUF、真实 `test/1.png`、deterministic 解码、whole-page OCR、bubble-first 融合、逐块翻译、失败块覆盖、`probe_report.json`、`clean_text_diagnostic.json`、`1_ocr_probe_text.txt`、`1_debug_boxes.png`、`1_translated_overlay.png`、Koharu external artifact gate，以及 v1.18+ 必需的 report-only / detector-lite 受限 shadow 报告；只跳过 lexicon / Vision API / slice / crop experiment / line shadow / batch / 纠错翻译对照 / contact sheet 等高成本对照和诊断 PNG。Agent C 通过 manifest 的 `probeMode`、`probeSkippedReason`、`probeFastPathEnabled`、`probeSkippedDiagnostics`、`probeOutputRetainedFiles` 和 `probeReportSummary` 区分快验、快速探针与全量探针。
- v1.57 起 bundle-lite / promotion gate 会消费 TextBox -> SegmentMask linkage，输出 linkage breakdown、review / blocked blocks 和 TXT 摘要；v1.58 起 convergence 继续消费这些 linkage work item / gate，使最终收敛层能阻塞弱 linkage。v1.64 起 external TextBox shadow OCR 会在真实 artifact ready 后对竖排或接近 90/180/270 度的 TextBox 执行有上限的 orientation-aware rotation OCR，并记录 `orientationShadowPathPartialBlocks`、`orientationUnsupportedBlocks`、`orientationUnsupportedReasonBreakdown`、attempted rotations、selected rotation、Vision language profile 和 unsupported reason；line polygon warp 与任意角度 deskew 继续进入 `WI-external-textbox-orientation-shadow-path` / `G-external-textbox-orientation-shadow-path` convergence blockers，不能误判 `closedReportOnly`。v1.65 起 validator 会输出 `orientationMetadataSummary`，CI manifest 会透传 `koharuArtifactValidationOrientationSummary`；v1.66 起 validator / CI 还会透传 `artifactIdentitySummary` / `koharuArtifactValidationIdentitySummary`；v1.67 起 App 探针 runtime 也会在 `externalArtifactReadinessReport.artifactIdentityReceipt` 和 contract dry-run 中记录 source image / 四件套 size 与 SHA256；v1.68 起 `koharuArtifactIdentityReconciliationReport` 把 App receipt 规范化为 CI manifest 字段路径，Actions 会输出 `koharuArtifactIdentityReconciliationMatch.matchVerdict`。v1.69 起真实 artifact ready 后 `WI/G-external-textbox-shadow-ocr-coverage` 同时要求 App 侧 identity receipt、identity reconciliation ready、contract dry-run ready、dry-run 边界安全、shadow OCR 已执行、`candidateCount > 0`、`ocrExecutedCount > 0` 且 `ocrSucceededCount > 0`，未闭合时 gate 为 blocked。v1.80 起 App runtime readiness 与 identity reconciliation 也会解析并强制 manifest `sourceImageSHA256` 匹配 runtime 可见的 bundle `test/1.png` SHA256，CI 摘要会透传 declared / expected / matches。该路径仍是 report-only gate，不新增 LLM / PNG，也不改变主 OCR、翻译、覆盖图或 active artifact。
- v1.70 起只要 workflow 填写 Koharu artifact archive，就必须使用 `ci-fast` 或 `full`，不能用 `probe_mode=skip`；云端 smoke 会核对 coverage / orientation work item 与 gate ID、status、App-side identity 摘要和 `ocrSucceededCount`，orientation blockers 未清空时不得误判 passed。
- v1.92 更新 v1.64 的 orientation 边界：合法、非退化的凸四点 `linePolygons` 会用有上限的透视校正生成逐行 shadow OCR crop，只有 warp OCR 输出被选中时才标记 `deskewExecuted`；bbox fallback、warp 失败和任意角度 deskew 继续作为 convergence blockers。该能力仍只消费真实四件套，保持 shadow-only，不改变主 OCR、翻译或覆盖图。
- v1.74 起 `ci-fast/full` 的未加密 manifest 和 failure summary 也会直接汇总 v1.39-v1.46 native-lite detector / shadow OCR / refinement / closed-loop / instance-lite / SegmentMask refinement / bundle / promotion gate 的 verdict、counts、linkage blockers 和 convergence work item / gate 状态，避免 Agent C 为了确认 native-lite 阻塞再深挖完整 `probe_report.json`。
- 云端探针验收口径是报告可解析、`engineUsed = Local GGUF`、`totalBlocksDetected > 0`、关键 JSON/TXT/PNG 可用；`overallPassed=false` 仍可能是当前模型质量基线，不单独作为 CI 失败。若探针超时，结果包会保留 `manga-probe.log`、`app-console.log` 和 `output/manga_probe_progress.json`；`ci-fast` 启动后 180 秒未创建 progress 会提前失败、progress 300 秒不更新会提前收束，`full` 分别为 300 秒和 600 秒。
- 显式启用 UI evidence 的 App 候选 CI 会复用当前 Debug simulator build，生成 14 张 `ui-evidence/`（12 张 compact iPhone + 2 张 wide iPad）和 manifest；wide iPad 同时覆盖文本空态和图片成功/风险复查态，音频矩阵分别覆盖 recognizing + Reduce Motion 与 translating + 取消入口。v1.87 全 App interaction contract 与 v1.88 文本首页 contract 分别作为独立 testcase，截图或任一契约失败都会阻塞候选分支；当前不采集 Mac 运行态证据。

## 当前界面

- `文本`：首页使用静态技术网格与输入到译文的导向线路承托工作区，语言、粘贴、Prompt、翻译、模型状态和译文保持清晰动作层级。系统纯文本“粘贴”只在点击时读取剪贴板；空输入直接填入，已有输入换行追加且不自动翻译。软件键盘提供“完成”按钮，翻译、新会话或离开文本页前会先结束输入焦点。iPad 宽屏使用输入/输出并排，iPhone 自动降为单列。
- `图片`：图片检查区保持主视觉，有低置信或方向待定块时显示本次复查完成/总数/剩余进度；进度仅由当前图片会话的 Store 内存状态持有，界面重建后仍可继续，新图、取消和清空会重置。入口按进度明确显示开始或继续；每个风险结果行可直接完成并定位下一块，也可撤销，主行点击仍只负责图片定位与局部放大。打开“修正识别文字”后，sheet 会显示带黄色框的当前文字块局部图；局部图无法生成时明确提示但不阻止手工修正，低置信/方向待定时还会说明复查原因。`trim` 后仍等于当前原文的编辑不再被误当成未保存修改：按钮仍为“确认无误”，取消或下滑关闭不会额外要求放弃；真正改字时才保留既有确认和只重译该 block。多行修正输入的键盘工具栏提供可访问“完成”动作，滚动 sheet 时也会交互式收起键盘；取消、打开忽略确认或保存前仍会先收起键盘。开始“保存并重译”后，当前 OCR 输入会锁定至这一次翻译完成或失败，避免异步完成覆盖刚保存后的新增输入。上述行为都不改变校验、discard protection、Store 输入或关闭后焦点交接。取消、放弃未保存修正或无修改时下滑关闭该 sheet，会在完全关闭后回到发起位置：结果行入口回到结果行，局部放大铅笔入口回到同一局部预览；成功修正或忽略仍沿用下一结果行、下一块局部放大、完成态或忽略行等更具体目的地。局部放大保留完成/撤销及前后切换，全部完成后可重新开始；v3.43 让筛选首尾的前后按钮在 disabled 时读出“当前已是第一个／最后一个文字块”，可用时读出定位上一个／下一个文字块，结果行主操作也会按是否已定位读出取消定位或在图片预览中定位；v3.44 让前后按钮进一步读出当前筛选位置（如 `当前位置 1 / 3`）。v3.45 让完整预览中的 OCR 覆盖块复用同一套定位提示，点击图片覆盖块与点击结果行会听到一致的已定位／未定位操作语义；v3.46 让加载/失败卡片也明确读出状态和下一步，重试只生成屏幕预览，不会重复 OCR 或翻译；v3.47 让照片／文件、取消、重试、重新识别、导出和清空按钮分别说明其会影响当前图片、OCR／翻译或导出文件的范围，照片选择器还会动态说明是首次选择还是替换当前图片。完整预览中的 OCR 覆盖块可直接点选或取消；点击 OCR 结果行会把图片工作区带回视口并高亮对应块。目标语言菜单与旁贴/覆盖 segmented control 位于工具区，照片、文件、取消、重试、导出和 OCR 块状态集中展示。任务从 loading 起冻结内容语言；失败或取消后修改输入/目标语言只更新下次 Retry 选择，选回当前内容语言会撤销对应待重试更改，菜单和状态不会继续显示无效差异；免费模式的图片输入语言菜单显示锁定图标，尝试修改时明确提示 Pro 且不污染文本页语言；结果标题始终显示当前内容实际语言，已完成图片选择不同语言后会重新识别或翻译。
- `图片操作状态边界`：OCR 结束后的逐块翻译仍保留截图、文字块、选中定位和局部预览，方便确认进度；不过 loading／recognizing／translating、失败或不完整状态都不会开放修正、恢复、忽略恢复、覆盖方式切换、开始／重启复查或完成／撤销复查。只有完整 `.translated` 才能写入这些变更；导出重绘期间还会继续锁住改变结果的操作，避免用户在中间态看到 block 就提前提交复查结论。v3.42 在当前仍有文字块且锁定存在时显示一条警示状态行；每个禁用的改变结果或复查入口也会给 VoiceOver 复用相同的状态化原因。逐块翻译明确可继续查看／定位但不可提交，失败明确提示先重试，导出重绘只说明图片编辑暂时锁定。
- `图片局部修正快捷入口`：选中 OCR block 后，局部放大窗在关闭按钮下提供 44pt 铅笔入口；仅当当前图片已完整翻译、未在导出重绘且 block 仍在当前活动集合时才会打开修正页。它复用既有修正 sheet 和 revision-checked 关闭后焦点交接；取消、放弃未保存修正或无修改关闭会回到同一局部预览，结果行入口仍回到结果行。
- `音频`：实时长按识别与音频文件识别分区展示，保留取消入口和 locale、离线要求、耗时、词数、片段、置信度与失败原因。
- `历史`：搜索、恢复、删除、归档、导入、导出和清空使用一致命令层级；空历史和无搜索结果使用系统空状态。
- `设置`：集中管理 Pro、提示词、模型、本地数据和受保护的开发者入口。提示词支持方向、新建、复制、编辑、删除和内置锁定；模型页支持 Mock/Local、GGUF 下载/导入/移除、自检和生成参数。
- `开发`：仅在开发者模式开启后显示。raw prompt/output、批量 raw probe、固定素材测试和漫画探针报告采用高密度、可选择的等宽文本布局；漫画报告存在时会额外只读展示 Koharu 四件套 readiness、缺件和 nextAction，便于复制给工件提供方，不触发新 probe、不改变任何探针字段或执行语义。
- 音频识别页会显示本次运行的输入名、locale、本机识别要求、耗时、词数、分段数、平均置信度、文本或失败原因；识别和翻译状态分开，检查中、识别中或翻译中均可取消。取消会先失效当前 run，再取消识别与模型翻译 Task；旧授权、识别、翻译或摘要回调不会覆盖立即重试后的新状态。

## Pro / 内购占位

当前已接入 StoreKit 2 订阅骨架，但还没有 App Store Connect 线上商品。发布前需要在 App Store Connect 创建同 ID 自动续期订阅，并把价格配置为约 1 美元/月：

- 预留商品 ID：`com.local.aitrans.pro.monthly`。
- 展示价格：`$0.99/月`。
- `开通 Pro` 会尝试读取 StoreKit 商品并购买；如果 App Store Connect 未配置商品，会显示未找到商品。
- `校验订阅` 会读取 `Transaction.currentEntitlements` 并同步 Pro 状态。
- `开发解锁` 仍保留为本地调试开关，便于真机测试未上架功能。
- 免费：中文、英语文本翻译。
- Pro：解锁日语、法语、德语目标语言。
- Pro：解锁同声传译入口。同声传译在音频页长按麦克风开始采集，松手结束，Apple Speech 本机识别结果先进入文本框，再点击按钮交给当前 Mock/Local 翻译接口。识别侧使用 `requiresOnDeviceRecognition = true`，支持情况取决于设备、系统和语言包。
- Pro：音频文件断网识别测试入口。选择音频后，App 会复制到沙盒，用 `SFSpeechURLRecognitionRequest` 和 `requiresOnDeviceRecognition = true` 做 Apple 本机识别，成功后自动交给当前大模型翻译。
- Pro：`test/` 音频/OCR 测试入口。App 会扫描 bundle 内 `test/` 的首个匹配文件；音频支持 `.m4a`、`.wav`、`.mp3`、`.caf`，图片支持 `.png`、`.jpg`、`.jpeg`、`.heic`。未找到文件时会显示 `test/ 未找到可测试音频/图片`。
- Pro：后台一键翻译入口已做开发占位。iOS 普通 App 不能像 Android 一样常驻覆盖其他 App 的任意悬浮窗；可行路线是 Share Extension 处理截图/文本，或 ReplayKit Broadcast Upload Extension 在用户显式启动屏幕广播后处理屏幕帧。
- Pro：图片翻译已接入开发版流程。选择图片后，App 使用 Apple Vision `VNRecognizeTextRequest` 本机 OCR 得到文字块和 `boundingBox`，逐块交给当前本地模型翻译，并在图片预览上提供“旁贴”和“覆盖”两种定位展示模式。

## 离线 OCR / 屏幕翻译方案

当前结论基于 Xcode 26.5 SDK 头文件能力：

- 图片翻译不一定需要额外 OCR 模型。Apple Vision 提供 `VNRecognizeTextRequest`，能返回 `VNRecognizedTextObservation`，包含识别文本和位置框；这适合断网本机 OCR。
- 当前图片翻译流水线是：图片输入 -> Vision OCR 得到文字块和 `boundingBox` -> 按行/块送入本地 Gemma 翻译 -> 在图片预览层按原坐标旁贴译文，或用半透明译文块覆盖原文区域。
- 如果后续需要更强的漫画/竖排/复杂版面能力，可以增加本地 OCR/版面分析模型，但第一版优先用 Vision，减少模型体积和部署风险。
- 后台一键屏幕翻译不能依赖普通 App 悬浮窗覆盖全系统页面。iOS 可考虑两条合规路线：用户分享截图到 App/Share Extension；或用 ReplayKit Broadcast Upload Extension 获取屏幕帧，再在扩展或主 App 协作里跑 OCR/翻译。

## 本地数据

数据保存在 App sandbox 的 Application Support 目录：

```text
Application Support/AITRANS/state.json
```

`state.json` 保存：

- 当前会话：转录行、摘要、语言、模式、提示词、模型引擎、会话时长。
- 历史记录：最多保留 60 个会话。
- 提示词模板：内置模板和用户新增模板；新版本同时保存 `英译中` 和 `中译英` 两套指令。旧 JSON 只有单个 `instruction` 时会自动迁移为两个方向，不丢自定义提示词。
- App 设置：当前引擎、语言、选中的提示词、temperature、max tokens、开发期 Pro 开关。

历史页的 `导出 JSON` 会先额外写出：

```text
Application Support/AITRANS/aitrans-export.json
```

随后会打开系统文件导出面板，方便把同一份 JSON 保存到 Files、iCloud Drive 或其他位置。

历史页的 `导入 JSON` 可以选择同结构的 `aitrans-export.json` 或 `state.json`。导入时会先归档当前会话，再合并历史和提示词；内置提示词会保留，同 ID 的会话或提示词不会重复导入。

模型页的 `运行自检` 会检查：

- 本地 JSON 是否可写入并解码。
- Gemma 1.5B Mock 是否能生成非空输出。
- LLM 接口是否能收到模拟输入，并从当前适配器回传非空输出。
- Local 模式是否已经安装 GGUF 模型。

模型页的 `运行接口自测` 会按当前语言方向构造翻译输入，要求输出非空、不能等于原文、不能包含原文、不能是占位答复，并且要像目标语言。英译中曾出现模型原样返回英文的问题，后续排查必须优先使用接口自测和开发页 raw 探针，不要只看 UI 结果猜测。

开发页 raw 探针用于定位这几类问题：

- “运行原始接口”：按当前语言方向测试单条输入。Local 模式下这是送入 `llama.cpp` 的真实字符串和 raw 输出；Mock 模式只展示模拟请求预览。
- “运行批量探针”：依次跑 `Keep the model on device.`、`The meeting starts at 9:30 tomorrow.`、`Save the transcript locally.`、`请把会议记录保存在本地。`、`明天九点半开始会议。`，覆盖英译中和中译英。
- “大模型实际输入”：完整展示 app 当前送给模型的 prompt，包括语言方向、输入文本和当前方向提示词。Local 模式下这是送入 `llama.cpp` 的真实字符串。
- “大模型实际输出”：展示 `llama.cpp` raw 输出，不做 trim、clean、重试或 fallback；批量探针会逐条显示 prompt、raw output/error 和判定。
- “错误代码”：模型缺失、加载失败、上下文过长、分词失败或 decode 失败时直接显示错误类型和 `localizedDescription`。
- 如果 raw 输出正常但普通翻译失败，优先查 `GemmaLocalService.cleanTranslationOutput` 和目标语言校验；如果 raw 输出已经复读原文，优先查 prompt、采样和模型质量。

当前默认提示词是极简模板：

- 英译中：`把以下翻译成中文：`
- 中译英：`Translate the following into English:`

Local prompt 现在只拼当前方向指令和输入文本，减少长预设污染模型 raw 输出。JA/FR/DE Pro 翻译仍走通用 fallback。

## 语音 / OCR 测试规范

`test/` 用于固定测试素材，不用于保存用户数据或模型文件：

```text
test/
  sample.m4a
  sample.png
```

测试流程：

1. 把语音或图片放进项目根目录 `test/`。
2. 重新构建安装 App，因为 `test/` 是 bundle resource。
3. 在 App 内打开 `设置`，使用有效订阅或开发者模式下的 `开发解锁` 解锁 Pro。
4. 点击 `运行 test/ 音频` 或 `运行 test/ OCR`。
5. 音频会走 `SFSpeechURLRecognitionRequest` + `requiresOnDeviceRecognition = true`，识别文本再交给当前 Mock/Local 翻译接口。
6. 图片会走 `VNRecognizeTextRequest`，识别文字块和 `boundingBox` 后逐块翻译。

### 语音识别质量探针

开发页的语音质量探针读取 `test/speech_corpus/manifest.json`，逐项校验音频 SHA256 和字节数，再按每项 `localeIdentifier` 使用 `SFSpeechURLRecognitionRequest` 与 `requiresOnDeviceRecognition = true` 识别。最终文本返回后才与参考 transcript 计算指标，并写入 `Application Support/AITRANS/Output/speech_quality_report.json` 和 `.txt`。

- 英文等空格分词语言报告词级 WER 和字符级 CER；中文、日文只报告 CER。
- 报告包含 corpus/manifest 身份、设备与系统、识别文本、延迟、分段、平均置信度、失败分类和加权汇总。
- 参考 transcript 只用于事后评估，不进入 Speech 请求、候选选择、纠错或产品路径。
- v1.95 未提交或生成占位音频。当前 validator 输出 `manifestMissing` 和 `qualityExecuted=false`；这不是质量失败，也不是质量通过。
- v1.96 上传真实文件后按 `test/speech_corpus/README.md` 生成 manifest，再在目标设备运行并保留报告，才能比较实际 WER/CER 和延迟。

### 漫画覆盖翻译探针

开发页新增 `运行漫画覆盖翻译探针`，固定读取 bundle 内 `test/1.png`，不污染当前会话、历史或普通图片翻译状态。流程：

1. 读取 `test/1.png`。
2. 先按可配置比例裁掉浏览器 UI / 广告 / 底部导航，本次默认 `topRatio = 0.235`、`bottomRatio = 0.14`。
3. 对裁切后的漫画内容做 2x 放大，再做 0°、90°、180°、270° 四次 Apple Vision OCR，识别英文文字块。
4. 将旋转图坐标还原成原图像素 bbox，坐标约定为左上角原点 `[x, y, width, height]`；输出绘制同样使用左上角坐标，避免上下翻转。
5. 对 OCR 候选按 bbox 重叠、文本相似度、相邻行/段落空间关系合并，目标是一个对话框对应一个逻辑块。
6. 对每个合并块复用当前英译中极简提示词 `把以下翻译成中文：` 和当前引擎翻译；Local 模式记录真实 `llama.cpp` prompt/raw output，Mock 模式标记为模拟。
7. 质量判定失败时也会绘制覆盖层，文本为 `翻译失败` + OCR 原文，并在报告写入失败原因，避免静默跳过。
8. 生成 `Application Support/AITRANS/Output/1_debug_boxes.png`、`1_translated_overlay.png` 和 `probe_report.json`。

探针报告字段：

- `sourceImage`：固定 `test/1.png`。
- `engineUsed`：实际适配器显示名。
- `totalBlocksDetected`：最终保留文本块数。
- `blocks[].bbox`：原图像素坐标，左上角原点。
- `blocks[].rotationAngleUsed`：该块来自 0/90/180/270 哪个 OCR 角度。
- `blocks[].prompt` / `rawOutput` / `errorCode`：真实输入输出或错误。
- `blocks[].checks`：`ocrNotEmpty`、`translationNotEmpty`、`translationNotEqualOriginal`、`translationNotContainOriginal`、`looksLikeChinese`。
- `blocks[].translationCandidate`：从 raw output 中抽取出来用于质量判定和覆盖绘制的候选译文。
- `blocks[].rawOutputClassification` / `candidateClassification` / `failureCategory`：分别记录模型 raw 输出类型、候选译文类型和失败归因。常见值包括 `chinese`、`nonChinese`、`placeholder`、`repeatedOriginal`、`symbolsOnly`、`modelOutputFailure`、`ocrInputSuspect`、`ruleFalseFailureSuspected`。
- `blocks[].bestGroundTruthText` / `ocrGroundTruthSimilarity` / `ocrQualityLabel`：把当前 OCR 文本和 `test/1.ground_truth.json` 的人工真值做相似度对照，用于判断翻译差是否先由 OCR 输入错误导致。
- `blocks[].bestGroundTruthType` / `groundTruthMatch` / `groundTruthMatchThreshold`：人工真值匹配类型与是否可信匹配。低于阈值的块会标为 `unmatched`，不再强行配一个错误真值，也不纳入准确率统计。
- `blocks[].ocrLegacySimilarity` / `wordOrderPreserved`：保留旧相似度作对照，同时新增词级编辑距离相似度和词序信号。词序错乱的 OCR 会明显降分，避免旧算法把乱序文本算成高准确。
- `blocks[].failureReasons`：失败块的具体原因，例如 `翻译等于原文`、`翻译是占位答复`、`中文字符不足`、`翻译不像中文`。
- `blocks[].qualityNotes`：非硬失败排查线索，例如 `translationContainsFullOCRText`、`latinLetters=27`、`cjkCharacters=4`。
- `blocks[].translationDecisionTrace` / `translationFailureDetail`：逐块记录 raw 分类、候选分类、每条硬判定布尔值和失败归因；用于确认失败来自模型 raw 输出、OCR 输入、候选抽取，还是规则误伤。
- `blocks[].ocrProbeNotes`：逐块记录 OCR 和人工真值的相似度、质量标签、已知 OCR 混淆提示。
- `diagnostics`：本轮整体排查汇总，统计通过/失败块、空候选、占位输出、复读原文、非中文输出、raw 输出类型、候选抽取丢弃、平均 OCR 真值相似度、疑似 OCR 问题块、疑似规则误伤块、失败分类分布、中文候选失败原因分布、翻译语言质量通过/失败块、硬通过但质量可疑块。
- `diagnostics.averageCoreDialogueOCRSimilarity` / `averageDecorativeOCRSimilarity` / `groundTruthMatchedBlocks` / `groundTruthUnmatchedBlocks` / `wordOrderFailedBlocks` / `repeatedKeywordFailures`：可信匹配后的核心对话/装饰文字分开统计、未匹配块计数、词序失败块和高频专有名词损坏追踪。
- `configuration.currentBlockSource`：记录当前块来源。当前是 `fusedWholePageBubble`，即 whole-page OCR 与 bubble-first OCR 的无真值融合主流程。
- `configuration.preprocessing`：记录本轮预处理增强开关，包含灰度、对比亮度、自适应二值化、裁切放大、锐化和放大倍数。
- `configuration.customLexiconEnabled` / `customLexicon`：记录 Vision `customWords` 是否启用和本轮词表。
- `blocks[].rawOcrText` / `afterPreprocessingOcrText` / `finalTextUsedForTranslation`：记录原始 OCR、裁切预处理后二次 OCR、最终送翻译文本。当前预处理结果只用于对比展示，不替换整图 OCR 主流程。
- `blocks[].correctionEnabled` / `afterCorrectionText` / `correctionRejectedReason` / `correctionPrompt` / `correctionRawOutput` / `correctionErrorCode`：记录 OCR 纠错后处理链路。护栏拒绝时 `afterCorrectionText` 保留原文，`correctionRejectedReason` 写明长度、词数、新词或空输出原因。
- `blocks[].deterministicCorrectionText` / `deterministicCorrectionAppliedRules` / `deterministicCorrectionSimilarity`：记录探针专用确定性 OCR 纠错候选。当前只做量化对比，不替换 `finalTextUsedForTranslation`，避免半修正文本污染翻译链路。
- `blocks[].deterministicCorrectionTranslationCandidate` / `deterministicCorrectionTranslationRawOutput` / `deterministicCorrectionTranslationPassed` / `deterministicCorrectionTranslationFailureDetail`：仅对确定性纠错相似度确有提升的块，额外跑一次纠错文本翻译对照。该结果只用于探针，不替换主覆盖图和主翻译输入。
- `correctionGuardrailTest`：固定构造 `XQZ 12 ///` -> `The City Battler Tournament starts in a few days.` 的过度纠错测试，用来证明护栏会拒绝模型瞎猜。
- `lexiconComparison`：同图同参数下 Vision `customWords` 开/关对比，记录总块数和发生变化的块编号。
- `visionAPIComparison`：旧 `VNRecognizeTextRequest` 与 iOS 18+ Swift 原生 `RecognizeTextRequest` 的 0° OCR 对比。当前只作为独立探针，不替换主流程。
- `frameworkComparison`：整页 OCR 与 bubble-first 对照。差集列表、交集数量和汇总准确率都从最终明细现场计算，`consistencyPassed` 必须为 `true`；如果计数和列表不一致，会写入 `consistencyWarnings`。
- `fusionComparison` / `fusionResults`：whole-page 与 bubble-first 的融合主流程审计。候选选择只用 bbox、bubbleID、文本相似度、OCR 置信度和文本质量等无真值信号；ground truth 只用于事后评估。报告会记录被选来源、竞争候选、替换原因和拒绝原因。
- `internalStructureBottleneckReport`：v1.18 新增的内部结构瓶颈路由报告。它聚合 OCR 相似度、bubble / TextBox / SegmentMask proxy、post-fusion cleanup、crop / line blockers、翻译失败分类和渲染状态，为每块写出 `primaryBottleneck`、`secondaryBottlenecks`、`recommendedNextAction`、`evidence` 和 `mustNotPromoteReasons`；只做诊断路由，不替换主流程文本。
- `routingDrivenTranslationComparisonReport`：v1.19 新增的路由驱动 strict prompt 对照报告。它只从 `internalStructureBottleneckReport.primaryBottleneck = modelTranslationQuality` 的块中最多选择 5 个，额外用 deterministic `strictChineseOnlyV1` prompt 生成诊断译文，复用主质量判定 helper，记录 improvement / failure；不替换主流程 prompt、译文、`blockPassed`、失败分类或覆盖图。
- `ocrCharacterDamageAuditReport`：v1.19 新增的 OCR 字符损坏审计报告。它只分析 `ocrCharacterDamage`、`ocrInputSuspect` 或低相似度块，使用 ground truth 做探针诊断，输出 damaged / missing / extra / substitution token、line break risk、TextBox / SegmentMask 证据、crop blockers 和 recommended action；不替换 `finalTextUsedForTranslation`，也不参与生产候选排序。
- `readingOrderStructureAuditReport`：v1.20 新增的阅读顺序与结构计划审计报告。它用最终 blocks、bbox、bubbleID、BubbleMask、TextBox / SegmentMask proxy、post-fusion cleanup 和路由证据生成 `proposedReadingOrderIndex`、同气泡 sibling、归属/分割/重复风险、保护标记和 `recommendedStructureAction`；只写 JSON / TXT，不改变 `blocks` 顺序、翻译输入、覆盖图、`blockPassed` 或候选选择。
- `structureActionCandidateReport`：v1.21 新增的结构动作候选矩阵和 shadow 执行评估报告。它把 v1.20 的结构建议转成 `readingOrderReindex`、`bubbleAssignmentReview`、`bubbleSplitShadow`、`sameBubbleSiblingLayout`、`duplicateFragmentProtection`、`textBoxEvidenceRequired`、`segmentMaskEvidenceRequired`、`renderSafeAreaReflow` 和 `manualReviewOnly` 候选，记录 control/shadow metrics、delta、promotion verdict、blockers 和 next step；只复用已有几何、渲染和 shadow OCR 摘要，不新增昂贵 OCR / LLM，不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、post-fusion cleanup 或候选选择。缺真实 Koharu artifact 时只输出 `blockedByMissingRealArtifact` / `provideRealKoharuArtifact`，不能用内部 proxy 冒充 detector。
- `koharuArtifactDAGReport`：v1.22 新增的 Koharu 式 Artifact DAG 阶段账本。它把 SourceImage / ContentCrop / OCR / BubbleMask / TextBoxes / SegmentMask / Translation / Render / v1.21 结构动作候选组织成 dependency edges、stage summaries 和逐块 trace，定位 `firstBlockingStage` 与 `downstreamImpact`；只复用既有探针证据，不新增 OCR / LLM 调用，不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、post-fusion cleanup 或候选选择。缺真实 active artifact 时只阻塞真实 TextBoxes / BubbleMask / SegmentMask promotion，不把当前主流程整体判废。
- `koharuStageGapReplicationReport`：v1.23 新增的 Koharu canonical stage 差距复刻计划层。它把 v1.22 DAG 阶段账本转成 `SourceImage` / `TextBoxes` / `SegmentMask` / `BubbleMask` / `OcrText` / `Translations` / `Inpainted` / `RenderedSprites` / `FinalRender` 等阶段的当前能力、差距、work package、promotion gate 和逐块复刻计划；只写 JSON / TXT，不新增 OCR / LLM 调用，不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类或 cleanup。缺真实 `test/koharu_artifacts/` 时，真实 TextBoxes / BubbleMask / SegmentMask 仍保持 `manifestMissing` / `stopUntilArtifactsProvided` 阻塞，不能用 Vision OCR、pre-crop plan、line plan 或内部 proxy 冒充。
- `koharuNativeReplicationScoreboardReport`：v1.24 新增的 Koharu 本地复刻评分层。它只使用 AITRANS 自己的探针输出，把 v1.23 stage gap / work package 转成 stage scorecard、gate ledger、block scorecard 和 next work items；真实 external artifact 缺失只记为 `externalOptionalMissing`，不阻塞 native scoreboard。该报告区分 ground-truth-free decision signals 与 evaluation-only ground truth metrics，明确 native / proxy / shadow / stop / model-limited / render-stable 状态，并把已证伪的 crop / line / deskew 本地试参加入 stoplist；不新增 OCR / LLM 调用，不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、post-fusion cleanup 或候选选择。
- `nativeTextBoxProxyLedgerReport`：v1.25 新增的 Native TextBox proxy 质量账本。它执行 v1.24 的 `WI-native-textbox-artifact-scorecard`，聚合现有 TextBox / crop / line / BubbleMask / SegmentMask / OCR damage / scoreboard 证据，输出逐块质量状态、候选来源、word preservation / protected keyword / bubble / segment / OCR damage / model floor / render gate、stoplist 和下一步动作；ground truth 只进 evaluation signals，不参与候选排序、冻结或晋级。该报告冻结已证伪的 crop / line / deskew 试参，只允许证据完整的候选进入未来 shadow-only 复核；不新增 OCR / LLM，不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、post-fusion cleanup 或候选选择。
- `bubbleMaskAssignmentSplitScoreboardReport`：v1.26 新增的 BubbleMask 归属分割评分板。它执行 v1.24 的 `WI-bubblemask-assignment-split-scorecard`，聚合现有 BubbleMask proxy、assignment correction、split candidate、reading order、structure action、native scoreboard 和 Native TextBox ledger 证据，输出逐块归属状态、split risk、same-bubble sibling layout、mask safe rect、render gate、实例级 bubble scorecard、split candidate ledger、sibling layout scorecard 和 gate ledger。该报告只做 report-only 诊断；ground truth 只进 evaluation signals，不参与 assignment / split / sibling layout / nextAction / promotion 决策；不新增 OCR / LLM，不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、post-fusion cleanup、候选选择、`safeLayoutRect` 或 `configuration.currentBlockSource`。
- `segmentMaskProxyCoverageScoreboardReport`：v1.27 新增的 SegmentMask proxy 覆盖评分板。它执行 v1.24 的 `WI-segmentmask-proxy-coverage-scorecard`，聚合现有 glyph mask、SegmentMask proxy、TextBox 覆盖、BubbleMask 覆盖、safe rect、背景填充和渲染碰撞证据，输出逐块 coverage / cleanup / render mask 状态、glyph 清字边界账本、gate ledger 和下一步动作。该报告明确 `proxyNotRealSegmentMask = true`，只做 report-only 诊断；ground truth 只进 evaluation signals，不参与 coverage / cleanup / nextAction / gate 决策；不新增 OCR / LLM，不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、post-fusion cleanup、候选选择、`safeLayoutRect`、`glyphMaskFillRects`、背景填充行为或 `configuration.currentBlockSource`。
- `koharuArtifactConvergenceReport`：v1.28 新增的 Koharu artifact 收敛矩阵与下一步决策账本。它聚合 v1.22 DAG、v1.23 stage gap、v1.24 native scoreboard、v1.25 TextBox、v1.26 BubbleMask、v1.27 SegmentMask、external artifact gate、clean text model floor、diagnostics 和最终 blocks，输出 canonical stage convergence matrix、逐块 artifact path、work item closure ledger 和 gate ledger；v1.58 起还消费 bundle-lite / promotion gate 的 TextBox -> SegmentMask linkage work item 与 gate。该报告只做 report-only 诊断；ground truth 只进 evaluation signals，不参与 firstBlockingArtifact、nextAction、work item status 或 gate 决策；不新增 OCR / LLM，不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、post-fusion cleanup、候选选择、`safeLayoutRect`、`glyphMaskFillRects`、背景填充行为、渲染逻辑或 `configuration.currentBlockSource`。
- `translationModelFloorComparisonReport`：v1.29 新增的 Translation Model Floor 对照矩阵。它复用 `cleanTextDiagnostic` 的 dialogue 真值输入，额外用 deterministic `strictChineseOnlyV1` prompt 生成 clean text 变体对照，并汇总 noisy final blocks、routing strict prompt、tagged batch 和 Koharu convergence work item，输出模型 / prompt 地板判断、gate ledger、clean case 和 noisy block 摘要。该报告只做 report-only 诊断；clean text ground truth 只用于模型地板评估，不参与 noisy OCR 候选选择、主 prompt、主译文、覆盖图、`blockPassed` 或失败分类。
- `koharuRenderRegressionLockReport`：v1.30 新增的 Koharu RenderedSprites / FinalRender 回归锁账本。它聚合现有 `safeLayoutRect`、mask-safe rect、render collision、render mask overflow、glyph mask、background fill、失败块 fallback 覆盖文本和核心输出文件状态，输出逐块 `blockLocks[]`、`artifactStages[]`、`outputFileChecks[]` 和 `gateLedger[]`，并把 `WI-render-regression-lock` 联动进 convergence work item。该报告只做 report-only 诊断；不重新渲染、不解析 PNG 像素证明逐块文字、不改变覆盖绘制、`safeLayoutRect`、`glyphMaskFillRects`、背景填充、主 OCR、主翻译、`blockPassed` 或失败分类，并明确 `proxyNotRealKoharuRenderer = true`。
- `koharuPipelineResolverReport`：v1.31 新增的 Koharu Pipeline Resolver 影子 DAG。它用 `needs` / `produces` / DAG resolver / Op preview 的形式组织现有 SourceImage、ContentCrop、Vision OCR、Bubble/TextBox/SegmentMask proxy、OCR text、Translations、RenderedSprites proxy、FinalRender 和 ExternalArtifacts 证据，输出 `nodes[]`、`edges[]`、逐块 `blockTraces[]`、`executionQueue[]`、`opPreviews[]` 和 `gateLedger[]`。该报告只做 report-only 诊断；不新增 OCR / LLM，不改变主 OCR、翻译、覆盖、候选选择或失败分类，ground truth 只进 evaluation signals。
- `koharuWorkOrderRouterReport`：v1.32 新增的 Koharu WorkOrder Router 执行工作单与收益预算账本。它从 v1.31 resolver 派生固定 work orders、逐块 routes、CI/full/external 预算、stoplist、模型地板和 render lock gate，并把 `WI-koharu-workorder-router` / `G-koharu-workorder-router-executed` 联动进 convergence。该报告只做 report-only 诊断；不新增 OCR / LLM，不改变主 OCR、翻译、覆盖、候选选择或失败分类，ground truth 只进 evaluation signals。
- `koharuExternalArtifactRequestPacketReport`：v1.33 新增的 Koharu 外部 Artifact 请求包与准入缺口账本。它从 v1.32 router、external readiness gate、external TextBox shadow OCR、TextBox / BubbleMask / SegmentMask scoreboards、render lock、translation floor 和 convergence 聚合真实 `test/koharu_artifacts/` 交付需求，固定枚举 `1.manifest.json`、`1.textboxes.json`、`1.bubbles.json`、`1.segment_mask.json`，输出 required files、artifact requirements、逐块 request、validator commands、forbidden active sources 和 gate ledger。缺 active artifact 时稳定输出 blocked / missing，不创建、不复制、不伪造 `test/koharu_artifacts/`；ready 后只允许进入 external shadow OCR gate。该报告只做 report-only 诊断；不新增 OCR / LLM，不改变主 OCR、翻译、覆盖、候选选择、失败分类、safe layout、glyph mask、背景填充或 `configuration.currentBlockSource`，ground truth 只进 evaluation signals。
- `koharuNativeAlgorithmReplayMatrixReport`：v1.34 新增的 Koharu 本地算法复刻执行矩阵与探针评估账本。它从 resolver、router、request packet、convergence、TextBox / BubbleMask / SegmentMask scoreboards、translation floor、render lock、diagnostics 和最终 blocks 聚合 canonical stage matrix、固定 native replay candidate queue、逐块 route 和 budget / promotion gates，把候选分成 nativeExecutableNow、shadowOnly、stoplisted、externalArtifactBlocked、modelFloorBlocked、renderLocked 等状态。该报告只做 report-only 诊断；不新增 OCR / LLM，不改变主 OCR、翻译、覆盖、候选选择、失败分类、safe layout、glyph mask、背景填充、active artifacts 或 `configuration.currentBlockSource`，ground truth 只进 evaluation signals。
- `koharuBubbleIndexShadowLedgerReport`：v1.35 新增的 Koharu BubbleIndex 影子账本。它从现有 BubbleMask proxy、归属修正、split candidate、reading order、BubbleMask assignment / split scoreboard、render lock、native replay matrix 和最终 blocks 聚合 block / bubble / same-bubble sibling ledger，审计 majority mask 归属、mask-safe rect 对照、同气泡 sibling 分区、split 风险和 render lock。该报告只做 report-only 诊断；不新增 OCR / LLM，不改变主 OCR、翻译输入、覆盖图、`safeLayoutRect`、`blockPassed`、失败分类、候选选择、active artifacts 或 `configuration.currentBlockSource`，ground truth 只进 evaluation signals，`proxyNotRealBubbleMask = true`。
- `koharuDistanceFieldSafeAreaReport`：v1.36 新增的 Koharu BubbleIndex distance-field 安全区影子复刻。它只在现有 rounded-rect BubbleMask proxy ID mask 的 bubble bbox 内计算 chamfer distance field、safe pixels、最大安全矩形，并对 block / sibling 比较当前 `safeLayoutRect`、v1.35 shadow safe rect、distance-field safe rect 和 render sprite containment。该报告只做 report-only 诊断；不新增 OCR / LLM，不改变主 OCR、翻译输入、覆盖图、`safeLayoutRect`、`glyphMaskFillRects`、背景填充、`blockPassed`、失败分类、候选选择、active artifacts 或 `configuration.currentBlockSource`，ground truth 只进 evaluation signals，`proxyNotRealBubbleMask = true` 且 `usesRoundedRectProxyMask = true`。
- `koharuBubbleAdjacencySeamReport`：v1.37 新增的 Koharu BubbleMask instance adjacency / seam partition 影子账本。它基于现有 rounded-rect BubbleMask proxy、BubbleIndex、DistanceField、split candidate、same-bubble sibling、OCR damage 和 render lock 证据，输出 bubble pair、seam candidate 和 block seam ledger，解释邻接、切缝候选、归属/分割风险和真实 BubbleMask 需求。该报告只做 report-only 诊断；不新增 OCR / LLM，不改变主 OCR、翻译输入、覆盖图、`safeLayoutRect`、DistanceField safe rect、`glyphMaskFillRects`、背景填充、`blockPassed`、失败分类、候选选择、active artifacts 或 `configuration.currentBlockSource`，ground truth 只进 evaluation signals，`proxyNotRealBubbleMask = true` 且 `usesRoundedRectProxyMask = true`。
- `koharuRenderSpriteFitPlannerReport`：v1.38 新增的 Koharu RenderedSprites 字体 / 换行 / sprite fit 影子账本。它复用现有 `safeLayoutRect`、render diagnostics、Render Regression Lock、BubbleIndex、DistanceField 和 Bubble adjacency seam 证据，输出逐块 fit、layout candidate 和 same-bubble sibling fit ledger，并联动 convergence。该报告只做 report-only 诊断；不新增 OCR / LLM，不重新渲染 PNG，不改变主 OCR、翻译输入、覆盖图、`safeLayoutRect`、DistanceField safe rect、`renderFontSize`、`renderNonTransparentBounds`、`glyphMaskFillRects`、背景填充、`blockPassed`、失败分类、候选选择、active artifacts 或 `configuration.currentBlockSource`，ground truth 只进 evaluation signals，`proxyNotRealKoharuRenderer = true` 且 `proxyNotRealBubbleMask = true`。
- `koharuNativeTextBoxDetectorLiteReport`：v1.39 新增的 Koharu Native TextBox Detector-Lite 预 OCR 影子复刻。它只用 source image 像素、bubble geometry、BubbleMask proxy 和 glyph / SegmentMask proxy 为每个 bubble 生成有上限的 `nativeDetectorLite` TextBox 候选池（最多 4 个 component-cluster 候选 + 1 个不参与 shadow OCR 的 diagnostic union fallback），记录 bbox、方向、暗像素密度、连通域、投影峰、bubble coverage、glyph overlap、候选与当前 block 的 overlap / center / same-bubble relation、block / bubble ledger 和 gate，并联动 convergence。该报告默认不执行 shadow OCR，不使用 Vision OCR 文本、ground truth、pre-crop plan、line plan 或 TextRegion crop 结果生成 / 排序候选；不改变主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、`textRegionCropReport.adoptedCount`、active artifacts 或 `configuration.currentBlockSource`，`proxyNotRealKoharuTextBoxes = true`。
- `koharuNativeTextBoxDetectorLiteShadowOCRReport`：v1.40 新增的 detector-lite TextBoxes -> OCR 最小闭环评估。它只消费 v1.39 `shadowOCREligible = true` 的 `nativeDetectorLite` 候选，`ci-fast` 每块最多 1 个、`full` 每块最多 2 个，按当前 block 与候选 bbox 的 overlap / center containment 优先排序，调用受限 Vision crop OCR 并输出 raw / normalized text、empty / failed / better / worse / same、quality delta、word preservation、evaluation-only ground truth similarity、block ledger 和 gate。`verticalCandidate` 会额外做有上限的 `[0,90]` rotation shadow OCR 对照，使用 `ja-JP/ja/en-US/en` 的受限 Vision language profile 并记录 `rotationApplied`，只用无真值 OCR 质量和保词率选择 report-only 最佳结果。该报告不新增 LLM 调用，不使用 ground truth 决定候选、排序、OCR 执行、nextAction 或 gate；不替换主 OCR、`finalTextUsedForTranslation`、翻译输入、覆盖图、`blockPassed`、失败分类、`textRegionCropReport.adoptedCount`、active artifacts 或 `configuration.currentBlockSource`，`proxyNotRealKoharuTextBoxes = true` 且 `proxyNotRealKoharuOCR = true`。
- `koharuNativeTextBoxDetectorLiteRefinementReport`：v1.41 新增的 detector-lite 闭环二次候选与 refinement shadow OCR。它只消费 v1.39 detector-lite 候选和 v1.40 shadow OCR ledger，用 v1.40 outcome、失败分类、模型地板和渲染锁等无真值信号选择 target，从 detector-lite 父 bbox 内用暗像素 envelope、projection band、directional padding 和保守 bubble clip 生成 refined bbox，并在预算内执行 Vision crop OCR。该报告只写 JSON / TXT，不新增 LLM，不使用 ground truth 决定 target、bbox、排序、OCR 执行、nextAction 或 gate；不替换主 OCR、`finalTextUsedForTranslation`、翻译输入、覆盖图、`blockPassed`、失败分类、`textRegionCropReport.adoptedCount`、active artifacts 或 `configuration.currentBlockSource`，`proxyNotRealKoharuTextBoxes = true` 且 `proxyNotRealKoharuOCR = true`。
- `koharuNativeTextBoxDetectorLiteClosedLoopReport`：v1.42 新增的 detector-lite 闭环裁决与结构路由账本。它消费 v1.39 detector-lite、v1.40 shadow OCR、v1.41 refinement、final blocks、BubbleMask / SegmentMask proxy、翻译失败分类、translation model floor、render lock 和 external artifact readiness，把每块路由到保留主 fused OCR、report-only full probe 复核、停止本地 detector-lite 调参、等待真实 TextBoxes / BubbleMask / SegmentMask、模型地板或渲染锁。该报告不新增 OCR / LLM / PNG，不使用 ground truth 决定 route / nextAction / gate / candidate family verdict，不替换主 OCR、`finalTextUsedForTranslation`、翻译输入、覆盖图、`blockPassed`、失败分类、`textRegionCropReport.adoptedCount`、active artifacts 或 `configuration.currentBlockSource`，`proxyNotRealKoharuTextBoxes = true` 且 `proxyNotRealKoharuOCR = true`。
- `koharuNativeBubbleMaskInstanceLiteReport`：v1.43 新增的 Koharu Native BubbleMask Instance-Lite 像素实例掩码影子复刻。它在 v1.42 closed-loop 后、最终 convergence 前运行，只用源图像内容裁切区像素、现有 bubble geometry、final blocks、glyph / SegmentMask proxy、BubbleIndex / DistanceField / seam / RenderSprite / detector-lite closed-loop 证据生成近白连通域 instance-lite mask 账本，输出实例、逐块 majority ID 归属、由实例像素 erosion / projection 派生的 safe rect 对照、同 instance 多 block 的 block-scoped safe rect policy、block-scoped sprite containment preview、same-instance sibling sprite collision preview、sibling / adjacency 和 nextAction。单块可使用 mask-derived safe rect；同 instance 多块只在报告里保留各自 seed rect，避免把一个最大 safe rect 共享给 siblings；render non-transparent bounds 用于检查是否落在 block-scoped safe rect 内，并统计同 instance 多块 sprite bounds 是否互相重叠。该报告不新增 OCR / LLM / PNG，不创建 active `test/koharu_artifacts/`，不把 instance-lite 冒充真实 Koharu `BubbleMask`，不写回主 OCR、翻译输入、覆盖图、`safeLayoutRect`、DistanceField safe rect、`blockPassed`、失败分类、renderer 或 `configuration.currentBlockSource`，`proxyNotRealKoharuBubbleMask = true` 且 `nativeInstanceLite = true`。
- `koharuNativeSegmentMaskRefinementLiteReport`：v1.44 新增的 Koharu Native SegmentMask Refinement-Lite 文字像素掩码影子复刻。它在 v1.43 instance-lite 后、最终 convergence 前运行，只用源图像像素、detector-lite TextBox 候选、final blocks、v1.43 instance-lite BubbleMask、现有 glyph / SegmentMask proxy、render lock 和翻译失败分类，生成 TextBox 约束的文字像素 mask refinement 账本。报告输出 candidate ledger、逐块 block ledger、same-bubble sibling overlap ledger 和 gate；candidate / block ledger 记录来源 TextBox candidate verdict、shadow eligibility、block overlap、same-bubble relation、accepted/fallback/rejected linkage verdict、mask 对 TextBox / Bubble 的 containment ratio，以及基于 v1.43 instance-lite ledger 的 majority agreement。它不新增 OCR / LLM / PNG，不接 active artifact，不把 refinement-lite 冒充真实 Koharu `SegmentMask`，不写回主 OCR、翻译输入、覆盖图、`safeLayoutRect`、`glyphMaskFillRects`、背景填充、renderer、`blockPassed`、失败分类、`textRegionCropReport.adoptedCount` 或 `configuration.currentBlockSource`，`proxyNotRealKoharuSegmentMask = true` 且 `nativeRefinementLite = true`。
- `koharuNativeArtifactBundleLiteReport`：v1.45 新增的 Koharu Native Artifact Bundle-Lite 结构一致性闭环。它在 v1.44 refinement-lite 后、最终 convergence 前运行，只消费 final blocks、detector-lite / shadow OCR / refinement / closed-loop、BubbleMask instance-lite、SegmentMask refinement-lite、render fit / render lock、translation model floor 和 external readiness，组装每块 TextBox / BubbleMask / SegmentMask / OCR / Translation / Render 的 report-only bundle、consistency edges 和 worklist。该报告不新增 OCR / LLM / PNG，不接 active artifact，不替换主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、renderer 或 `configuration.currentBlockSource`；ground truth 只进入 evaluation signals，`nativeBundleLite = true`，且 TextBoxes / BubbleMask / SegmentMask 均明确标记为 proxy-not-real Koharu artifacts。
- `koharuNativePromotionGateLiteReport`：v1.46 新增的 Koharu Native Promotion Gate-Lite 探针驱动晋级门槛。它在 v1.45 bundle-lite 后、最终 convergence 前运行，只消费当前探针内存中的 final blocks、diagnostics、v1.39-v1.45 native-lite reports、render lock、render fit、translation model floor、clean text diagnostic 和 external readiness，为每块输出 TextBoxes / BubbleMask / SegmentMask / OcrText / Translations / RenderedSprites / FinalRender 的 report-only 晋级状态、stage gates、candidate export preview、work items 和 gate ledger。该报告不新增 OCR / LLM / PNG，不更换模型，不创建或修改 active `test/koharu_artifacts/`，不替换主 OCR、翻译输入、覆盖图、`blockPassed`、失败分类、renderer 或 `configuration.currentBlockSource`；ground truth 只进入 evaluation signals，candidate export 只是 JSON/TXT 预览，不能写出 active artifact。
- `koharuNativeArtifactContractDryRunReport`：v1.47 新增的 Koharu 四件套 artifact contract dry-run。它在 v1.46 promotion gate 后生成，把 candidate export preview 映射到 `test/koharu_artifacts/1.manifest.json`、`1.textboxes.json`、`1.bubbles.json`、`1.segment_mask.json` 的必需字段、禁止来源和 validator 命令，并联动 `koharuArtifactConvergenceReport` 的 reference report、work item 和 gate；v1.80 起 required manifest 字段包含 `sourceImageSHA256=<runtime test/1.png sha256>`，App-side identity gate 也要求 `sourceImageSHA256Matches=true`；只写 JSON / TXT，不创建或修改 active artifact，不新增 OCR / LLM / PNG，不把 native-lite / proxy 预览导出为真实 Koharu artifact，`activeExportAllowed = false`。
- `koharuArtifactIdentityReconciliationReport`：v1.68 新增的 artifact identity reconciliation 账本。它只消费 App 侧 `artifactIdentityReceipt` 和 contract dry-run required files，把 SourceImage + 四件套的 App size / SHA256 对齐到 `ci-artifact-manifest.koharuArtifactValidationIdentitySummary` 字段路径，并联动 convergence 的 `WI-koharu-artifact-identity-reconciliation` / `G-koharu-artifact-identity-reconciliation-ready`；v1.80 起顶层还输出 `sourceImageSHA256Declared`、`sourceImageSHA256Expected`、`sourceImageSHA256Matches`，且只有 match 为 true 才允许 `readyForCIManifestComparison=true`；不读取 CI manifest、不创建或修改 active artifact、不新增 OCR / LLM / PNG、不改变主流程。Actions 在真实 artifact 注入探针后会把 validator identity 与 App rows 做 size/SHA256 比对并写入 `koharuArtifactIdentityReconciliationMatch`。
- `preCropTextBoxPlanReport`：TextRegion crop 前生成的 Koharu 式上游 TextBox plan artifact。每块最多保留 3 个 plan，使用 fused seed bbox、BubbleMask majority、subRegion、split candidate、safe rect 和 glyph/SegmentMask proxy 等无真值信号排序；只用于 shadow OCR，不替换 `finalTextUsedForTranslation`。
- `cropExperimentReport`：TextRegion crop 的 shadow-only 实验矩阵。control 使用当前 TextRegion crop，每块最多额外跑 3 个候选；v18 起优先使用 `preCropTextBoxPlan.*` 作为 shadow 来源。`bestShadowCandidate` 只进入 JSON/TXT 报告，不替换 `finalTextUsedForTranslation`，也不放宽 `textRegionCropReport.adoptedCount`。
- `textBoxPlanFailureReport`：v19 新增的 plan / candidate / block 三级失败归因和晋级门槛审计。它用 `sourcePlanID` 关联 pre-crop plan 与 shadow OCR candidate，记录 promotion checks、blockers、primary failure category 和 recommended action；只解释为什么 `betterThanControl` 未晋级，不改变 `finalTextUsedForTranslation`、主覆盖图或 `adoptedCount`。
- `lineTextBoxPlanReport` / `lineCropExperimentReport`：v20 新增的 Koharu 式行级 TextBox / deskew shadow 验证层。目标块动态来自 `textBoxPlanFailureReport.continueGeometryResearchBlocks`，当前为 `[1, 6, 10]`；每块最多 4 个 line-level plan，候选变体以 `lineTextBoxPlan.*` 开头，只进 shadow OCR 和报告，不写回 `finalTextUsedForTranslation`、主覆盖图、`blockPassed` 或 `textRegionCropReport.adoptedCount`。
- `externalArtifactReadinessReport`：v21 / v1.11 新增的真实 TextBoxes / BubbleMask / SegmentMask artifact 适配前证据闸门。它只解析 `test/koharu_artifacts/` 下 manifest 和外部 JSON，做 schema、坐标、bbox 和 block alignment 校验；没有真实 artifact 时必须输出明确阻塞，不把现有 Vision OCR、pre-crop plan 或 line plan 伪装成 detector 输出。v1.12 新增 `md/koharu研究/artifact_contract/` 契约、非活动 examples 和 `scripts/validate-koharu-artifacts.py` 离线 validator；v1.14 validator 额外输出 `requiredFiles`、`nextAction` 和 `readinessBlockers`，CI 结果包 manifest 会汇总 active artifact validation、App 侧 readiness 和 shadow OCR 状态；v1.65 validator 额外输出 `orientationMetadataSummary`。示例 manifest 必须带 `contractExampleOnly=true`，即使误放到 active 目录也不能得到 `readyForShadowOCR`。当前 validator 和 Swift readiness 会硬拦 `schemaVersion` 缺失/不匹配、manifest path 使用绝对路径或 `..` 逃逸 active 目录、缺 `sourceImage`、缺 / 非 64 位 hex / 不匹配 runtime `test/1.png` 的 `sourceImageSHA256`、缺或非布尔 `contractExampleOnly`、TextBox 可选方向元数据非法，以及 active manifest 的 `generatedBy` 缺失或声明为 manual / fixture / Vision OCR / proxy / ground truth / handwritten 来源。
- `externalTextBoxShadowOCRReport`：v1.13 新增的 external TextBoxes shadow-only OCR 接入口。只有 readiness 完整时才执行，选择只使用 bbox IoU、中心点包含、confidence、bubble alignment 和面积比例等无真值信号。v2.0 使用稳定最大基数二分匹配保证 block / TextBox 一对一并记录完整 outcome partition；v2.1 再把 assignment geometry 与 OCR outcome 分账，只有中心包含或 `IoU >= 0.10` 且 block / TextBox external Bubble ID 相同才计入 trusted geometry。weak overlap 和缺 Bubble 的 unknown assignment 可继续 shadow OCR，但不得关闭 coverage gate。结果仍只写报告 / TXT，不替换 `finalTextUsedForTranslation`、主覆盖图、`blockPassed` 或 promotion。
- `fusionComparison.postFusionCleanup.rejectedBlocks[]`：v1.18 起会为保守重复/碎片拒绝记录 `reason`、`relatedKeptBlockIndex`、`qualityScore`、`protectedTextMatched` 和 ground-truth-free `evidence`。保护文本和 decorative 标题不能因为短或像碎片被误删。
- `cleanTextDiagnostic` / `output/clean_text_diagnostic.json`：跳过 OCR，直接把 `test/1.ground_truth.json` 中的 dialogue 真值送入当前翻译链路，用于判断失败来自 OCR 噪声还是当前 Local 模型/判定链路。v1.29 起 `translationModelFloorComparisonReport` 会在其后运行 strict clean text 变体对照，但不替换 clean text baseline 或主流程 prompt。
- `overallPassed`：至少 1 个块、所有块通过质量判定、两张 PNG 非空才为 `true`。
- `outputDirectoryCleaned` / `retainedOutputFiles` / `outputCleanupPolicy`：记录本轮输出目录是否按清理策略重建，以及本轮最终保留的 PNG/JSON 文件名。

探针验收重点：

- `probe_report.json` 不应包含广告横幅、地址栏、翻页导航、系统状态栏等浏览器 UI 文本。
- `1_debug_boxes.png` 中同一对话框不应出现密集堆叠红框；一个气泡通常对应一个块。
- `1_translated_overlay.png` 中所有绘制文本必须正向可读；即使某块来自旋转 OCR，也不允许倒置或镜像。
- `blockPassed = false` 的块也必须出现在覆盖图上，并保留失败原因，不能静默隐藏。
- 每次运行会先清空 App 沙盒 `Application Support/AITRANS/Output/`，`scripts/export-probe-output.sh` 也会先清空项目根 `output/`，避免新旧 PNG / JSON 混在一起。
- 输出包括 `1_debug_boxes.png`、`1_translated_overlay.png`、`1_ocr_text_overlay.png`、`1_deterministic_correction_overlay.png`、`1_deterministic_translation_overlay.png`、`1_block_crops.png`、`1_preprocessed_content.png`、`1_bubble_debug.png`、`1_bubble_seed_debug.png`、`1_bubble_crops.png`、`1_bubble_text_overlay.png`、`1_probe_contact_sheet.png`、`1_ocr_probe_text.txt`。其中 `1_block_crops.png` 是块裁切放大预处理拼图，用于目测二值化/锐化是否过激；`1_deterministic_correction_overlay.png` 用于直接对比原 OCR 和确定性纠错候选；`1_deterministic_translation_overlay.png` 用于查看“纠错后再翻译”是否通过；`1_bubble_text_overlay.png` 把 bubble-first OCR 文本贴回原图，方便和全图 OCR overlay 并排看；`1_probe_contact_sheet.png` 把全图 OCR、气泡 OCR、气泡裁切、翻译覆盖和纠错对照拼成 2 列总览；`1_ocr_probe_text.txt` 是每块 OCR、纠错候选、raw output、候选译文、失败原因、质量备注和判定轨迹的纯文本快照，便于不用 `jq` 也能排查 OCR 原句。
- 翻译失败排查规范：先看 `failureCategory`，再看 `rawOutputClassification` 和 `candidateClassification`。如果 `failureCategory = ocrInputSuspect`，优先修 OCR/合并/裁切；如果是 `modelOutputFailure`，优先换模型、调采样或 prompt；如果是 `translationLanguageQualityFailure`，说明模型给了中文或半中文，但候选本身过短、混入未翻译英文或像解释文本；如果是 `ruleFalseFailureSuspected`，才优先放宽判定规则。不要只看覆盖图猜测失败原因。
- 翻译规则排查规范：如果 `likelyRuleFalseFailureBlocks = []`，说明本轮没有发现“真实中文译文被规则误杀”。`cjkButFailedCandidates` 只表示“候选含中文但未过端到端质量门”，需继续看 `cjkFailureBreakdown`；中文候选存在不等于通过，硬通过前还会检查解释/列表型输出、长原文短译文、中英混排和 OCR 输入质量。
- OCR 质量排查规范：先按 `ocrGroundTruthSimilarity` 从低到高看 `blocks[].finalTextUsedForTranslation`。本探针用 `test/1.ground_truth.json` 做真值；换测试图时应同步更新真值文件，否则相似度只作参考。
- 可信准确率规范：只看 `groundTruthMatch = matched` 的块；核心对话和 `decorative` 装饰标题分开统计。`unmatched` 块必须保留在明细里，但不能混进平均准确率。旧版 `accuracyVsGroundTruth = 0.8378 / 0.8755` 使用了不完整真值和过宽相似度，只能作为历史对照，不能再作为验收数字。
- 干净文本诊断规范：如果 `cleanTextDiagnostic.passRate` 仍低，说明即使没有 OCR 噪声，当前翻译模型/输出风格也不稳定；这时不要只调 OCR 或放宽质量规则。
- 解码规范：用户实际翻译仍使用 `sampled` 路径，llama.cpp 采样链为 top-k/top-p/min-p + `temperature = 0.2` + 随机 seed；漫画探针、`clean_text_diagnostic`、逐块 raw 翻译、确定性纠错翻译对照和 tagged batch 诊断使用 `deterministic` 路径，固定 `seed = 42` 并走 `temperature = 0` + greedy 解码。`probe_report.json` 顶层、`configuration`、`cleanTextDiagnostic` 和 `batchTranslationComparison` 会记录 `decodingMode` / `decodingSeed`。
- 跨版本指标规范：版本历史、关键决策和验证结果统一写入 `update_log.md`。涉及漫画探针或翻译链路的可量化版本，推荐先跑完整探针并导出 `output/`，再执行 `python3 scripts/append-version-metrics.py --version vN --notes "..."`。`metrics/version_history.csv` 不放在 `output/`，不会被 `scripts/export-probe-output.sh` 清理；不要覆盖或删除历史行。

模拟器跑完后，把沙盒输出导出到项目根 `output/`：

```sh
scripts/export-probe-output.sh
```

如需指定设备：

```sh
scripts/export-probe-output.sh booted
```

也可用 DEBUG 环境变量自动触发，适合命令行探针：

```sh
AITRANS_RUN_MANGA_PROBE=1
```

空目录预期结果：

- `运行 test/ 音频`：显示 `test/ 未找到可测试音频`。
- `运行 test/ OCR`：显示 `test/ 未找到可测试图片`。

模型文件不放进仓库。模型页可以直接下载内置最小模型，也可以手动 `导入 GGUF`。内置模型固定为：

- `Gemma 3 270M IT QAT Q4_0`
- 下载地址：[`ggml-org/gemma-3-270m-it-qat-GGUF`](https://huggingface.co/ggml-org/gemma-3-270m-it-qat-GGUF)
- 文件：`gemma-3-270m-it-qat-Q4_0.gguf`
- 大小：`241,410,624 bytes`，约 230 MB
- SHA256：`3626e245220ca4a1c5911eb4010b3ecb7bdbf5bc53c79403c21355354d1e2dc6`

App 下载时会写入临时 `model.gguf.download`，成功校验后原子替换为 `model.gguf`。同名模型只保留一个；如果已安装，点击下载不会重复保存。`移除模型` 会删除 `model.gguf` 和未完成的临时下载文件。

无论内置下载还是手动导入，模型都会复制到 App 沙盒内并统一命名为：

```text
Application Support/Models/Gemma-1.5B/model.gguf
```

如果没有这个文件，界面会显示 Local 未就绪。普通翻译会临时回退 Mock，避免界面卡死；LLM 接口自测和诊断会明确提示缺少模型。`移除模型` 只删除 App 沙盒中的 `model.gguf` 和临时下载，不影响原始文件或远程模型。

## 本地 LLM 模型准备

当前项目先按 `llama.cpp + GGUF` 方向接入。原因是 `llama.cpp` 官方仓库带有 `examples/llama.swiftui`，说明它是一个在 iPhone 上运行本地推理的 SwiftUI 示例；而本项目的模型导入器已经限制 `.gguf` 文件。MLC LLM 的 iOS 路线也可行，但它需要 `mlc_llm package` 生成 runtime、tokenizer 和已转换/编译的 MLC 权重，不是直接导入 `.gguf`。

先下载其中一个 `.gguf` 到 Mac：

- 首推翻译冒烟：[`Qwen/Qwen2.5-0.5B-Instruct-GGUF`](https://huggingface.co/Qwen/Qwen2.5-0.5B-Instruct-GGUF)，文件选 `qwen2.5-0.5b-instruct-q4_k_m.gguf`，约 469 MB。体积仍小，中文/英文测试更稳。
- 极小体积冒烟：[`unsloth/SmolLM2-135M-Instruct-GGUF`](https://huggingface.co/unsloth/SmolLM2-135M-Instruct-GGUF)，文件选 `SmolLM2-135M-Instruct-Q4_K_M.gguf`，约 101 MB。适合测加载和接口，不适合判断翻译质量。
- 最小 Gemma 路线：[`ggml-org/gemma-3-270m-it-qat-GGUF`](https://huggingface.co/ggml-org/gemma-3-270m-it-qat-GGUF)，文件选 `gemma-3-270m-it-qat-Q4_0.gguf`，约 230 MB；或 [`ggml-org/gemma-3-270m-it-GGUF`](https://huggingface.co/ggml-org/gemma-3-270m-it-GGUF) 的 `Q8_0` 版本。

Mac 机外先测：

```sh
brew install llama.cpp

llama-cli \
  -m ~/Downloads/qwen2.5-0.5b-instruct-q4_k_m.gguf \
  -p "Translate to Simplified Chinese: Keep the model on device." \
  -n 128
```

如果想让 `llama.cpp` 直接从 Hugging Face 拉模型，也可以先试：

```sh
brew install llama.cpp
llama-cli -hf Qwen/Qwen2.5-0.5B-Instruct-GGUF:Q4_K_M \
  -p "Translate to Simplified Chinese: Keep the model on device." \
  -n 128
```

放进 App 测试时，不需要手工改名。运行 App 后进 `模型` -> `下载 Gemma`，或 `导入 GGUF` 选择下载好的 `.gguf`，App 会复制并命名为 `model.gguf`。

当前已用 `gemma-3-270m-it-qat-Q4_0.gguf` 做过命令行冒烟：

```sh
llama-cli -m llm/gemma-3-270m-it-qat-Q4_0.gguf \
  -st --no-display-prompt --no-warmup --no-perf \
  -n 80 --temp 0.1 \
  -p "Translate to Simplified Chinese. Output only the translation: Keep the model on device so private meeting content never leaves the phone."
```

结果：模型能正常加载并生成，速度约 130 tokens/s，但输出容易复读英文原句或 prompt，不稳定翻译成中文。因此它适合验证 GGUF 下载、加载、App 接口和闪退风险，不适合作为翻译质量测试模型。要验证翻译质量，优先换 `Qwen2.5-0.5B-Instruct-GGUF` 的 `q4_k_m` 文件。

如果本地需要重建 iOS `llama.xcframework`：

```sh
git clone --depth 1 https://github.com/ggml-org/llama.cpp.git third_party/llama.cpp
bash Tools/build-llama-ios-xcframework.sh
```

## 大模型接入点

- `AITRANS/Models/TranscriptModels.swift`
  - `LocalLanguageModeling` 是统一模型协议。
  - `ModelGenerationRequest` 包含任务类型、语言、提示词、上下文和采样参数。
  - `ModelGenerationResult` 返回文本、摘要、引擎名、token 数和耗时。
  - `ModelStreamEvent` 和 `LocalLanguageModeling.stream(_:)` 预留逐 token 流式输出接口；默认实现会把一次性生成结果包装成流式事件，真实推理层可覆写。
- `AITRANS/Services/MockGemmaService.swift`
  - 当前模拟输出，用于没有 GGUF 时测试 UI、历史和回退路径。
- `AITRANS/Services/GemmaLocalService.swift`
  - 真实本地模型层，使用 `llama.cpp` 的 `llama.xcframework` 加载沙盒中的 `model.gguf` 并生成。
- `AITRANS/Services/LlamaRuntime.swift`
  - 封装 llama.cpp C API，负责加载 GGUF、分词、逐 token 生成、UTF-8 拼接和运行时锁。
- `AITRANS/Services/TranslationSessionStore.swift`
  - 负责页面共享状态、本地 JSON 存储、导入/导出、历史清理、自检、Mock/Local 回退和生成请求组装。

真实模型替换时，优先换 `model.gguf` 或调整 `GemmaLocalService` 的 prompt/采样参数，不需要改 UI 和历史数据结构。

## 后续对话指引

- 新开对话时，先读本 README，再读 `git status --short` 和最近 5 条 `git log --oneline`。
- 排查翻译问题时，先用模型页 `运行接口自测` 和开发页 raw 探针确认输入、输出、错误，再改 prompt 或清洗逻辑。
- 英译中优先用这些探针句测试：`The meeting starts at 9:30 tomorrow.`、`Keep the model on device.`、`Save the transcript locally.`。
- 中译英优先用这些探针句测试：`请把会议记录保存在本地。`、`明天九点半开始会议。`。
- 当前内置 Gemma 270M 适合验证下载、加载、接口和闪退风险，不适合作为翻译质量基准；质量验证优先换 `Qwen2.5-0.5B-Instruct-GGUF` 的 `q4_k_m`。
- 每次功能更新或 bug 修复后，都要在 `update_log.md` 追加简短记录，写清改了什么、验证了什么、还有什么风险；涉及漫画探针或翻译链路的可量化版本，再 append-only 追加 `metrics/version_history.csv`，便于后续对话继承上下文和画趋势。

## 当前验证

- `plutil -lint AITRANS/Resources/Info.plist AITRANS.xcodeproj/project.pbxproj` 通过。
- `jq empty AITRANS/Resources/Assets.xcassets/.../Contents.json` 通过。
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild ... generic/platform=iOS Simulator ... CODE_SIGNING_ALLOWED=NO build` 通过。本次构建日志里 CoreSimulatorService 有沙盒警告，但构建最终成功。
- `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild ... generic/platform=iOS ... CODE_SIGNING_ALLOWED=NO build` 通过。
- 已确认 Debug iOS Simulator app bundle 内嵌 `llama.framework`。
- `git diff --check` 通过。
- `test/1.png` 漫画覆盖探针已在 iPhone 17 Pro 模拟器运行并导出：`output/probe_report.json`、`output/1_debug_boxes.png`、`output/1_translated_overlay.png` 均非空。调试图框线明显；覆盖图能盖住部分原文并绘制译文，但受 OCR 小块切分和当前 Local GGUF 翻译质量影响，结果仍不适合作为最终产品效果。
