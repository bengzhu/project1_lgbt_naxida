# AITRANS Agent B 提示词：v1.47（Koharu Native Shadow Artifact Export-Lite 候选 artifact 影子导出）

## 版本号
v1.47（漫画探针 / Koharu Native Shadow Artifact Export-Lite 候选 artifact 影子导出）

## 版本分配依据
人工确认 v1.46 已完成，要求 Agent A 继续按流程阅读 `md/koharu研究/v1.38-current-gap-to-koharu.md` 和其他文档，项目继续向 Koharu 的算法结构框架优化、复现。测试仍主要使用 AITRANS 自己的漫画探针输出，分析 OCR 准确率、气泡归属 / 分割、翻译失败分类、覆盖渲染和结构瓶颈；当前模型不更改，继续做 Koharu 算法复刻优化。

本轮 Agent A 已阅读：

- `README.md`
- `AGENTS.md`
- `git status --short`
- `git log --oneline -5`
- `update_log.md`
- `md/flow/flow.md`
- `md/flow/flowchart.md`
- `md/test/test.md`
- `md/prompt/README.md`
- `md/koharu研究/v1.38-current-gap-to-koharu.md`
- `md/koharu研究/koharu图像识别链路研究.md`
- `md/koharu研究/v1.17-artifact-first-pass.md`
- `md/koharu研究/artifact_contract/README.md`
- `md/prompt/v1（漫画探针）/v1.46（KoharuNativePromotionGateLite探针驱动晋级门槛）.md`
- `AITRANS/Models/TranscriptModels.swift`
- `AITRANS/Services/MangaOverlayProbeService.swift`
- `AITRANS/Services/TranslationSessionStore.swift`
- `test/1.ground_truth.json`
- `metrics/version_history.csv`
- 最新可解析的仓库根 `output/probe_report.json`
- 最新可解析的仓库根 `output/clean_text_diagnostic.json`
- 最新可解析的仓库根 `output/1_ocr_probe_text.txt`

Agent A 开始时当前本地分支为：

```text
smalldata_test
```

最近提交：

```text
eaa809b Merge PR #37: v1.46 Native Promotion Gate-Lite
8134828 feat: add promotion gate lite report
1138090 Merge PR #36: v1.45 Native Artifact Bundle-Lite
d89a67f fix: use bubble safe rect signal
0915ec2 feat: add artifact bundle lite report
```

`git status --short` 显示一个未跟踪文件：

```text
?? md/云端协作流程/通用云端迭代升级提示词.md
```

该文件不是本轮 Agent A 产物。Agent B 不要误删、覆盖或纳入无关提交，除非人工明确要求处理。

## 当前基线和判断
当前仓库根 `output/` 仍是旧基线，不含 v1.46 新字段；它只能作为历史基线和字段参考，不能作为 v1.46 或 v1.47 验收结果。v1.47 可信验收以 Agent B PR 后 GitHub Actions `AITRANS CI Results` artifact 为准。

当前可信历史数字：

- `engineUsed = Local GGUF`
- `configuration.currentBlockSource = fusedWholePageBubble`
- `totalBlocksDetected = 13`
- `groundTruthMatchedBlocks = 13`
- `groundTruthUnmatchedBlocks = 0`
- `averageCoreDialogueOCRSimilarity = 0.7106`
- `averageDecorativeOCRSimilarity = 0.8000`
- `wholePageAccuracyVsGroundTruth = 0.5972`
- `bubbleFirstAccuracyVsGroundTruth = 0.7300`
- `fusion.fused.accuracyVsGroundTruth = 0.7384`
- `cleanTextDiagnostic.passRate = 0.4545`
- `passedBlocks = 1`
- `failedBlocks = 12`
- `translationFailureBreakdown = { modelOutputFailure: 2, ocrInputSuspect: 7, translationLanguageQualityFailure: 3 }`
- `textRegionCropReport.adoptedCount = 0`
- `cropExperimentReport.promotedShadowBlocks = []`
- `lineCropExperimentReport.promotedLineShadowBlocks = []`
- `externalArtifactReadinessReport.readinessVerdict = manifestMissing`
- `externalArtifactReadinessReport.nextAction = stopUntilArtifactsProvided`
- 仓库根旧 `output/probe_report.json` 中 `koharuNativePromotionGateLiteReport` 尚不存在；v1.46 是否通过必须看 PR #37 云端结果包。

v1.39-v1.46 已形成 native-lite 影子结构链：

```text
detector-lite TextBoxes
  -> detector-lite shadow OCR / refinement shadow OCR
  -> closed-loop router
  -> BubbleMask instance-lite
  -> SegmentMask refinement-lite
  -> Native Artifact Bundle-Lite
  -> Native Promotion Gate-Lite
```

v1.46 已把每块 TextBoxes / BubbleMask / SegmentMask / OcrText / Translations / RenderedSprites / FinalRender 的晋级状态和 `nativeCandidateExportPreview` 写进报告，但仍只是 JSON 内预览。下一步应把这些 native-lite 候选收束成一个机器可检查的 shadow artifact export 包，放在探针 Output 目录中，供 CI artifact、TXT 摘要和 validator gate 检查。

本轮不是接入真实 Koharu artifact，不是把 proxy 变成 active artifact，也不是替换主流程。它的价值是把 AITRANS 自己的结构探针证据转成接近 Koharu artifact 形状的非活动输出，从而暴露字段缺口、坐标缺口、气泡归属 / 分割缺口、SegmentMask 缺口、OCR / 翻译 / render 阻塞，并为以后真实 artifact 或人工首包对齐提供机器可读模板。

## 本轮目标
从最新 `origin/smalldata_test` 开分支：

```text
codeb/v1.47-native-shadow-artifact-export-lite
```

新增报告：

```text
koharuNativeShadowArtifactExportLiteReport
```

新增非活动 shadow export 输出目录，建议位于 App 探针沙盒 Output 下：

```text
Output/koharu_native_shadow_artifacts/
  1.native_manifest.json
  1.native_textboxes.json
  1.native_bubbles.json
  1.native_segment_mask.json
  1.native_artifact_bundle.json
```

报告和文件目标：

1. 在漫画探针中新增 Koharu native shadow artifact export-lite 阶段，执行顺序建议在 `koharuNativePromotionGateLiteReport` 之后、最终 `koharuArtifactConvergenceReport` refresh 之前。
2. 只消费当前探针 runtime 内存中的 final blocks、diagnostics、v1.39-v1.46 native-lite reports、Render Regression Lock、RenderSprite Fit、Translation Model Floor、clean text diagnostic 和 external artifact readiness。
3. 将 v1.46 `nativeCandidateExportPreviews`、v1.45 bundle ledger、v1.43 BubbleMask instance-lite、v1.44 SegmentMask refinement-lite 和 final block OCR / translation / render 证据组装成非活动 JSON 输出包。
4. 所有 shadow 文件必须显式标记 `shadowOnly = true`、`forbiddenAsActiveArtifact = true`、`contractExampleOnly = true` 或等价阻断字段，并写明 `readyForActiveArtifact = false`、`wouldCreateActiveArtifact = false`、`activeArtifactsWritten = false`。
5. Shadow export 目录不得是 `test/koharu_artifacts/`，不得写入仓库根 `test/`，不得修改 active artifact readiness 的 `manifestMissing` 结果。
6. 顶层报告必须记录 exported files、file existence / non-empty checks、record counts、per-block export ledger、field completeness、coordinate validation、active-artifact forbidden gate、proxy boundary gate 和 downstream analyzer notes。
7. ground truth 只能进入 `evaluationSignals`，例如 OCR 相似度、核心 / 装饰类型、人工真值匹配状态和质量趋势；禁止用于是否导出某块、bbox、气泡归属、SegmentMask 选择、candidate ranking、stage gate、route、nextAction 或 worklist。
8. `koharuArtifactConvergenceReport.referenceReports`、`workItemLedger` 和 `gateLedger` 必须联动 v1.47。
9. `1_ocr_probe_text.txt` 必须新增 shadow export summary、导出文件清单、逐块 export ledger、validator / gate 摘要和 convergence v1.47 work item / gate 摘要。
10. 如实现 Python validator，必须只校验 shadow export 目录，不复制、不迁移、不生成 active `test/koharu_artifacts/`；对 shadow manifest 的正确结果是 `readyForActiveArtifact = false` 且 `forbiddenAsActiveArtifact = true`。

## 非目标和硬禁止
本轮严禁：

- 不更换默认模型，不引入 Qwen 或其他模型对照。
- 不下载、提交或接入 detector / segmenter 权重、ONNX、Core ML、safetensors、GGUF 或大二进制文件。
- 不创建、复制、修改或提交 active `test/koharu_artifacts/`。
- 不把 shadow export 包、contract examples、Vision OCR blocks、pre-crop plan、line plan、BubbleMask proxy、SegmentMask proxy、ground truth、native bundle-lite 或手写理想框放进 active artifact。
- 不让 `externalArtifactReadinessReport` 因本轮 shadow export 变成 `readyForShadowOCR`；没有真实 active artifact 时必须继续 `manifestMissing` / `stopUntilArtifactsProvided`。
- 不把 `koharuNativeShadowArtifactExportLiteReport` 冒充真实 Koharu artifact、真实 detector、真实 TextBoxes、真实 BubbleMask、真实 SegmentMask、真实 OcrText 或真实 RenderedSprites。
- 不使用 `test/1.ground_truth.json` 选择 TextBox、BubbleMask、SegmentMask、OCR 候选、导出记录、导出 eligibility、stage gate、route、nextAction、worklist 或 promotion。
- 不改变主 OCR、whole-page / bubble-first 融合、post-fusion cleanup、candidate selection 或 `configuration.currentBlockSource`。
- 不改变 `finalTextUsedForTranslation`、translation prompt、模型、`blockPassed`、`failureCategory` 或质量判定。
- 不改变主覆盖图绘制、`safeLayoutRect`、DistanceField safe rect、`renderFontSize`、`renderNonTransparentBounds`、`glyphMaskFillRects`、背景填充、failure overlay 文本或实际清字策略。
- 不把 shadow export 结果用于 TextRegion crop clamp、OCR 输入、overlay renderer、background fill、`textRegionCropReport.adoptedCount` 或主流程 promotion。
- 不新增 OCR 调用，不新增 LLM 调用，不新增 PNG 输出；本轮只允许新增 JSON / TXT 类输出和可选离线 validator。
- 不重开 v17-v20 已证伪的 crop / line / deskew 调参路线。
- 不追加 `metrics/version_history.csv` 漫画指标行，除非本轮实际重新跑完整探针并明确是质量版本收口。

## Agent B 开始前必读
1. `README.md`
2. `AGENTS.md`
3. `git status --short`
4. `git log --oneline -5`
5. `update_log.md`
6. `md/flow/flow.md`
7. `md/flow/flowchart.md`
8. `md/test/test.md`
9. `md/koharu研究/v1.38-current-gap-to-koharu.md`
10. `md/koharu研究/koharu图像识别链路研究.md`
11. `md/koharu研究/v1.17-artifact-first-pass.md`
12. `md/koharu研究/artifact_contract/README.md`
13. `md/prompt/v1（漫画探针）/v1.43（KoharuNativeBubbleMaskInstanceLite像素实例掩码影子复刻）.md`
14. `md/prompt/v1（漫画探针）/v1.44（KoharuNativeSegmentMaskRefinementLite文字像素掩码影子复刻）.md`
15. `md/prompt/v1（漫画探针）/v1.45（KoharuNativeArtifactBundleLite结构一致性闭环）.md`
16. `md/prompt/v1（漫画探针）/v1.46（KoharuNativePromotionGateLite探针驱动晋级门槛）.md`
17. `AITRANS/Models/TranscriptModels.swift`
18. `AITRANS/Services/MangaOverlayProbeService.swift`
19. `AITRANS/Services/TranslationSessionStore.swift`
20. `test/1.ground_truth.json`
21. 最新 `output/probe_report.json`
22. 最新 `output/clean_text_diagnostic.json`
23. 最新 `output/1_ocr_probe_text.txt`
24. `metrics/version_history.csv`

如果本地有 `reference/koharu-main`，可只读 artifact DAG / renderer / BubbleIndex / SegmentMask refine 相关源码；没有则不要联网拉取。

## 实现要求
### 1. 新增报告模型
在 `AITRANS/Models/TranscriptModels.swift` 新增 Codable / Equatable / Sendable 模型。命名建议：

```swift
MangaKoharuNativeShadowArtifactExportLiteSignal
MangaKoharuNativeShadowArtifactFileRecord
MangaKoharuNativeShadowArtifactTextBoxRecord
MangaKoharuNativeShadowArtifactBubbleRecord
MangaKoharuNativeShadowArtifactSegmentMaskRecord
MangaKoharuNativeShadowArtifactBundleRecord
MangaKoharuNativeShadowArtifactBlockLedger
MangaKoharuNativeShadowArtifactGate
MangaKoharuNativeShadowArtifactExportLiteReport
```

`MangaOverlayProbeReport` 顶层新增：

```text
koharuNativeShadowArtifactExportLiteReport
```

顶层至少输出：

- `enabled`
- `source = "AITRANSProbe"`
- `referencePipeline = "Koharu"`
- `referenceConcept = "NativeShadowArtifactExportLite.ReportOnlyCandidatePacket"`
- `referenceWorkItemID = "WI-koharu-native-shadow-artifact-export-lite"`
- `evaluatedBlockCount`
- `exportedFileCount`
- `textBoxRecordCount`
- `bubbleRecordCount`
- `segmentMaskRecordCount`
- `bundleRecordCount`
- `blockLedgerCount`
- `gateCount`
- `outputDirectory`
- `relativeOutputDirectory = "koharu_native_shadow_artifacts"`
- `activeArtifactsDirectory = false`
- `activeArtifactsWritten = false`
- `shadowOnly = true`
- `forbiddenAsActiveArtifact = true`
- `readyForActiveArtifact = false`
- `groundTruthUsedForDecision = false`
- `groundTruthUsedForEvaluationOnly = true`
- `wouldChangeMainFlow = false`
- `diagnosticOnly = true`
- `nativeShadowExportLite = true`
- `proxyNotRealKoharuTextBoxes = true`
- `proxyNotRealKoharuBubbleMask = true`
- `proxyNotRealKoharuSegmentMask = true`
- `proxyNotRealKoharuOCR = true`
- `proxyNotRealKoharuRenderer = true`
- `externalArtifactsRequiredForThisReport = false`
- `exportVerdict`
- `fieldCompletenessBreakdown`
- `coordinateValidationBreakdown`
- `recordSourceBreakdown`
- `primaryBlockingArtifactBreakdown`
- `nextActionBreakdown`
- `files`
- `textBoxRecords`
- `bubbleRecords`
- `segmentMaskRecords`
- `bundleRecords`
- `blockLedgers`
- `gateLedger`
- `notes`

### 2. Shadow export 文件内容
在 App 沙盒 Output 下新增目录：

```text
koharu_native_shadow_artifacts/
```

该目录由每轮探针输出清理机制自然清理，不能从旧输出复用。

建议写出以下 JSON：

#### `1.native_manifest.json`
至少包含：

- `schemaVersion = "aitrans.koharu_native_shadow_artifact.v1"`
- `sourceImage = "test/1.png"`
- `coordinateSpace = "originalImageTopLeftPixels"`
- `generatedBy = "AITRANS native-lite probe"`
- `shadowOnly = true`
- `contractExampleOnly = true`
- `forbiddenAsActiveArtifact = true`
- `readyForActiveArtifact = false`
- `activeArtifactsDirectory = false`
- `activeArtifactsWritten = false`
- `groundTruthUsedForDecision = false`
- `wouldChangeMainFlow = false`
- `textBoxesPath = "1.native_textboxes.json"`
- `bubbleMaskPath = "1.native_bubbles.json"`
- `segmentMaskPath = "1.native_segment_mask.json"`
- `bundlePath = "1.native_artifact_bundle.json"`
- `sourceReports` 包含 v1.39-v1.46 相关报告名
- `notes` 明确不能复制到 `test/koharu_artifacts/`

#### `1.native_textboxes.json`
逐块或逐候选记录 TextBox shadow candidate。每条至少包含：

- `blockIndex`
- `textBoxID`
- `bbox = [x, y, width, height]`
- `sourceReport`
- `sourceCandidateID`
- `sourceDirection`
- `confidence`
- `fieldCompleteness`
- `coordinateValid`
- `promotionStatus`
- `exportEligibility`
- `forbiddenAsActiveArtifact = true`
- `proxyNotRealKoharuTextBox = true`
- `decisionSignals`
- `evaluationSignals`

TextBox 来源优先级建议：

1. v1.46 `nativeCandidateExportPreview` 中 target stage 为 TextBoxes 且 bbox 有效的 preview。
2. v1.45 bundle ledger 的 selected TextBox lite component。
3. v1.39 detector-lite block ledger / candidate ledger 的 selected candidate。

不得从 ground truth、手写理想框或旧 `output/` 读取 bbox。

#### `1.native_bubbles.json`
逐气泡或逐块记录 BubbleMask instance-lite summary。每条至少包含：

- `bubbleID` 或 `instanceLiteID`
- `bbox`
- `sourceReport`
- `majorityBlockIndexes`
- `maskValue`
- `pixelCount`
- `safeRectSummary`
- `assignmentStatus`
- `splitRisk`
- `forbiddenAsActiveArtifact = true`
- `proxyNotRealKoharuBubbleMask = true`
- `decisionSignals`
- `evaluationSignals`

来源优先使用 v1.43 `koharuNativeBubbleMaskInstanceLiteReport`，可辅以 BubbleIndex / DistanceField / seam 账本。

#### `1.native_segment_mask.json`
记录 SegmentMask refinement-lite summary，不要求写 PNG 或真实 mask。至少包含：

- `width = 576`
- `height = 1280`
- `sourceReport`
- `candidateCount`
- `glyphPixelCount`
- `connectedComponentCount`
- `blockSummaries`
- `textboxConstrained = true`
- `bubbleConstrained = true`
- `forbiddenAsActiveArtifact = true`
- `proxyNotRealKoharuSegmentMask = true`
- `decisionSignals`
- `evaluationSignals`

来源优先使用 v1.44 `koharuNativeSegmentMaskRefinementLiteReport`，可辅以现有 glyph / SegmentMask proxy 证据。

#### `1.native_artifact_bundle.json`
逐块记录 TextBox / BubbleMask / SegmentMask / OCR / Translation / Render 的 shadow bundle。每条至少包含：

- `blockIndex`
- `textBoxID`
- `bubbleID`
- `segmentMaskCandidateID`
- `finalTextUsedForTranslation`
- `translationCandidate`
- `failureCategory`
- `blockPassed`
- `renderSafeRect`
- `renderFitStatus`
- `promotionEligibility`
- `primaryBlockingArtifact`
- `nextAction`
- `forbiddenAsActiveArtifact = true`
- `wouldChangeMainFlow = false`
- `groundTruthUsedForDecision = false`
- `decisionSignals`
- `evaluationSignals`

### 3. 逐块 block ledger
`koharuNativeShadowArtifactExportLiteReport.blockLedgers[]` 必须覆盖每个 final block，至少包含：

- `blockIndex`
- `bubbleID`
- `blockPassed`
- `failureCategory`
- `finalTextUsedForTranslation`
- `textBoxExportRecordID`
- `bubbleExportRecordID`
- `segmentMaskExportRecordID`
- `bundleExportRecordID`
- `textBoxCoordinateStatus`
- `bubbleCoordinateStatus`
- `segmentMaskSummaryStatus`
- `fieldCompletenessStatus`
- `promotionGateStatusFromV146`
- `shadowExportEligibility`
- `primaryBlockingArtifact`
- `nextAction`
- `mustNotUseAsActiveArtifactReasons`
- `decisionSignals`
- `evaluationSignals`
- `groundTruthUsedForDecision = false`
- `wouldChangeMainFlow = false`
- `diagnosticOnly = true`

`shadowExportEligibility` 可使用：

- `exportedShadowOnly`
- `exportedWithMissingTextBoxFields`
- `exportedWithMissingBubbleMaskFields`
- `exportedWithMissingSegmentMaskFields`
- `blockedByMissingPromotionPreview`
- `blockedByInvalidCoordinates`
- `blockedByProxyBoundary`

无论 eligibility 如何，不能写入 active artifact。

### 4. Gate ledger
至少新增这些 gate：

- `G-native-shadow-artifact-export-lite-output-files`：JSON 文件存在且非空。
- `G-native-shadow-artifact-export-lite-no-active-write`：未写 `test/koharu_artifacts/`，`activeArtifactsWritten=false`。
- `G-native-shadow-artifact-export-lite-forbidden-active`：manifest 和所有记录均标记 forbidden / shadow-only。
- `G-native-shadow-artifact-export-lite-no-ground-truth-decision`：所有 decision signals 不使用 ground truth。
- `G-native-shadow-artifact-export-lite-block-coverage`：`blockLedgerCount == totalBlocksDetected`。
- `G-native-shadow-artifact-export-lite-coordinate-validation`：bbox 为正且在 `576 x 1280` 范围内，或明确 blocked reason。
- `G-native-shadow-artifact-export-lite-proxy-boundary`：所有 proxy-not-real Koharu flags 为 true。
- `G-native-shadow-artifact-export-lite-no-main-flow-mutation`：不改变 OCR / translation / overlay / renderer / blockPassed / failureCategory / currentBlockSource。
- `G-native-shadow-artifact-export-lite-external-readiness-unchanged`：缺真实 active artifact 时 external readiness 仍是 `manifestMissing`。
- `G-native-shadow-artifact-export-lite-ci-retained`：输出文件路径进入 `probe_report.json` / TXT，并由 CI artifact 保留。

### 5. 可选 validator
可以新增或扩展脚本，例如：

```text
scripts/validate-koharu-shadow-artifacts.py
```

要求：

- 只读指定 shadow export 目录。
- 不写 `test/koharu_artifacts/`，不复制文件，不把 shadow 包转换为 active 包。
- 对 `koharu_native_shadow_artifacts/` 的正确 verdict 应是 `shadowOnlyValid` 或 `validShadowExport`，但 `readyForActiveArtifact = false`。
- 若用户误把 shadow export 目录当 active contract 校验，必须能从 manifest 的 `contractExampleOnly = true` / `forbiddenAsActiveArtifact = true` 看出不能 active。
- 可加入 `--expect-shadow-valid`、`--allow-missing` 等轻量参数，但不要引入外部依赖。

### 6. TXT 快照
`1_ocr_probe_text.txt` 必须包含：

- `koharuNativeShadowArtifactExportLiteReport` summary。
- Shadow export 文件路径和非空状态。
- 每块 `nativeShadowArtifactExportBlockLedger`。
- `nativeShadowArtifactFile` 摘要。
- 关键 gate 摘要。
- `externalArtifactReadiness` 仍为 active artifact 缺失时的 `manifestMissing`。

### 7. Convergence 联动
`koharuArtifactConvergenceReport` 必须：

- `referenceReports` 包含 `koharuNativeShadowArtifactExportLiteReport`。
- `workItemLedger` 新增 `WI-koharu-native-shadow-artifact-export-lite`。
- `gateLedger` 新增 `G-koharu-native-shadow-artifact-export-lite-executed`。
- 工作项状态应区分 `closedShadowExportOnly`、`blockedByMissingNativeLiteInputs`、`blockedByInvalidCoordinates`、`blockedByProxyBoundary`、`manualReviewOnly`。
- 不把 shadow export 解释为真实 TextBoxes / BubbleMask / SegmentMask 已 ready。

## 关键文件
预计需要修改：

- `AITRANS/Models/TranscriptModels.swift`
- `AITRANS/Services/MangaOverlayProbeService.swift`
- `AITRANS/Services/TranslationSessionStore.swift`
- `README.md`
- `md/flow/flow.md`
- `md/flow/flowchart.md`
- `md/test/test.md`
- `update_log.md`
- 可选：`scripts/validate-koharu-shadow-artifacts.py`

不要修改：

- `test/koharu_artifacts/`
- GGUF 或任何模型文件
- `test/1.ground_truth.json`，除非人工明确要求
- `metrics/version_history.csv`，除非实际重新跑完整探针并按规则收口质量版本
- 无关 UI / StoreKit / 普通图片 OCR / 普通翻译页面

## 测试要求
本地默认只跑轻量检查，不跑本机完整 Xcode build / 漫画探针，除非人工明确要求。

Agent B 本地至少运行：

```sh
git diff --check
python3 -m json.tool test/1.ground_truth.json
python3 -m json.tool output/probe_report.json
python3 -m json.tool output/clean_text_diagnostic.json
python3 scripts/validate-koharu-artifacts.py --root md/koharu研究/artifact_contract/examples/valid
python3 scripts/validate-koharu-artifacts.py --root md/koharu研究/artifact_contract/examples/invalid/coordinate_mismatch --expect-fail
python3 scripts/validate-koharu-artifacts.py --root md/koharu研究/artifact_contract/examples/invalid/invalid_bbox --expect-fail
python3 scripts/validate-koharu-artifacts.py --root md/koharu研究/artifact_contract/examples/invalid/missing_textboxes --expect-fail
python3 scripts/validate-koharu-artifacts.py --root test/koharu_artifacts --allow-missing
```

如新增 shadow validator，补跑脚本自身的 smoke。可以用临时目录或脚本内 fixture，但不得写入 active `test/koharu_artifacts/`：

```sh
python3 scripts/validate-koharu-shadow-artifacts.py --help
```

若 Agent B 能做 Swift 轻量语法检查，可对改动文件做 `swiftc -parse` 或项目既有轻量 parse；如果因 SwiftUI / Apple framework import 不适合 parse，最终回复要说明未跑原因。不要把未跑的本机 build / 探针写成已通过。

Swift / Xcode / 漫画探针相关重验证默认交给 GitHub Actions：

```text
push codeb/v1.47-native-shadow-artifact-export-lite
create PR: base=smalldata_test, head=codeb/v1.47-native-shadow-artifact-export-lite
GitHub Actions AITRANS CI Results runs ci-fast
```

## 云端 CI 期望
`AITRANS CI Results` `ci-fast` 应证明：

- `probe_report.json` 可解析。
- `clean_text_diagnostic.json` 可解析。
- `engineUsed = Local GGUF`。
- `configuration.currentBlockSource = fusedWholePageBubble`。
- `totalBlocksDetected > 0`。
- `koharuNativePromotionGateLiteReport.enabled = true`。
- `koharuNativeShadowArtifactExportLiteReport.enabled = true`。
- `koharuNativeShadowArtifactExportLiteReport.evaluatedBlockCount == totalBlocksDetected`。
- `blockLedgerCount == totalBlocksDetected`。
- `exportedFileCount >= 5`。
- `textBoxRecordCount >= 1` 或明确 blocked / missing source reason。
- `bubbleRecordCount >= 1` 或明确 blocked / missing source reason。
- `segmentMaskRecordCount >= 1` 或明确 blocked / missing source reason。
- `bundleRecordCount == totalBlocksDetected` 或明确 blocked reason。
- `activeArtifactsDirectory = false`。
- `activeArtifactsWritten = false`。
- `shadowOnly = true`。
- `forbiddenAsActiveArtifact = true`。
- `readyForActiveArtifact = false`。
- `groundTruthUsedForDecision = false`。
- `wouldChangeMainFlow = false`。
- `diagnosticOnly = true`。
- proxy-not-real flags 均为 true。
- `externalArtifactsRequiredForThisReport = false`。
- 缺真实 active artifact 时 `externalArtifactReadinessReport.readinessVerdict = manifestMissing` 仍不变。
- Output artifact 中存在并保留 `koharu_native_shadow_artifacts/1.native_manifest.json`、`1.native_textboxes.json`、`1.native_bubbles.json`、`1.native_segment_mask.json`、`1.native_artifact_bundle.json`。
- `1_ocr_probe_text.txt` 包含 shadow export summary、文件清单、逐块 ledger 和 gate 摘要。
- `koharuArtifactConvergenceReport.referenceReports` 包含 `koharuNativeShadowArtifactExportLiteReport`。
- convergence `workItemLedger` / `gateLedger` 包含 v1.47 work item / gate。

如果云端没有运行完整 full 模式，不影响本轮验收；`ci-fast` 足够，因为本轮只新增 JSON / TXT shadow export，不新增 heavy OCR / LLM / PNG。

## 文档更新要求
Agent B 必须按影响同步：

- `update_log.md`：新增 v1.47 记录，说明是 shadow export report-only 输出，不刷新仓库根旧 `output/`，不追加指标行。
- `md/flow/flow.md`：在漫画探针当前流程中加入 `Koharu Native Shadow Artifact Export-Lite`，说明输出非活动 shadow artifact 包。
- `md/flow/flowchart.md`：在 v1.46 promotion gate 后、最终 convergence 前加入节点。
- `md/test/test.md`：新增 v1.47 CI 验收字段和 shadow output 文件要求。
- `README.md`：只补当前功能说明，不写历史流水账。

如新增 validator，文档要说明它校验的是 shadow export，不是 active `test/koharu_artifacts/`。

## 验收标准
Agent C 验收时至少检查：

1. PR base 是 `smalldata_test`，head 是 `codeb/v1.47-native-shadow-artifact-export-lite`，没有合并到 `main`。
2. diff 不包含 GGUF、模型权重、大二进制或 active `test/koharu_artifacts/`。
3. `probe_report.json` 中存在 `koharuNativeShadowArtifactExportLiteReport`，且关键布尔边界正确。
4. Output artifact 中存在 shadow JSON 文件，文件非空，manifest 明确 shadow-only / forbidden active。
5. `externalArtifactReadinessReport` 没有被 shadow export 误改成 ready。
6. `1_ocr_probe_text.txt` 包含报告 summary、文件清单、逐块 ledger 和 gate。
7. convergence 已联动 v1.47 reference / work item / gate。
8. `finalTextUsedForTranslation`、主覆盖图、`blockPassed`、失败分类、renderer、`configuration.currentBlockSource` 和 `textRegionCropReport.adoptedCount` 没有被本轮 shadow export 改动。
9. 本地轻量检查和云端 CI 结果包一致；不得拿旧 `output/` 或旧 artifact 当新验收。

## Agent B 最终回复必须包含
- 分支名。
- PR 链接。
- commit SHA。
- push 结果。
- GitHub Actions run / CI 入口。
- 本地已运行检查和结果。
- 未运行测试及原因。
- 明确写：未跑本机 build / 探针，按规则交给云端验证。
- 云端 artifact 名称和应检查的 shadow output 文件。
- 若 Actions 尚未完成，说明等待云端结果。

## 风险和提醒
- v1.47 是把 native-lite 证据转成非活动 artifact 形状，不代表真实 Koharu artifact 到位。
- Shadow export 文件越像 active artifact，越必须明确写 forbidden / shadow-only，防止被后续误复制到 `test/koharu_artifacts/`。
- 当前模型仍是 Gemma 270M；翻译质量低不能通过 artifact export 解决。
- 当前英文横排 `test/1.png` 不能证明竖排日语能力；本轮只为后续 artifact 对齐和结构复刻打基础。
- 如果 v1.46 云端字段在本地旧 `output/` 不可见，不能据此回退需求；以当前代码和 v1.46 merge 后云端 artifact 为准。
