# v1.91 提示词：Koharu 真实 artifact ci-fast + Speech 人工矩阵

## 背景
截至 v1.90：UI 首页/宽屏证据/Speech runToken 诊断已合入 smalldata_test。Koharu 仍差真实 TextBoxes/BubbleMask/SegmentMask 与模型地板。

## 目标
1. 若有 Release 四件套：workflow_dispatch `ci-fast` + Koharu artifact 注入，按 AGENTS 核对 identity/coverage/orientation/shadow OCR。
2. 无四件套：只做 gate 可读性与文档，不伪造 ready。
3. Speech：把 md/test 人工矩阵补录音取消/重试 run-id 场景（不换引擎）。

## 分支
`codeb/v1.91-koharu-artifact-ci-fast-speech-matrix` → smalldata_test

## 禁止
main；本机 Xcode/sim 默认验收；伪造 artifact；改生产 paste 隐私模型。
