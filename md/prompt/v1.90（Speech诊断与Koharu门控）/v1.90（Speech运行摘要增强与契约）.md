# v1.90 Agent 实现说明：Speech 运行摘要增强与契约

## 目标
- 提升 AI 语音识别可诊断性（不换引擎）
- 云端静态契约覆盖取消 run-id 隔离与摘要字段
- 不跑本机 Xcode/sim；验收只看 GitHub Actions

## 变更
1. `SpeechRecognitionRunSummary.runToken`：短 run 身份，便于对照取消/重试
2. UI 摘要展示：本机能力、终态、Run token
3. Speech contract +2：cancel 先 invalidate；panel 字段接线
4. 文档/update_log 候选记录

## 非目标
- 不换 ASR、不改 Pro 商业逻辑、不跑漫画探针
- 不 merge main

## 分支
`codeb/v1.90-speech-diagnostics-koharu-gates` → `smalldata_test`
