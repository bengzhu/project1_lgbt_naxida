v3.279 对齐 Koharu `inference_text_blocks` 的单 TextRegion prediction 边界：当 known owner 的 line coverage 不完整而触发既有整块 Vision crop 时，主／反向读取先合成并按现有日语规则去重为同一 `blockFallback`；只有该 fallback 集合以显式 `.crop` 资格重新通过 v3.278 的 exact-owner、可靠日语质量、geometry 与一对一 source-line complete coverage proof，且所有 fallback observation 都携带同一 block owner，才移除 `refined` 中该 owner 的 partial `.verticalLine` 结果，避免 v3.277 owner-first layout 把“局部 line 文本 + 完整 fallback 文本”重复拼接。旧 line coverage skip 默认仍只接受 `.verticalLine`；弱／空／覆盖不完整／ownerless／foreign-owner fallback 不替代，已成功的 partial lines 继续保留，ownerless block 保持历史兼容。新增 `scripts/test-v3279-koharu-block-fallback-replacement-contract.py` 与 cloud-only policy evaluator，工程版本为 `3.279`。detector、line OCR、16 block／8 orientation fallback 预算、crop／warp、confidence／日语密度 gate、owner grouping、翻译、UI、渲染和非图片路径不变。本地 274 个无进程入口合同、311 个 Python AST、3 个 workflow YAML、19 个 shell 语法、4 个 plist／工程版本、新合同 `7/7` 与 `git diff --check` 通过，27 个实际调用 `subprocess` 的合同按约束跳过；未运行本地 Xcode、Swift evaluator、Rust/Cargo、Core ML 或 app/runtime。初始 full [31641057742](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31641057742) 的应用代码、Koharu parity、Xcode、Speech／UI／Home／Paste 与 manga probe 均通过，但 v3.279 Python 合同错误读取只由独立 parity job checkout 的 ignored `reference/koharu-main`，使 JUnit 为 `9/10`；合同随后改为读取仓库内 pinned cloud smoke 边界。最终 SHA `388280cb7db23119cc99fdf2812537cd5ec0c705` 的 exact-SHA full [31643409108](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31643409108) 成功：Koharu parity job `94271228157`、macOS job `94272780695`、云端 v3.279 evaluator、Xcode 26.6、manga probe、JUnit `10/10` 与 receipt 全部通过；固定 `test/jap.jpg` 为 7/7 accepted，`ニコッ` confidence `0.59093773`、`今度こそ` `0.99994445`。PR [#360](https://github.com/bengzhu/project1_lgbt_naxida/pull/360) fast [31658387394](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31658387394) 成功，并以 merge SHA `3a953b6e8fa06951e6bff81476489e9dbeaa471a` 合入 `smalldata_test`；合入后 push CI [31658445570](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31658445570) 复用候选 full receipt 成功。GPL Koharu/MIT48 权重与 runtime 仍只用于云端 reference parity，不打包进 AITRANS、不外推固定样图为通用日语 OCR／翻译质量提升；main 未动，研发分支最终删除后本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.278 收紧 Koharu owner line coverage 的整块 crop 跳过条件：普通 observation fusion 仍允许 ownerless Vision fallback 与 known owner 兼容，但 `hasCompleteJapaneseLineCoverage` 现在要求 known `verticalTextRegionOwner` block 的每个源 line candidate 与对应可靠 line OCR result 都精确携带同一 owner，ownerless／歧义 line 不能再单独证明某个 detector TextRegion 已完整识别；ownerless block 继续使用历史 geometry compatibility。证明不完整时沿用既有 bounded block crop 与最多 8 次方向 fallback，不新增请求上限、不生成空文字块；line OCR、owner-first grouping、confidence／日语密度 gate、crop／warp、detector bbox ownership、布局、翻译、UI、渲染和非图片路径不变。新增 `scripts/test-v3278-koharu-line-coverage-owner-contract.py` 与 cloud-only owner policy evaluator，工程版本为 `3.278`。本地 273 个无进程入口合同、300 个 Python AST、YAML／shell／plist、新合同 `6/6` 与 `git diff --check` 通过，27 个可能启动外部进程的合同按约束跳过；未运行本地 Xcode、Swift evaluator、Rust/Cargo、Core ML 或 app/runtime。最终 SHA `b00c6ba9071e3b12b23486606ae62bd3edd3b6cb` 的 exact-SHA full [31637022274](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31637022274) 成功：Koharu parity job `94249821250`、macOS job `94251604097`、云端 owner evaluator、Xcode 26.6、UI／Home／Paste／Speech、manga probe、JUnit `10/10` 与 receipt 全部通过；MIT48 固定 crop 为 7/7 accepted，`ニコッ` confidence `0.59093773`、`今度こそ` `0.99994445`。PR [#359](https://github.com/bengzhu/project1_lgbt_naxida/pull/359) fast [31639423729](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31639423729) 成功，并以 merge SHA `41f4d5ba707ae7c42378b97d4833f8280788a8f5` 合入 `smalldata_test`；合入后 push CI [31639486578](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31639486578) 复用候选 full receipt 成功。GPL Koharu/MIT48 权重与 runtime 仍只用于云端 reference parity，不打包进 AITRANS、不外推固定 `test/jap.jpg` 为通用日语 OCR／翻译质量提升；main 未动，研发分支最终删除后本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.277 完成 Koharu `Mit48pxOcr::inference_text_blocks` 的同 `block_index` 强归组语义：只在日语 `prefersMangaReadingOrder` 的竖排 layout 中，known-owner observations 先按 `verticalTextRegionOwner` 建立唯一 cluster，同一 TextRegion 的远距离 line 不再被普通列距／间隙门控拆成多个最终 block；block 内继续按稳定 top-to-bottom 顺序无分隔拼接，并对各 line confidence 做算术平均。ownerless observations 继续使用历史 geometry clustering，可明确命中一个 known owner 的单行 geometry 时维持双向兼容合并；若同时命中多个 known owner，则保持 ownerless 独立，不能成为跨 TextRegion 的桥，也不使用远距离同 owner lines 的 union rect 吸附中间几何。不同 known owner、protected boundary、最终 owner 不进入 `ImageTranslationBlock`／Store／UI、请求预算、confidence／日语密度 gate、detector bbox ownership、crop／warp／方向、翻译、渲染和非图片路径不变。新增 `scripts/test-v3277-koharu-owned-line-grouping-contract.py` 与 cloud-only owner grouping evaluator，工程版本为 `3.277`。候选 `a4b243e213e528f25d1655e79f880de354fe0912` 的 full [31631827061](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31631827061) attempt 1 已证明 UI owner evaluators、云端 Xcode 26.6 与 JUnit `10/10` 通过；parity 两次均在 OCR 前准备 pinned `runtime:llama` 时遭遇 GitHub Release 瞬态故障，因此 cloud smoke 只对同时匹配 `runtime:llama`、pinned `b8935` URL 与 5xx／429／HTTP2／timeout／reset 的下载错误做最多 3 次有界重试，并复用同一 `KOHARU_DATA_ROOT`；模型加载、OCR、JSON 与质量失败仍立即退出。最终 SHA `53e4209a0fdae623b6bedb4333079240e2087fbb` 的 exact-SHA full [31634058829](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31634058829) 两个 job 均成功：Koharu parity 7/7 accepted，`ニコッ` confidence `0.59093815`、`今度こそ` `0.99994445`，UI 合同与 `AITRANS CI/full-validation=success` receipt 通过；最终提交仅改 cloud smoke，Xcode 按 changed-file scope 跳过，应用编译证据沿用同实现候选。PR [#358](https://github.com/bengzhu/project1_lgbt_naxida/pull/358) CI [31635714958](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31635714958) 成功，并以 merge SHA `2476d54596e2be99bd75ffdc1437847cdb11f5e1` 合入 `smalldata_test`；合入后 push CI [31635811775](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31635811775) 成功。本地 272 个无进程入口合同、299 个 Python AST、YAML／shell／plist 与 diff 检查通过；没有运行 `xcodebuild`、Rust/Cargo、Core ML 或 app/runtime probe，但一次误选历史 v3.219 合同内置 `xcrun swiftc` evaluator，已在约 4 秒内完成且无残留进程，后续只运行预扫确认无进程入口的静态合同。GPL Koharu/MIT48 runtime 仍只用于云端 parity，不打包进 AITRANS、不外推固定样图为通用质量提升；main 未动，研发分支删除后本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.276 继续迁移 Koharu `inference_text_blocks` 的 `block_index` 分组边界：日语竖排 recognition pass 为最多 16 个已有 vertical layout block 分配临时 owner；Vision line／perspective／axis／geometry-only／block crop 与 bundled Manga OCR batch request/result 全程传递该 owner，known-owner observation 只能被同 owner block 消费，两个不同 known owner 不再被 OCR 去重、完整覆盖、列合成或 layout cluster 合并。无法唯一匹配的 observation 保持 ownerless 并沿用历史 geometry fallback；compact recovery 即使 ownerless 也继续保留既有 protected boundary，owner 不写入 `ImageTranslationBlock`、Store 或 UI。请求预算、confidence／日语密度 gate、detector bbox ownership、crop／方向、翻译、渲染和非图片路径不变。新增 `scripts/test-v3276-koharu-line-owner-boundary-contract.py`，工程版本为 `3.276`。本地 271 个不具备进程启动入口的 Python 合同、298 个 Python 合同 AST、YAML／shell 语法、工程 plist 与 `git diff --check` 通过，27 个可能启动外部进程的合同按约束跳过；严格未运行 `xcodebuild`、`swiftc`、`xcrun`、Rust/Cargo、Core ML 或 runtime evaluator。最终候选 SHA `291f5dfe99915d8a3ed46f3b8dec3c3e8acfd99f` 的 exact-SHA full [31621643593](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31621643593) attempt 2 成功，Koharu parity job `94206481482`、macOS job `94208330602`、云端 owner evaluator、Xcode/simulator build、manga probe、JUnit `10/10` 与 receipt 全部通过；parity 为 7/7 accepted，`ニコッ` confidence `0.59093773`、`今度こそ` `0.99994445`。PR #357 fast [31626589820](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31626589820) 成功，并以 merge SHA `0884de0d1e50c72a709c516c3e0fc9da2c62caf6` 合入 `smalldata_test`；合入后 push CI [31626815282](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31626815282) 复用候选 full receipt 成功。GPL MIT48 权重/runtime 仍只在云端 parity 使用，不打包进 AITRANS、不外推固定样图为通用日语 OCR／翻译提升；main 未动，研发分支最终删除后本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.275 继续修复固定 `test/jap.jpg` 的 Koharu MIT48 左列竖排 crop：审计发现旧 `left-column-c` `(405,86)-(491,448)` 截断首个黑字连通组件，改为按实测 envelope 使用 `(388,86)-(452,448)`；云端质量 gate 提升为 7/7 accepted，并要求该 crop 输出 `今度こそ`、confidence `>=.55`，compact `ニコッ` 仍要求 confidence `>=.55`。历史 v3.270–v3.272 合同已同步当前 gate。MIT48 GPL 权重/runtime 仍只在云端 parity 使用，不打包进 AITRANS、不替换 bundled Apache Manga OCR、不外推固定样图为通用日语 OCR／翻译／识别质量提升。本地只做 Python 静态合同与 `git diff --check`，不要本地编译。最终 SHA `161d83a928cd4f3aa5d2092c70adfeab28340040` 的 exact-SHA full [31601245479](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31601245479) 成功，Koharu parity job `94128921614`、macOS job `94130819057`、云端 Xcode/simulator build、manga probe、UI 合同、JUnit `10/10` 与 receipt 全部通过。PR #356 已合入 `smalldata_test`，merge SHA `b5e5638d`；研发分支已删除，main 不动，本地／origin 最终仅保留 `main` 与 `smalldata_test`。

v3.274 修复 v3.273 后图片 OCR 复查阈值文案漂移：`ImageOCRReviewPresentation.lowConfidencePercent` 直接读取 `ImageOCRResultSummary.lowConfidenceThreshold`，结果筛选 VoiceOver、低置信空态、修正页警告不再硬编码“低于 50%”；OCR／Koharu crop、geometry、ownership、方向、翻译、布局和非图片路径不变。新增 `scripts/test-v3274-image-ocr-review-threshold-copy-contract.py`，工程版本为 `3.274`。本地项目未运行 `xcodebuild`、app Swift、Rust／Cargo、Core ML 或 runtime 编译；一次误触发的历史 `xcrun swiftc` evaluator 已立即中断且未完成，后续跳过所有 compile-bearing 合同。新合同、静态检查与 full 云端验证均通过；精确 full [31594790350](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31594790350) 对实现 SHA `d5d3c9440840d0bce898d209210be4778abd273f` 的 Koharu parity、云端 Xcode/JUnit/UI/Home/Paste/Speech 与 receipt 全部成功；PR #355 已以 merge SHA `36a2efb8500ac0262251906137ea1183fa5a3119` 合入 `smalldata_test`。GPL MIT48 权重/runtime 仍只在云端 parity 使用，不打包进 AITRANS；研发分支已删除，本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.270 继续把 Koharu MIT48 parity 从“能编译”推进到可验证的竖排质量边界：云端 smoke 现按 Koharu image-rs `rotate270` 的等价 Pillow `rotate(90)` 生成横向 line canvas；依据 `test/jap.jpg` 黑字连通组件收紧 compact `ニコッ` crop 到 `(532,1050)-(578,1142)`，并把质量 gate 固定为 7 个 crop 至少 6 个通过日语密度 `>=.5`、compact 必须输出 `ニコッ` 且 confidence `>=.55`。最终云端 evidence 为 `ニコッ` confidence `0.590938`、6/7 accepted，主列稳定读出 `持ち帰る！`、`この爆乳を`、`前は生意気に`、`俺の誘い断り`；仍只证明 pinned GPL Koharu reference 的云端 crop parity，不把 GPL MIT48 权重/runtime 打进 AITRANS，不替换 bundled Apache Manga OCR，也不外推固定样图为通用日语 OCR／翻译／识别质量提升。新增 `scripts/test-v3270-koharu-mit48-cloud-orientation-quality-contract.py`，工程版本为 `3.270`。本地严格遵守“不要本地编译”，只做 Python 合同、YAML／shell 语法与 `git diff --check`；exact-SHA full [31578984648](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31578984648) 对实现 SHA `56473a2803dcfc94d9b198409e2dca4df4bcb3e4` 的 Cloud Koharu parity、云端 Xcode/JUnit/UI/Home/Paste/Speech 与 receipt 全部成功；PR #351 合入 `smalldata_test` 的 merge SHA `7651dacb2a8cc4e768f57fefdc0e2c38c0376a56`，合入后 push CI [31580218296](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31580218296) 通过。研发分支已删除，本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.269 把真实 Koharu MIT48 reference parity 建成云端可执行边界：固定 Hugging Face `mayocream/mit48px-ocr` revision `205395b155a041b068fd754a6e417cd71b4cb1de`，校验 `config.json`、`alphabet-all-v7.txt`、`model.safetensors` 与 README/license（权重 263375432 bytes、词典 186651 bytes、config 334 bytes）；云端 checkout `mayocream/koharu` revision `35f3e6d1a418d9617fd922e2bc865fe5b8fff818`，显式使用 Koharu `.cargo/config.toml` 的 `LLAMA_CPP_TAG=b8935`，在 Ubuntu 24.04 构建 `koharu-ml/bin/mit48px-ocr`，对 `test/jap.jpg` 固定竖排 crop + rotate270 做 CPU smoke，并要求非空日语脚本密度 `>=.5`。MIT48 GPL artifact、Koharu GPL-3.0-only 源码与 runtime 只存在云端 parity，不打包进 AITRANS、不替换现有 bundled Apache Manga OCR，也不把固定样图外推为通用日语 OCR／翻译／识别质量提升。图片单块 scoped rerecognition cancel 现在还会同步 `dataTransferMessage`、failure generation 并 `persist()`，取消后保留原 block 状态、其它译文与复查进度。新增 `scripts/validate-koharu-mit48px-artifact.py`、`scripts/run-koharu-mit48px-cloud-smoke.sh`、v3.269 云端 parity 与 scoped-cancel 合同、手动 parity workflow，工程版本为 `3.269`。本地只做 Python 合同、YAML／shell 语法与 `git diff --check`，未运行本地 `xcodebuild`、`swiftc`、Rust、Core ML 或 runtime 编译；精确 SHA full [31573726047](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31573726047) 对实现 SHA `031e1c98b198a0aadf3e78c5448701ba964d4a01` 的 Cloud Koharu MIT48 parity job 与 full validation 均成功，PR #350 合入 `smalldata_test` 的 merge SHA `e827e53252bad8b8e9a9e2190d619abdc52ea9c0`，合入后 push CI [31575090711](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31575090711) 通过。研发分支已删除，本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.268 继续把 Koharu `TextRegion -> line crop -> OCR` 的可验证边界推进到 geometry-only recall：在已有日语竖排 layout block 内，Vision `characterBoxes` 形成的候选必须至少 2 个字符、有有效 quad、严格竖排且只对应一个日语竖排 block，通过覆盖／面积／尺寸／重复 geometry 门控后，最多占用既有 8 个 line OCR 请求中的 2 个保留位，调用现有 bundled `MangaOCRService` 的 bounded line crop 与 rotate270。输出仍须经过清洗、confidence `>=.55` 和日语脚本密度 `>=.5`，是 recognition-only supplement，不生成空文本、不夺取可靠 detector bbox owner；既有 Vision fallback、取消、预算、布局、翻译、渲染和非图片路径不变。新增 `scripts/test-v3268-koharu-geometry-only-line-recall-contract.py`，工程版本为 `3.268`。本地只做相关 Python 合同、YAML 解析与 `git diff --check`，未运行本地 `xcodebuild`、`swiftc` 或 runtime 编译；exact-SHA full [31570211484](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31570211484) 对实现 SHA `e64d5b19f31b999d9c3232c4ccb731b24e988a32` 的云端 Xcode/JUnit/UI/Home/Paste/Speech 全部通过；PR #349 合入 `smalldata_test` 的 merge SHA `6f02c217119afaed13d7bcb93df44bbd7052a50e`，合入后 push CI [31571032333](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31571032333) 通过。该版本不声称 GPL MIT48px/PaddleOCR-VL artifact 已迁移，不外推固定 `test/jap.jpg` 为通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.267 继续把 Koharu `TextRegion -> line crop -> OCR` 的可验证边界迁入图片日语路径：Vision 仅对已有且严格判定为竖排 line 的 geometry 候选，调用现有 bundled `MangaOCRService` 的 line-specific bounded crop 与 rotate270；最多 8 个受控请求，结果必须经过清洗、有限 confidence `>=.55` 和日语脚本密度 `>=.5`，作为 recognition-only supplement，不夺取可靠 detector bbox owner，既有 Vision perspective／axis fallback、预算、布局、翻译、渲染和非图片路径不变。图片单块翻译重试新增 scoped cancel：命令栏、结果行、局部预览和 VoiceOver 都能取消当前 block，恢复该 block 的终态并保留其它译文与复查进度。新增 `scripts/test-v3267-koharu-manga-ocr-line-region-contract.py` 与 `scripts/test-v3267-image-translation-block-retry-cancel-contract.py`，工程版本为 `3.267`。本地 `288` 个 `scripts/test-v*.py` 合同、`test/jap.jpg` 单页 5 blocks／长页 20 blocks／compact `ニコッ` runtime、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；exact-SHA full [31567998753](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31567998753) 对实现 SHA `6d582e931241440f3d19b5ddadb0be468816de0c` 使用 Xcode/JUnit/UI/Home/Paste/Speech 全部通过，发布 `AITRANS CI/full-validation=success`；PR #348 合入 `smalldata_test` 的 merge SHA `891452ef662f1725d64394f02ec26979dc1cbf78`，合入后 push CI [31568653502](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31568653502) 通过。该版本是受控 line-crop 复读与复查操作改进，不声称真实 Koharu MIT48px/PaddleOCR-VL artifact 已迁移，也不外推固定样图为通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。研发分支已删除，本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.266 继续把 Koharu 的 vertical `warp_line_region` 边界推进到 bundled Manga OCR：严格 vertical quad 现在 direct-first 使用 bounded target canvas、canonical DeviceRGB projective bilinear sampler 与 rotate270，direct geometry／采样失败时才回到 Core Image natural projection；detector bbox 仍为主路径，quad 仍只在弱 bbox 结果时作 fallback。Vision 90°/270° 旋转侦察只在日语脚本密度 `>=.5` 且映射 geometry 高宽比 `>=1.05` 时携带 `.vertical` source provenance，避免横排字幕继承错误方向。图片结果行与局部预览在单块重识别进行中显示“取消重新识别此文字块”，只调用既有 scoped block task cancel，保留其它 OCR、译文与复查进度；新增 `scripts/test-v3266-image-ocr-inline-rerecognition-cancel-contract.py`，工程版本为 `3.266`。本地 `286` 个 `scripts/test-v*.py` 合同已通过；exact-SHA full [31564844704](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31564844704) 对实现 SHA `824e62bd4052a35ad38a50ce3d73ab295de8296c` 使用 Xcode 与 JUnit 全部通过，发布 `AITRANS CI/full-validation=success`；PR #347 合入 `smalldata_test` 的 merge SHA `b53fe37390a56bf76b62aeeb0927745178cd79ad`，合入后 push CI [31565649871](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31565649871) 重跑成功。该版本只收紧 Koharu crop／方向 provenance 与单块操作边界，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。研发分支已删除，本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.265 继续对齐 Koharu `warp_line_region` 的垂直 line quad 采样边界：Vision 日语 `perspectiveCorrectedLineImage` 现在复用 `MangaOCRService` 的 bounded target-canvas geometry 与 image-rs `warp_into(..., Bilinear)` 等价的 canonical DeviceRGB projective sampler，成功后使用 Vision 自身 `rotatedImage(..., angle: 270)`；不再把 Core Image natural projection + Core Graphics `.high` resize 作为正常 Vision vertical line 路径。direct geometry／采样失败时保留自然投影兼容 fallback；detector bbox ownership、line overlap、12/24 candidate 与 16M perspective pixel budget、OCR／翻译／布局／渲染和非图片路径不变。新增 `scripts/test-v3265-koharu-vision-vertical-quad-warp-contract.py` 与 runtime，工程版本为 `3.265`；共享 helper 的历史 v3.241/v3.264 合同已接受内部可见性。 本地 `285` 个 `scripts/test-v*.py` 合同、`test/jap.jpg` 单页 5 blocks／长页 20 blocks／quad fallback／方向 crop／compact `ニコッ`／nearest／luma／triangle runtime、确定性 checksum `e0288a1c4c38d1f8`、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；exact-SHA full [31562167103](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31562167103) 对 SHA `54dec1ccc92de16bff907a4e45f85857a3287ad5` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`；PR #345 合入 `smalldata_test` 的 merge SHA `1ea7910692a556037d94df1549f1b2de1f9da571`，合入后 push CI `31562843157` 按当前流程运行中。该版本只改善 Vision vertical line crop 的 Koharu 像素采样一致性，不声称日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。代码研发分支已删除，receipt 合入后本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.264 对齐 Koharu `warp_line_region` 的垂直 line polygon 采样边界：quad 目标 canvas 仍由 `quad_axis_lengths` 的长短轴和 4M/4096 bounds 决定，但 vertical quad 不再先用 Core Image 自然投影再走 Core Graphics `.high` 二次缩放；现在把 canonical DeviceRGB crop 直接映射到目标 canvas，使用 image-rs `warp_into(..., Interpolation::Bilinear)` 等价的 floor 四邻域、常量黑边采样，再 rotate270。detector bbox 仍为 Manga OCR 主路径，quad 仍只在弱 bbox 结果时作 fallback；无效 quad、转换失败、generic/non-vertical quad 保留自然投影 fallback。新增 `scripts/fixtures/v3264-koharu-vertical-quad-warp-harness.swift`、`scripts/test-v3264-koharu-vertical-quad-warp-contract.py` 与 runtime，工程版本为 `3.264`。本地 `284` 个 `scripts/test-v*.py` 合同、确定性 4×4→5×7 checksum `e0288a1c4c38d1f8`、既有 `test/jap.jpg` quad fallback／方向 crop runtime、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；exact-SHA full [31559883595](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31559883595) 对 SHA `a8816fd2a11a64317625f0970c9eeace7ea72d1f` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`；PR #343 合入 `smalldata_test` 的 merge SHA `d19f0c9e715d0856f80c4d2a906caa6eb5a193d5`，合入后 push CI [31560465371](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31560465371) 通过。该版本只改善 Koharu 垂直 quad crop 的像素采样一致性，不声称日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。代码研发分支已删除，receipt 合入后本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.263 修复图片 OCR 单块重新识别的取消边界：单块进行中时，命令栏显示“取消重新识别”，只调用 scoped block task 的 cancel，不再复用整图 `cancelImageTranslation()`；Store 保留图片 task ID、已完成译文、reviewed block IDs 与 translated session，CancellationError 仍由当前 request/content guard 接管，恢复 `previousState`、清理 scoped task、写入取消消息并递增 failure generation，旧 block／OCR／翻译／geometry／布局／导出与非图片路径不变。状态 VoiceOver 明确说明不会取消整图或清除复查进度；全局读取、整图 OCR／翻译和换图取消路径保持原有清理语义。新增 `scripts/test-v3263-image-ocr-scoped-rerecognition-cancel-contract.py`，工程版本为 `3.263`。本地 `283` 个 `scripts/test-v*.py` 合同、scoped／历史 accessibility 合同、YAML、project plist、Python 语法与 `git diff --check` 通过；exact-SHA full [31557987897](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31557987897) 对 SHA `92b64f404eb3cf82ad033bfedfd59b4740ab367c` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`；PR #341 合入 `smalldata_test` 的 merge SHA `540f24fbc39a716ecac06fe11a01080c9421bb3b`，合入后 push CI [31558745662](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31558745662) 通过。该版本只改善图片复查取消操作和 VoiceOver 反馈，不声称日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。代码研发分支已删除，当前本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.262 对齐 Koharu comic-text detector 的 640×640 输入缩放：`ComicTextBubbleDetectorService` 先将源图规范化为 8-bit DeviceRGB，再用 image-rs 等价的 separable Triangle filter（pixel-centred sampling、下采样 support 扩展、边缘权重归一化、水平 pass 后统一 round）生成 RGB 平面，最后显式写入 Core ML 32BGRA；不再依赖 Core Graphics `.high` 插值。detector threshold、ownership bbox／quad、Manga OCR、Vision fallback、12／48 请求预算、取消传播、布局、翻译、渲染和非图片路径不变。新增 `scripts/fixtures/v3262-koharu-detector-triangle-harness.swift`、`scripts/test-v3262-koharu-detector-triangle-contract.py` 与 `scripts/test-v3262-koharu-detector-triangle-runtime.sh`，工程版本为 `3.262`；历史 v3.254 region diagnostic 只接受已实测的 Triangle 几何宿主值 `0.940558`、`0.940838`、`0.940873`。本地 `282` 个 `scripts/test-v*.py` 合同、Triangle checksum `e7e9d1fe4bb47c45`、单页／长页／方向 crop／region diagnostic／RGB luma Core ML runtime、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；exact-SHA full [31556290782](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31556290782) 对 SHA `2fd53c4a555b5fe2d41f82a9860b3e3e55c7dc61` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`；PR #339 合入 `smalldata_test` 的 merge SHA `c7517492a66b3b095bc6e12f8e50d8090d558328`，合入后 push CI [31557042896](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31557042896) 通过。固定 `test/jap.jpg` 仅验证预处理、detector geometry 与既有 OCR/runtime 回归（云端 detectorRegions=5、长页 20 blocks），不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 已在祖先链；代码研发分支已删除，当前本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.261 修复图片 OCR“恢复 Vision OCR”后的 VoiceOver 焦点来源：局部预览入口记录 `.preview`，结果行入口记录 `.row`；确认恢复成功后按来源回到局部预览或结果行。若恢复使 block 离开当前筛选，焦点回退到当前可见结果、复查完成态、筛选控件或空态；origin 仅为 View 状态，新图片 revision 和确认框关闭会清理。Store 的 Vision baseline、OCR、翻译、取消传播、generation、geometry、布局、导出与非图片路径不变。新增 `scripts/test-v3261-image-ocr-restore-focus-origin-contract.py`，更新等价历史 restore 合同，工程版本为 `3.261`。本地 `281` 个 `scripts/test-v*.py` 合同、单页／长页／方向 crop／region diagnostic／RGB luma Core ML runtime、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；exact-SHA full [31505154226](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31505154226) 对 SHA `c74205dbae3549b151ec536e9556c05b7a639207` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`；PR #337 合入 `smalldata_test` 的 merge SHA `1fa5ddaf2127946d817734da03565390515fa99a`，合入后 push CI [31506507801](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31506507801) 通过。该版本改善图片 OCR 复查的操作连续性，不声称日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 已在祖先链；代码研发分支已删除，receipt 合入后本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.260 对齐 Koharu Manga OCR 的 RGB→luma 预处理：输入先以 canonical 8-bit DeviceRGB 读取，再按 image-rs `grayscale()` 的整数 floor 公式 `(2126R + 7152G + 722B) / 10_000` 写入 luma，随后继续使用 224×224 nearest floor coordinate mapping。这样不再依赖 Core Image 的隐式 DeviceGray profile／rounding；Manga OCR decoder、batch／单 crop fallback、取消传播、detector bbox／quad、布局、翻译、渲染和非图片路径不变。新增 `scripts/test-v3260-koharu-manga-ocr-rgb-luma-contract.py` 与 `scripts/test-v3260-koharu-manga-ocr-rgb-luma-runtime.sh`，工程版本为 `3.260`。本地 `280` 个 `scripts/test-v*.py` 合同、RGB luma runtime、既有 nearest／单页／长页／方向 crop／region diagnostic Core ML runtime、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；exact-SHA full [31501763083](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31501763083) 对 SHA `a2963ff338a065a78112b451c37f3e1558f3e2a0` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`；PR #335 合入 `smalldata_test` 的 merge SHA `68657d18ca192cd52c793064203f519d8d5c770e`，合入后 push CI [31503137128](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31503137128) 通过。该版本只对齐输入灰度预处理，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 已在祖先链；研发分支已删除，本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.258 收紧图片日语 OCR 的 detector-owner 可靠性并修复重新识别后的 VoiceOver 过期焦点：RT-DETR `.comicTextBubble` 请求携带 detector confidence provenance，只有 Manga OCR 自身 confidence `>=.55`、日语脚本密度 `>=.5` 且 detector owner（如有）confidence `>=.55` 时才保留为可靠 owner；Vision supplement 不伪造 detector score，弱 owner 的高模型分数不能阻止更可靠的 Vision 候选接管。可靠 owner 的 bbox／quad crop、方向、12／48 请求预算、取消传播、OCR、布局、翻译、渲染与非图片路径不变。重新识别的 VoiceOver focus intent 在用户切换 block、筛选、预览、修正方向或 review 队列后立即失效，完成／失败 guard 只允许仍匹配的 block 回焦，不取消 OCR／翻译任务。补齐 CI v3.236 历史合同路由，新增 `scripts/test-v3258-image-japanese-detector-confidence-gate-contract.py`，工程版本为 `3.258`。本地 `278` 个 `scripts/test-v*.py` 合同、YAML、project plist、Python 语法与 `git diff --check` 通过；真实 Core ML `test/jap.jpg` 单页仍为 5 个 vertical blocks，长页为 20 个 vertical blocks、4 个 compact `ニコッ`、底部 `y=0.940558`。exact-SHA full [31494116765](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31494116765) 对 SHA `3c4d3c172c59c3b40d202a476f67899ca03598f6` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #331 已合入 `smalldata_test`，merge SHA `9c38c9941a1ef1d1da3399ada4d81a47d82a2693`，合入后 push CI [31495336273](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31495336273) 通过。该版本只改善弱 detector owner 防护与焦点安全，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.257 修复图片 OCR 修正页改文字方向后的 VoiceOver 焦点来源：从结果行进入修正页继续记住 `.row`，从局部预览进入继续记住 `.preview`；修正页方向 Picker 延迟焦点现在消费该来源，取消／关闭且 block 仍在当前筛选时分别回到原结果行或局部预览；若方向覆盖让 block 离开当前筛选，仍由 v3.256 的安全 fallback 聚焦可见结果行、完成态、筛选控件或空态。新 revision、保存成功、忽略 block 与 sheet dismiss 清理 view-only origin；方向覆盖仍只更新显示、筛选和导出，不重跑 OCR、翻译或整图请求。OCR、Store、取消传播、generation、geometry、布局、渲染和非图片路径不变。新增 `scripts/test-v3257-image-ocr-correction-direction-focus-origin-contract.py`，工程版本为 `3.257`。本地 `277` 个 `scripts/test-v*.py` 合同、YAML、project plist、Python 语法与 `git diff --check` 通过；没有发现可由现有 `test/jap.jpg` fixture 证明的额外 OCR 质量提升，因此不改预处理或模型 artifact。exact-SHA full [31491372691](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31491372691) 对 SHA `f4e1a5d31090ed72b6d3e2fc29ac89468d3a40b9` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #329 已合入 `smalldata_test`，merge SHA `26c0de278c3ca298afae209025a073717f906ec8`，合入后 push CI [31492342250](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31492342250) 通过。该版本只改善修正页方向变更后的操作可发现性与焦点连续性，不声称日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.256 修复图片 OCR 复查方向筛选变化后的 VoiceOver 焦点边界：校正页的自动／横排／竖排覆盖继续只更新当前 block 的显示、筛选和导出方向，不重跑 OCR、翻译或整页请求；若覆盖让 block 离开当前筛选，立即清理隐藏 selection，并把焦点安全移到当前可见结果行、完成态、筛选控件或空态；校正 sheet 延迟焦点也会拒绝过期的隐藏 row／preview ID。OCR、Store、翻译、取消、generation、geometry、布局、渲染和非图片路径不变。CI 变更路径同时覆盖 MangaOCR service 与资源，避免 OCR/UI runtime 合同被漏检。新增 `scripts/test-v3256-image-review-direction-filter-focus-contract.py`，工程版本为 `3.256`。本地 `276` 个 `scripts/test-v*.py` 合同、YAML、project plist、Python 语法与 `git diff --check` 通过；固定 `test/jap.jpg` 既有真实 Core ML runtime 保持单页 5 个 vertical blocks，长页 20 个 vertical blocks、4 个 compact `ニコッ`、底部 `y=0.940558`。exact-SHA full [31488626549](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31488626549) 对 SHA `97db12152b6d8b4c5fd55305439f01558b7044c9` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #327 已合入 `smalldata_test`，merge SHA `5c6651b45f72eddd81507e819d889bc5b21365ed`，合入后 push CI [31489553054](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31489553054) 通过。该版本只改善筛选变化后的操作可发现性与焦点安全，不声称日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.255 修复 bundled Manga OCR 可变长度 batch decoder 的位置对齐：Core ML decoder 每轮只返回各行最终 sequence position 的 logits，已结束的行现在继续追加 EOS 占位，使活跃行不会因右侧 `[SEP]` padding 取错 logits；保留 Koharu `0..<300` token／EOS 语义、最多 4 crop batch、单 crop fallback、取消传播、请求预算、detector bbox／quad、布局、翻译与非日语路径不变。新增 `scripts/test-v3255-image-japanese-manga-ocr-batch-eos-alignment-contract.py` 并接入 CI，工程版本为 `3.255`。本地 `275` 个 `scripts/test-v*.py` 合同、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；`test/jap.jpg` 单页真实 Core ML batch runtime 为 5 个 vertical blocks，长页为 20 个 vertical blocks、4 个 compact `ニコッ`、底部 `y=0.940558`。exact-SHA full [31484005038](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31484005038) 对 SHA `276527069366433ed4adccf12cc92f29d6e09b92` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #325 已合入 `smalldata_test`，merge `1091a98ca2ec970497b0304fd2b41b777f62f3fc`，合入后 push CI [31484915985](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31484915985) 通过。该版本只修复 batch 解码执行边界，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.254 继续向 Koharu 的 `TextRegion -> crop -> Manga OCR` 迁移：在保留 detector bbox-primary、quad fallback、取消传播与 12／48 请求预算的前提下，长页仅对短而紧的日语候选（2–4 个 scalar、宽高各 `<=.08`）启用受限 compact recovery；候选必须通过有限 detector confidence、Vision `.crop`／`.verticalLine` 严格重叠、confidence `>=.40`、日语脚本密度 `>=.5`、日语字符数与邻域几何门控，弱 detector owner 才允许让位，可靠 owner 不变。新增诊断 harness／contract，工程版本为 `3.254`。本地 274 个 `scripts/test-v*.py` 合同、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；`test/jap.jpg` 单页继续为 5 个 vertical blocks，长页诊断为 20 个 vertical blocks、4 个 compact vertical blocks、底部 `y=0.940558`，本地四个 compact read 为 `ニコッ`；高图宽比环境的 OCR 文本允许 host variant，仍严格校验 ownership、几何、方向 provenance 与可靠 baseline，不把固定样图外推为通用日语 OCR／翻译质量。exact-SHA full [31478786162](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31478786162) 对 SHA `622c6112f9833bd657bdcedfc855f5bfe99fd946` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #323 已合入 `smalldata_test`，merge `046c6339a126fd800105e02e939b3a192fa83a48`，合入后 push CI [31479548671](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31479548671) 通过。Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.253 改善图片 OCR 单块重新识别的 VoiceOver 可发现性：结果行只有在 `canRerecognize && !isRerecognizing` 时才提示“重新识别此文字块”，局部预览的所有 geometry 分支统一追加相同的可执行／进行中提示；`恢复 Vision OCR` 仅在人工修正且 `canEdit` 时提示，锁定状态不误报。Store、OCR、翻译、取消传播、generation、焦点回跳、布局、渲染与非图片路径不变。新增 `scripts/test-v3253-image-ocr-rerecognition-accessibility-hint-contract.py`，并让 v3.3138/v3.368 历史合同接受等价 helper 结构，工程版本为 `3.253`。本地 `273` 个 `scripts/test-v*.py` 合同、YAML、project plist、Python 语法与 `git diff --check` 通过；exact-SHA full [31471779981](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31471779981) 对 SHA `b091f248f03ca22049acd78bd358fe58b01e4bb2` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #321 [31471794330](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31471794330) 通过并以 merge `bcfe1af6835ea313d79550e2f01019074e5d85b7` 合入 `smalldata_test`，合入后 push CI [31472763286](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31472763286) 通过。该版本只改善操作可发现性，不声称日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.252 收紧固定样图中短而弱的日语 detector owner：保留 Koharu bbox-primary 默认，只有 detector-owned 小区域（有限 confidence `<0.80`、宽高各 `<=.08`、最多 4 个 scalar）同时被严格重叠的 Vision `.crop`／`.verticalLine` 日语复读以 `>=.40` confidence、脚本密度 `>=.5`、更多日语字母和有限邻域几何覆盖时，才移除该弱 owner，让紧 crop 恢复 `ニコッ`，不改变可靠 owner。detector bbox／quad fallback、source direction provenance、12／48 请求预算、取消传播、OCR、布局、翻译、渲染与非日语路径不变。新增 `scripts/test-v3252-image-japanese-compact-crop-recovery-contract.py`，工程版本为 `3.252`。本地 `272` 个 `scripts/test-v*.py` 合同、YAML、project plist、Python 语法与 `git diff --check` 通过；固定 `test/jap.jpg` 单页仍为 5 个 vertical blocks，但紧凑块由 `こっ、` 恢复为 `ニコッ`，长页仍为 17 blocks／16 vertical／底部 `y=0.940558`，横排 `ニコッ` 保留。exact-SHA full [31469872746](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31469872746) 对 SHA `7aad047d7338fde2190a6ebde3cfb625acf75254` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #319 [31469897145](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31469897145) 通过并以 merge `f81969291f9c05c582bba578f0f78bd005db0956` 合入 `smalldata_test`，合入后 push CI [31470555207](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31470555207) 通过。固定 `test/jap.jpg` 只作 compact crop／ownership／runtime 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.251 修复图片 OCR 单块重新识别成功后的辅助功能焦点上下文：图片预览入口记录 `.preview`，结果行入口记录 `.row`；replacement 成功且当前 revision/generation、block、终态仍有效时，VoiceOver 分别回到当前预览文字块或结果行。失败／取消继续聚焦图片翻译状态行；revision 变化清理旧 origin，过期请求、隐藏 block、旧图片、OCR、翻译、geometry、布局、渲染、取消传播和非图片路径不变。新增 `scripts/test-v3251-image-ocr-rerecognition-focus-origin-contract.py`，更新 v3.247 历史焦点合同，工程版本为 `3.251`。本地 `271` 个 `scripts/test-v*.py` 合同、YAML、project plist、Python 语法与 `git diff --check` 通过；exact-SHA full [31454922438](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31454922438) 对 SHA `7ff333b2702cd416e3e2179c67f45030fa9188a1` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #317 [31467723651](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31467723651) 通过并以 merge `1148ffa0dff8cc5403c8d70105c6cc008ac5a00c` 合入 `smalldata_test`，合入后 push CI [31467791183](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31467791183) 通过。固定 `test/jap.jpg` 只作既有 OCR/runtime/UI 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.250 修正 v3.249 对 Koharu `crop_text_block_bbox` role boundary 的误迁移：Koharu 只有 `ctd` 或非空 `line_polygons` 才扩展 TextRegion crop；RT-DETR `comic-text-bubble-detector` 没有这两类字段，因此 `.comicTextBubble` Manga OCR 现在直接使用原始 `region.rect`，不再把相邻竖列带入模型输入。Vision `.vision` supplement 继续使用 `cropRectHint ?? rect`、竖排 `.18/.12` font-relative padding 与相邻列 bisector；detector bbox ownership、既有 `sourceDirectionHint` provenance、quad fallback、12/48 请求预算、取消传播、OCR、布局、翻译、渲染和非日语路径不变。新增 `scripts/test-v3250-image-japanese-detector-role-boundary-contract.py` 并让 v3.215/v3.220/v3.231/v3.240/v3.249 历史合同接受 raw-bbox early return，工程版本为 `3.250`。本地 `270` 个 `scripts/test-v*.py` 合同、YAML、project plist、`git diff --check` 通过；单页真实 Core ML runtime 为 `5/5` vertical blocks，长页为 `17` blocks／`16` vertical／底部 `y=0.940558`，bbox-primary 与方向 crop runtime 通过。exact-SHA full [31453792838](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31453792838) 对 SHA `59c9b145da31ee4240895321aa9f18c665065147` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #315 [31454386498](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31454386498) 通过并以 merge `30163b918698cafc8d48ae6f1298cd5ade190afe` 合入 `smalldata_test`，合入后 push CI [31454423404](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31454423404) 通过。固定 `test/jap.jpg` 只作 detector crop／ownership／runtime/UI 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.249 对齐 Koharu `crop_text_block_bbox` 的 detector role 与方向感知 padding：bundled RT-DETR `.comicTextBubble` TextRegion 没有 `source_direction`，Manga OCR crop 使用 Koharu 横排默认 `pad_x=.12`／`pad_y=.18`；Vision `.vision` supplement 继续使用竖排 `.18`／`.12`，并保留 `cropRectHint ?? rect`、detector bbox ownership、相邻列 bisector、`sourceDirectionHint`、quad provenance 与 12／48 请求预算。方向感知 helper 的 normalized fallback 同步交换比例，整页 detector crop 默认仍为竖排；OCR、翻译、布局、渲染、取消传播和非日语路径不变。新增 `scripts/test-v3249-image-japanese-detector-role-padding-contract.py`，并让 v3.215/v3.240 历史合同接受等价 helper；工程版本为 `3.249`。本地定向合同、YAML、project plist 与 `git diff --check` 通过；exact-SHA full [31451224061](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31451224061) 对 SHA `ddea0a13e236e73b61e85e5b63ddae4cdc9ee6be` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #313 [31451898240](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31451898240) 通过并以 merge `ff6ed75d0a9f0b1370b8aaf7439e90e3efa8bb49` 合入 `smalldata_test`。固定 `test/jap.jpg` 只作 detector role／crop geometry／runtime/UI 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.248 修复图片 OCR 单块重新识别失败／取消后的辅助功能焦点边界：当前 request/content guard 通过且 scoped 失败或取消消息写入后，`TranslationSessionStore` 才递增独立 failure generation；`ImageTranslationPanel` 校验 revision、generation、无 active rerecognition 与终态，再把 VoiceOver 焦点移到图片翻译状态行，过期请求、旧图片与成功 replacement 不触发该路径。OCR block、翻译、方向、geometry、布局、渲染、取消传播和非图片路径不变。新增 `scripts/test-v3248-image-ocr-rerecognition-failure-focus-contract.py` 并接入 CI，工程版本为 `3.248`。本地全量合同、YAML、project plist 与 `git diff --check` 通过；exact-SHA full [31445579893](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31445579893) 对 SHA `7df0b57233a1ce2bdd5d0b573384ba8a7d3d3da2` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #311 [31446307417](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31446307417) 通过并以 merge `a7e5a6f4147b8316d21d8544e8e2b6720759b45c` 合入 `smalldata_test`。固定 `test/jap.jpg` 只作现有 OCR／UI 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。

v3.247 继续收紧 Koharu Manga OCR 后处理与图片 OCR 单块复查焦点：bundled Manga OCR 先去空白、归一化省略号、合并 `.`／`・`，再把 ASCII 标点统一为全角，因此 `...` 稳定输出 `．．．`；单块重新识别只有在 replacement 成功提交且图片进入终态后才递增独立 completion generation，复查页校验 revision／generation／block／state，仍可见时聚焦更新行，离开当前筛选时清理旧 selection 并聚焦下一行、完成态或空态。OCR、翻译、布局、渲染、取消、过期结果与非日语路径不变。新增 `scripts/test-v3247-image-ocr-rerecognition-review-focus-contract.py` 与 `scripts/test-v3247-image-japanese-manga-ocr-postprocess-contract.py` 并接入 CI，工程版本为 `3.247`。本地全量 `269` 个合同文件通过；单页真实 Core ML runtime 保持 `5` 个 vertical blocks，长页保持 `17` blocks／`16` vertical、底部 `y=0.940558`，省略号为全角；exact-SHA full [31442768415](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31442768415) 对 SHA `001eb996ea76cf17c9bd715ce20c66a9ec1f8a48` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`。固定 `test/jap.jpg` 只作后处理/runtime/UI 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。合入完成后本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.246 继续对齐 Koharu 的方向感知 crop padding：图片日语 scoped block retry 现在把 `effectiveSourceDirection` 传入共享的 font-relative crop envelope，横排使用 Koharu `pad_x=.12`／`pad_y=.18`，竖排与 unknown 保持 `.18`／`.12`；normalized fallback 同步交换方向比例，整页 detector crop 仍保持竖排默认。恢复 Vision OCR 时保留用户后来设置的 `sourceDirectionOverride`，避免恢复动作抹掉方向决定；模型、Vision fallback、取消、ownership、预算、整页 OCR、翻译、布局、渲染和非日语路径不变。新增 `scripts/test-v3246-image-japanese-directional-koharu-padding-contract.py`，扩展 v3.245 runtime harness 覆盖 horizontal override，并更新 v3.3160/v3.3175/v3.322 历史合同接受等价方向实现，工程版本为 `3.246`。本地全量 `267` 个合同文件通过；真实 Core ML runtime 的 vertical scoped crop 返回 `今度こそこの爆乳を持ち帰る！`、confidence `0.9994928`，horizontal override 返回同一 fixture、confidence `0.99932444`。exact-SHA full [31440504163](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31440504163) 对 SHA `b05b0ca03c5f2efa3c3cee1575a7c5e1eda4c456` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；随后通过 PR #307 合入 `smalldata_test`，merge SHA `18194a396893b0a837ed8e30e87979220c140a87`，PR fast [31441269747](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31441269747) 通过。固定 `test/jap.jpg` 只作方向／crop／runtime 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，当前本地／origin 仅保留 `main` 与 `smalldata_test`。

v3.245 让图片日语单块重新识别把当前 block 的 `effectiveSourceDirection` 传到 Manga OCR crop：竖排覆盖请求 `rotate270`，但 RT-DETR detector 的宽 ownership bbox 只有在 crop 高度超过宽度 `1.75` 倍、明显像 Koharu line crop 时才旋转，宽 detector crop 保持 natural 输入以避免破坏已有识别；日语继续复用 font-relative crop padding，模型／Vision fallback、取消、ownership、整页 OCR、翻译、布局、渲染和非日语路径不变。新增 `scripts/test-v3245-image-japanese-directional-manga-ocr-crop-contract.py` 与真实 Core ML runtime harness 并接入 CI，工程版本为 `3.245`。旧诊断 run [31435877273](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31435877273) 暴露 harness 字符串编译边界后，修复提交 `ec80a3efaa5087608ab064acfed022af7609c5ff` 的 exact-SHA full [31437080333](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31437080333) 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；随后通过 PR #305 合入 `smalldata_test`，merge SHA `9f3d6784645902afe8c3ee90998a23c798af96b9`，PR fast [31437769765](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31437769765) 通过。固定 `test/jap.jpg` 只作方向／crop／runtime 回归，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，当前本地／origin 只保留 `main` 与 `smalldata_test`，无待 cherry-pick。

v3.244 让图片日语单块重新识别真正消费当前 block 的 `effectiveSourceDirection`：用户设为竖排时 scoped crop retry 优先 `[270, 90]`，设为横排时只用 `[0]`，自动方向继续 `[270, 90, 0]`；覆盖仍只作为 provenance/display override，整页 OCR、非日语路径、翻译、布局、渲染、取消和旧结果边界不变。同步 v3.214/v3.218/v3.238/v3.239 的 standalone Core ML harness，使其精简 `ImageTranslationBlock` 与生产模型共享 `sourceDirectionOverride`／`effectiveSourceDirection` 契约。新增 `scripts/test-v3244-image-japanese-direction-override-rerecognition-contract.py` 并接入 CI，工程版本为 `3.244`。首轮 exact-SHA full [31428789542](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31428789542) 暴露旧 harness 编译边界后，修复提交 `1859d8a3615b502e67fd3a8abc93590f96ae250f` 的 exact-SHA full [31429739511](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31429739511) 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；随后通过 PR #303 合入 `smalldata_test`，merge SHA `d38ac11aff9afae1aba6a67cef856cb547bd1e26`，PR fast [31430655623](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31430655623) 通过。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 仍在祖先链；当前本地／origin 只保留 `main` 与 `smalldata_test`，无待 cherry-pick。固定 `test/jap.jpg` 只作方向／布局／runtime 回归，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.243 为图片 OCR 结果补充用户可控的来源文字方向覆盖：`ImageOCRBlock` 保留 detector／OCR 原始 `sourceDirection`，新增可选 `sourceDirectionOverride` 与派生 `effectiveSourceDirection`；复查页、结果行和局部预览提供自动／横排／竖排选择，摘要、筛选、竖排渲染、PNG 导出与 VoiceOver 均消费 effective direction。覆盖只更新当前 block 的方向与导出／显示结果，不重跑 OCR、翻译、布局或整页请求；重新识别、忽略／恢复和旧 block 结果继续保留覆盖边界。新增 `scripts/test-v3243-image-japanese-direction-override-contract.py` 与 executable evaluator 并接入 CI，工程版本为 `3.243`。本地 v3.243 合同 `7/7` 通过；exact-SHA full [31426517160](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31426517160) 对 SHA `ef37c1a2c36cbb0792a933868ae4f70bf3af6149` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；随后通过 PR #301 合入 `smalldata_test`，merge SHA `8675a7dca6fbff27ed5bc1a8725cff967d5637e1`，PR fast [31427377302](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31427377302) 通过。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 仍在祖先链，当前本地／origin 仅保留 `main` 与 `smalldata_test`，无待 cherry-pick；固定 `test/jap.jpg` 只作方向／布局／渲染回归，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

# v3.242 同时继续对齐 Koharu VerticalRl 与 text-node crop 消费边界：竖排预览与 PNG 导出共享强调符号归并（`!!`/`??`/`!?`/`?!`，含全角输入）、全角标点分类与 Unicode vertical presentation glyph，并在 bounded cell 中居中全角标点；`.replace` 且来源为可信竖排并含 CJK 时才启用，Latin-only、`.adjacent`、横排来源与 OCR/layout/翻译边界不变。图片结果行和局部预览新增单块“重新识别此文字块”：沿用当前 block bbox，日语先走 bundled Manga OCR、失败回退 Vision crop，其它语言走 Vision crop；成功只替换原文／置信度并刷新该块翻译，保留 UUID、geometry、方向与顺序，失败／取消／过期结果保留原 block，不重跑整页 detector、聚类或布局。新增 `scripts/test-v3242-image-japanese-vertical-punctuation-contract.py`、`scripts/test-v3242-image-ocr-block-crop-retry-contract.py` 及 evaluator 并接入 CI，工程版本为 `3.242`。本地 v3.242 定向合同、竖排标点 evaluator 与真实 Manga OCR harness 通过；exact-SHA full [31422608636](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31422608636) 对 SHA `bf9cb74163c4fbccf5c92cfa5f47829ec8bdffa4` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；随后通过 PR #299 合入 `smalldata_test`，merge SHA `5702eaa8349a6d6f9d0e0dd7d10b1fa0aaf36e50`，PR fast [31423431910](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31423431910) 通过。固定 `test/jap.jpg` 只作既有 runtime／渲染回归，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

# AGENTS.md

v3.240 修复 v3.239 后真实产品请求路径仍可能把 Vision `cropRectHint` 当作 detector Manga OCR 基础 crop 的边界：`japaneseMangaOCRCropRect` 现在先按 detector role 选择 crop base，`.comicTextBubble` 始终使用 RT-DETR `region.rect`，只有 `.vision` supplement 才允许使用严格门控的 `region.cropRectHint ?? rect`；detector 的 `cropQuadHint` 继续交给 Manga OCR 作为弱 bbox 的内容回退。detector `textRect` ownership、bbox-first／quad-fallback、batch／单 crop 故障隔离、取消传播、12／48 请求预算、Vision fallback、布局、翻译、渲染和非日语路径不变。新增 `scripts/test-v3240-image-japanese-detector-bbox-crop-contract.py` 并接入 CI，工程版本为 `3.240`；本地 `240` 份合同、历史 detector bbox／quad fallback 合同、Python／shell 语法与 `git diff --check` 通过。exact-SHA full [31410644573](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31410644573) 对 SHA `22ff6b1491aba22b8233bd65811333591cbf74a1` 使用 Xcode `26.6 (17F113)` 完成，JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；随后通过 PR #295 合入 `smalldata_test`，merge SHA `62a6c8ca3ca33c6d5a328677dea1ad76fbb8540c`。固定 `test/jap.jpg` 只用于既有 crop／ownership 回归，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.239 按 Koharu 当前 `manga_ocr.rs` 的真实消费顺序，将扩展 detector TextRegion bbox 恢复为 Manga OCR 主 crop；严格 line quad 只在 bbox 结果缺失、非日文、日文脚本密度 `<0.5`、置信度非有限或 `<0.55` 时补跑，之后继续按可靠性等级、有限置信度与日文字符数择优。这样即使 line quad 错投到相邻日文列且模型给出高置信，也不能再把邻列文字归给当前 detector owner；bbox 弱时仍保留 quad 恢复能力。batch／单 crop 故障隔离、取消传播、detector ownership、12／48 请求预算、Vision fallback、布局、翻译、渲染和非日语路径不变。新增 `scripts/test-v3239-image-japanese-manga-ocr-bbox-primary-contract.py` 与真实 Core ML 相邻列错配 runtime 并接入 CI，工程版本为 `3.239`；本地新 runtime 中 target `今度こそこの爆乳を持ち帰る！` 置信度 `0.99948496`，distractor `前は生意気に俺の誘い断りやがって...` 置信度 `0.9421599`，错配 quad 后仍返回 target。exact-SHA full [31407753378](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31407753378) 对 SHA `6dc7388a4c1eece3cad4f6e7bc864e0edb245b64` 使用 Xcode `26.6 (17F113)` 完成真实 bbox-primary／bbox→quad fallback、单页／长页 runtime、static/UI/Speech/home/paste 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；云端 target 为 `0.99948496`，distractor 为 `0.94102454`，错配后仍返回 target，单页 5 个 vertical blocks，长页 16 blocks／16 vertical、底部 y=`0.940558`；本地长页保留一个非重叠横排 `ニコッ`，因此为 17 blocks／16 vertical。审计 Koharu 当前 `mayocream/manga-ocr` SHA `4380edba990b959c508752350955350c1c80c31c` 后确认其 `264/264` 个共有 tensor 与随包 `kha-white/manga-ocr-base` SHA `aa6573bd10b0d446cbf622e29c3e084914df9741` 完全相等，仅多 `position_ids` buffer，因此不做无识别收益的 200 MB 模型替换。固定 `test/jap.jpg` 只验证 ownership/crop 回归，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.238 补齐严格 line-quad Manga OCR 的内容质量回退：Koharu 的扩展 detector bbox 现在始终随 quad 首选 crop 保留；quad 推理为空、失败、非日文、日文脚本密度 `<0.5`、置信度非有限或 `<0.55` 时，只对该项批量补跑 bbox，并先按同一可靠性等级、再按有限置信度和日文字符数择优。可靠 quad 不增加推理，bbox-only 请求不重复运行；batch 错误继续逐 crop 隔离，`CancellationError` 与并发取消继续终止整个有界批次。detector ownership、12／48 请求预算、Vision fallback、布局、翻译、渲染和非日语路径不变。新增 `scripts/test-v3238-image-japanese-quad-bbox-fallback-contract.py` 与真实 Core ML blank-quad→detector-bbox runtime 并接入 CI，工程版本为 `3.238`。本地 `238` 份合同、fallback／单页／长页真实 Core ML runtime 通过；exact-SHA full [31403964016](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31403964016) 对 SHA `12b4f17c864019d88b3128373b4d393dd61ae628` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode build 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`。云端 blank quad 置信度 `0.17586856`、文本 `それは、`，bbox 回退以 `0.99948084` 恢复 `今度こそこの爆乳を持ち帰る！`；单页保持 5 个 vertical blocks，四页长图保持 16 个 vertical blocks、底部 y=`0.940558`。固定 `test/jap.jpg` 只用于回退路径和既有单页／长页回归，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.237 修复日语图片 OCR detector 取消传播边界：`recognizeJapaneseMangaOCR` 不再用 `try? await` 吞掉 RT-DETR detector 的 `CancellationError`；用户取消会终止当前有界 OCR 批次，普通 detector 加载／推理错误仍回退 Vision，而普通错误分支会再次检查任务取消状态，避免底层 Core ML 错误掩盖并发取消。detector ownership、Manga OCR crop／batch fallback、12／48 请求预算、Vision fallback、布局、翻译、渲染和非日语路径不变。新增 `scripts/test-v3237-image-japanese-detector-cancellation-contract.py` 并接入 CI，工程版本为 `3.237`；v3.216 历史 detector 合同同步接受新的取消安全 fallback。候选 exact-SHA full [31398074816](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31398074816) 对 SHA `c7c5fa7383b15c2c1167feccc63a56c33641e2a9` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode build 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。本地单页真实 Core ML runtime 保持 5 个 vertical blocks，四页长图保持 17 blocks／16 vertical／底部 y=`0.940558`。本版本只收紧取消与回退控制流，不声称固定 `test/jap.jpg` 的通用日语 OCR、翻译或识别质量提升。

v3.236 继续迁移 Koharu `llm.translate_texts` 的批量消费边界：日语图片批翻译按全局 `[N]` ID 建索引，允许本机小模型返回乱序、未知标签或部分有效块；已解析块直接保留，缺失块才逐块补译，完全无效输出仍回退既有逐块路径。Manga block cleaner 同样只要求至少一个已知且非空译文，不再因顺序／完整性差异丢弃整批。取消传播、批次 8 块／1800 字符边界、Koharu 漫画提示词、普通语言路径、OCR、布局、渲染和导出不变。新增 `scripts/test-v3236-image-japanese-koharu-tolerant-batch-translation-contract.py` 并接入 CI，工程版本为 `3.236`。候选 exact-SHA full [31392104569](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31392104569) 对 SHA `968a11c94a9d296ab08066bb9e84782c5ec13bcf` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode build 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。该版本只改善批翻译结果消费的容错与回退范围，不声称固定 `test/jap.jpg` 代表通用日语 OCR、翻译或识别质量提升。

v3.235 对齐 Koharu Manga OCR decoder 的 `max_length` 语义：单 crop 与 batch 两处从 `1..<maximumTokens` 改为 `0..<maximumTokens`，保留 300 上限、EOS 提前结束、取消传播、batch／单 crop fallback 与请求预算。新增 `scripts/test-v3235-image-japanese-manga-ocr-token-budget-contract.py` 并接入 CI，工程版本为 `3.235`。exact-SHA full [31386561223](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31386561223) 对 SHA `e96e621a3a9e57560b830d898b56d80a1c3f17a6` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode build 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`；Koharu artifact readiness 为 `manifestMissing / stopUntilArtifactsProvided`。固定样图只验证 token budget／执行回归，不声称通用日语 OCR、翻译或识别质量提升。v3.234 已通过 exact-SHA full [31384120344](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31384120344)（SHA `b67284f30f91fa23d48965f4633f91ce0e85d669`，Xcode 26.6、JUnit `10/10`、Xcode build success、probe skip），并通过 PR #289 合入 `smalldata_test`，merge SHA `31ce1c2f38db0bc3b25ffe4d52ec942497b0f423`。

v3.234 新增图片 OCR 复查面板的 `竖排` presentation filter：按 `sourceDirection == .vertical` 显示所有竖排文字块，包括高置信度块，便于逐列检查固定日语样图中的错字；`.needsReview` 仍只表示低置信／方向待定风险 union。筛选仅影响 inspector 列表与 VoiceOver 文案，预览、导出、Store、翻译、OCR、ownership 和布局不变。新增 `scripts/test-v3234-image-japanese-vertical-review-filter-contract.py` 及可执行 evaluator 并接入 CI，工程版本为 `3.234`；exact-SHA full [31384120344](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31384120344) 对 SHA `b67284f30f91fa23d48965f4633f91ce0e85d669` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode build 与 JUnit `10/10`（0 failures），probe 按配置为 `skip`。v3.233 已通过 PR #288 合入 `smalldata_test`，merge SHA `b0fca98d6f9e6b7df0459efdfaa31ee6c3e61ef0`。

v3.233 修复 v3.232 line quad 只取字符角点 median、导致 crop 退回 bbox 的几何缺口：Vision 字符框仍在旋转坐标中校验，改用所有有效 glyph 的外侧 `minX/maxX/minY/maxY` 构造覆盖整行的 `ImageOCRLayoutQuad`，再映射回原图并沿用 overlap `>=0.80`、detector coverage `>=0.55`、candidate coverage `>=0.80`、面积比 `0.35...1.05`、横向 coverage `>=0.45`、纵向 coverage `>=0.85` 与宽度至少收窄 `10%` 的严格门控；非法／不完整／门控失败继续回退 detector bbox。detector ownership、12／48 请求预算、batch／单 crop fallback、取消传播、Vision fallback、布局、翻译、渲染与非日语路径不变。新增 `scripts/test-v3233-image-japanese-line-quad-coverage-contract.py` 并接入 CI，工程版本为 `3.233`。固定 `test/jap.jpg` 只验证整行 quad geometry 与既有单页／长页 runtime 边界，不声称通用日语 OCR／翻译质量；exact-SHA full [31381929512](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31381929512) 对 SHA `fea4967b2dd94c7aff735c5c57139cbe66784fd6` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode build 与 JUnit `10/10`（0 failures），probe 按配置为 `skip`，Koharu artifact readiness 为 `manifestMissing / stopUntilArtifactsProvided`。v3.157/v3.158 merge 仍在祖先链，v3.233 已合入 `smalldata_test` merge SHA `b0fca98d6f9e6b7df0459efdfaa31ee6c3e61ef0`，候选分支已清理。

v3.232 继续迁移 Koharu `extract_text_block_regions` 的真实 line polygon 消费边界：Vision 字符框在严格 detector coverage／面积／纵向支持门控后，形成可选 `ImageOCRLayoutQuad`，随 detector-owned `JapanesePixelFirstRegion` 进入 `MangaOCRRequest.cropQuad`；bundled Manga OCR runtime 用 Core Image `CIPerspectiveCorrection` 做 bounded line crop，四边形缺失、非法、投影失败或模型异常都回退既有扩展 `cropRect`。`textRect` 仍由 RT-DETR `rect` 负责 layout／ownership，12／48 请求预算、batch／单 crop fallback、取消传播、Vision fallback、去重、布局、翻译、渲染与非日语路径不变。新增 `scripts/test-v3232-image-japanese-koharu-line-quad-manga-ocr-contract.py` 并接入 CI，工程版本为 `3.232`。本地 v3 合同共 `232` 份，单页 runtime 继续精确返回 5 个 vertical blocks，四页长页继续返回 17 blocks／16 vertical／底部 y 约 `0.940558`；exact-SHA full [31379487189](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31379487189) 对 SHA `f030b9582d1eadb45a3c73acdf8c24219b35ba20` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe 按配置为 `skip`。固定 `test/jap.jpg` 只证明 line crop geometry 与既有 runtime 边界，不声称通用日语 OCR／翻译质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。v3.157/v3.158 merge 仍在祖先链，v3.232 已合入 `smalldata_test`，merge SHA 为 `100a7807a5c86c1aceb8cbe30e74f6dd22074863`，本地／origin 仅保留 main 与 smalldata_test。

v3.231 继续沿 Koharu `TextRegion + line_polygon -> crop -> OCR` 收紧 detector-owned 日语竖排 crop：RT-DETR 的 `rect` 仍是布局与 ownership 几何，Vision 字符 envelope 只有在至少两个字符、竖排几何、overlap `>=0.80`、detector coverage `>=0.55`、candidate coverage `>=0.80`、面积比 `0.35...1.05`、横向 coverage `>=0.45`、纵向 coverage `>=0.85` 且宽度至少收窄 `10%` 时才作为 Manga OCR crop-only hint；严格门控失败时回退 detector bbox。hint 不改变 detector ownership、12／48 请求预算、模型失败／取消传播、Vision fallback、布局、翻译、渲染或非日语路径。新增 `scripts/test-v3231-image-japanese-detector-tight-crop-hint-contract.py` 并接入 CI，版本为 `3.231`。本地 `249` 份合同和单页／长页真实 Core ML runtime 通过；exact-SHA full [31372501552](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31372501552) 对 SHA `bb579e97f9ded42d7b0352dcccc5592194610e20` 使用 Xcode `26.6 (17F113)` 完成 UI/Speech/home/paste、JUnit `10/10` 与 `AITRANS CI/full-validation=success`，probe `skip`。固定 `test/jap.jpg` 只作 crop geometry 回归，不声称通用日语 OCR、翻译或识别质量；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 仍在祖先链，本地／origin 活动分支和 stale ref 已清理。

v3.230 修复 v3.229 云端真实 runtime 暴露的 harness 解析边界：单页输出中的 `batchInference=true` 与 `blocks=5` 元数据不再被当作 OCR block，解析器只消费 `horizontal|vertical|unknown` 制表符记录后再检查五个样例全部为 `vertical`。旧 run [31368735253](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31368735253) 已证明 batch 模型实际加载且五个结果均为 vertical，但旧 parser 因把 `blocks=5` 纳入 provenance 检查而错误失败；新增 `scripts/test-v3230-image-japanese-batch-runtime-parser-contract.py` 锁定修复。exact-SHA full [31370139122](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31370139122) 对 SHA `064810987125742b31ff0e5cc422d35b576a37f3` 完成单页／长页真实 batch runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`；四页长图为 16 个 vertical blocks，最底 y=`0.940558`。v3.157/v3.158 merge 仍在祖先链，本地／远端旧分支已清理。

v3.229 修复 v3.228 云端真实 runtime 暴露的 flexible-batch encoder 图错误：ViT CLS token 不再以动态 `tile(reps)` 扩展，改为动态 shape `fill` 广播；encoder 的 1…4 batch 使用 `EnumeratedShapes` 专化，decoder 保留动态 sequence 与 legacy 单 crop 回退。v3.228 云端 Xcode build 已通过，但 batch encoder runtime 因 `tile` shape inference 回退 Vision，run [31366707811](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31366707811) 的 UI contract 正确失败；新增 `scripts/test-v3229-image-japanese-batch-model-shape-contract.py` 锁定模型规格与失败边界，新的 Core ML runtime 尚待验证。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86`、v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 仍在祖先链，本地／远端旧分支已清理。

v3.228 将 v3.227 的 flexible-batch Manga OCR 从可选接口推进为实际随包资源：encoder／decoder 均为 1…4 flexible batch，runtime 仍保留单 crop 模型作为兼容回退；单页和长页 harness 会明确校验 batch 模型已加载，坏 batch 仍逐 crop 隔离回退。新增 `scripts/test-v3228-image-japanese-bundled-batch-runtime-contract.py`，云端 Core ML 编译与真实 runtime 尚待当前提交验证。该版本只证明执行路径和性能资源可用，不声称日语 OCR 准确率提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.227 继续向 Koharu 的真实 batch Manga OCR 迁移：`scripts/convert-manga-ocr-coreml.py` 支持上限为 4 的 flexible-batch encoder／decoder，Swift `MangaOCRRuntime` 仅在成对可选 batch 模型存在时启用，并在 batch 错误后逐 crop 回退；当前入库的 batch=1 模型与既有单页 12／长页 48 请求预算不变。`ImageTranslationVerticalText` 现在按行列容量主动追加省略号，和 PNG 导出一致；补入 Xcode 已引用的 `AITRANS/Resources/MangaOCR/conversion.json`。新增 `scripts/test-v3227-image-japanese-batch-preview-contract.py`，云端 full 尚待本版本提交后验证。该版本不声称 OCR 准确率提升，Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.226 收紧 Koharu detector TextRegion ownership 的质量边界：bundled Manga OCR 结果只有在 confidence 有限且 `>=0.55`、日文脚本密度 `>=0.5` 时才继续作为受保护 detector owner，弱结果仍保留为普通候选并允许 page-level Vision fallback 参与，避免低质量文本压制更可靠的页面识别。模型加载、单 crop 故障隔离、取消传播、长页预算、布局、翻译、渲染与非日语路径不变。新增 `scripts/test-v3226-image-japanese-manga-ocr-quality-gate-contract.py`，更新 v3.219/v3.221/v3.223 历史合同接受等价质量门；本地完整 `226` 份合同全部通过，真实单页／四页长图 Core ML runtime、`plutil`、workflow YAML 与 `git diff --check` 通过。候选 exact-SHA full [31361469086](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31361469086) 对 SHA `a2258f8de4205e7cbb99ca059bd30307ffba96b6` 使用 Xcode `26.6 (17F113)` 完成，JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`；probe 按配置为 `skip`。Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，固定 `test/jap.jpg` 只证明当前 ownership 回归，不外推通用日语 OCR、翻译或识别质量。

v3.225 将 Koharu `source_direction` 继续迁移到图片翻译渲染：`ImageTranslationBlock.prefersVerticalWriting` 只有在来源方向为 `.vertical` 且当前显示文本含 CJK 时才启用竖排；SwiftUI 覆盖使用右到左列、列内上到下的布局，旁贴模式保持水平信息卡。PNG 导出新增 `drawImageTranslationText`，CJK 译文使用 bounded VerticalRl 风格列绘制，英文与横排来源继续水平绘制，过长文本安全截断并追加省略号。

新增 `scripts/test-v3225-image-japanese-vertical-render-contract.py` 并接入 CI。固定 `test/jap.jpg` 单页继续返回 5 个 vertical blocks，四页长图继续返回 17 blocks、16 vertical，底部结果约 `y=0.940558`；这些固定 fixture 只验证方向 provenance 与渲染／布局回归，不外推通用日语 OCR、翻译或识别质量。云端 full [31359607372](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31359607372) 对 exact SHA `e2965b6a8b32b6a796402faeb395b43883f97b6a` 使用 Xcode `26.6 (17F113)` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`；UI screenshot evidence 与 probe 按配置为 `skip`。Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86`（PR #221）与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c`（PR #222）均在当前祖先链；两条候选分支已从本地与 `origin` 清理，无待 cherry-pick。

v3.224 继续沿 Koharu 的 detector→crop→OCR 分层修复 Manga OCR resilience：模型加载错误仍作为整批 Vision fallback，单个 detector crop 的解码／Core ML 输出错误只跳过该 crop 并继续消费其它 TextRegion；`CancellationError` 不被吞掉，用户取消会终止当前有界批次。普通日语链路仍先用 bundled Manga OCR，再保留 Vision line／block／tile fallback；非日语路径不变。图片翻译状态、空结果、完成态与 VoiceOver 文案改为准确描述本机 OCR，日语识别阶段明确显示 Manga OCR + Vision。新增 `scripts/test-v3224-image-japanese-manga-ocr-resilience-contract.py`，并更新 v3.214/v3.221 历史合同接受取消传播的等价调用。固定 `test/jap.jpg` 与四页长图 runtime 只用于回归现有 geometry／layout 边界，不外推通用日语 OCR、翻译或识别质量；Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。本地 v3.157+ 共 `216` 份合同、真实单页／长页 Core ML runtime、project/workflow 语法与 `git diff --check` 通过；云端 full [31357934638](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31357934638) 对 exact SHA `b868ac52ab91ed3450c48da18cbc75010ec55b09` 完成 Xcode、static/UI/Speech/home/paste 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`。

v3.223 分离 Koharu detector TextRegion 的 ownership provenance 与日语竖排方向提示：bundled Manga OCR 成功结果使用独立 `detectorTextRegion` role，同时保留 detector 没有 `source_direction` 时所需的 `.vertical` hint；detector-owned supplement 过滤改为依据 boundary provenance，不再依赖 `.verticalLine`，横排与竖排 layout 都阻止两个受保护 detector nodes 被通用 clustering 合并。模型失败、取消、Vision fallback、翻译、渲染、非日语路径与既有 detector ownership 边界不变。新增 `scripts/test-v3223-image-japanese-detector-direction-provenance-contract.py`，并更新 v3.214/v3.221 历史合同。单页真实 Core ML runtime 继续精确返回 5 个竖排块；四页 `1136x6400` 长页继续返回 17 blocks、16 vertical，底部覆盖保留，未重叠的 `ニコッ` 横排噪声保留。当前本地定向合同、真实 Core ML runtime、`plutil`、Python/YAML 语法与 `git diff --check` 通过；本机只有 CommandLineTools，云端 full [31356368877](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31356368877) 对 SHA `13864d12154b77f86eb177127d77fc1a25eed321` 使用 Xcode 26.6 完成，static/UI/Speech/home/paste 与 JUnit `10/10` 均通过并发布 `AITRANS CI/full-validation=success`，probe `skip`。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 仍在当前祖先链，本地／远端均无活动候选分支或待 cherry-pick；Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称通用日语 OCR、翻译或识别质量提升。

v3.222 修复图片翻译最后一个 scoped block retry 成功后的 VoiceOver 焦点边界：`TranslationSessionStore` 公开独立的 `imageTranslationBlockRetryCompletionGeneration`，只在剩余空译文块归零并进入 `.translated` 时递增；`ImageTranslationPanel` 监听该 generation，并在当前内容 `imageTranslationRevision`、generation 与终态仍匹配时复用既有结果／状态焦点。部分失败、取消、过期 retry、新图片、OCR、日语 `[N]` 批翻译、导出重绘和非图片路径不变。新增 `scripts/test-v3222-image-translation-block-retry-focus-contract.py`，旧 v3.221 合同改为接受后续版本。实现 `5fbccb16fd4db4489ac4bb054f0422ea0c591af9` 的 exact-SHA full [31354629914](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31354629914) 完成 Xcode、静态／UI／speech 合同与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`；本地定向历史／当前合同与语法检查通过。本版本只改善 retry 完成后的辅助功能焦点，不声称日语 OCR、翻译或识别质量提升，Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.221 对齐 Koharu 在 Manga OCR 后只保留 detector TextRegion text node 的消费边界：日语最终 layout 前，成功 bundled Manga OCR 的受保护 vertical detector region 会以最小面积覆盖 `>= 0.60` 作为 owner，移除与其重叠的 page／90°／270° Vision supplement；非重叠的真实横排音效继续保留。过滤只发生在日语最终融合，detector 模型失败或取消时 detector owner 为空，完整保留历史 Vision fallback，普通语言、crop／line／tile reread、布局、翻译、渲染与输出边界不变。`test/jap.jpg` 单页真实 runtime 继续精确返回 5 个竖排块；四页 `1136x6400` 长图本地输出 17 blocks／16 vertical，云端 Core ML runner 输出 16 blocks／16 vertical，长页 gate 只要求 `>=16` 且四个季度各至少 4 个 vertical。重叠的 `やがってま` 被拒绝，本地未重叠的 `ニコッ` 保留，四份 `爆乳` 与 `挨拶` 均通过。新增 `scripts/test-v3221-image-japanese-detector-owned-vision-noise-contract.py`，并把长页 runtime 提升为几何 ownership gate；固定重复样图只证明 detector-owned supplement 过滤路径改善，不外推为通用日语 OCR、翻译或识别质量，Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。已合入 `smalldata_test`（merge `159ef9f1d30f24842056d6d6a66c24098e03afcc`）；full [31353229779](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31353229779) 对实现 SHA `6ba4797feac679d5dbce19602e4b966bfec03e2e` 完成 Xcode 与 JUnit `10/10`（0 failures），`AITRANS CI/full-validation=success`，probe `skip`。

v3.220 修复 v3.219 长页先被全局 `ThumbnailMaxPixelSize=1800` 压缩、再进入 Koharu `ImageSlicer` 的分辨率边界：普通图片保持历史 1800px 上限，显示高宽比超过 `3.5` 的长页按显示宽度最多 `1800px` 与总像素最多 `16,000,000` 计算安全缩放，并保留 detector TextRegion 的完整 crop ownership，不让 Vision supplement bisector 截断专用区域。`test/jap.jpg` 四次拼接的 `1136x6400` 长页本地返回 `18` blocks、云端返回 `19` blocks，两者均为 `16` vertical／`0` unknown；四份 `爆乳` 与 `挨拶` 均通过，弱横排 Vision 噪声不作目标文字声明。普通单页继续精确返回 5 个历史块，detector-only 长页继续返回 17 regions。本地 v3.157-v3.220 共 `70` 份合同／`373` tests、三条真实 Core ML runtime 和 generic Simulator build 通过；exact-SHA full [31350115177](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31350115177) 对 SHA `51e779ac81ec1706c4a846dc44ab55ba0d14f161` 完成 Xcode 与 JUnit `10/10`（0 failures），full-validation status `success`，probe `skip`。v3.157 merge `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 与 v3.158 merge `c940815a43e300685667d8b01888e53af910ec9c` 仍在当前祖先链上，无活动分支或待 cherry-pick；Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

候选 metadata [31350695078](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31350695078) 对 docs SHA `f8cb059827fc639e0fe8cf340dc14f93ab15027a` 复用实现 full；PR #284 fast [31350751298](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31350751298) 与 merge fast [31350811748](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31350811748) 均复用成功 receipt，merge SHA `e878ada9ab11e747a361d482cefe61c9dfdebcdb`；后三者 JUnit `10/10`、Xcode skipped，不是新的编译证据，v3.220 功能分支已清理。

v3.219 修复 v3.218 长页 Manga OCR 已覆盖 4 份页面、layout 却仍把上一页尾 `...お願いします` 与下一页头 `前は生意気に...` 合并为同一竖排块的边界。bundled Manga OCR 结果现在携带 detector TextRegion boundary provenance；日语去重即使选择 Vision 文本也保留两侧 provenance 的 OR，最终 layout 只拒绝两个受保护 detector TextRegion 互相合并。Vision-only 行碎片继续使用历史同列 clustering，普通单页、detector、预算、crop ownership、模型失败／取消、Vision fallback、翻译、渲染与非日语路径不变。这直接对齐 Koharu 在 OCR 后仍把每个 detector TextRegion 保持为独立 text node 的消费语义。

新增 `scripts/test-v3219-image-japanese-detector-boundary-contract.py` 和可执行 Swift layout evaluator，并把完整产品长页 runtime 提升为边界 gate。4 份 `test/jap.jpg` 经真实 `VisionOCRService + RT-DETR + Manga OCR + layout` 本地返回 `18` blocks、云端返回 `17` blocks，两者均为 `16` vertical／`0` unknown，四个纵向季度各精确保留 4 个 detector OCR 块；4 份页头和页尾均独立存在，v3.218 曾出现 3 次的 `お願いします前は...` 串联归零。本地仍有 2 个、云端 1 个弱横排 Vision 噪声，并保留 `撮乳`／`城乳`／`授乳` 等识别错字；单页 runtime 继续精确返回 5 个历史块，detector-only 长页仍为 17 regions。本地 v3.157-v3.219 共 `63` 份合同／`328` tests、三条真实 Core ML runtime、generic Simulator Xcode build、project/YAML/shell/Python 语法和 `git diff --check` 通过；exact-SHA full [31348116248](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31348116248) 对 SHA `f90f48a6a98c8d4d3ca722f9d1abf993bae633a1` 完成三条真实 runtime、Xcode 与 JUnit `10/10`（0 failures），full-validation status `success`，probe `skip`。重复固定 fixture 只证明 detector boundary layout 路径改善，不外推为通用日语 OCR 或翻译质量；Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

候选 metadata [31348550765](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31348550765) 对 docs SHA `d7ccbe2f07f9878bafaa59905348d83cc4ced057` 复用实现 full；PR #283 fast [31348617791](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31348617791) 与 merge fast [31348684301](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31348684301) 均复用成功 receipt，merge SHA `6c6bb7ae2b300d0934fd30d7a170979ba66d390f`；后三者 JUnit `10/10`、Xcode skipped，不是新的编译证据。

v3.218 修复 v3.217 长图 detector 已覆盖整页、Manga OCR 却仍被全局前 12 个高 confidence 区域截断的边界：`ComicTextBubbleDetectorService` 公开与真实推理共用的 slice count；普通图片继续精确保留历史 12 请求和同一 crop ownership 集合，只有多 slice 长页才按每片 12 次扩展，CPU-only 总上限 48。专用 detector regions 优先按纵向 band 分配 quota，未用满时再让 Vision supplement 进入同一公平选择，最后才按 confidence／几何消费 overflow；长页 crop 可看到完整邻区以保持列 ownership，模型失败、取消、Vision fallback、布局、翻译、渲染与非日语路径不变。这向 Koharu “全部 TextRegion 都交给 Manga OCR”靠近，但保留 iOS 响应性上限，不声称完全等价。

新增 `scripts/test-v3218-image-japanese-long-page-ocr-budget-contract.py` 和完整产品长页 runtime。4 份 `test/jap.jpg` 经 `VisionOCRService + RT-DETR + Manga OCR + layout` 在本地返回 15 块、云端返回 14 块，两者均为 13 个 vertical／0 个 unknown；v3.217 基线为 11 个 vertical／2 个 unknown。四个纵向季度各至少 3 个 vertical，4 份 `今度こそ...` 都由 vertical Manga OCR 产出且最底结果 y 约 `0.943`；单页 runtime 继续精确返回 5 个历史块，detector-only 长页仍为 17 regions。本地 v3.157-v3.218 共 `62` 份合同／`321` tests、三条真实 Core ML runtime、generic Simulator Xcode build、project/YAML 语法和 `git diff --check` 通过；exact-SHA full [31346445800](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31346445800) 对 SHA `0a275445fcbe3ce3c86e14be0567ab4204251f06` 完成三条真实 runtime、Xcode 与 JUnit `10/10`（0 failures），full-validation status `success`，probe `skip`。本地／云端总块数差异来自弱横排 Vision 噪声是否保留，不影响长页 vertical 覆盖 gate；重复固定 fixture 只证明该长页预算路径和方向 provenance 改善，不外推为通用日语 OCR 或翻译质量；Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`。

候选 metadata [31346910257](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31346910257) 对 docs SHA `80ad7e136f9ab16392259b604a8bf12a1fbb5924` 复用实现 full；PR #282 fast [31346989809](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31346989809) 与 merge fast [31347051715](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31347051715) 均复用成功 receipt，merge SHA `e19c1187c8b9f79b2d97ce2f4418b2f37539cb37`；后三者 JUnit `10/10`、Xcode skipped，不是新的编译证据，v3.218 功能分支已在本地和远端清理。

v3.217 继续迁入 Koharu `comic_text_bubble_detector::ImageSlicer` 的真实长图执行语义：只有 `height / width > 3.5` 才切片，目标高宽比 `3.0`、20% 纵向重叠，末段不超过目标高度 70% 时并入最后一片且保证触底；每片都运行 bundled RT-DETR Core ML，再把局部 y／height 还原到整页归一化坐标。跨片结果先按同 label 执行 containment `>= 0.85`、IoU `>= 0.50` 或受限纵向相邻门控，再进入 text label 的 IoU／containment TextRegion union；数组移除也改为 Koharu `swap_remove` 顺序。非长图仍只运行一次 detector，模型、0.30 threshold、12 个 Manga OCR 请求预算、Vision fallback、翻译、渲染与非日语路径不变。

新增 `scripts/test-v3217-image-japanese-comic-detector-slicer-contract.py` 和真实 Core ML 长页 runtime：将 `test/jap.jpg` 纵向拼接 4 次为 `1136x6400`，按参考几何执行 2 个重叠 slice，最终稳定返回 `17` 个 detector regions，四个纵向季度各至少 4 个且最底区域 y 约 `0.94`；原单页产品 runtime 继续精确返回 v3.216 的 5 个完整竖排块。本地 v3.157-v3.217 共 `61` 份合同／`314` tests、两条真实 Core ML runtime、generic Simulator Xcode build、project/YAML 语法和 `git diff --check` 通过；exact-SHA full [31323013519](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31323013519) 对 SHA `8c037482e175818bb5c3f3714c5a3992fbf70644` 完成长页／单页真实 runtime、Xcode 与 JUnit `10/10`（0 failures），full-validation status `success`，probe `skip`。Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不将重复固定 fixture 外推为通用长页或日语 OCR 质量。

候选 metadata [31323359308](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31323359308) 对 docs SHA `30990b6c78671c27448aafed23e59652688740d3` 复用实现 full；PR #281 fast [31323414600](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31323414600) 与 merge fast [31323579377](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31323579377) 均复用成功 receipt，merge SHA `32e7b30c1b3f069a405c2d1b1aaaceb8b2660442`；后三者 Xcode skipped，不是新的编译证据，v3.217 功能分支已在本地和远端清理。

v3.216 直接迁入 Koharu 默认的真实 comic text detector，而不再只用 Vision text rectangle 代理 TextRegion：固定 `ogkalu/comic-text-and-bubble-detector` revision `16e8a622f91fabc6b5b65c96d32d1183f8843546`，将 RT-DETR-v2 R50 转为 iOS 17 Core ML，RGB `640x640`、`1/255` 输入，FP16 compute／linear-symmetric INT8 权重约 `43 MB`，Apache-2.0 license、notice、conversion metadata 与可复现脚本一并入库。actor 缓存的 CPU-only runtime 复刻 Koharu top-300 sigmoid、`0.30` threshold、text label 1/2 过滤及 IoU／containment TextRegion 合并；GPU `.all` 在当前转换图上会触发不可恢复的 MPSGraph assertion，因此不进入可恢复 error boundary。专用 detector geometry 优先供 Manga OCR 使用，Vision detector 只补充未覆盖区域，合计仍最多 12 个请求；模型加载／推理失败则完整保留历史 Vision fallback，普通语言、翻译、渲染、Store 和 output 边界不变。

真实 `test/jap.jpg` 产品链路现返回精确 `5` 个 detector 分组竖排块：`前は生意気に俺の誘い断りやがって...`、`今度こそこの爆乳を持ち帰る！`、`そのせいでつまんねー女に絡まれるし...`、`...では最後に監督より挨拶をお願いします` 与 `こっ、`；补回 v3.215 仍漏失的 `やがって...` 并减少碎片化。新增 `scripts/test-v3216-image-japanese-comic-text-detector-contract.py`；本地 v3.157-v3.216 共 `60` 份合同／`307` tests、真实 Core ML runtime、generic Simulator Xcode build、project/JSON/YAML 语法与 `git diff --check` 通过；exact-SHA full [31321620928](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31321620928) 对 SHA `e25bf798323dbbabe154c1849436711d90ab8888` 完成真实 runtime、Xcode 与 JUnit `10/10`（0 failures），full-validation status `success`，probe `skip`。Koharu mask artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，只声明固定 fixture 改善，不声称通用日语 OCR、翻译或识别质量。

候选 metadata [31322010365](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31322010365) 对 docs SHA `fb9385645ed8524bf0a6dc58df22d7064153d1be` 复用实现 full；PR #280 fast [31322080575](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31322080575) 与 merge fast [31322150176](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31322150176) 均复用成功 candidate receipt，merge SHA `f0a85c2b3f60d5d37652dc76c83aa8703692a245`；后三者 Xcode skipped，不是新的编译证据，v3.216 功能分支已在本地和远端清理。

v3.215 修复真实 Manga OCR TextRegion 的相邻竖列 ownership：Vision 字符框中偶发的跨列宽框不再直接撑大 envelope，而是仅从宽／高至少为各自最大值 `35%` 的 glyph-sized boxes 取中位中心与中位宽度，且只在新宽度为 broad envelope 的 `35%...82%` 时收紧，纵向范围与安全 fallback 不变；Manga OCR 的 2–4 字偏下候选只有在相邻列字符更多、中心距离／顶部差／高度／纵向重叠同时过门时才向上对齐，并让字体 padding 在相邻列中心 bisector 截止但绝不裁掉 detector core。真实 `test/jap.jpg` 仍为 `12` 个竖排块，v3.214 的 `今度こそこの暴れ`／`の爆乳を`、`そのせいでつまりまーズ`／`うまんねー女に` 与 `いします` 改为独立的 `今度こそ`、`この爆乳を`、`そのせいで`、`つまんねー女に`、`お願いします`；仍漏读 `やがって...`，保留 `．では最後に`、`こっ、` 等错误，只声明固定 fixture 改善。新增 `scripts/test-v3215-image-japanese-manga-column-ownership-contract.py` 并把真实 runtime 提升为精确 12 块与旧错误拒绝 gate；本地 v3.157–v3.215 共 `59` 份合同／`299` tests、真实 Core ML runtime 和 generic Simulator Xcode build 通过；exact-SHA full [31317279248](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31317279248) 对 SHA `a9433f2649c22388aa54a72530c2c8db1404fcdd` 完成真实 runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`；翻译、渲染、非日语路径及 Koharu artifact readiness `manifestMissing / stopUntilArtifactsProvided` 不变。

候选 metadata [31317671420](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31317671420) 对 docs SHA `2e047f835f1aaedb91dc4c648be3579b67c8d776` 复用实现 full；PR #279 fast [31317731083](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31317731083) 与 merge fast [31317789538](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31317789538) 均复用成功 receipt，merge SHA `83111349937a5163ea1aad5402df55b194972dc2`；后三者 Xcode skipped，不是新的编译证据，v3.215 功能分支已在本地和远端清理。

v3.214 首次把 Koharu 实际使用的专用 Manga OCR 识别器迁入普通图片日语路径，而不是继续用 Vision 模拟模型边界：`kha-white/manga-ocr-base` 固定 revision `aa6573bd10b0d446cbf622e29c3e084914df9741` 转为 iOS 17 Core ML ViT encoder／BERT decoder，encoder FP16、decoder FP32（避免 causal-mask FP16 overflow），常量权重 linear-symmetric INT8 后约 `106 MB`，Apache-2.0 license／notice／vocab 与可复现转换脚本一并入库。actor 缓存的 CPU-only runtime 直接复用 Vision 已完成 EXIF 校正与有界缩放的同一张图，对最多 12 个 pixel-first TextRegion 执行 224×224 灰度归一化、最多 300 token greedy decode、几何平均 token confidence 与日文字符门控，先于原 Vision line/block/tile fallback 写入 `verticalLine` provenance；模型加载／推理失败仍返回空并保留旧回退。真实 `test/jap.jpg` 产品链路由 v3.213 的 `3` 个弱块提升为 `12` 个竖排块，命中 `前は生意気に`、`持ち帰る`、`監督より挨拶を`，macOS CPU-only 冷启动约 `3.75s`；该固定 fixture 仍有 `今度こそこの暴れ`、`うまんねー女に` 等错误，只证明此样图的实测改进，不外推到通用日语 OCR corpus。新增静态合同与真实 Core ML runtime CI；本地 v3.157–v3.214 共 `58` 份合同／`293` tests、runtime fixture、generic Simulator Xcode build 通过；exact-SHA full [31315520990](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31315520990) 对 SHA `0a92317fae8a9e6dae89fe5592dec27b94995000` 完成真实 runtime、Xcode 与 JUnit `10/10`（0 failures），probe `skip`；Koharu mask artifact readiness 仍独立为 `manifestMissing / stopUntilArtifactsProvided`。

候选 metadata [31316016491](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31316016491) 对 docs SHA `02c3ea0632298878baffdfbec4af1fe8d9660f69` 复用实现 full；PR #278 fast [31316067683](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31316067683) 与 merge fast [31316119282](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31316119282) 均复用成功候选 receipt，merge SHA `ad7a265abfa620e93ea345429d14558ed61536be`；后三者 Xcode skipped，不是新的编译证据。

v3.213 对齐 Koharu `TextRegion.line_polygons` 的紧行几何消费边界：普通图片日语 pixel-first detector 在 90°／270° 侦察时开启 Vision character boxes，把至少两个有效字符框逐个映射回原图并合成紧 envelope；只有 envelope 仍为有效竖排候选、与宽 request box minimum-area overlap `>= 0.80`、面积比在 `0.25...1.05`、横向覆盖 `>= 0.35` 且纵向覆盖 `>= 0.60` 时才用于 crop，缺失／单字符／非法／不完整字符框安全回退宽框。新增 `scripts/test-v3213-image-japanese-koharu-character-envelope-contract.py`，并更新 v3.208 历史合同接受后续紧几何；本地 v3.157–v3.213 共 `57` 份合同／`286` tests 和 Simulator Xcode build 通过，v3.157/v3.158 仍已合入且无活动分支；exact-SHA full [31310411961](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31310411961) 对 SHA `2ba635fd6e39f24954d8e731cbdfb59dc83b9900` 完成 Xcode/JUnit `10/10`，probe `skip`；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

候选 metadata [31310728862](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31310728862) 对 docs SHA `bf0ed503ad534870f33ef17f30d6eeca06c71c97` 复用实现 full；PR #277 fast [31310775114](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31310775114) 与 merge fast [31310809139](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31310809139) 均复用成功候选收据，merge SHA `6ef85fce62d24a1f668a60484369dd24774caf59`；后三者 Xcode skipped，不是新的编译证据。

v3.212 对齐 Koharu `MangaOcr::inference` 的直接解码边界：普通图片日语 block／line／perspective line crop reread 关闭 Vision `usesLanguageCorrection`，页级 reconnaissance 与非日语路径保留默认 correction；request helper 显式先设 `true`，只有受限 crop caller 传入 `false` 时关闭，以兼容 v3.156 历史合同。新增 `scripts/test-v3212-image-japanese-koharu-raw-crop-recognition-contract.py`；本地 v3.157-v3.212 共 `56/56` 个合同通过，v3.157/v3.158 已合入当前基线、无活动分支；exact-SHA full [31302657064](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31302657064) 对 SHA `bd7c510b99ac78c22ca330ae2e125a5193610fe4` 完成 Xcode/JUnit `10/10`，probe `skip`；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

候选 metadata [31309651292](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31309651292) 对 docs SHA `174a7180d229381b41c5184e1525a2ad7651baa3` 复用实现 full；PR #276 fast [31309712340](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31309712340) 与 merge fast [31309783552](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31309783552) 均复用候选成功 receipt，merge SHA `ccab9e318b0c71447b59cb2b370d5778a9c68904`；后三者 Xcode skipped，不是新的编译证据。

v3.211 将普通图片日语竖排 line/block/tile reread 的 Vision 语言集合收敛为 `ja-JP`/`ja`，页级 90°／270° reconnaissance 仍保留 `ja-JP`/`ja`/`en-US`/`en` 混合集合；不支持日语 profile 时 language-specific reread 安全返回空结果。新增 `scripts/test-v3211-image-japanese-vertical-language-gate-contract.py`；exact-SHA full [31300669764](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31300669764) 对 SHA `c0c26aaddb3db641c59cb878d1434207b9879f54` 完成 Xcode/JUnit `10/10`，probe `skip`；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.210 改善图片翻译失败后的恢复体验：保留已识别文字块、几何和已完成译文，在 `.failed`／可恢复的 `.translated` 状态下只允许空译文块单独重试；日语块继续复用 `[N]` 漫画批翻译协议，非日语复用单块翻译，绝不重新调用 Vision OCR。retry/content ID 防止过期结果写回，部分失败继续保留其它块，全部译文完成后才恢复 `.translated` 并重绘导出；结果行、局部预览和 VoiceOver 均提供受门控的重试 action。新增 `scripts/test-v3210-image-translation-block-retry-contract.py`，本地 v3.157–v3.210 共 54 个合同通过；exact-SHA full [31299660925](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31299660925) 对 SHA `b9ec296d0cdafbe0bfbbe0aebc90e1255a44d6d2` 完成 Xcode/JUnit `10/10`（0 failures）；候选 metadata [31300023503](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31300023503) 对 docs SHA `0dc5b019e231aa35137de705a6a2b8e0a377d029` 复用成功 full，PR #274 fast [31300071078](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31300071078) 与 merge fast [31300107560](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31300107560) 均复用候选 full，merge SHA `ee7e41f0679fd999b8f9337a3bf0622742e095c3`，后续 fast Xcode skipped，不是新的编译证据；v3.157/v3.158 已合入当前基线，无活动分支；probe `skip`，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.209 对齐 Koharu 的 line-first dispatch：普通图片日语竖排先执行 `verticalLine` perspective／轴对齐 reread，再让 pixel detector 与 tile fallback 只侦察可靠 line 结果未覆盖的区域，最后才进入 block crop。可靠 line coverage 要求非空文本、`confidence >= 0.48`、日语脚本密度 `>= 0.5` 和有效紧 geometry（缺失时安全回退宽 `rect`）；pixel detector 结果保留 `verticalLine` provenance，tile fallback 保持历史 `.crop` provenance。新增 `scripts/test-v3209-image-japanese-koharu-line-first-dispatch-contract.py`，并让 v3.208 历史合同接受等价 provenance；本地 v3.157–v3.209 共 53 个日语合同通过。实现 exact-SHA full [31297254547](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31297254547) 对 SHA `17f19bb2505d504e1255ab925d2aa7572020435a` 完成 Xcode/JUnit `10/10`；PR #273 fast [31297894114](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31297894114) 与 merge fast [31297941634](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31297941634) 均复用候选 full，merge SHA `4e2b8fdceec51359bb923bd583687fa2e3ed9e24`；后三者 Xcode skipped，不是新的编译证据；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.208 对齐 Koharu 的 detector→TextRegion→crop/OCR 分层：普通图片日语竖排路径在已有 page／block observation 之外，使用系统 `VNDetectTextRectanglesRequest` 对 90°／270° 旋转图做 pixel-first 几何侦察；映射回原图后只保留 `height / width >= 1.15`、高度 `>= 0.025`、宽度 `<= 0.30` 且未被现有 vertical block 覆盖的最多 12 个候选，再复用 Koharu 风格 grayscale／有界放大／crop／Vision OCR，最多 4 次相反方向 fallback。detector 只提供 geometry，不伪装 Manga OCR/PaddleOCR 模型；普通语言、已有 page／block／line／tile 路径、翻译、渲染、Store、探针、ground truth、metrics 与 output 边界不变。新增 `scripts/test-v3208-image-japanese-koharu-pixel-detector-contract.py`；exact-SHA full [31295791350](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31295791350) 对 SHA `41eff6cb86073900332d9785eea32606a5688dce` 完成 Xcode/JUnit `10/10`；PR #272 fast [31296131671](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31296131671) 与 merge fast [31296221789](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31296221789) 均复用候选 full，merge SHA `7c8642af855fcbf79cfad7a3a9052a5465d83632`；后三者 Xcode skipped，不是新的编译证据；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.207 对齐 Koharu line-first OCR 的“可用结果才算覆盖”边界：普通图片日语竖排 block 只有在每个 source line 都被独立 `verticalLine` reread 以 `confidence >= 0.48`、日语脚本密度 `>= 0.5` 通过质量门控时才跳过 block crop；多 glyph source line 不接受单 glyph 结果作为完整覆盖，弱／非日语／空结果继续安全回退 block crop。新增 `scripts/test-v3207-image-japanese-koharu-line-coverage-quality-contract.py`，v3.157–v3.206 历史合同继续回归。本地 51 个合同通过；候选 exact-SHA full [31294146132](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31294146132) 对 SHA `f7a18e4c008903fd50f15183fa3b9a8629216bef` 完成 Xcode/JUnit `10/10`（0 failures）；候选 metadata [31294486910](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31294486910)、PR #271 fast [31294542723](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31294542723) 与 merge fast [31294614418](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31294614418) 均复用候选 full，merge SHA `15577b7a184f9b6a8d281f1551367b1c72231a65`，后三者不是新的 Xcode 编译证据。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.206 对齐 Koharu `sort_manga_reading_order` 的行级无切分回退：普通图片日语竖排 observation 在 Recursive XY-cut 没有有效切分或分区失效时，按 `4 × min_gap_y` 行桶先读上方、同一行从右到左，再用几何／文字／confidence 做确定性 tie-breaker；混合 block 已有 fallback、非日语交错路径与 OCR/翻译/探针边界不变。新增 `scripts/test-v3206-image-japanese-koharu-observation-row-fallback-contract.py`，v3.157/v3.158 历史合同继续回归。实现 exact-SHA full [31293120347](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31293120347) 对 SHA `5aefe027e97f37aa209c09d2a9d33ecf0a1d848c` 完成 Xcode/JUnit `10/10`；PR #270 fast [31293135057](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31293135057) 复用候选 full，merge fast [31293388944](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31293388944) 以 merge SHA `9513cd7c9d33610f0b93a4e435f9e3f1867328bb` 复用候选 receipt；后三者中后两者 Xcode skipped，不是新的编译证据。v3.157/v3.158 已合入当前基线，不存在未合入的活动分支。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.205 修复 Koharu line-first OCR 的完整覆盖边界：普通图片日语竖排 block 只有在每个可枚举 source line 都被独立、有效且紧 geometry 对齐的 `verticalLine` reread 覆盖时才跳过 block crop；部分成功、合成 envelope、噪声结果或缺失 line geometry 继续安全回退 block crop，避免多行 TextRegion 丢失未读文字。新增 `scripts/test-v3205-image-japanese-koharu-line-coverage-fallback-contract.py`，并保留 v3.157/v3.158 历史合同回归。实现 exact-SHA full [31292332659](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31292332659) 对 SHA `7891cfeaf3486eb6a507d1b2045a9b662b8c66ca` 完成 Xcode/JUnit `10/10`；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.204 对齐 Koharu `extract_text_block_regions` 的 line-first OCR 边界：普通图片日语竖排 block 有有效 line polygon proxy 时先执行 line perspective／轴对齐 reread，只有 line OCR 没有可用 observation 才回退 block crop；保留 v3.203 tight ownership、line/block direction provenance、方向 fallback 与有界预算。新增 `scripts/test-v3204-image-japanese-koharu-line-first-ocr-contract.py`，并继续回归 v3.157/v3.158 历史合同。实现 exact-SHA full [31290525270](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31290525270) 对 SHA `ae922bda0bf566cc14d422a6d9c9a4c042b34218` 完成 Xcode/JUnit `10/10`；候选 metadata [31290904290](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31290904290)（candidate SHA `5e288e4fb477ecbc6489e71b19415ebade6b8137`）复用实现 full，PR #268 fast [31290942391](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31290942391) 复用候选 receipt，merge fast [31290981606](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31290981606) 以 merge SHA `b65653bc577ba65c51ed6cc1c2bd373b00e76aec` 复用候选 receipt；后三者 Xcode skipped，不是新的编译证据。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.203 对齐 Koharu `TextRegion` 对 line polygon 的 ownership 边界：普通图片日语竖排 line candidate、碎片合成与 block line-envelope 现在在已有宽 request-level overlap 之外，有效 `lineRegionRect` 必须与当前 vertical block 相交；紧 geometry 缺失或非法时安全回退宽 `rect`。新增 `scripts/test-v3203-image-japanese-koharu-line-block-ownership-contract.py`。实现 exact-SHA full [31287319601](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31287319601) 对 SHA `01fdaf16dde9029079231eb7c5406042fcab8cfc` 完成 Xcode/JUnit `10/10`；候选 metadata [31287648270](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31287648270)（candidate SHA `cc3d24f68dfb4b391ba019d2549a1084381d1305`）复用实现 full，PR #267 fast [31287677451](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31287677451) 复用候选 receipt，merge fast [31287707581](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31287707581) 以 merge SHA `295c59a72a886cbc19cd9c3126d9e162ce525afd` 复用候选 receipt；后三者 Xcode skipped，不是新的编译证据。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.202 对齐 Koharu `extract_text_block_regions` 的紧 line-region 消费边界：普通图片日语竖排 line candidate 的高宽比／高度门控现在优先读取 observation 的 `lineRegionRect`，只有缺少紧 geometry 时才回退 request-level `rect`；与 vertical block 的重叠仍使用宽 `observation.rect`，因此布局与去重外框不变。新增 `scripts/test-v3202-image-japanese-koharu-tight-line-candidate-gate-contract.py`，并让 v3.160 历史合同接受该等价紧区域门控。实现 exact-SHA full [31286506178](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31286506178) 对 SHA `2f198f9f12e62c5e10fe7a73b76cdc0af9d69107` 完成 Xcode/JUnit `10/10`；候选 metadata [31286843872](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31286843872)（candidate SHA `a2ddc0819ecc9d92c6cbcb1d21d9473d8d2b1a94`）复用实现 full，PR #266 fast [31286875844](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31286875844) 复用候选 receipt，merge fast [31286908329](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31286908329) 以 merge SHA `839c5e9d9f5705f50c85d65e7085157c5b876b07` 复用候选 receipt；后三者 Xcode skipped，不是新的编译证据。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.201 对齐 Koharu `warp_line_region` 的竖排 line 方向 provenance：普通图片日语竖排 perspective／轴对齐 line reread 现在携带 `verticalLine` 角色，经过原图映射、合成与最终去重后仍以 `rotate270` 为有界主方向偏好；page、block、tile 继续保留各自历史 90°／270° 边界，弱结果仍可走 opposite fallback。新增 `scripts/test-v3201-image-japanese-koharu-line-orientation-provenance-contract.py`；实现 full [31262554391](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31262554391) 对 SHA `97120cf38d85729f605e3d8bdbc836a0271e1c99` 完成 Xcode/JUnit `10/10`，候选 metadata [31262979444](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31262979444) 复用该 full，PR #265 fast [31263017146](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31263017146) 与 merge fast [31263072991](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31263072991) 均复用成功 receipt；merge SHA `3aac10326a987a47ea1786cd76c37b96b3ca9b36`，后续 fast 均跳过 Xcode，不是新的编译证据。readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.200 对齐 Koharu `expanded_text_block_crop_bounds` 的字体锚点：普通图片日语竖排 block 在把相关 Vision `lineRegionRect` 并入原 block envelope 后，仍以原始 `block.rect` 的最小边作为 `fontSizeReference` 计算 `8%/18%/12%` 方向感知 padding，再把 padding 应用于 union envelope；避免多行 envelope 变大后过度扩边。缺失或非法 geometry 继续回退旧 crop，line、tile、非日语、翻译、探针、metrics 与 output 边界不变。新增 `scripts/test-v3200-image-japanese-koharu-font-anchor-contract.py`，并让 v3.199 历史合同接受等价 helper；实现 exact-SHA full [31260969111](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31260969111) 对 SHA `c91858868cace78b846bd742f69a95f765b5737a` 完成 Xcode/JUnit `10/10`，候选 metadata [31261967648](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31261967648) 复用该 full，PR #264 fast [31261881073](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31261881073) 与 merge fast [31261995539](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31261995539) 均复用成功 receipt；readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.199 对齐 Koharu `crop_text_block_bbox` 的 block line envelope：普通图片日语竖排 block crop 先把与原 block 相关且有效的 Vision `lineRegionRect` 归一化区域并入原 block envelope，再执行既有方向感知 padding；始终不缩小原 block，缺失或非法 geometry 安全回退旧 crop。新增 `scripts/test-v3199-image-japanese-koharu-block-line-envelope-contract.py`，并让 v3.175/v3.178 历史合同接受等价 helper；候选 exact-SHA full `31260137161`、PR #263 fast `31260466644`、merge fast `31260501796`；候选 SHA `b85f12389eddf1cd82d8aa2892fe5f730d4b8a56` Xcode/JUnit `10/10`，merge SHA `20d5987c245bf6768257e61d6ee030cf2324aef0` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.198 对齐 Koharu `warp_line_region` 的竖排 line 方向：普通图片日语 perspective 与轴对齐 line reread 统一以 `rotate270`（270°）作为主读取方向，弱／空结果仍在既有 12 次页级预算内回退 90°；block、tile、非日语、翻译、探针、metrics 与 output 路径不变。新增 `scripts/test-v3198-image-japanese-koharu-vertical-line-orientation-contract.py`；候选 exact-SHA full `31259014271`、PR #262 fast `31259242739`、merge fast `31259272510`；候选 SHA `24c8f9c23f3bec3e6536ad4752e99a4703ff07f4` Xcode/JUnit `10/10`，merge SHA `0b7352be9393e17fc55224269604dfc6859d2351` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.197 对齐 Koharu `pp_doclayout_v3` 的竖排检测门控：普通图片日语 block crop 现在接受 `height / width >= 1.15` 且方向置信度 `>= 0.25`、归一化高度 `>= 0.035` 的中等高宽比竖排候选，保留原有 `1.45` 标准门控、紧凑列门控、最多 16 个 block、方向 fallback 与安全回退；非日语、整页 OCR、line/tile crop、翻译与渲染路径不变。新增 `scripts/test-v3197-image-japanese-koharu-vertical-threshold-contract.py`；候选 exact-SHA full `31258318641`、PR #261 fast `31258329662`（重跑后复用 full）、merge fast `31258606990`；候选 SHA `e099acb375cf799d9f3d26b203eead6505b9b65c` Xcode/JUnit `10/10`，merge SHA `bd91910ccfe38b78d2827095b7e2970c1347d881` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.196 将 Koharu `translate_texts` 的编号文本块协议迁入普通图片日语翻译：日语图片 OCR blocks 按全局 `[N]` 标签分成最多 8 块／约 1800 字符的有界批次，提示词要求保持角色语气、情绪、拟声词及标签顺序，不合并／拆分／遗漏／重排；输出缺 tag、乱序或空块时安全回退既有逐块翻译，非日语图片和修正 sheet 保持原路径。新增 `scripts/test-v3196-image-japanese-koharu-batch-translation-contract.py`；候选 exact-SHA full `31257482066`、PR #260 fast `31257709942`、merge fast `31257752177`；候选 SHA `4282395bd982c2c6fcb92058b3cdfb8409defd01` Xcode/JUnit `10/10`，merge SHA `dd007a40705903bcc92800f30a905ead44810503` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.195 对齐 Koharu `sort_manga_reading_order` 的混合版面消费边界：日语漫画阅读模式现在把横排与竖排 block 合并后统一递归 XY-cut，横向切分先读右侧、纵向切分先读顶部；无法切分时按 `4 × min_gap_y` 行桶从右到左稳定回退。非日语路径继续使用原有横／竖交错，新增 `scripts/test-v3195-image-japanese-mixed-layout-reading-order-contract.py`。候选 exact-SHA full `31233606259`、PR #259 fast `31233872614`、merge fast `31234023270`；候选 SHA `52963a4c1cbe16f8662a99ae443aea36f1dbb486` Xcode/JUnit `10/10`，merge SHA `12b0217c7c56a2993986f4a42ba6e6f98c7df6a2` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.194 继续迁移 Koharu `merge_slice_regions` 的同标签 IoU 去重：普通图片日语 OCR 融合在双方都有 Vision 字符范围 `lineRegionRect` 时，除 containment-like minimum-area overlap `>= 0.85` 外，额外接受紧区域 IoU `>= 0.50` 的几何重复；宽 request-level box、缺少任一紧区域与非日语路径仍要求原有文本相似度门控。新增 `scripts/test-v3194-image-japanese-tight-region-iou-dedupe-contract.py`，并让 v3.193 历史合同接受后续版本；候选 exact-SHA full `31232966715`、PR #258 fast `31233259741`、merge fast `31233297104`；候选 SHA `e4388409785bc60278b819e36e8b4afdc7ec7307` Xcode/JUnit `10/10`，merge SHA `5ce315332aa6ab073e3e9733f174cbf2ccac0121` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.193 对齐 Koharu `merge_slice_regions` 的“紧几何优先”去重边界：普通图片日语 OCR 融合在双方都有 Vision 字符范围 `lineRegionRect` 且 minimum-area overlap `>= 0.85` 时可直接合并几何上几乎包含的重复候选；缺少任一紧区域、只有宽 request-level box 或非日语路径仍要求原有文本相似度门控，避免相邻竖排被误合并。新增 `scripts/test-v3193-image-japanese-tight-region-dedupe-contract.py`，并让 v3.192 历史版本合同接受后续版本；候选 exact-SHA full `31232333781`、PR #257 fast `31232570686`、merge fast `31232612519`；候选 SHA `a645943b85345790309adde433344a53209bbccc` Xcode/JUnit `10/10`，merge SHA `c00ffc306a193351179e86b7fd7ad0783bd1ad4b` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.192 在 v3.191 的 Koharu `ImageSlicer` 局部窗口内补齐漫画阅读方向：日语竖排 fallback 先按窗口 x 坐标从右到左消费，再在每列内按 y 从上到下读取；总有效窗口仍最多 18 个，覆盖抑制、90°／270° OCR、日语过滤、灰度放大、原图映射与去重保持不变。新增 `scripts/test-v3192-image-japanese-manga-window-order-contract.py`；候选 exact-SHA full `31231339154`、PR #256 fast `31231689829`、merge fast `31231720315`；候选 SHA `d4a7811b634df7f300fb60a99e331a27f69287bd` Xcode/JUnit `10/10`，merge SHA `2aae0d7ff0e5394bed6a0c13edbb3a2624f67c1a` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.191 直接迁移 Koharu `ImageSlicer` 的日语竖排局部切片语义：每个横向侦察条以 `target ratio=3.0`、`overlap=0.20` 生成纵向窗口，末段不足 `0.70` 目标高度时并入最后窗口并保证触底；总有效窗口最多 18 个，既有覆盖抑制、90°／270° OCR、日语过滤、灰度放大、原图映射与去重保持不变。新增 `scripts/test-v3191-image-japanese-koharu-slicer-contract.py`；最终 exact-SHA full `31230729061`、PR #255 fast `31231091935`、merge fast `31231128313`；最终 SHA `32c350c6ce4c40d90dd1d9505a830c083a62ce49` Xcode/JUnit `10/10`，merge SHA `16cce70a48afda648f045edf9d97f9a1191890cd` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针 skip，不声称日语 OCR、翻译或识别质量提升。

v3.190 将普通图片日语漏列 reconnaissance 从全高窄条改为带纵向重叠的局部窗口：窗口高度取长图约 58% 并保留 18% overlap，强制覆盖底边，总共最多执行 12 个有效窗口，270° 弱结果 fallback 仍最多 4 次；已有竖排 block 覆盖、日语脚本／几何过滤、灰度放大、原图映射与去重保持不变。新增 `scripts/test-v3190-image-japanese-localized-tile-window-contract.py`；候选 exact-SHA full `31229567448`、PR #254 fast `31229946643`、merge fast `31229977158`；候选 SHA `44e0030aa1914468fceedfc5404eaf7510e9673b` Xcode/JUnit `10/10`，merge SHA `ec8ad6333005527042fd7f13f8f203bfa36fafe5` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针 skip，不声称日语 OCR、翻译或识别质量提升。

v3.189 修正 Koharu `TextBox.source_direction` 在单字 crop 上的保留边界：普通图片日语竖排 block／line／tile crop reread 映射回原图后，只要文本含 CJK 即使 Vision 只返回一个 glyph 也继续尊重 `sourceDirectionHint=.vertical`，再决定是否落入 `wideBox` 横排回退；仍要求日语 manga-order，页级 observation、普通语言、横排与非日语布局保持原几何路径。新增 `scripts/test-v3189-image-japanese-single-glyph-direction-hint-contract.py`；候选 exact-SHA full `31228731388`、PR #253 fast `31229051329`、merge fast `31229096792`；候选 SHA `8c89b0c0d1c0dff91efd3c115583ab5f04d14017` Xcode/JUnit `10/10`，merge SHA `6a510efece0b7ef6deeb9f1c3dd19c11f625ba1a` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.188 对齐 Koharu 的“同一右到左文字列内自上而下”读取顺序：普通图片日语竖排 observation 合并成 `Cluster` 后不再依赖 crop/tile 到达顺序，而是显式按 `rect.y` 上到下、同 y 时右侧优先，并以几何／文字／confidence 做确定性 tie-breaker 后拼接；横排换行与同列合并门控保持不变。新增 `scripts/test-v3188-image-japanese-vertical-cluster-reading-order-contract.py`；候选 exact-SHA full `31227754288`、PR #252 fast `31228156354`、merge fast `31228206021`；候选 SHA `1580a5c145efede4fefa23fd743c8170e077b1f9` Xcode/JUnit `10/10`，merge SHA `bbba2eee0a31a3bfd8b82508c77caac3677e1810` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.187 对齐 Koharu 的 `TextBox.source_direction` provenance：普通图片日语 block／line／tile crop reread 映射回原图后继续携带受限 `sourceDirectionHint=.vertical`，`ImageOCRLayoutEngine` 在日语 manga-order 且文本含 CJK 时先尊重该 hint，再决定是否落入 `wideBox` 横排回退；页级 observation、普通语言与非日语布局保持几何路径。新增 `scripts/test-v3187-image-japanese-crop-direction-hint-contract.py`；候选 exact-SHA full `31226671116`、PR #251 fast `31226855629`（重跑后复用 full）、merge fast `31227082382`；候选 SHA `881d2b4fe49d8adde181d8e33d210a38515fcdd8` Xcode/JUnit `10/10`，merge SHA `c1bb36ba956e5a48b583ff1e2a2214e3dcee916c` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.186 收敛 v3.185 全高 tile fallback 的候选边界：普通图片日语漏列 reconnaissance 结果先经过日语脚本密度与竖排几何过滤，高竖框保留，只有一／二字紧凑 CJK 片段在受限尺寸下保留；过滤后的结果才决定是否消耗 270° fallback 预算，避免 tile 内横排日语噪声进入最终布局。仅作用于 tile fallback，不改变既有 6 tile、18% overlap、4 次 fallback、block/line crop、普通语言、翻译、渲染、Store、Koharu gate、metrics 或 output。新增 `scripts/test-v3186-image-japanese-vertical-tile-filter-contract.py`，并让 v3.185 历史合同接受等价过滤 helper；候选 exact-SHA full `31225584307`、PR #250 fast `31225981653`、merge fast `31226027759`；候选 SHA `86cf3fd289b4d42c18658d48d5c93308e4f5f91c` Xcode/JUnit `10/10`，merge SHA `5bb34b44d0c93ab93d816a848266f65c95ad9d6c` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.185 对齐 Koharu 在 detector 未产出整列 TextBox 时的恢复边界：普通图片日语 OCR 在既有竖排 block crop 前增加最多 6 个带 18% 重叠的全高 tile reconnaissance，跳过已被竖排 block 覆盖的 tile；每个未覆盖 tile 复用日语灰度／放大、90° Vision crop pass，最多 4 次弱结果 270° fallback，再交回既有原图映射、日语去重与布局。仅作用于日语路径，不加载 Manga OCR/PaddleOCR 模型、不读取探针或 ground truth、不改变普通语言、翻译、渲染、Store、Koharu gate、metrics 或 output。新增 `scripts/test-v3185-image-japanese-vertical-tile-fallback-contract.py`；候选 exact-SHA full `31224644168`、PR #249 fast `31225019712`、merge fast `31225064534`；候选 SHA `0c0585a850f4a0a7a4fc4a5735c791439713c4c2` Xcode/JUnit `10/10`，merge SHA `ccd46c169ae7447d4f0485e4d982277f8ba33e46` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.184 对齐 Koharu 的 line-region 识别边界：普通图片日语 OCR 融合与竖排 line candidate 去重在双方都有 Vision 字符范围 `lineRegionRect` 时改用紧几何，缺少任一 hint 时回退 request-level `rect`，普通语言保持原路径；避免宽松外框把相邻竖排误合并，同时保留文本相似度、置信度、旋转评分与安全 fallback。新增 `scripts/test-v3184-image-japanese-line-geometry-dedupe-contract.py`；候选 exact-SHA full `31223348790`、PR #248 fast `31223808151`、merge fast `31223883384`；候选 SHA `1bc212b0cf5c8190a9aa9746fb44c0f03ae638dd` Xcode/JUnit `10/10`，merge SHA `211dab273bf9b0830b411f0586938ba77c93b46d` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.183 对齐 Koharu `warp_line_region` 的目标画布几何：普通图片日语竖排 perspective line warp 现在从四点 quad 的长短轴计算 `textHeight` 与 `ratio`，将自然透视结果有界重采样到 `(textHeight, textHeight × ratio)`，上限 4096 边长／4M 像素；几何缺失或重采样失败回退自然 warp。保留最多 24 条 line、每页 16M warp 像素预算、灰度／放大、90°／270° fallback、映射与去重，其他语言、block crop、布局、翻译、渲染、Store、探针和 Koharu gate 不变。新增 `scripts/test-v3183-image-japanese-line-warp-target-contract.py`；候选 exact-SHA full `31221970026`、PR #247 fast `31222386728`、merge fast `31222451794`；候选 SHA `6c6c040b095aadff18eea5f9f518ce50551fa8f7` Xcode/JUnit `10/10`，merge SHA `b6410f032d36e5f9205e15327107ca8069589c20` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.182 修正 v3.172 合成日语竖排 line 的 axis 路径：满足短日语片段、同列与 bounded gap 的 `synthesizeJapaneseVerticalLineCandidates` 现在只替代被其 `lineRegionRect` 重叠比 `>= 0.72` 覆盖的原始 axis 候选；原始候选仍保留在最多 24 条 perspective quad 路径，弱／失败 perspective 的方向 fallback、16M warp 像素预算、灰度／放大、映射与去重边界不变。仅作用于普通图片日语 line reread，其他语言、block crop、布局、翻译、渲染、Store、探针和 Koharu gate 保持边界。新增 `scripts/test-v3182-image-japanese-synthetic-line-replacement-contract.py`，并让 v3.172/v3.173 历史合同接受等价 `axisSeeds` helper；候选 exact-SHA full `31220601488`、PR #246 fast `31221025113`、merge fast `31221074919`；候选 SHA `42ad3abf8deba67d11b4fd3a93b16a1f09756657` Xcode/JUnit `10/10`，merge SHA `fb97ce105e5ae2d8fdda3d2acae631c86513be02` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.181 对齐 Koharu `warp_line_region` 的“成功的 line region 不再重复轴对齐读取”边界：普通图片日语竖排 perspective line reread 只有在 perspective 结果成功且不需要方向 fallback 时，才记录已覆盖的 line 候选；与其重叠比达到 `0.72` 的轴对齐候选跳过重复 Vision OCR，弱／失败 perspective 仍保留轴对齐与方向 fallback。最多 24 个 line 候选、16M warp 像素预算、灰度／放大、映射与去重保持不变。仅作用于普通图片日语 line reread，其他语言、block crop、布局、翻译、渲染、Store、探针和失败回退保持边界。新增 `scripts/test-v3181-image-japanese-line-path-dedupe-contract.py`；候选 exact-SHA full `31218314967`、PR #245 fast `31218876431`、merge fast `31218932836`；候选 SHA `e24ce08b798b1f205a4d812e626d27ba801db1de` Xcode/JUnit `10/10`，merge SHA `1107998858e3879750cba8dc8a27248ad1497589` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.180 对齐 Koharu `warp_line_region` 的局部几何边界：日语竖排 perspective line crop 先按四点 line polygon 的 bbox 裁剪源图，再把四点平移到局部坐标执行 `CIPerspectiveCorrection`，避免整页无关像素进入小文字行的透视复读；保留最多 24 个 line 候选、16M 像素预算、灰度／放大、方向 fallback 与原图映射。仅作用于普通图片日语 perspective line reread，其他语言、轴对齐 crop、布局、翻译、渲染和失败回退保持边界。新增 `scripts/test-v3180-image-japanese-line-warp-bbox-contract.py`；候选 exact-SHA full `31217320435`、PR #244 fast `31217749775`、merge fast `31217813652`；候选 SHA `eb522b28c1e9649278342f227aaef03995d67a41` Xcode/JUnit `10/10`，merge SHA `54b4cf750615efe54962f4247c72003d6d04f761` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.179 对齐 `reference/koharu-main/koharu-ml/src/manga_ocr/mod.rs` 的日语 `post_process` 顺序：先移除空白、统一省略号并压缩连续 `.`／`・`，再对压缩结果执行 ASCII 到全角映射；因此压缩后的点号不会留在半角形态。仅作用于普通图片日语 OCR 候选与既有 crop／line reread，普通语言、布局、翻译、渲染和失败回退保持边界。新增 `scripts/test-v3179-image-japanese-koharu-postprocess-order-contract.py`；候选 exact-SHA full `31216151856`、PR #243 fast `31216723888`、merge fast `31216783591`；候选 SHA `c16e5593ef63113e2d3ba5ef1b72d7a09ee2396a` Xcode/JUnit `10/10`，merge SHA `5f3c0aa1f45d9cee9774db4d0020370666b69273` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.178 让 v3.177 标记的紧凑日语竖排文字块进入既有 Koharu 风格 block crop／OCR 复读：普通高竖框继续使用 `aspectRatio >= 1.45`、`height >= 0.04`，只有方向原因为 `cjkCompactColumnTextRun` 的紧凑日语块才受限放宽到 `aspectRatio >= 1.20`、`height >= 0.022`，总预算仍为最多 16 个块，并继续走 `crop_text_block_bbox` 对齐的裁剪、预处理、方向 fallback、映射与去重。标准候选、非日语、非 compact 原因与失败回退保持边界。新增 `scripts/test-v3178-image-japanese-compact-block-crop-contract.py`；候选 exact-SHA full `31214729647`、PR #242 fast `31215410769`、merge fast `31215485897`；候选 SHA `ec80c63d1b0d25903f0d462a020dec6bca768f94` Xcode/JUnit `10/10`，merge SHA `4f6aeca133c9684c6800ec795a9f0fac4f24fdca` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.177 补齐紧凑日语竖排文字列的方向判定：`ImageOCRLayoutEngine.layout` 将 `prefersMangaReadingOrder` 传入 `resolveDirection`，仅在日语 manga-order、CJK 至少 2 个、`verticalRatio >= 1.35`、`height >= 0.022`、存在列邻居且不存在横排行邻居时标记 `cjkCompactColumnTextRun`，让小尺寸文字列进入既有 Koharu 风格 crop／line reread；简中、非日语、宽框横排、短单字列与高框 fallback 保持原边界。新增 `scripts/test-v3177-image-japanese-compact-vertical-direction-contract.py`；候选 exact-SHA full `31213076831`、PR #241 fast `31213569259`、merge fast `31213642909`；候选 SHA `9777d167cca71deb753f5d0f721f6c2f9f2af48f` Xcode/JUnit `10/10`，merge SHA `f2c8a33ba66666a69a941d24fb5d8d78284b1695` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.176 修正 Koharu 风格日语竖排 perspective line crop 的多 observation 拼接方向：源竖排 line 经 90° pass 后按旋转图 x 轴正序，经 270° pass 后按 x 轴逆序；同一 x 位置才以 y 和日语 observation score 稳定排序，避免 Vision 将一条 line 拆成多个 observation 后文字顺序反转。仅作用于日语 perspective line reread，其他语言、整页 OCR、翻译与渲染不变。新增 `scripts/test-v3176-image-japanese-line-reading-order-contract.py`；候选 exact-SHA full `31211585649`、PR #240 fast `31212154910`、merge fast `31212217877`；候选 SHA `6a61068f292e4e842b570a455eb357bd5b9a7c40` Xcode/JUnit `10/10`，merge SHA `eaa523f4d29f8be9e7e2f16131bbc21a9363706f` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.175 按 Koharu `detected_font_size_px` crop 边界改进普通图片日语竖排 reread：从源图片像素的文字块／line 尺寸推导字体大小，使用 `base=8%`、竖排水平 `18%`、垂直 `12%`（最小 2px）padding，再映射回 Vision 归一化坐标并保留安全 fallback；仅影响日语 block/line crop，其他语言、整页 OCR、翻译与渲染不变。新增 `scripts/test-v3175-image-japanese-font-size-padding-contract.py`；候选 exact-SHA full `31210073265`、PR #239 fast `31210705708`、merge fast `31210782269`；候选 SHA `e47014bb6cc68ec70029b3000d0b84c0156fe21e` Xcode/JUnit `10/10`，merge SHA `7b7a57b4d091fc3bd10305a6997e9dd24fba42ba` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.174 延续 Koharu 的“先形成文字块再交给 crop/OCR”边界：Vision 对日语竖排列给出高而窄的 line box 时，`ImageOCRLayoutEngine.shouldMergeVertically` 在保留同列／重叠门控的前提下，结合原宽度阈值与有界平均高度信号限制纵向 gap，避免在 line-region reread 前过早拆列；非日语、横排和 OCR 内容路径不变。新增 `scripts/test-v3174-image-japanese-vertical-cluster-gap-contract.py` 与显式 CI 路由；候选 exact-SHA full `31208462786`、PR #238 fast `31209161098`、merge fast `31209248983`；候选 SHA `49b987b3765e0df0c0511e30f955aa6aa7f487bf` Xcode/JUnit `10/10`，merge SHA `5efc690d0f8c3b41282518a8bc76d12559efa114` 复用候选 full，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.173 继续按 Koharu 的语言专用识别边界改进普通图片日语 OCR：最终布局去重、日语竖排 block/line 候选排序与弱方向 fallback 统一使用 `deduplicateJapaneseObservations`／`isBetterJapaneseObservation`，在原有置信度、长度、CJK 与旋转分数上增加有界日语脚本／标点证据；普通语言仍走原 `deduplicateObservations` 评分。新增 `scripts/test-v3173-image-japanese-observation-fusion-contract.py` 与显式 CI 路由，并让 v3.172 合同接受共享日语去重 helper。候选 exact-SHA full `31206796785`、PR #237 fast `31207387731`、merge fast `31207465845`；候选 SHA `d86f875d1040d69259b62b52754c73be3ccb59dd` Xcode/JUnit `10/10`，merge SHA `3fe6e719e064fe261f97530a7f16ff3b39ea4903` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.172 继续按 Koharu `extract_text_block_regions` 的“先形成 line-region 再 OCR”边界处理普通图片日语竖排：当 Vision 将同一竖排列拆成近方形短日语片段时，按既有 vertical block 内的列中心、短文本／日语脚本密度、连续垂直间隙与高宽比合成受限 line crop；合成候选最多 24 条，仅替换轴对齐碎片复读，原始四点 quad 仍保留给 perspective crop。新增 `scripts/test-v3172-image-japanese-vertical-fragment-line-contract.py` 与显式 CI 路由；候选 exact-SHA full `31204989011`、PR #236 fast `31205608084`、merge fast `31205688629`；候选 SHA `c2e7edd13818c9c46b65d1aa318e4c91c3479c09` Xcode/JUnit `10/10`，merge SHA `fab5ddb6d9ebdaa3d4a9dd34bfcdf6c0f676c84c` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.171 继续把 Koharu 的 crop 预处理边界覆盖到普通图片日语竖排 line reread：轴对齐 line 与 `CIPerspectiveCorrection` line 都先安全灰度化，再在最多 4M 像素内优先 2× 放大；轴对齐路径把实际 `cropScale` 传入原图坐标映射，透视路径按放大后像素计入每页 16M 预算。预处理失败回退原 crop，普通语言、整页 OCR、翻译、renderer/export 与探针不变。新增 `scripts/test-v3171-image-japanese-line-crop-preprocess-contract.py` 与显式 CI 路由，并让 v3.160/v3.162/v3.169 历史合同接受等价共享 helper；候选 exact-SHA full `31203452238`、PR #235 fast `31204110506`、merge fast `31204194868`；候选 SHA `9968f3083f9b19e9401dd9b48d9e35a480c99e9b` Xcode/JUnit `10/10`，merge SHA `df0145f9a2cb5dc5ea4ffdd515042f84cb8de108` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.170 继续把 Koharu `MangaOcr::preprocess_single_image` 的 crop 边界迁入普通图片日语竖排文字块：局部 crop 先安全灰度化，再按最多 4M 像素限制优先放大至 2×；缩放比例传入既有映射，识别失败或过滤器不可用时回退原 crop。仅作用于日语 block reread，line／perspective、普通语言与整页 OCR 不变。新增 `scripts/test-v3170-image-japanese-crop-preprocess-contract.py` 与显式 CI 路由；候选 exact-SHA full `31201978062`、PR #234 fast `31202618966`、merge fast `31202690968`；候选 SHA `0b2f011398457e410b366d1c10d80a902eecd173` Xcode/JUnit `10/10`，merge SHA `536b21f83670220ea5364b70badfe375a0df355c` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.169 继续按 Koharu 的 crop→OCR 方向边界处理普通图片日语竖排：文字块与 line crop 先沿当前 90°／270° 方向复读，结果为空、日语脚本密度低或置信度弱时才受页级预算驱动改用反方向复读；文字块最多 8 次、line 最多 12 次，统一 helper 负责旋转、Koharu 风格后处理、坐标回映射与既有去重。新增 `scripts/test-v3169-image-japanese-crop-orientation-fallback-contract.py` 与显式 CI 路由；候选 exact-SHA full `31200276655`、PR #233 fast `31200973375`、merge fast `31201060977`；候选 SHA `bbe47bd89e4413580482b07e52799867c844ec64` Xcode/JUnit `10/10`，merge SHA `a2ad829bf519a2f5cec02d82cc6d7b40168c2d62` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.168 继续按 `reference/koharu-main` 的 `post_process` 边界改进普通图片日语 OCR：去除空白、统一省略号与点号／中点串，并把 ASCII 标点归一到全角；Vision 日语路径在置信度窗口内读取最多 5 个候选，以日语脚本／标点密度做保守融合。非日语保持 top-1，旋转、文字块 crop、line 与 perspective 日语复读都启用同一 helper。新增 `scripts/test-v3168-image-japanese-ocr-postprocess-contract.py` 与显式 CI 路由；候选 exact-SHA full `31197172635`、PR #232 fast `31197811891`、merge fast `31197884476`；候选 SHA `9438e3d40ffb133073921fc4f4a0e1de36cc042d` Xcode/JUnit `10/10`，merge SHA `033d66b5c62434e5685b1e8d7d1feebdfa90c15e` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.167 让日语／横排 OCR 的行分组容差随当前 observation 的中位文字框高度变化：以 `median(rect.height) * 0.55` 计算，限制在 `0.012...0.04`，避免不同缩放或字体下同一行被拆开，也避免相邻面板被合并；RTL/LTR 排序和后续 OCR、翻译、导出路径保持不变。新增 `scripts/test-v3167-image-horizontal-band-dynamic-tolerance-contract.py` 并接入显式 CI 路由；候选 exact-SHA full `31195627325`、PR #231 fast `31196179149`、merge fast `31196269343`；候选 SHA `6c63dd0a5170a0fb230046d7d2129b26fd8dbb4d` Xcode/JUnit `10/10`，merge SHA `a0f1e72ea36be0932dae75fe774af1186ed29b1c` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.166 让日语竖排常见 CJK 标点与半角片假名也能作为列方向证据：扩展 `ImageOCRLayoutEngine.cjkCharacterCount` 覆盖 `U+3000–U+303F` 与 `U+FF61–U+FF9F`，但短 observation 仍必须通过列邻居、无横排行邻居和原有几何门控；宽框横排、孤立高框和非竖排语言不变。新增 `scripts/test-v3166-image-japanese-punctuation-column-contract.py`；候选 exact-SHA full `31193812409`、PR #230 fast `31194473761`、merge fast `31194535297`；候选 SHA `8c6dfe278a9644dd0dc37ffa5381a968dc7748c7` Xcode/JUnit `10/10`，merge SHA `0acfd7f62c9ecbe048c7630d0358c85dce325edb` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.165 让允许竖排的 CJK OCR 在 Vision 把日语单字拆成近方形 observation 时仍保留列证据：`ImageOCRLayoutEngine.resolveDirection` 仅在短 CJK、存在列邻居且不存在横排行邻居、几何达到下限时标为竖排，使既有 Koharu 风格 TextBox→crop／line reread 能覆盖单字列；宽框横排、孤立高框、横排行碎片和非竖排语言保持原边界。新增 `scripts/test-v3165-image-japanese-glyph-column-contract.py`；候选 exact-SHA full `31192480905`、PR #229 fast `31193220150`、merge fast `31193292477`；候选 SHA `5f24c4b7d2de47a095ee15b19994087ebde4dff7` Xcode/JUnit `10/10`，merge SHA `631c4d25acacb6b0497e8c95dab41f9a22e6c266` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.164 让日语源图片的混合版面横排 OCR 也显式沿用 Koharu 漫画阅读方向：`ImageOCRLayoutEngine.layout` 增加默认关闭的 `prefersMangaReadingOrder`，日语 Vision 主路径与受限 crop 路径开启后，在同一横排行内保持上到下行序、右到左列序；非日语保持原有左到右默认。新增 `scripts/test-v3164-image-japanese-horizontal-reading-order-contract.py`；候选 exact-SHA full `31190984866`、PR #228 fast `31191645282`、merge fast `31191716497`；候选 SHA `7e584045f12fefa995866b7479db4cd440d52a03` Xcode/JUnit `10/10`，merge SHA `3943843d61f331630f7c6764f5639273aea4bd90` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.163 继续迁移 Koharu `sort_manga_reading_order` 的 Recursive XY-Cut 到普通图片日语竖排布局：按文字块中位宽／高中位数估计动态空白阈值，递归选择横向／纵向最大间隙，右侧面板优先、竖排列自上而下，并保留稳定方向回退；只改 `ImageOCRLayoutEngine` 的 reading order。新增 `scripts/test-v3163-image-japanese-reading-order-contract.py`；候选 exact-SHA full `31189049773`、PR #227 fast `31189799793`、merge fast `31189875449`；候选 SHA `c37808634df8d87cfb9f24c22acadc472f71d3c0` Xcode/JUnit `10/10`，merge SHA `e93f3844c359214c0cdfe09cd609fb11c51b924d` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.162 继续把 Koharu `extract_text_block_regions` 的四点 line-region 透视校正边界迁入普通图片日语竖排 OCR：保留 Vision `VNRectangleObservation` 四角 geometry 与稳定 request-level box，最多 24 条竖排 line crop 使用 Core Image `CIPerspectiveCorrection`、2× Vision 复读，并受单条 4M／总计 16M 输出像素门控；透视失败安全回到既有轴对齐 crop，geometry 穿过整页／局部旋转映射。新增 `scripts/test-v3162-image-japanese-line-perspective-ocr-contract.py`；候选 exact-SHA full `31186264941`、PR #226 fast `31186901253`、merge fast `31186979637`；候选 SHA `8a8e653f953c233f5b0d28249bb9b324ef0baab3` Xcode/JUnit `10/10`，merge SHA `b1c272b9fea90e07967e21db082538be50c8b516` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.161 继续把 Koharu 的 line-region 几何边界迁入普通图片日语竖排 OCR：读取 Vision `VNRecognizedText.boundingBox(for:)` 的整段字符范围作为更紧的 line-region crop hint，保留 request-level box 用于布局／去重，并在整页 90°／270° 与 2× crop 映射路径传递；Vision 不提供范围几何时安全回退原框。新增 `scripts/test-v3161-image-japanese-line-geometry-contract.py`；候选 exact-SHA full `31184241208`、PR #225 fast `31184939184`、merge fast `31185021159`；候选 SHA `9164066706faed78494384d79ec1544d46084c20` Xcode/JUnit `10/10`，merge SHA `1eb026c2ce3df861b6efcc7ae9d61a5d08fd86ea` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.160 继续把 Koharu 的 `extract_text_block_regions` 边界迁入普通图片日语竖排 OCR：在既有竖排 block 中按重叠与纵向候选筛选最多 24 个 line-region proxy，做方向感知扩边、2× crop 复读（`minimumTextHeight=0.002`、关闭自动语言）并把结果按缩放比例映射回原图；无 polygon 时明确这是保守代理，不伪装成 Manga OCR/PaddleOCR 模型。新增 `scripts/test-v3160-image-japanese-line-region-ocr-contract.py`；候选 exact-SHA full `31182335743`、PR #224 fast `31183007517`、merge fast `31183084173`；候选 SHA `68c1c1b8eb1390b808204c3c16b7b1dbc72a28b9` Xcode/JUnit `10/10`，merge SHA `19b018101a4937474e2f3b030a1e24dc58807704` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.159 让普通图片日语源语言的 OCR 识别状态按语言分流：Vision OCR 进行中明确说明正在识别日语文字并复查竖排方向与文字块位置，其他语言保留通用文案；状态继续复用既有 `imageTranslationMessage`，因此图片状态行与 VoiceOver 空态上下文同步，不新增 OCR、翻译或持久化流程。新增 `scripts/test-v3159-image-japanese-ocr-status-context-contract.py`；候选 exact-SHA full `31180141884`、PR #223 fast `31180615748`、merge fast `31180708039`；候选 SHA `f30fbab503ff9c694af0d4f2c123113b1802648d` Xcode/JUnit `10/10`，merge SHA `9c68b5c9f7e5e5d341a3cfaec1f764964b71b9f0` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称日语 OCR、翻译或识别质量提升。

v3.158 继续按 Koharu 的 `TextBoxes → crop_text_block_bbox → OCR` 分层，在普通图片日语竖排候选上增加最多 16 个文字块的 Vision 裁剪复读：按 90°／270° 方向读取、映射回原图、再去重并交给既有布局；新增 `scripts/test-v3158-image-japanese-crop-ocr-contract.py`。候选 exact-SHA full `31178774530`、PR #222 fast `31179342519`、merge fast `31179390133`；候选 SHA `ee21c07d5175b38b41161822043b7ce1bbeea3ff` Xcode/JUnit `10/10`，merge SHA `c940815a43e300685667d8b01888e53af910ec9c` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.157 在 v3.156 的日语方向复查上补齐受限 90°／270° 双向比较：两次 Vision 结果都映射回原图、统一去重，再交给既有日语竖排／右到左布局，新增 `scripts/test-v3157-image-japanese-bidirectional-orientation-ocr-contract.py`；候选 exact-SHA full `31177442783`、PR #221 fast `31177914749`、merge fast `31177971252`；候选 SHA `894c7063e18a6dc40ea047dca015e7cf73af8e65` Xcode/JUnit `10/10`，merge SHA `1266de53935525c1014ec0b4cbecb9b7f20b6e86` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.156 参考 `reference/koharu-main` 的检测／布局与识别分层，在普通图片日语 OCR 中加入受限的 90° 方向复查：使用 `ja-JP/ja/en-US/en` profile、较低文字高度门槛，映射旋转框回原图后去重并交给既有日语竖排／右到左布局；新增 `scripts/test-v3156-image-japanese-orientation-ocr-contract.py` 与 `test/jap.jpg` fixture。候选 exact-SHA full `31176163879`、PR #220 fast `31176662793`、merge fast `31176739499` 均通过；候选 SHA `99a333a8297faf193c8058d7f919626bb17daf80` Xcode/JUnit `10/10`，merge SHA `7750c7e62b82cb952ab302b9afd206ecf15068dd` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，探针默认 skip，不声称日语 OCR、翻译或识别质量提升。

v3.155 让普通图片在没有可显示 OCR 文字块、但当前图片仍可重试且没有待重试语言变更时，在结果空态提供就地“重试当前图片”按钮与同名 VoiceOver action；两者直接复用既有 `store.retryImageTranslation`，当前重试语言已更新时保留上方状态入口，避免重复 action。hint 明确使用当前图片语言重新识别并翻译，源图片不可用或处于其他阶段时不暴露入口。新增 `scripts/test-v3155-image-empty-result-retry-action-contract.py` 并接入 UI/full fail-fast；候选 exact-SHA full `31173412868`、PR #219 fast `31173840102`、merge fast `31173897707`；候选 SHA `a6283b1be84ec4e6b227b6d5fbf74961a4fd108f` Xcode/JUnit `10/10`，merge SHA `2c26886ee6676c549b88ad48b0447e595c636a40` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.154 让普通图片“没有可显示识别结果”空态的可见标题与说明随图片处理状态变化：idle、读取／OCR／翻译进行中、translated 与 failed 分别说明当前阶段和恢复边界；保留既有 VoiceOver label/value/hint、受 `store.canRerunImageRecognition` 门控的“重新识别” action 与可见按钮。新增 `scripts/test-v3154-image-empty-result-state-contract.py`，并同步 v3.133/v3.152/v3.153 历史合同接受 View-only 动态标题／说明；候选 exact-SHA full `31171837188`、PR #218 fast `31172320096`、merge fast `31172393014`；候选 SHA `11028f3de4886aad18e911dd8dc3f60e6593ba9f` Xcode/JUnit `10/10`，merge SHA `b51ab8a880f3a1998a5a4e249e6c7113e0a3c451` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.153 让普通图片翻译已完成且没有可显示 OCR 文字块时的结果空态获得稳定 VoiceOver 焦点 identity：保留源图片且处于 `.translated` 时聚焦可操作的空态，优先级仍是全部忽略空态，其次是翻译完成空态，否则回到图片状态行；焦点请求继续受 `imageTranslationRevision` 与 View 私有 generation guard 约束。新增 `scripts/test-v3153-image-empty-result-focus-contract.py`；候选 exact-SHA full `31170387940`、PR #217 fast `31170963538`、merge fast `31171022668`；候选 SHA `6c838ef220470753cb6abf4867babc48a6ea795c` Xcode/JUnit `10/10`，merge SHA `a938b8b73803e0570e0ec9bb8e6ec354e3cf85b0` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.152 让普通图片在翻译已完成但没有可显示 OCR 文字块时，在结果空态提供可见“重新识别”按钮；仅在既有 `store.canRerunImageRecognition` 为真时显示，直接复用 `store.rerunImageRecognition`，并保留 v3.144 VoiceOver action 与当前图片 Vision OCR／翻译范围提示。新增 `scripts/test-v3152-image-empty-result-rerun-button-contract.py` 并接入 UI/full fail-fast；候选 exact-SHA full `31167004721`、PR #216 fast `31170006883`、merge fast `31170055419`；候选 SHA `0c08bfda4548b996a2e3bad86d2adde950276378` Xcode/JUnit `10/10`，merge SHA `29b42e3d839b5d9f225658fb24e4f88e4ec4d69d` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.151 让 Developer Console 的漫画探针在报告没有逐块文字块时提供就地“重新运行漫画覆盖翻译探针”按钮，并在探针未运行时向 VoiceOver 暴露同名 action；运行中按钮保持 disabled 且不暴露无效 action。入口复用既有 `store.runMangaOverlayProbe`，hint 明确会重新读取 `test/1.png`、清理 Output 并只更新漫画探针诊断，不改变普通图片 OCR、翻译或覆盖图。新增 `scripts/test-v3151-manga-probe-empty-retry-action-contract.py`，并让 v3.56 历史状态合同接受受门控的第二入口；候选 exact-SHA full `31165387991`、PR #215 fast `31165964091`、merge fast `31166051842`；候选 SHA `8579bdabc8f0dbbeafe19b4804cfedcea9dfbe04` Xcode/JUnit `10/10`，merge SHA `79f92aad2a12e682eba609a1f49814025e9920a7` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.150 让普通图片人工修正后的文字块在局部放大预览中提供可见“恢复 Vision OCR”按钮与同名 VoiceOver action；仅在 `isManuallyCorrected && canEdit` 时暴露，直接复用面板现有确认入口，锁定时保留具体禁用 hint。新增 `scripts/test-v3150-image-focus-restore-action-contract.py`；候选 exact full `31163470178`、PR #214 fast `31164127307`、merge fast `31164207376`；候选 SHA `04cef3c01b802627366587dc1a3c76eddc534e3f` Xcode/JUnit `10/10`，merge SHA `b52dabc245f0d140115074b37c34975ce0b743c9` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.149 修正普通图片“本次复查完成”空态的 VoiceOver action 门控：只有 `canReviewImageTranslation` 为真才暴露“重新复查” action，锁定时保留稳定完成上下文、具体 `imageReviewUnavailableDetail` hint 和禁用可见按钮。直接复用 `restartReviewQueue()`，新增 `scripts/test-v3149-image-review-completion-action-gate-contract.py`，并让 v3.128/v3.129/v3.148 历史合同接受同一 View-only helper；候选 exact full `31161816278`、PR #213 fast `31162344568`、merge fast `31162426726`；候选 SHA `b0fc332c565fe501c8e2e939a086b79c142c9853` Xcode/JUnit `10/10`，merge SHA `137d8d61a4c6b10d4126b3ae07e9163edbf07878` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.148 修正普通图片全部 OCR 文字块被忽略时的 VoiceOver 空态门控：只有 `canModifyImageTranslation` 为真才暴露“恢复全部” action，锁定时保留空态上下文、具体 hint 和禁用可见按钮。新增 `scripts/test-v3148-image-ignored-empty-state-action-gate-contract.py`；候选 exact full `31160052402`、PR #212 fast `31160532637`、merge fast `31160619661`；候选 SHA `01231917a86696cfc3a864d9a6382119b8c13455` Xcode/JUnit `10/10`，merge SHA `dc55005a501c4a2f59036de27312cc1163c84b3e` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.147 让普通图片 OCR 修正 sheet 的“修正后的文字”输入框提供明确 VoiceOver label/value/hint，按空文本、实际修改、确认无误与保存中状态说明边界；保存／重译进行中明确输入和“忽略此文字块”不可用，保留当前图片会话与恢复范围。新增 `scripts/test-v3147-image-ocr-correction-input-accessibility-contract.py`；候选 exact full `31158590713`、PR #211 fast `31159215608`、merge fast `31159309690`；候选 SHA `3a60ad6b431acfc11f2296ec59ec86609d107546` Xcode/JUnit `10/10`，merge SHA `7e0bb80e4c3d077d56143d7375b34b6e6ef5ee61` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.146 让普通图片“已忽略 OCR 文字块”行的父级 VoiceOver 容器在可修改状态下提供同名“恢复” action，直接复用既有恢复回调；父级 hint 说明恢复范围与待复查回队列，锁定时不暴露父级 action，保留 44pt 子按钮、禁用原因和焦点 identity。新增 `scripts/test-v3146-image-ignored-row-restore-action-contract.py`；候选 exact full `31157259172`、PR #210 fast `31157792746`、merge fast `31157872257`；候选 SHA `21768ac2c9a9cd5efdb87aebae62def5f1e20071` Xcode/JUnit `10/10`，merge SHA `f850351e67734fb28d1f5672cd55bfd4de28e87f` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.145 让普通图片文件导入的 VoiceOver hint 与照片入口保持一致：无图片时说明从文件首次选择，已有图片时说明更换当前图片并开始新的本机 OCR 与翻译；读取／OCR／翻译进行中继续说明新选择会取消当前任务并开始新任务，文件入口保持可用。新增 `scripts/test-v3145-image-file-selection-replacement-hint-contract.py`；候选 exact full `31155971109`、PR #209 fast `31156530851`、merge fast `31156622662`；候选 SHA `92f495212a40910be1540c06f28cf4402c0b956f` Xcode/JUnit `10/10`，merge SHA `f66f6e2fa7877a212e057648ec18663ae8ba9c83` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.144 让普通图片 OCR 的“没有可显示文字块”空态在已完成且保留源图片时提供受 `canRerunImageRecognition` 保护的同名“重新识别” VoiceOver action，直接复用 Store `rerunImageRecognition()`；提示明确只重跑当前图片 Vision OCR 与翻译，源文件不可重跑时不暴露 action。新增 `scripts/test-v3144-image-empty-result-rerun-action-contract.py`；候选 exact full `31154791726`、PR #208 fast `31155305272`、merge fast `31155356211`；候选 SHA `ff31f66f71698c6f34a7e1b7e52a940485984eea` Xcode/JUnit `10/10`，merge SHA `914bf3d5c35354ce4e23e5f6bc470d6e2ba886f0` 复用候选 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.143 让普通图片 OCR 结果行的 VoiceOver hint 动态列出当前真正暴露的修正、恢复和复查 action；锁定、非人工修正或非风险状态不虚构 action，同时保留定位与几何不可用上下文。新增 `scripts/test-v3143-image-review-row-action-hint-contract.py`；候选 exact full `31153705887`、PR #207 fast `31154097383`、merge fast `31154147898`；候选 SHA `3a6da4be92c3275c2efb31dab550b5a227af5da8` Xcode/JUnit `10/10`，merge SHA `6bd71ae8ed1e34f2ad4ce0675c6b05e2c9e8b936` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.142 让普通图片风险 OCR 结果行在 `isReviewRequired && canReview` 时提供同名“完成并继续复查／撤销本次复查” VoiceOver action，复用既有 `toggleReviewCompletion()`；非风险或锁定时不暴露 action，保留可见复查按钮和禁用原因。新增 `scripts/test-v3142-image-review-row-review-action-contract.py`；候选 exact full `31152734900`、PR #206 fast `31153171846`、merge fast `31153229469`；候选 SHA `e94fb6a1fdfbeaddd64a0995dda5d1e872e91d84` Xcode/JUnit `10/10`，merge SHA `28101601c1ed455950736bcc7ae010ff580f5636` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.141 让已人工修正的普通图片 OCR 结果行在 `isManuallyCorrected && canEdit` 时提供同名“恢复 Vision OCR” VoiceOver action，复用既有 `restoreVisionOCR()`；未修正或锁定时不暴露 action，保留可见恢复按钮和禁用原因。新增 `scripts/test-v3141-image-review-row-restore-action-contract.py`；候选 exact full `31151758844`、PR #205 fast `31152271664`、merge fast `31152319773`；候选 SHA `3a98ecb36bcfef8dfc77823b0eae26b06f0980bd` Xcode/JUnit `10/10`，merge SHA `7b0684765acdf7f79ae01b462b8a0eb1ddaee674` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.140 让普通图片 OCR 结果行在 `canEdit` 时提供同名“修正识别文字” VoiceOver action，直接复用既有 `edit()`；锁定时不暴露 action，保留可见修正按钮、定位／焦点语义和 `modificationUnavailableHint`。新增 `scripts/test-v3140-image-review-row-edit-action-contract.py`；候选 exact full `31150859808`、PR #204 fast `31151298078`、merge fast `31151339355`；候选 SHA `9571f142b35193cc86151b4e2971ccd6becfafbc` Xcode/JUnit `10/10`，merge SHA `44394ba2068f7e173a7ba72223fc4210eda89239` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.139 让图片局部放大容器在对应邻居存在时提供同名“上一个文字块／下一个文字块” VoiceOver action，复用既有 `selectPrevious()`／`selectNext()`；首尾或单项筛选时隐藏不可用 action，保留当前位置 value 和可见按钮 disabled hint。新增 `scripts/test-v3139-image-focus-navigation-action-contract.py`；候选 exact full `31149836170`、PR #203 fast `31150269494`、merge fast `31150318388`；候选 SHA `044e137c1fae3ca08bccbfd2ab37422bdce95c40` Xcode/JUnit `10/10`，merge SHA `8a337da354936c3e7b0327cd154f7519b64fbed3` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.138 让图片局部放大容器在需要复查且 `canReview` 时提供同名“完成并继续复查／重新加入待复查” VoiceOver action，直接复用既有 `toggleReviewCompletion()`；不需要复查或复查被锁定时不暴露 action，父级 hint 说明现有 `reviewUnavailableHint`。新增 `scripts/test-v3138-image-focus-review-action-contract.py`；候选 exact full `31148861374`、PR #202 fast `31149234166`、merge fast `31149285259`；候选 SHA `52e78935232cff22ef4bb45285a45218e6bd1b85` Xcode/JUnit `10/10`，merge SHA `490c98062bd989f09334e202b73942f684c7c5f2` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.137 让图片局部放大容器在 `canEdit` 时提供同名“修正识别文字” VoiceOver action，直接复用既有 OCR 修正入口；锁定时不暴露 action，父级 hint 说明修改不可用原因。新增 `scripts/test-v3137-image-focus-edit-action-contract.py`；候选 exact full `31147358078`、PR #201 fast `31147793085`、merge fast `31147924273`；候选 SHA `46f617f8ce9a78628c4bdef54a800e4a4dc4e5a3` Xcode/JUnit `10/10`，merge SHA `9f8a05bd0b6001e8cfc0a94213cfee8a5f2e3583` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.136 让图片局部放大预览容器提供同名“关闭局部放大” VoiceOver action；可见关闭按钮 hint 明确返回当前文字块结果行，复用既有 View 私有焦点交接。新增 `scripts/test-v3136-image-focus-close-action-contract.py`；候选 exact full `31144595687`、PR #200 fast `31144958126`、merge fast `31144998556`；候选 SHA `0c4bddf96354989d9d2efc445987de3e8a3eafb4` Xcode/JUnit `10/10`，merge SHA `321f0fbcdcea704455174772da486a0a00f04754` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.135 让图片预览加载／失败状态提供动态 VoiceOver hint；失败时明确“重试预览”只重建屏幕预览，加载时说明完成后可定位文字块和失败恢复边界。新增 `scripts/test-v3135-image-preview-status-hint-contract.py`；候选 exact full `31143646549`、PR #199 fast `31144019839`、merge fast `31144057333`；候选 SHA `728b96a06491f41f5a8809fc2649486bdae81444` Xcode/JUnit `10/10`，merge SHA `b45bd83b24a7d021e3f414990ad85be60b494312` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.134 让图片预览失败状态在当前 revision 失败时提供同名 VoiceOver “重试预览” action；可见按钮、状态上下文与 action 共用既有 `retryPreview()`，只重建屏幕预览，不重新 OCR/翻译。新增 `scripts/test-v3134-image-preview-status-retry-action-contract.py`；候选 exact full `31142629553`、PR #198 fast `31142975439`、merge fast `31143030561`；候选 SHA `94e26b435226a966a3c866fa222da92e7eff69c3` Xcode/JUnit `10/10`，merge SHA `730312af21e2bc081d8d414f3d6b28acd0e3277b` 复用 full receipt，readiness `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.133 让普通图片空预览与识别结果空态成为稳定 VoiceOver 上下文，动态说明当前阶段和恢复边界；新增 `scripts/test-v3133-image-empty-result-accessibility-context-contract.py`。候选 exact-SHA full `31140850232`、PR #197 fast `31141276534`、merge fast `31141320676` 均通过；候选 SHA `a1bc1ba4a73d4337e83c2a99f911ce3f709dc207` Xcode/JUnit `10/10`，merge SHA `30678f089d5764b06bdf2d459c55ba2677d872cb` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.132 让普通图片 OCR 在所有文字块都被忽略时提供可操作的空态：空态成为稳定 VoiceOver 上下文，读出忽略数量与恢复边界，并提供同名“恢复全部” action；同一状态只显示一个受 `canModifyImageTranslation`／导出重绘门控的可见恢复按钮，部分忽略状态仍保留下方批量入口。最后一个 block 被忽略后，View 私有焦点在 sheet 收起后交给该空态，终态也保留空态上下文；新增 `scripts/test-v3132-image-ignored-empty-state-action-contract.py`，历史 v3.330/v3.333/v3.335 合同允许这条额外合法焦点目的地。候选 full `31139110055`、PR #196 fast `31139576598`、merge fast `31139633331` 均通过；候选 SHA `012f25ffd7edc4009b33c600bb57d0d6d65005c2` Xcode/JUnit `10/10` 成功，merge SHA `df3f7ecbf59319a0c59d13164bb6b0f9cd8ab553` 复用候选 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.131 让普通图片 OCR 的已忽略文字块区域提供带确认的“恢复全部”操作；按原始顺序恢复快照并保留修正／Vision OCR 元数据，单次重建转录和导出，受 `.translated`／导出重绘门控并把 VoiceOver 焦点交给首个恢复行。新增 `scripts/test-v3131-image-ignored-blocks-bulk-restore-contract.py`。候选 full `31090186819`、PR #195 fast `31137606603`、merge fast `31137651196` 均通过；候选 SHA `26fc6bba61c277747673f9fb29e4a6e1eb849aaf` Xcode/JUnit `10/10`，merge SHA `8514924340486fd86344298f3b9493540fe9ab4c` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.130 让 Developer Console 漫画诊断筛选空态提供可见“显示全部诊断”按钮与同名 VoiceOver action；只改 View 私有 `diagnosticFilter`，保留无文字块探针空态。新增 `scripts/test-v3130-manga-diagnostic-filter-empty-action-contract.py`。候选 full `31088767018`、PR #194 fast `31089351045`、merge fast `31089424245` 均通过；候选 SHA `f3965b43c38b7682decec9aaf46d046cafc3f16f` Xcode/JUnit `10/10`，merge SHA `ed719558db8edca6aff02124517fb0035bc3c458` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.129 让普通图片筛选空态提供可见“显示全部结果”按钮和同一 VoiceOver action，复用 `prepareReviewFilterChange(to: .all, focusID: nil)`；新增 `scripts/test-v3129-image-review-filter-empty-action-contract.py`。候选 full `31087461275`、PR #193 fast `31088057693`、merge fast `31088114103` 均通过；候选 SHA `b4617eacf4ff4ef0c7ccce847fe3742ff7002861` Xcode/JUnit `10/10`，merge SHA `5b2792d065fcb83ec10ccb4c2b55cb5ddab84bc8` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.128 让普通图片复查完成空态成为稳定 VoiceOver 上下文，读出完成／总风险块与当前筛选并提供受 `canReviewImageTranslation` 保护的“重新复查”action，复用 `restartReviewQueue()`；新增 `scripts/test-v3128-image-review-completion-action-contract.py`。候选 full `31085406753`、PR #192 fast `31085987796`、merge fast `31086053876` 均通过；候选 SHA `d31745f8461ec6eeee0e8ce75e5965874a10b7b7` Xcode/JUnit `10/10`，merge SHA `14a11feff4413058f5c02348ce5d16485044702f` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.127 让图片失败状态行按 share/export/image 优先级提供直接 VoiceOver retry action；新增 `scripts/test-v3127-image-failure-status-actions-contract.py`。候选 full `31084281958`、PR #191 fast `31084713250`、merge fast `31084803922` 均通过；候选 SHA `a7ef8ce984234bf4631f206186f70bbec3ff2b64` Xcode/JUnit `10/10`，merge SHA `03d5b82b97c102043a0c262775061401cc38ac8a` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.126 补齐普通图片导出重绘／分享失败后的 VoiceOver 终态焦点：share/export state 进入 `.failed` 时回到图片状态行，运行中变化不抢焦点；新增 `scripts/test-v3126-image-export-share-failure-focus-contract.py`。候选 full `31082994159`、PR #190 fast `31083400009`、merge fast `31083557316` 均通过；候选 SHA `244f97435d340207c7684c3a2ab553b552b3b780` Xcode/JUnit `10/10`，merge SHA `e023e9c016c64a65596fe70151ad6f6a2d6615d9` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.125 修复普通图片直接失败没有 VoiceOver 终态焦点：无新 revision 的文件选择／Pro 门控失败回到图片状态行，revision-scoped OCR／翻译失败保持原路径；新增 `scripts/test-v3125-image-direct-failure-focus-contract.py`。候选 full `31081494834`、PR #189 fast `31081976028`、merge fast `31082019649` 均通过；候选 SHA `1c068d538728a1195fdd08197f16f7e82d06dd4b` Xcode/JUnit `10/10`，merge SHA `9bd54490d09573d351c1e09da148393c3036a20` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.124 修复普通图片清空后的 VoiceOver 焦点断点：仅当 revision 变化后图片数据为空且状态为 idle 时聚焦“等待图片”空态，新图片 loading 不抢焦点；新增 `scripts/test-v3124-image-clear-empty-focus-contract.py`。候选 full `31080208334`、PR #188 fast `31080768687`、merge fast `31080830286` 均通过；候选 SHA `e02b7a6d2c0f41098eb2cf82e6aa97f9b40c1ff9` Xcode/JUnit `10/10`，merge SHA `738f82384e027058ae0e53cad5f7842ce92a010f` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.123 修复普通图片屏幕预览失败／重试后的 VoiceOver 焦点断点：预览状态使用稳定焦点 identity，失败和重试开始时回到状态行；新增 `scripts/test-v3123-image-preview-status-focus-contract.py`。候选 full `31079060685`、PR #187 fast `31079520917`、merge fast `31079590205` 均通过；候选 SHA `92e68b60e74dd61fb471584bba9cf00bf1696868` Xcode/JUnit `10/10`，merge SHA `6309370bc47974964a2aa181075469fb29e928e7` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.122 修复普通图片读取／OCR／翻译取消后的 VoiceOver 焦点断点：仅在状态从 loading／recognizing／translating 转为 idle 时把焦点交给既有“图片翻译状态”行，初始 idle、成功、失败和清空不抢焦点。新增 `scripts/test-v3122-image-cancel-status-focus-contract.py`。候选 full `31077891466`、PR #186 fast `31078311141`、merge fast `31078359581` 均通过；候选 SHA `9c0ed87838d7eb621fb762c67230df74321f5ab6` Xcode/JUnit `10/10`，merge SHA `61401075429df063b7726037b81b91aade9178d3` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.121 让普通图片 OCR 的失败／取消状态在源图片仍可用且没有待重试语言变更时提供受 `canRetryImageTranslation` 保护的“重试当前图片”VoiceOver action；待重试语言状态继续保留单独 action，避免重复入口。新增 `scripts/test-v3121-image-status-retry-accessibility-contract.py`。候选 full `31076710802`、PR #185 fast `31077094866`、merge fast `31077152440` 均通过；候选 SHA `9551c0c53bae0b8816d490a3da03c9472995e859` Xcode/JUnit `10/10`，merge SHA `16caff957c0252c2014750cc6c3d56dfa8463c29` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.120 让普通图片 OCR 的待重试语言状态提供受 `canRetryImageTranslation` 保护的“重试当前图片”VoiceOver action，直接复用 Store retry；新增 `scripts/test-v3120-image-retry-language-accessibility-action-contract.py`。候选 full `31075361390`、PR #184 fast `31075697828`、merge fast `31075745893` 均通过；候选 SHA `25855f7e6b757c2ae901c794c56990215399cb68` Xcode/JUnit `10/10`，merge SHA `205e78e0f7dd39c51a9c8dbf93748c1a5ef0a090` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.119 修复普通图片 OCR 失败／取消后的待重试语言提示 VoiceOver 焦点断点：输入或目标语言实际改变后，既有 retry-language summary 状态行获得 revision-scoped、generation 仲裁的焦点，并用 label/value/hint 说明下一次重试会使用的语言；不改变 OCR、翻译或 Store。新增 `scripts/test-v3119-image-retry-language-focus-contract.py`，候选 full `31074379707`、PR #183 fast `31074819588`、merge fast `31074863470` 均通过；候选 SHA `5248bd705fc6c0d963146060917e2a6a94a6c421` Xcode/JUnit `10/10`，merge SHA `de5ccebdeda7294a491183c53676e65caaaefb6a` 复用 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.118 修复 Developer Console 漫画探针报告到达后的 VoiceOver 焦点优先级：既有 readiness report 阻断真实 Koharu 四件套、契约修正或真实 detector 来源声明时，先聚焦“Koharu 工件就绪状态”，筛选切换仍回到逐块结果。新增 `scripts/test-v3118-manga-koharu-readiness-focus-contract.py`，并让 v3.336 历史合同接受共享焦点 binding 的等价初始化。该 View/report-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、probe_report、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31073337578`、PR #182 fast `31073688262`、merge fast `31073828173` 均通过；候选 SHA `067077bcb146e2e0c8bb2e066350cac2466b7460` Xcode/JUnit `10/10` 成功，merge SHA `036bada7aebd70da50d6d31cc4e4da2cce4b8dda` 复用候选 full receipt，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.117 修复 Developer Console 漫画探针诊断筛选切换后的 stale 展开状态：筛选变化先递增 View 私有 expansion reset token，逐块行收起旧详情并抑制 reset 触发的焦点回抢，再由 generation requester 聚焦新筛选首项。新增 `scripts/test-v3117-manga-diagnostic-filter-expansion-reset-contract.py` 并接入 UI/full fail-fast。该 View/report-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、probe_report、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31072107788`、PR #181 fast `31072405558`、merge fast `31072447592` 均通过；候选 SHA `4bda6f5a0ea20e7d8223546d0b380758d839c253` 的 Xcode/JUnit `10/10` 成功，readiness 仍未提供真实工件，不声称质量提升。
v3.116 修复 Developer Console 漫画探针诊断焦点的多入口竞态：`MangaProbeSection` 统一用 View 私有 request generation 仲裁筛选、重跑终态和逐块展开／收起的 VoiceOver handoff，最新请求才会生效；新探针 loading 会使旧请求失效。新增 `scripts/test-v3116-manga-diagnostic-focus-generation-contract.py` 并接入 UI/full fail-fast，同时让 v3.113 历史合同接受等价共享 requester。该 View-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、probe_report、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31071423891`、PR #180 fast `31071714254`、merge fast `31071752236` 均通过；候选 SHA `4788bb213eeff010775a35798dc6fb28aabb7c0c` 的 Xcode/JUnit `10/10` 成功，readiness 仍未提供真实工件，不声称质量提升。
v3.115 修复普通图片 OCR VoiceOver 焦点请求竞态：View 私有 request generation 与 imageTranslationRevision 双重 guard 只允许最新焦点请求生效，新图片会使旧请求失效。新增 `scripts/test-v3115-image-focus-request-generation-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31070650744` 与 `31070655111`、PR #179 fast `31070940503`、merge fast `31070976672` 均通过；候选 SHA `50029c449f86fbf4f0d43e7e35967a0c1e0ec8ce` 的 Xcode/JUnit `10/10` 成功，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.114 继续隔离漫画探针逐块诊断展开状态：新探针 loading 时由 View 私有 reset token 收起旧详情，并抑制 reset 触发的旧焦点 handoff；结果行 VoiceOver 明确展开／收起状态和下一步。新增 `scripts/test-v3114-manga-diagnostic-expansion-state-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31069913494` 与 `31069918901`、PR #178 fast `31070264175`、merge fast `31070323190` 均通过；候选 SHA `e62a168e57e2f2cbc6cb47b5a8c8dde571d73f42` 的 Xcode/JUnit `10/10` 成功，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.113 修复 Developer Console 漫画探针逐块诊断展开后的 VoiceOver 焦点断点：展开后聚焦稳定的“详细诊断”容器，收起后回到原文字块结果行；继续复用既有 report-only 风险、下一步和执行边界上下文。新增 `scripts/test-v3113-manga-diagnostic-expansion-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不重跑探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31068769954` 与 `31068778764`、PR #177 fast `31069311041`、merge fast `31069349841` 均通过；候选 SHA `fd2cf8d32b9576dc2620ce3c281403421aa1ca02` 的 Xcode/JUnit `10/10` 成功，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.112 修复普通图片 OCR 新图／重试终态的 VoiceOver 焦点断点：新的 `imageTranslationRevision` 进入 `.translated` 或 `.failed` 后，有 blocks 时聚焦当前筛选的首个 OCR 结果，没有 blocks 时聚焦动态图片状态行；revision guard 阻止旧任务抢回焦点。既有筛选、选中项、修正 sheet 和复查焦点边界保持不变。新增 `scripts/test-v3112-image-translation-terminal-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31067968394`、PR #176 fast `31068324104`、merge fast `31068365757` 均通过；候选 SHA `a150982ab83dac47000bb6bce34caa9aa74ecf26` 的 Xcode/JUnit `10/10` 成功，merge SHA `f467a72a3de9d5ab6e876a51e228e19e037f4174` 复用候选 full receipt，探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.111 修复 Developer Console 漫画探针重跑后的 VoiceOver 焦点断点：新报告有 blocks 时聚焦当前筛选的首个诊断结果，没有 blocks 时聚焦“未生成逐块诊断”并保留 test/1.png、Output 清理与重试边界；loading 会先清除旧诊断焦点。新增 `scripts/test-v3111-manga-probe-terminal-focus-contract.py` 并接入 UI/full fail-fast。该 View-only 改动不新增 Store／持久化、不重新运行探针、不改变 OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31067283530`、PR #175 fast `31067583454`、merge fast `31067629934` 均通过；候选 SHA `70222e95035c7cd5a71799735e11539f755b5d08` 的 Xcode/JUnit `10/10` 成功，merge SHA `9a28692cebf3360ca00b5e0b7f77463f103bdaa2` 复用候选 full receipt，探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.110 修正普通图片 OCR 筛选焦点的意图竞争：用户切换筛选时仍聚焦第一个可见结果行；开始／重启复查、恢复忽略 block、预览直接选中和完成／撤销复查的程序化筛选变化保留明确的行／预览／完成态焦点，revision 重置清除旧意图并保留空态回退。新增 `scripts/test-v3110-image-filter-focus-intent-contract.py`，并同步 v3.15/v3.16/v3.17/v3.29/v3.81/v3.93 历史合同接受 helper。候选 full `31066203170`、PR #174 fast `31066589776`、merge fast `31066628727` 均通过；候选 SHA `43e75f22be1f1acc55045942f9c617bb0e4675e9` 的 Xcode/JUnit `10/10` 成功，merge SHA `08577f31c3dcd3da09ef64c6d9aa050e8d639794` 复用候选 full receipt，探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
v3.109 让普通图片 OCR 的识别结果筛选在切换类别后将 VoiceOver 焦点交给第一个可见 OCR 结果行；继续保留隐藏选中项清理、revision-scoped 焦点和无结果空态回退，焦点仍为 View 私有。新增 `scripts/test-v3109-image-filter-result-focus-contract.py`。候选 full `31064198524`、PR #173 fast `31064487760`、merge fast `31064532453` 均通过；探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.108 让 Developer Console 漫画诊断筛选在切换到有结果的类别后，将 VoiceOver 焦点交给第一个可见诊断结果行；筛选仍只读既有报告，空筛选继续交给可操作空态，焦点 identity 保持 View 私有。新增 `scripts/test-v3108-manga-filter-result-focus-contract.py`，历史 v3.399 报告交接合同改为接受额外 View-only 行上下文。候选 full `31063355633`、PR #172 fast `31063761078`、merge fast `31063805810` 均通过；候选 SHA `c6bee294a7a5e59b51e39aa3b27cc09c5a913616` 的 Xcode/JUnit `10/10` 成功，merge SHA `859bac7b6e47f31ab3b14220737fe3d9c4048a07` 复用候选 full receipt，探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.107 让普通图片 OCR 与 Developer Console 漫画探针筛选器在结果为空时把 VoiceOver 焦点交给可操作的空态：明确当前筛选、显示 `0 / 总数` 和恢复路径；图片筛选切换与漫画诊断筛选切换都保持 View-only，不新增 Store／持久化、不改变 OCR、翻译、renderer/export、探针报告、Koharu active gate、metrics 或 `output`。候选 full `31020576411`、PR #171 fast `31062338507`、merge fast `31062372361` 均通过；候选 full `f513bcd71f48ec34c2820941b86da7093bc86761` 的 Xcode/JUnit `10/10` 成功，merge SHA `0e7e692543aa34f8fb368cea766fb82391295929` 复用候选 full receipt，探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。
本文是 AITRANS 的核心入口记忆、项目总览、硬规则和多 Agent 工作流。保持精简；历史细节看 `update_log.md`，当前架构看 `md/flow/flow.md`，测试选择看 `md/test/test.md`。

## 0. 角色召唤和身份标识
- 用户消息以 `agenta`、`a:` 或 `A:` 开头，表示召唤 Agent A。
- 用户消息以 `agentb`、`b:` 或 `B:` 开头，表示召唤 Agent B。
- 用户消息以 `agentc`、`c:` 或 `C:` 开头，表示召唤 Agent C。
- 用户消息以 `agentx`、`x:` 或 `X:` 开头，表示召唤 Agent X；Agent X 面向大目标，自动循环调度 Agent A/B/C 和并发子 agent 协作推进、验证、修复与再迭代，单轮最多开启 6 个子 agent，并及时重分配任务以加速收口。
- 没有这些前缀时，按普通 Codex 任务处理；若任务需要 A/B/C 边界，先提醒用户指定角色或说明本轮按普通任务执行。
- Agent A 最终回复第一行必须写：`我是 Agent A。`
- Agent B 最终回复第一行必须写：`我是 Agent B。`
- Agent C 最终回复第一行必须写：`我是 Agent C。`
- Agent X 最终回复第一行必须写：`我是 Agent X。`

## 1. 项目核心事实
v3.109 的普通图片 OCR 筛选焦点只使用 `AccessibilityFocusState`：筛选切换后从 `visibleImageTranslationBlocks` 取第一个 block，并通过既有 revision-scoped focus helper 聚焦其结果行；无结果时继续走 v3.107 空态，隐藏选中项清理和复查状态保持不变。该 View-only 改动不新增 Store／持久化、不改变 Vision OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。候选 full `31064198524`、PR #173 fast `31064487760`、merge fast `31064532453` 均通过，不声称质量提升。

v3.108 的漫画诊断筛选结果焦点只使用 `AccessibilityFocusState`：筛选变为非空时在下一次主线程让渡后聚焦第一个可见 block，空筛选仍聚焦可操作空态；`MangaProbeBlockRow` 接收既有 report 与 View-only focus binding，不新增 Store／持久化、不运行探针、不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。候选 full `31063355633`、PR #172 fast `31063761078`、merge fast `31063805810` 均通过，真实四件套仍缺失，不声称质量提升。

v3.106 让普通图片 OCR 与 Developer Console 漫画探针筛选器的 VoiceOver value 统一读出当前筛选类别、显示数量和总数量；图片筛选在有风险块时额外读出已复查／剩余数量。该 View-only 改动不新增 Store／持久化、不改变 OCR、翻译、renderer/export、探针报告或 Koharu active gate。候选 full `31017118790`、PR #170 fast `31017809552`、merge fast `31017909329` 均通过；候选 full Xcode/JUnit `10/10` 成功，PR/merge fast 复用 full receipt，探针默认 skip，真实四件套仍缺失，不声称质量提升。

v3.105 让 Developer Console 漫画探针诊断总览只读消费既有收敛报告的闭环快照：汇总开放／已闭环／要求停止工单、block path 与 work-item ledger 规模、状态 breakdown 及真实外部工件边界，并把同一快照用于状态 tone、可复制 summary 和 VoiceOver。存在开放或阻断收敛项时保持 warning/仅报告，不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31015086472`、PR fast `31015765732`、merge fast `31015838087` 均通过；候选 full Xcode build 成功且 JUnit `10/10`，PR/merge fast 复用 full receipt，探针默认 skip，真实四件套仍缺失，不声称质量提升。

v3.104 让 Developer Console 漫画探针逐块结果只读消费既有 `MangaKoharuArtifactConvergenceReport` 的 block path、开放工单与执行边界：结果行、可复制 action summary 和 VoiceOver 共用首阻断、结构瓶颈、真实工件等待、工单状态及 CI-fast/full/外部工件边界；存在未闭环工单或 report-only 边界时统一显示 warning/仅报告，不再把收敛未完成误读为成功。该 View/report-only 改动不新增 Store／持久化、不运行第二次探针、不读取 ground truth，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 full `31013385953`、PR fast `31014071238`、merge fast `31014141913` 均通过；候选 full Xcode build 成功且 JUnit `10/10`，PR/merge fast 复用 full receipt，探针默认 skip，真实四件套仍缺失，不声称质量提升。

v3.103 让 Developer Console 漫画探针诊断总览只读消费既有 native promotion、artifact contract dry-run、artifact identity reconciliation 与 convergence 字段，显示晋级 verdict、dry-run/active export 边界、真实工件与 CI manifest 身份对账、停止本地调参与未闭环工单，并同步进入视觉、可复制摘要和 VoiceOver；不新增 Store／持久化、不运行探针或读取 ground truth，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。候选 full `31011231211`、PR fast `31011777761`、merge fast `31011846424` 均通过；探针默认 skip，真实四件套仍缺失，不声称质量提升。

v3.102 让 Developer Console 漫画探针逐块结果只读消费既有 pipeline resolver、work-order、外部工件请求包与 native replay 字段，显示执行项、首阻断、CI-fast/full/外部工件门、禁止本地调参与 shadow-only 边界，并同步进入视觉与 VoiceOver；不新增 Store／持久化、不运行探针或读取 ground truth，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。候选 full `31009117560`、PR fast `31009686004`、merge fast `31009749466` 均通过；探针默认 skip，真实四件套仍缺失，不声称质量提升。

v3.101 让 Developer Console 漫画探针逐块结果只读消费既有 bottleneck、model-floor、render-fit 与 artifact-DAG 字段，显示诊断依据并同步进入 VoiceOver；不新增 Store／持久化、不运行探针或读取 ground truth，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。候选 full `30993659770`、PR fast `30994207268`、merge fast `30994272480` 均通过；探针默认 skip，真实四件套仍缺失，不声称质量提升。

v3.100 让 Developer Console 漫画探针逐块结果只读消费既有 internal/floor/render/DAG report ledger，显示本块推荐下一步及真实 Koharu 工件门控上下文，并同步进入 VoiceOver；不新增 Store／持久化、不运行探针或读取 ground truth，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。候选 full `30992318412`、PR fast `30992932438`、merge fast `30993004271` 均通过；探针默认 skip，真实四件套仍缺失，不声称质量提升。

v3.99 让 Developer Console 漫画探针逐块结果接收当前只读 `MangaOverlayProbeReport`，显示共享 OCR／翻译／布局风险标签，并将同一上下文提供给 VoiceOver；不新增 Store／持久化、不运行探针或读取 ground truth，不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。候选 full `30991030339`、PR fast `30991418709`、merge fast `30991478674` 均通过；探针默认 skip，真实四件套仍缺失，不声称质量提升。

AITRANS 是 SwiftUI iOS 本地 AI 翻译原型。当前重点是漫画截图 OCR、本地翻译、覆盖合成和探针诊断链路。

- 默认 `MockGemmaService` 用于 UI 和数据流冒烟。
- `Local` 模式通过 `GemmaLocalService` + `LlamaRuntime` + `llama.cpp` 加载 GGUF。
- 当前内置最小模型是 `Gemma 3 270M IT QAT Q4_0`，适合验证下载、加载、接口和闪退风险，不适合作为翻译质量基准。
- 更强小模型对比可以考虑 `Qwen2.5-0.5B-Instruct-GGUF q4_k_m`，但不要在没有任务要求时擅自更换模型。
- GGUF 不进仓库。云端手动探针从 Release `model-gemma-3-270m-it-qat-q4_0-v1` 下载并缓存 `gemma-3-270m-it-qat-Q4_0.gguf`，按 SHA256 校验后导入模拟器 App 沙盒。
- 当前正式版本号 `3.153`：普通图片 OCR 的 blocks 会在逐块翻译期间继续供用户查看、定位和局部预览，但所有会改变当前结果或复查进度的动作只在 `imageTranslationState == .translated` 开放。`ImageTranslationPanel` 的 `canModifyImageTranslation` 还会在导出重绘期保持禁用，统一保护 OCR 修正、Vision OCR 恢复、已忽略 block 恢复与旁贴／覆盖方式；`canReviewImageTranslation` 保护开始／继续／重启复查及行／局部预览的完成／撤销。Store 的 mark／reopen／reset 同样拒收未完成状态，成功 OCR 修正会先恢复 translated 再沿用既有自动复查，避免 blocks 在逐块翻译中已可见时提前写入会话进度。v3.42 在仍有实际 block 而操作被状态门锁住时显示 View 私有警示状态行；覆盖方式、开始／重启复查、局部预览与结果行的修正／恢复／复查及忽略 block 恢复，都会复用 `imageModificationUnavailableDetail` 或 `imageReviewUnavailableDetail` 作为 VoiceOver 的具体禁用原因。v3.43 继续保留局部预览前后按钮的 disabled 边界，并让可用状态读出“定位上一个／下一个文字块”、首尾状态读出“当前已是筛选结果中的第一个／最后一个文字块”；结果行主定位提示按 `isSelected` 在“取消此文字块在图片中的定位”和“在图片预览中定位此文字块”之间切换。v3.44 让同一对前后按钮再读出 `navigationPositionAccessibilityValue`：有位置时为“当前位置 (positionText)”（如 `1 / 3`），无位置时为“未显示筛选位置”，不新增 Store 状态。v3.45 让完整图片预览中的 OCR 覆盖块也复用结果行的定位提示：已定位时读出“取消此文字块在图片中的定位”，未定位时读出“在图片预览中定位此文字块”，保持图片入口和列表入口语义一致。v3.46 让图片预览加载与失败状态提供稳定的 VoiceOver label/value，重试提示明确只重建屏幕预览、不重新 OCR 或翻译。v3.47 让图片命令栏的照片／文件、取消、重试、重新识别、导出与清空操作提供作用域明确的 VoiceOver hint，照片选择器会按是否已有图片动态区分首次选择与替换；v3.48 让完整图片预览提供稳定的容器 label/value/hint，汇总识别块数量、待复查数量和当前定位位置，并把重复朗读的原始背景图设为无障碍隐藏；这些只是 View 语义，不新增 Store 或持久化状态。它覆盖 loading／recognizing／translating／failed 和导出重绘，并明确逐块翻译仍可查看／定位；本版不改变 Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径、ground truth、metrics 或 `output`，不能作为 OCR、翻译、识别或 Koharu 质量提升证据。v3.37–v3.40 的规范化无语义关闭、弃改保护、确认无误分流、键盘／滚动收起、保存期输入锁定和 revision-scoped 关闭后焦点交接继续保留。v3.36 的开发控制台 readiness 摘要仍只读已有报告；缺真实四件套时仍显示 `manifestMissing / stopUntilArtifactsProvided`。v3.26 CI receipt 传播规则不变，artifact 继续记录父 SHA、state 和元数据判定；传播路径不是新的 Swift/Xcode 编译证据。v3.49 让图片输入语言与目标语言菜单的 VoiceOver hint 按运行中、Pro 门槛、无图片、已完成和失败／取消重试状态分流：运行中明确需完成或取消，Pro 锁定说明不会污染文本页语言，已完成输入语言说明会重新识别和翻译、目标语言说明会重新翻译当前图片，失败／取消说明下一次重试使用新语言；选回当前内容语言会撤销待重试差异。v3.50 让照片与文件导入按钮在读取、OCR 或翻译进行中明确说明选择新图片会取消当前任务并开始新的本机 OCR 与翻译，同时保持有意设计的替换入口、Store run-id 隔离与现有 import controls 可用。v3.51 让图片状态行成为单一的 VoiceOver 状态元素，动态读出当前阶段、逐块进度和下一步操作，并区分载入／Vision OCR／翻译、导出重绘、分享准备、失败和完成状态。该改动只改善 View 语义，不新增 Store／持久化状态，也不改变 OCR、翻译、renderer/export、探针或质量基线。v3.52 让图片结果行的 VoiceOver value 在定位状态之外读出 OCR 置信度、低置信／方向待定、人工修正、复查进度和等待翻译，并继续沿用定位 hint；只改善 View 语义，不新增 Store／持久化或改变 OCR、翻译、renderer/export、探针和质量基线。v3.53 让已忽略 OCR 文字块恢复行成为稳定的 VoiceOver 上下文，读出不在图片预览／导出／转录、是否保留现有译文和恢复是否可用，同时保留恢复按钮的禁用原因与焦点交接；只改善 View 语义，不新增 Store／持久化或改变 OCR、翻译、renderer/export、探针和质量基线。v3.54 修复图片状态行 VoiceOver value 的实际实现回归：使用动态 `statusTitle`／`statusDetail` 插值，而不是字面量，保证当前阶段、逐块进度、失败、导出重绘和完成详情能被实时朗读；同步强化 v3.51 回归合同，仍不新增 Store／持久化或改变 OCR、翻译、renderer/export、探针和质量基线。v3.55 让开发控制台的 Koharu readiness 状态成为稳定的 VoiceOver 上下文，读出 verdict、缺失四件套、下一步和 shadow-only 边界；缺少工件时明确给出 `test/koharu_artifacts/` 与四个文件名。该改动只改善开发者操作与诊断可理解性，不新增 Store／持久化，不调用第二次探针，不改变 active artifact gate、普通图片 OCR、翻译、renderer/export、Koharu 主路径或质量基线。v3.56 让漫画覆盖翻译探针状态行成为单一、稳定的 VoiceOver 上下文，按载入、Vision OCR、翻译、绘制、完成和失败读出状态详情；运行按钮明确 bundle 内 `test/1.png`、Output 诊断文件和只影响漫画探针的边界。该改动只改善开发者操作语义，不新增 Store／持久化，不改变普通图片 OCR、翻译、renderer/export、Koharu 主路径或质量基线。v3.57 让开发控制台的漫画探针逐块结果成为稳定的 VoiceOver 诊断上下文，按 block index 读出 PASS/FAIL、OCR 原文、旋转角度、置信度、质量标签、译文与失败详情；展开提示明确只属于漫画探针，不改变普通图片 OCR、翻译或覆盖图。该改动只改善开发者操作语义，不新增 Store／持久化状态、不读取 ground truth 作为候选、不改变漫画探针诊断、renderer/export、Koharu 主路径或质量基线。v3.58 让图片复查结果行提供稳定的 VoiceOver label/value：label 明确“图片文字块 + OCR 原文”，value 在等待翻译时读出等待状态、完成后读出译文，并为空 OCR 提供稳定回退；只改善 View 语义，不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。v3.59 让完整图片预览的覆盖文字块与图片复查结果行共用稳定的 VoiceOver label“图片文字块 + OCR 原文”，空 OCR 回退为“空”，并保留既有等待翻译／译文 value、定位 hint 和选中状态；该改动只改善 View 语义，不新增 Store／持久化状态，不改变选择、Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。 v3.60 让完整图片预览的覆盖文字块与图片复查结果行对齐 VoiceOver value：读出 OCR 置信度（clamp 到 0–100%）、人工修正、低置信／方向待定、待复查／本次已复查及等待翻译／译文；相邻与替换模式共用这套上下文，只改善 View 语义，不新增 Store／持久化状态，不改变选择、Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。 仓库尚无真实 Koharu 四件套、Speech 音频或真实竖排图片 corpus，不声称 OCR、翻译或识别质量提升。

- v3.81 结果行或完整图片覆盖块选中 OCR block 后，将 VoiceOver 焦点交给对应局部预览；取消定位时回到对应结果行。该 View-only handoff 只在 AccessibilityFocusState 中运行，不进入 Store、OCR、翻译或 renderer/export 链路。
- v3.82 修正漫画覆盖探针失败 fallback 的显式换行布局测量：`wrappedLines` 按段落保留显式换行和空行，使 fit plan 与实际绘制共享垂直预算；新增 `scripts/test-v382-manga-render-newline-contract.py`。该诊断性修复只影响漫画探针失败覆盖的布局可观测性，不改变 OCR 候选、翻译模型／prompt、ground truth、Koharu active artifact gate、普通图片 OCR 主路径、metrics 或 output，也不代表 OCR、翻译或 Koharu 质量提升。
- v3.83 让 `makeKoharuRenderSpriteFitPlannerReport` 复用 `wrappedLines` 的显式换行／空行预算，并把实际 `renderTextTruncated` 纳入失败 fallback 风险；新增 `scripts/test-v383-koharu-fit-budget-contract.py`。该 report-only 修复只校正 Koharu 诊断可观测性，不改变 OCR 候选、翻译模型／prompt、ground truth、生产 renderer/export、active artifact gate、普通图片 OCR、metrics 或 output，也不代表质量提升。full `30870546266`、同 SHA ci-fast `30870974176`、PR #147 fast 和 merge fast `30871766042` 均通过；block 5 的报告从旧误报 fit 变为 12 行 overflow/long-text risk，实际探针仍保留截断。
- v3.84 让 `makeKoharuRenderSpriteFitPlannerReport` 复用既有 `renderMinFontSizeReached` 证据：逐 block ledger、decision signal、汇总 `renderMinFontSizeReachedBlocks` 与 `G-render-sprite-fit-min-font-evidence` report-only gate 保持一致；新增 `scripts/test-v384-koharu-render-min-font-contract.py`，并同步 v3.82/v3.83 历史合同和 CI 路由接受后续正式版本。该诊断性改动不改变 OCR 候选、翻译模型／prompt、ground truth、生产 renderer/export、Koharu active artifact gate、普通图片 OCR、metrics 或 output，也不代表质量提升。full `30873895093`、同 SHA ci-fast `30874183417`、PR #148 fast `30874885165`、merge fast `30874929145` 均通过；探针报告只作为诊断证据，block 5 记录最小字号压力与实际截断。
- v3.85 让 `MangaKoharuRenderRegressionLockReport` 汇总既有 block/render lock 的 `renderMinFontSizeReached`，并把该信号写入每个 render-lock decision trace、`renderMinFontSizeReachedBlocks`、`G-render-min-font-evidence` report-only gate 和开发者摘要；新增 `scripts/test-v385-koharu-render-lock-min-font-contract.py`，同步历史 v3.82–v3.84 合同与 CI 路由接受后续正式版本。该诊断性改动不改变 OCR 候选、翻译模型／prompt、ground truth、生产 renderer/export、Koharu active artifact gate、普通图片 OCR、metrics 或 output，也不代表质量提升。full `30875436621`、同 SHA ci-fast `30875716372`、PR #149 fast `30876264990`、merge fast `30876301079` 均通过；block 5 的最小字号压力与实际截断仍只是诊断证据。
- v3.61 让图片复查结果行和完整图片预览的 VoiceOver 上下文消费既有 Vision OCR 方向证据：已判定的横排／竖排与有限方向置信度会进入 value，结果行显示已知方向，OCR 置信度显示安全夹到 0–100%；只改善 View 语义，不新增 Store／持久化状态，不改变 OCR、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。
- v3.62 让图片识别结果摘要复用既有 `ImageOCRResultSummary.horizontalBlockCount` 与 `verticalBlockCount`，在摘要中显示横排／竖排数量；不新增 Store／持久化状态，不重新运行 OCR／翻译，不改变 renderer/export、漫画探针、Koharu 主路径或质量基线。
- v3.63 让“识别结果”摘要成为单一 VoiceOver header，复用既有摘要 value，并按无图片、翻译未完成、没有待复查块和可复查状态给出下一步 hint；不新增 Store／持久化状态，不重新运行 OCR／翻译，不改变 renderer/export、漫画探针、Koharu 主路径或质量基线。
- v3.64 统一图片 OCR 置信度安全边界：`ImageOCRResultSummary`、布局引擎、结果行和覆盖层把非有限值回退为 0、越界值夹到 0–1；无效置信度继续进入低置信复查，不得污染平均值或触发百分比转换崩溃。该修复不改变 OCR 候选、翻译、renderer/export、漫画探针或 Koharu 主路径。
- v3.69 在图片结果行和完整图片预览的 VoiceOver 摘要中提前报告无效或过期 OCR 框的“定位不可用”数量；结果行保留 OCR 修正和切换文字块入口，并以位置不可用图标提供可见提示。该版本只消费 `NormalizedImageRect.normalizedToUnit()` 的既有边界，不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、漫画探针、Koharu、ground truth、metrics 或 output；真实 Koharu 四件套、Speech corpus 与真实竖排图片 corpus 仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。
- v3.70 让完整图片预览的 VoiceOver hint 按无效或过期 OCR 框数量分流：有效文字块才说明可打开局部放大，异常文字块明确局部预览不可用；保留 OCR 修正、切换文字块和既有状态门。该版本只消费同一 `NormalizedImageRect.normalizedToUnit()` 边界，不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、漫画探针、Koharu、ground truth、metrics 或 output；真实 Koharu 四件套、Speech corpus 与真实竖排图片 corpus 仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。
- v3.71 让开发控制台的 Koharu readiness 摘要显示坐标、mask payload、mask 拓扑和工件身份门控，并在 VoiceOver/可复制摘要中说明阻塞项与 CI 对账要求；只读既有 report，不创建或修改 active `test/koharu_artifacts`，不放宽 readiness gate，不改变普通图片 OCR、翻译、renderer/export、漫画探针或 Koharu 主路径。真实四件套、Speech corpus 与真实竖排图片 corpus 仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。
- 当前 App bundle ID 是 `com.local.aitransform114`；云端探针必须从构建产物 `Info.plist` 动态读取，禁止在 workflow 再硬编码。
- 当前可信基线以 `update_log.md`、`metrics/version_history.csv`、最新 `output/probe_report.json` 和 `output/clean_text_diagnostic.json` 为准，不在本入口长篇复制指标。

- v3.65 让图片 OCR 修正 sheet 的低置信度提示复用 ImageOCRResultSummary.normalizedConfidence，非有限／越界值在百分比格式化前安全归一化；历史 v3.47–v3.64 图片合同同步接纳后续正式版本。该改动只改善 View 语义，不新增 Store／持久化状态，不改变 OCR、翻译、renderer/export、漫画探针或 Koharu 主路径。
- v3.66 对 Vision OCR bounding box 做有限、正面积、整矩形单位坐标归一化，布局引擎过滤 NaN/∞、零面积和完全越界 observation，避免异常 geometry 进入覆盖、定位或阅读排序；新增 geometry evaluator，历史 v3.47–v3.65 图片合同同步接纳后续正式版本。该安全边界不改变 OCR 候选、翻译、renderer/export、漫画探针或 Koharu 主路径。
- v3.67 将 finite／正面积／单位空间整矩形边界复用到 Codable `NormalizedImageRect` 的图片预览、局部定位和导出 renderer；旧会话或外部解码的异常框跳过显示/绘制，不新增 Store／持久化状态，不改变 OCR 候选、翻译、漫画探针或 Koharu 主路径。历史 v3.47–v3.66 图片合同同步接纳后续正式版本。
- v3.68 让无效或过期 OCR 框的局部放大明确显示“局部预览不可用”，不再把整图误作当前文字块；保留关闭、编辑 OCR 原文和切换文字块入口，并用 VoiceOver hint 说明边界。图片 OCR 修正对照仍可编辑，不新增 Store／持久化状态，不改变 Vision OCR、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。

v3.86 修正漫画探针 render-lock 输出工件 ledger 的时序误报：`probe_report.json` 在最终重写 `1_ocr_probe_text.txt` 前组装，现将两者标记为 planned final write，避免实际非空 OCR 文本被错误记录为 `presentButEmptyOrUnchecked`，并让 `coreOutputFilesNonEmpty` 与 `G-render-core-png-retained` 反映真实探针结果；失败的最终写入仍会中止成功报告持久化。新增 `scripts/test-v386-koharu-render-output-ledger-contract.py`，历史 v3.82–v3.85 合同同步接受后续正式版本。该 report-only 修复不改变 OCR 候选、翻译模型／prompt、ground truth、生产 renderer/export、普通图片 OCR、Koharu active artifact gate、metrics 或仓库 output，也不代表 OCR、翻译或 Koharu 质量提升。full `30876931497`、同 SHA ci-fast 探针 `30877262645`、PR #150 fast `30877905238`、merge fast `30877976728` 均通过；ci-fast 13 blocks 的 block 5 仍真实记录文字截断，active Koharu gate 仍为 `manifestMissing / stopUntilArtifactsProvided`。

v3.87 修正漫画探针 render-lock 输出检查的推荐动作：`plannedFinalReportWrite` 与 `plannedFinalOCRTextRewrite` 既然表示最终写入已计划且 `nonEmpty=true`，就必须显示 `keepReportOnly`；只有缺失或未检查输出才显示 `inspectRenderOutputExport`。新增 `scripts/test-v387-koharu-render-output-action-contract.py` 并接入 Koharu changed-file/full 静态路由。候选 full `30878519259`、同 SHA ci-fast `30878916261`、PR #151 fast `30879584339`、merge fast `30879645680` 均通过；这只是 report-only 诊断修正，不改变 OCR、翻译、renderer/export、Koharu active artifact gate、metrics 或 output。探针仍保留 block 5 截断，真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失。

- v3.88 对齐 Koharu render-lock 核心输出 gate：required ci-fast 输出全部 retained 且 non-empty 时，`G-render-core-png-retained` 与 `outputFileChecks` 统一为 `keepReportOnly`；缺失或空输出才使用 `inspectRenderOutputExport`。新增 `scripts/test-v388-koharu-render-core-output-gate-action-contract.py`，同步 v3.82–v3.87 历史合同与 CI 路由。该 report-only 修复不改变 OCR、翻译、renderer/export、Koharu active artifact gate、metrics 或 output。候选 full `30880132762`、同 SHA ci-fast `30880751340`、PR #152 fast `30881554748`、merge fast `30881613654` 均通过；block 5 截断仍被诚实保留，真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，不声称质量提升。

- v3.89 让开发控制台的 `outputFiles` 摘要显示 required 输出的 `recommendedAction` breakdown，并与 `G-render-core-png-retained`／`outputFileChecks` 共用既有报告状态；新增 `scripts/test-v389-koharu-render-output-summary-action-contract.py` 与 CI 路由。该 report-only UX 不新增 Store／持久化，不改变 OCR、翻译、renderer/export、Koharu active artifact gate、metrics 或 output。候选 full `30882033347`、同 SHA ci-fast `30882428016`、PR #153 fast `30883254522`、merge fast `30883306577` 均通过；ci-fast 输出文本出现 `actionBreakdown=keepReportOnly=5`，但探针仍有 block 5 截断、`openRenderIssueDetected`，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不得声称质量提升。

v3.90 修正漫画探针失败覆盖的显示排版：保留完整 OCR fallback 与首行 `翻译失败` 标记，仅把 OCR continuation 的显式换行压缩为空格，并让安全布局诊断、fit planner 与实际覆盖绘制共用同一显示变换。该 report/render-only 修复不改变 OCR 候选、翻译输入、Store、普通图片 renderer/export、ground truth、Koharu active artifact gate、metrics 或仓库 `output`；新增 `scripts/test-v390-koharu-render-failure-overlay-compaction-contract.py` 并接入 Koharu changed-file/full 路由。候选分支 `codeb/v3.90-koharu-failure-overlay-compaction` 的 commit `3344a8cd192684bf66e6cdff3397314c3fd8da05` 已通过 PR #154 合入，merge SHA `ee6046d491f327d6c71744876e0a4b4b6aab3947`，远端候选分支已删除，`main` 未触碰；full `30884104150`、同 SHA ci-fast 探针 `30884547044`、PR fast `30885582974`、merge fast `30885667505` 均通过。ci-fast 报告显示 `renderLockVerdict=renderStableWithProxyBoundaries`、`renderTextTruncatedBlocks=[]`，block 5 无截断且未触达最小字号，覆盖 PNG 非空；Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.91 让 Developer Console 的漫画探针结果使用只读 `MangaProbeDiagnosticTriageSummary` 汇总既有 OCR 疑似、翻译模型／语言质量、覆盖布局和 Koharu 工件下一步，并让失败 block 行显示现有 failureCategory 的“模型输出失败／译文质量失败／OCR 疑似损坏”标签与 VoiceOver 分流上下文；新增 `scripts/test-v391-koharu-diagnostic-triage-contract.py`。该 View-only 改动不新增 Store／持久化、不运行第二次探针、不改变 OCR 候选、翻译 prompt／model、renderer/export、metrics 或 output。候选 SHA `bbb73b14a90a10438d4cacf46344881d21d6206e` 已经 PR #155 合入，merge SHA `1ab18c3e9f2d04fbd51680b5b1b606113d86d032`，远端候选分支已删除，`main` 未触碰；full `30886955217`、同 SHA ci-fast `30887582600`、PR fast `30888608909`、merge fast `30888676363` 均通过。ci-fast 仍为 13 blocks／12 failures、render lock stable、model floor variant regress、readiness `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.92 让普通图片 OCR 结果增加低置信／方向待定本地筛选，待复查仍保持风险并集；筛选、空态、VoiceOver 与忽略后的当前队列定位均只属于 View/复查展示，不改变 blocks、Store、Vision OCR、翻译、renderer/export 或复查门控。Developer Console 增加全部／失败／OCR／翻译／布局漫画诊断筛选，只读既有报告并在探针新运行时重置，不修改 probe_report 或主流程。新增 `scripts/test-v392-image-review-risk-filter-contract.py` 与 evaluator；候选 SHA `83a3e30d25483cb67d788babd4998f05afb42c08` 已经 PR #156 合入，merge SHA `118c8c039d752a24a6c992e8f942ec34cb43e009`，full `30889811326`、PR fast `30890241624`、merge fast `30890322575` 均通过，`main` 未触碰。探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.93 修复普通图片 OCR 复查筛选跨图片残留：`imageTranslationRevision` 变化时清除 View 私有筛选、旧选中 block 与焦点，让新图默认显示全部结果；不写 Store／持久化，不改变 OCR、翻译、预览、导出或漫画探针。新增 `scripts/test-v393-image-review-filter-reset-contract.py`；候选 SHA `a3cea5a202f1de8a9b13aa4809db583b55480dcd` 已经 PR #157 合入，merge SHA `3f6565f65cc8ff965ba909f5d6e27ad0a508436c`，full `30890823578`、PR fast `30891431628`、merge fast `30891485989` 均通过，`main` 未触碰。探针默认 skip，Koharu readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.94 修复漫画探针缺失 `test/1.png` 时的失败入口：新尝试先清空旧 report/blocks，随后重建 App 沙盒 `Output`；失败报告记录清理数量与成功状态，清理失败时不把旧输出冒充本轮结果。新增 `scripts/test-v394-manga-probe-failure-cleanup-contract.py` 并接入 Koharu changed-file/full 路由。该 report-only/状态隔离修复不改变 OCR 候选、翻译 prompt/model、生产 renderer/export、普通图片 OCR 或 active Koharu gate；候选 full `30893309273`、PR fast `30893920011`、merge fast `30893993759` 均通过，merge SHA `ea6be1ddacaa34953b0c7f3389c342dc6e9ff4e3`，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

v3.95 让 Developer Console 的漫画探针结果在 `mangaOverlayProbeBlocks` 为空时显示明确的“本次探针未生成文字块”状态，隐藏诊断筛选器，并让 VoiceOver 说明当前失败上下文与重试范围；有逐块 blocks 时保持现有全部／失败／OCR／翻译／布局筛选和诊断行。新增 `scripts/test-v395-manga-probe-empty-state-contract.py` 并接入 UI/full 路由。该 View-only/诊断 UX 修复不新增 Store／持久化，不重新运行探针，不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active gate、metrics 或 `output`。候选 SHA `802103e413261cc1632d1129362695728af45215` 已通过 PR #159 合入，merge SHA `a57e65b2c8220de39b59177ec873a394a3398781`；full `30984932342`、PR fast `30985360673`、merge fast `30985413482` 均通过，full Xcode/JUnit `10/10`，探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.96 修正 `MangaProbeDiagnosticTriageSummary` 的状态色优先级：`artifactBlocked` 时始终使用 warning，只有 readiness 不阻断且既有 report `overallPassed` 时才显示 success，避免缺少真实四件套时误导开发者。新增 `scripts/test-v396-koharu-triage-tone-contract.py` 并接入 UI/full 路由。该 View-only/report-only 修复不新增 Store／持久化、不调用探针、不改变 OCR、翻译、renderer/export、普通图片 OCR、Koharu active artifact gate、metrics 或 `output`。候选 SHA `ab4d0ae59fbf2e0d6c6747fce331060ecfcc57ee` 已通过 PR #160 合入，merge SHA `12140ca11d3e888e74a974dffcfda41a0ca8357d`；full `30985776084`、PR fast `30986258687`、merge fast `30986307343` 均通过，full Xcode/JUnit `10/10`，探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.97 让 Developer Console 的漫画探针布局筛选与 `MangaProbeDiagnosticTriageSummary` 共用 `mangaProbeRenderRiskBlockSet`，消费既有 fit planner 的 `fontBudgetRiskBlocks`、`spriteContainmentRiskBlocks`、`siblingOverlapRiskBlocks`、`failureOverlayRiskBlocks` 及 render-lock 的 issue/min-font/truncation 信号；只读报告，不修改 blocks、Store、探针或 renderer。新增 `scripts/test-v397-koharu-layout-triage-contract.py` 并接入 UI/full 路由。基于 ci-fast [30986469563](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/30986469563) 的 10/7/6 风险分流，候选 SHA `c552e6170cfd7b6daab4cbebd885bdb44314b007` 已通过 PR #161 合入，merge SHA `87b102cdd0d8f08bcea876cfcd08645ddc10cc58`；full `30987210261`、PR fast `30987676638`、merge fast `30987725142` 均通过，full Xcode/JUnit `10/10`，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.98 让 Developer Console 的漫画探针 OCR 与翻译筛选、triage 摘要共享只读 risk set，合并 diagnostics、model-floor、translation failureCategory 与 OCR 疑似证据，避免 floor 报告存在时丢掉 diagnostics；新增 `scripts/test-v398-koharu-diagnostic-risk-union-contract.py` 并接入 UI/full 路由。该 View-only/report-only 修复不新增 Store／持久化、不运行探针、不改变 OCR、翻译、renderer/export、Koharu active gate、metrics 或 `output`。候选 SHA `7a352a5050c8846765ad4b139e7a6a125dfb4712` 已通过 PR #162 合入，merge SHA `3b953b83d12a092df1995c974cb33522dc02331a`；full `30988262491`、PR fast `30988802078`、merge fast `30988876405` 均通过，full Xcode/JUnit `10/10`，fast 明确复用 full receipt；探针默认 skip，readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`，不声称质量提升。

## 2. 每轮必读
每轮开始先读：

1. `README.md`
2. `AGENTS.md`
3. `git status --short`
4. `git log --oneline -5`
5. `update_log.md`
6. `md/flow/flow.md`
7. `md/flow/flowchart.md`
8. `md/test/test.md`

涉及漫画探针、OCR、覆盖绘制、翻译质量或报告模型时，继续读：

- `AITRANS/Services/MangaOverlayProbeService.swift`
- `AITRANS/Services/TranslationSessionStore.swift`
- `AITRANS/Models/TranscriptModels.swift`
- `test/1.ground_truth.json`
- 最新 `output/probe_report.json`
- 最新 `output/clean_text_diagnostic.json`
- 最新 `output/1_ocr_probe_text.txt`

README 不再承载历史基线；涉及验收时以当前代码、`update_log.md`、最新 `output/`、`metrics/version_history.csv` 和实际测试结果为准。

## 3. 架构硬边界
- `TranslationSessionStore` 是 UI 状态、模型调用、历史、诊断和持久化的统一调度中心。
- UI 层只触发 store 方法，不绕开 store 直接改持久化、模型状态或报告状态。
- Speech 授权、识别和翻译回调必须按当前 run ID 隔离；取消或重试后，旧回调不得覆盖新状态。
- Speech 质量 corpus 的参考 transcript 只能在 Apple Speech 返回最终文本后参与评估；禁止用于识别请求、候选选择、纠错或生产翻译。中日文没有稳定分词器时只报告 CER，不把字符编辑率标成 WER。
- 普通图片 OCR 使用 `VisionOCRService`；漫画覆盖探针使用 `MangaOverlayProbeService` 的独立诊断链路。
- 用户实际翻译和 summary 走 sampled 解码；漫画探针、raw 诊断、clean text、batch 对照和纠错翻译对照走 deterministic 解码。
- `test/1.ground_truth.json` 只能用于探针验证和统计，不能用于真实产品路径或生产候选选择。
- action 打包软件包当前有密码保护；不要为 Agent C 验收改动或解密该包。Agent C 只看独立未加密的 CI 结果包。

v3.72 继续只读 Koharu readiness report：当 schema 为 v1 且 bubble/segment payload verdict 都是 `legacySummaryOnly` 时，Developer Console 显示“未要求（v1 summary-only）”与“未要求（v2 拓扑）”，并在 VoiceOver/可复制摘要中说明 v2 门控尚未要求；真实 v2 工件仍保留实际 payload/topology 失败与阻塞。该版本不创建或修改 active 工件、不放宽 gate、不改变 OCR、翻译、renderer/export、漫画探针或 Koharu 主路径，也不能作为质量提升证据。

v3.73 让空 OCR 的已忽略文字块在视觉行显示“空 OCR 原文”，VoiceOver label 显示“空”，避免空字符串让用户误以为行缺失；非空文本、恢复入口、焦点和 Store ownership 保持不变。该 View-only 改动不改变 Vision OCR、翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。

v3.74 让普通图片 OCR 结果行与旁贴／覆盖预览在 `block.original` 为空时显示稳定“空 OCR 原文”回退；旁贴／覆盖仍保持非空译文优先。该 View-only 变化不新增 Store／持久化状态、不改变 Vision OCR、模型翻译、renderer/export、漫画探针、Koharu 主路径或质量基线。
v3.75 让 `ImageOCRCorrectionReferencePreview` 与 `ImageTranslationFocusPreview` 在 `block.original` 为空时用 View 私有 `accessibilityOriginalText` 提供“空”回退，避免参考／局部定位 VoiceOver value 变成无上下文空白；非空原文保持原样。该修复不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu、ground truth、metrics 或 output；真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.76 隐藏 `ImageTranslationFocusPreview` 中仅用于视觉提示的“局部放大”角标，避免该装饰标签与父容器的“已定位文字块局部放大”VoiceOver label/value 重复朗读；参考预览原有装饰角标隐藏、关闭／修正／复查／前后定位与 OCR 上下文保持不变。该 View-only 修复不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu、ground truth、metrics 或 output；真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。
v3.77 让 `ImageTranslationFocusPreview` 的无效几何状态子视图从 VoiceOver 树隐藏，父容器继续提供不可用原因和“关闭、编辑 OCR 原文或切换文字块”的统一 hint，避免 `contain` 层级重复朗读；参考预览、关闭／修正／复查／前后定位与 OCR 上下文保持不变。该 View-only 修复不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu、ground truth、metrics 或 output；真实 Koharu 四件套、Speech corpus 与竖排图片 corpus 仍缺失，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.78 让关闭 `ImageTranslationFocusPreview` 后的 VoiceOver 焦点回到对应 OCR 结果行，避免局部预览容器消失后焦点丢失；使用既有 View 私有 focus identity，不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu、ground truth、metrics 或 output。该版本只改善图片操作可理解性，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.79 让 `selectAdjacentBlock(offset:)` 在按当前筛选顺序选中目标 OCR block 后立即把 VoiceOver 焦点交给新的局部预览容器，前后导航继续保留位置 value、首尾 disabled 边界和 View 私有 focus identity；同步让 v3.14 历史合同接受等价的局部 target ID 赋值。该 View-only 改动不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu、ground truth、metrics 或 output，不声称 OCR、翻译、识别或 Koharu 质量提升。

v3.80 让筛选器隐藏当前选中 block 时清除失效的局部预览选择，并将 VoiceOver 焦点交给首个可见结果行；若筛选结果为空则交给复查完成状态或筛选器本身，确保焦点不留在已消失的预览容器。该 View-only 改动不新增 Store／持久化状态，不改变 Vision OCR、模型翻译、renderer/export、复查、漫画探针、Koharu、ground truth、metrics 或 output，不声称 OCR、翻译、识别或 Koharu 质量提升。

## 4. 漫画探针硬规则
漫画覆盖翻译探针固定读取 bundle 内 `test/1.png`：

```text
test/1.png
  -> 裁掉浏览器 UI / 广告 / 底部导航
  -> Vision OCR
  -> 合并 OCR observations 为逻辑文字块
  -> 极简英译中 prompt：把以下翻译成中文：
  -> 覆盖绘制译文或失败文本
  -> 输出 JSON / TXT / PNG 到 App 沙盒 Output
  -> scripts/export-probe-output.sh 导出到项目根 output/
```

必须遵守：

- 失败块必须保留在 `probe_report.json` 的 `blocks` 明细中。
- 失败块必须在覆盖图上显示 `翻译失败 + OCR 原文`，不能静默跳过。
- 失败块必须写入 `blockPassed = false`、`failureReasons`、`translationDecisionTrace`、`translationFailureDetail`。
- 输出目录每轮必须清理，不能混入旧 PNG / JSON。
- 可信匹配必须能拒绝；低于阈值写 `groundTruthMatch = "unmatched"`，且不纳入平均准确率。
- OCR 相似度使用词级 Levenshtein；保留 `ocrLegacySimilarity` 和 `wordOrderPreserved`。
- 核心对话 `dialogue` 和装饰标题 `decorative` 必须分开统计。

禁止：

- 不要用 ground truth 做生产候选选择。
- 不要把旧 `accuracyVsGroundTruth = 0.8378 / 0.8755` 当当前基线。
- 不要只看 `cjkCharacters > 0` 判定翻译成功。
- 不要因为 clean text 失败就继续盲目调 OCR。
- 不要在未证明收益前把 deterministic correction、bubble-first 或 batch translation 替换为主流程。

## 5. Git 分支和云端验证工作流
- `main`：外观展示分支。禁止 Agent B / C 把日常开发成果合并到 `main`。
- `smalldata_test`：本仓库实际工作主分支。若外部提示词写成 `samlldata_test`，以当前远端真实分支 `origin/smalldata_test` 为准。
- `codeb/vX.Y-短标题`：Agent B 候选实现分支，例如 `codeb/v2.0-cloud-ci-workflow`。
- Agent B 每轮从最新 `smalldata_test` 开 `codeb/...` 分支，完成后 push。
- Agent B push 后默认创建 Pull Request，base 为 `smalldata_test`，head 为 `codeb/...`。
- Agent C 从 PR / 远端 `codeb/...` 分支验收；通过后优先通过 PR merge 合并到 `smalldata_test`。
- 任何 Agent 在 `git push`、`git merge`、删除远端分支或改变远端状态前，都必须确认目标不是 `main`。
- Agent C 合并后必须删除远端 `codeb/...` 候选分支，或确认 GitHub PR 的 delete branch 已执行；没有权限删除时必须说明，避免候选分支无限堆积。

默认验证路径：

- 除非人工明确要求“本机测试”“本地 build”“本地跑探针”“本地 xcodebuild”，否则完整 build、Xcode 测试、漫画探针、报告生成和重验证默认交给 GitHub Actions。
- 本地仍可做 `git diff --check`、JSON 解析、YAML smoke 等轻量检查；这些不算重负载本机测试。
- Agent B 完成版本核心代码后 push `codeb/...`：`validationProfile=full`，只运行本任务涉及的领域契约；App 构建相关变更再跑一次 Xcode build。成功后 workflow 为该 SHA 写 `AITRANS CI/full-validation` status，并上传未加密结果包。
- PR 只在 opened / reopened / ready-for-review 时运行 `validationProfile=fast`；不监听 synchronize，避免修复 push 同时触发 full + PR fast。fast 只跑基础静态/路由契约并记录 skip reason，不重复 Xcode、Speech/UI/Koharu 大契约或截图。
- 合并到 `smalldata_test` 后，workflow 读取 merge 第二父 SHA 的 full-validation status；只有 `success` 才走 fast follow-up，并把该 receipt 传播到 merge SHA，否则自动回退 full。C 退回后新的核心修复 push 必须重新产生 full 收据。
- 其后的纯 README / AGENTS / update log / `md/` / metrics `smalldata_test` 提交只有父 receipt 为 success 时可走 fast；父 receipt 缺失或失败时必须强制当前头部 Xcode build，不能用文档提交掩盖未验证代码。结果包必须保留 smalldata 父 SHA、state、元数据判定和强制 full 判定。
- Speech 功能默认只跑 Xcode build、Speech run-id/取消/翻译链路契约、质量算法契约和 corpus validator，不采 UI 截图；缺少 `test/speech_corpus/manifest.json` 时 validator 必须写 `manifestMissing`、`qualityExecuted=false`，不能伪造质量结果。漫画/翻译改动需要结果图时手动跑 `ci-fast/full`，只验收探针输出 PNG，不等同 UI evidence。
- UI evidence 默认跳过；只有重大 UI 任务在候选核心 commit 使用 `[ui evidence]`，或手动 `workflow_dispatch ui_evidence_mode=full` 才运行。普通 UI 小改、Speech、PR 和 merge 不截图。
- Koharu artifact validator 的完整 invalid fixture 矩阵只在 Koharu validator、artifact contract 或 CI workflow 相关 full 任务中运行；其他任务不加载该领域套件。
- GitHub Actions push 默认 `probe_mode=skip`，不启动模拟器漫画探针；需要云端探针验收时手动 `workflow_dispatch` 选择 `ci-fast` 或 `full`。
- 现有加密打包 workflow 只在软件包交付时手动 `workflow_dispatch`，不再随 `smalldata_test` merge 自动 archive；Agent C 不以该包验收。
- 独立 CI 结果包必须未加密，至少包含 `junit.xml`、`xcodebuild.log`、`ci-artifact-manifest.json`、`ci-failure-summary.md`；`xcodeBuildRequired=true` 时还必须包含 `.xcresult`，手动探针运行还必须包含可用的 `output/` 报告。
- 若 `workflow_dispatch` 注入 Koharu artifact archive，Agent C 必须核对 Release / artifact / App / CI identity、validator、orientation、dry-run 与 reconciliation 证据。external shadow OCR 除 executed / candidate / OCR count sanity 外，还必须核对 TextBox ID 非空唯一、matched / succeeded / failed / skipped 分区一致、`duplicateAssignedTextBoxIDs = []`、`coverageVerdict = complete`、`successfulCoverageRatio = 1`，且 `WI/G-external-textbox-shadow-ocr-coverage` closed / passed；不得用任意一个 OCR 成功或注入步骤日志冒充完整 coverage。
- 若注入 artifact 的 TextBox 带 `sourceDirection`、`linePolygons` 或 `rotationDegrees`，Agent C 还必须核对 `orientationShadowPathPartialBlocks`、`orientationUnsupportedBlocks`、`orientationUnsupportedReasonBreakdown`、convergence 的 `WI/G-external-textbox-shadow-ocr-coverage` 和 `WI/G-external-textbox-orientation-shadow-path`，确认 no-candidate / partial / unsupported 未被误判为 `closedReportOnly` 或 passed。
- 注入 contract v2 artifact 时，Agent C 必须同时核对 validator / App / manifest 的 `maskTopologyGateReady`、`maskTopologyValidation` / `maskTopologyReport`，以及 convergence `WI/G-external-mask-topology-linkage`。assignment 必须复用 external shadow OCR 的稳定一对一 TextBox 结果；missing / duplicate block 或 TextBox、foreign / no-bubble / orphan / multiply-assigned pixels、cross-Bubble / orphan component 或 partition 不守恒时不得 passed。
- 若云端失败，Agent B 根据结果包中的失败摘要、日志路径和 manifest 修复后继续 push，不改回默认本机循环。

## 6. Agent A/B/C 职责
### Agent A
- 默认不改代码。
- 读取入口文档、历史、流程、测试规范和相关源码。
- 输出写给 Agent B 的版本化提示词，保存到 `md/prompt/vX（阶段）/vX.Y（任务）.md`。
- 提示词必须明确目标、非目标、分支名建议、测试层级、CI 期望、验收标准和禁止项。

### Agent B
- 从最新 `smalldata_test` 开 `codeb/vX.Y-短标题` 分支。
- 按 Agent A 提示词小步实现，不做无关重构。
- 默认本地只跑轻量检查；除非人工明确要求，不跑本机完整 Xcode build 或漫画探针。
- 完成后集中 push 核心候选 commit，让 GitHub Actions 运行一次 task-scoped full；通过后再创建 PR 到 `smalldata_test`，PR 只跑 fast follow-up。若 C 退回，修复 push 重新跑对应 full。
- 最终回复必须列出分支名、PR 链接、commit SHA、push 结果、CI 入口或 run 信息、本地已跑检查、未跑测试原因、artifact 名称；若 Actions 尚未完成，必须说明等待云端结果。

### Agent C
- 拉取 `codeb/...` 分支，查看实际 diff、文档同步、架构边界、GitHub Actions 结论、日志和 artifacts。
- 只能验收与当前 `codeb/...` HEAD 完全一致的 `commitSha`。
- 必须核对 `ci-artifact-manifest.json` 中的 `version`、`branch`、`commitSha`、`runId`、`runAttempt`、`workflowName`、`validationProfile`、`validationReason` 和 full-validation 收据字段，确认没有拿旧包、错包或其他分支的包。fast 结果不能单独冒充候选编译证据。
- 必须查看 `.xcresult` 或摘要、`junit.xml`、`xcodebuild.log`、`ci-failure-summary.md`；涉及探针时还必须检查云端生成或上传的 `probe_report.json`、`clean_text_diagnostic.json`、`1_ocr_probe_text.txt` 和关键 PNG。涉及 external TextBox 时，还必须检查 shadow OCR coverage、orientation partial / unsupported 摘要及 convergence gate 状态。
- 有 bug 或云端验证失败时，输出退回清单，说明应由 Agent B 修复的日志位置和失败原因，不合并。
- 通过后更新版本号和核心文档，通过 PR merge 合并到 `smalldata_test`，push。严禁合并到 `main`。
- 合并完成后删除远端 `codeb/...` 候选分支，或在最终回复说明未删除原因。

## 7. 测试选择
- 非 App 构建相关修改至少运行 `git diff --check`，可加 JSON/YAML smoke。
- 非 App 构建相关变更的云端 CI 可接受 `xcodeBuildRequired=false` 的 build-skip 结果包；Agent C 必须核对 manifest 的 skip reason，不能把它当作 Swift/Xcode 编译证据。
- Swift 或 Xcode 工程修改默认不在本机跑完整 build；按规则推分支交给 GitHub Actions 快验。
- Speech 质量算法修改至少运行 `scripts/test-speech-quality-contract.py`、纯 Swift evaluator contract 和 `scripts/validate-speech-corpus.py`；没有真实音频时只能验收算法与接线，不能给出 WER/CER 改善结论。
- 漫画探针、翻译链路或报告模型修改需要云端探针证据时，手动 `workflow_dispatch` 运行 `ci-fast` 或 `full` 生成报告；若当前云端因模拟器、GGUF、App 容器或外部 artifact 缺失不能稳定运行，必须在最终回复和文档中列明未验证范围、缺失依赖、是否影响验收和需要人工提供什么。
- 不得伪造测试结果，不得把旧 artifact 当新结果。

本地轻量命令：

```sh
git diff --check
python3 -m json.tool test/1.ground_truth.json
python3 -m json.tool output/probe_report.json
python3 -m json.tool output/clean_text_diagnostic.json
```

人工明确要求本机 build 时使用：

```sh
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild \
  -project AITRANS.xcodeproj \
  -scheme AITRANS \
  -destination 'generic/platform=iOS Simulator' \
  -derivedDataPath .derivedData \
  CODE_SIGNING_ALLOWED=NO build
```

## 8. 文档和版本收口
- `AGENTS.md` 是唯一核心入口文档。
- `update_log.md` 记录版本历史、关键决策、验证结果和遗留问题。
- `md/flow/flow.md` 只写当前真实架构和运行流程。
- `md/flow/flowchart.md` 必须与 `flow.md` 同步。
- `md/test/test.md` 是测试选择依据。
- `md/prompt/` 保存 Agent A 的版本化实现提示词。
- 功能更新或 bug 修复后，按影响同步更新 `update_log.md`、flow/test 文档和 `metrics/version_history.csv`；README 不再写更新记录，只保留项目说明、当前用法和稳定规则。
- 流程制度变更不伪装成漫画探针质量版本；未重新跑完整探针时不追加 `metrics/version_history.csv` 漫画指标行。

## 9. 最终回复格式
最终回复使用中文，至少包含：

- 改了什么。
- 关键文件。
- 当前分支名和是否触碰远端。
- 已运行的验证命令和结果。
- 未运行的测试及原因。
- 未跑本机 build / 探针时，明确写“未跑本机 build / 探针，按规则交给云端验证”。
- Agent C 通过后，说明 git commit / push / merge 结果和提交哈希；若未执行，说明原因。
- 涉及漫画探针或翻译链路时，汇总关键数字。
- 已知风险和下一步建议。
v3.259 对齐 Koharu Manga OCR 的最近邻预处理边界：bundled Manga OCR 的 224×224 灰度输入改用 `UpsampleNearest2D` 等价的 floor/nearest 空间采样（保留 iOS `DeviceGray` 渲染，未宣称 RGB/luma 位级相等），新增独立 checksum/runtime harness；同时修正图片 OCR 方向选择器提交待定 override 后仍显示“方向待定”的 review warning，并为 v3.238/v3.239/v3.245/v3.254 历史 runtime 路由补齐 checkout scope guards。OCR 模型、detector ownership、bbox／quad、batch／单 crop fallback、取消传播、12／48 预算、翻译、布局、渲染和非图片路径不变。新增 `scripts/test-v3259-koharu-nearest-manga-preprocess-contract.py` 与 `scripts/test-v3259-koharu-nearest-manga-preprocess-runtime.sh`，工程版本为 `3.259`。本地 `279` 个 `scripts/test-v*.py` 合同、nearest checksum harness、三条既有真实 Core ML runtime、YAML、project plist、Python／shell 语法与 `git diff --check` 通过；3×2 灰度 fixture checksum 为 `82320641c2833265`，单页仍为 5 个 vertical blocks，长页为 20 个 vertical blocks、4 个 compact `ニコッ`、底部 `y=0.940558`，方向 crop 目标文字保持 `今度こそこの爆乳を持ち帰る！`。exact-SHA full [31498693384](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31498693384) 对 SHA `4ac896f1826002a42cef181b44f264ff20f4b249` 使用 Xcode `26.6` 完成 static/UI/Speech/home/paste、Xcode 与 JUnit `10/10`（0 failures），发布 `AITRANS CI/full-validation=success`，probe `skip`；PR #333 已合入 `smalldata_test`，merge SHA `4365fa490270bd098164c867574c23252cd633f1`，合入后 push CI [31499851239](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31499851239) 通过。该版本只改善 Koharu 预处理对齐与方向复查的操作可发现性，不声称通用日语 OCR、翻译或识别质量提升；Koharu artifact readiness 仍为 `manifestMissing / stopUntilArtifactsProvided`；当前本地／origin 活动分支仅保留 `main` 与 `smalldata_test`。
v3.271 继续用云端 Koharu MIT48 reference parity 收紧 `test/jap.jpg` 右上竖排 line 的诊断边界：依据实际文字／背景分布，将 `right-column-a` crop 从 `(846,116)-(906,432)` 收紧到 `(864,136)-(908,342)`；云端输出仍为 `やがって`，confidence 从 v3.270 的 `0.14473826` 提升到 `0.23084585`，但仍低于现有可靠 `>=.55` 门槛，因此不把它伪装成可靠 owner。既有 7 crop 至少 6 个日语预测、compact `ニコッ` 精确文本与 confidence `>=.55` gate 保持不变，云端 compact 为 `ニコッ`／`0.59093773`、accepted `6/7`。这只是固定样图的云端 crop 质量诊断改进，不是通用日语 OCR／翻译／识别质量声明；不把 GPL MIT48 权重/runtime 打进 AITRANS，不替换 bundled Apache Manga OCR。新增 `scripts/test-v3271-koharu-mit48-right-column-quality-contract.py`，工程版本为 `3.271`。本地严格遵守“不要本地编译”，只做安全 Python 合同、YAML／shell 语法与 `git diff --check`；exact-SHA full [31580831851](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31580831851) 对实现 SHA `deed8f9c9b67c535167daa3989ce7e4654aed1a9` 的云端 Koharu parity、Xcode/JUnit/UI/Home/Paste/Speech 与 receipt 全部成功，parity job 为 `94063266497`；PR #352 的 CI [31582462714](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31582462714) 通过，合入 `smalldata_test` 的 merge SHA 为 `e3ab42b4d9a6ad368a4dd4c27e5e2a35ddbe315f`，合入后 push CI [31582532563](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31582532563) 通过。研发分支已删除，本地／origin 仅保留 `main` 与 `smalldata_test`。
v3.272 继续用云端 Koharu MIT48 reference parity 隔离右上竖排 line 的主文字：`right-column-a` 从 v3.271 的 `(864,136)-(908,342)` 收紧到四个主字符范围 `(864,136)-(908,298)`，去掉省略号与下方人物／背景干扰。固定 `test/jap.jpg` 云端仍准确输出 `やがって`，confidence 从 `0.23084585` 提升到 `0.98682830`；compact `ニコッ` 为 `0.59093815`，accepted `6/7`，既有 confidence、日语密度、ownership 与 GPL cloud-only 边界不变。这是固定样图的云端 crop 诊断改进，不是通用日语 OCR／翻译／识别质量声明；不把 GPL MIT48 权重/runtime 打进 AITRANS，不替换 bundled Apache Manga OCR。新增 `scripts/test-v3272-koharu-mit48-right-column-text-only-contract.py`，工程版本为 `3.272`。本地严格遵守“不要本地编译”，只做安全 Python 合同、YAML／shell 语法与 `git diff --check`；exact-SHA full [31582991645](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31582991645) 对实现 SHA `6c28599fbc205939fc1e50c0dc25d34cda28b184` 的云端 Koharu parity、Xcode/JUnit/UI/Home/Paste/Speech 与 receipt 全部成功，parity job 为 `94070186027`；PR #353 的 CI [31584467786](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31584467786) 通过，合入 `smalldata_test` 的 merge SHA 为 `4c9e50d7e41dc99a1289fa82e0601f5e7f924fb2`，合入后 push CI [31584537250](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31584537250) 通过。研发分支已删除，本地／origin 仅保留 `main` 与 `smalldata_test`。
v3.273 将图片 OCR 复查默认低置信阈值从 `0.50` 对齐到 bundled Manga OCR／Koharu parity 的 `0.55`：所有 `requiresReview`、低置信筛选、结果行、局部预览和 VoiceOver 原因提示共享该边界，严格 `<` 保持恰好 `0.55` 不标低置信，方向待定 union、OCR、detector ownership、请求预算、翻译、布局、渲染和非图片路径不变。同步修正 v3.300/v3.310/v3.392 历史 evaluator fixture，新增 `scripts/test-v3273-image-ocr-review-confidence-gate-contract.py`，工程版本为 `3.273`。本地遵守“不要本地编译”，只做 Python 合同／AST、YAML／shell 语法与 `git diff --check`；实现 SHA `0cb74436b92dc1f4a1998869499432371461e306` 的云端 Xcode build 成功，修复后的 test-only SHA `d3112bbdc1d00812bc154f4f5ecf7465a4761bc5` exact full [31592607326](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31592607326) 的 Koharu parity、UI contract 与 receipt 成功；PR #354 merge `1bcdb2ea8b41ac69f8ff81a60c33e8982d23a1d0`，合入后 push CI [31593661426](https://github.com/bengzhu/project1_lgbt_naxida/actions/runs/31593661426) 通过。该版本只改善复查风险可发现性，不声称通用日语 OCR／翻译质量提升；GPL MIT48 artifact/runtime 仍不进入 AITRANS，固定样图 parity 仍为 `やがって` `.98682830`、`ニコッ` `.59093815`、`6/7` accepted。
